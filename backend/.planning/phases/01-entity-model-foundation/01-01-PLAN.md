---
phase: 01-entity-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/resources/db/migration/V28__logh_domain_rename.sql
autonomous: true
requirements:
  - CHAR-01
must_haves:
  truths:
    - "V28 migration applies cleanly on the existing V27 schema"
    - "All 11 tables are renamed to LOGH domain names"
    - "All FK columns reference new table names"
    - "All stat columns renamed (strength->command, intel->intelligence, charm->administration)"
    - "3 new stat columns added with defaults (mobility, attack, defense + exp)"
    - "All resource/item columns renamed per CLAUDE.md mapping"
    - "All indexes renamed to match new table/column names"
    - "nation_aux_key enum type renamed to faction_aux_key"
  artifacts:
    - path: "backend/game-app/src/main/resources/db/migration/V28__logh_domain_rename.sql"
      provides: "Atomic Flyway migration for all LOGH domain renames"
      min_lines: 100
  key_links: []
---

<objective>
Write the V28 Flyway migration that atomically renames all tables, columns, indexes, and enum types from OpenSamguk terminology to LOGH domain terminology, and adds the 3 new stat columns (mobility, attack, defense).

Purpose: This is the foundation for all subsequent code changes. PostgreSQL RENAME operations are metadata-only (no data rewrite), making this fast and safe. All renames must happen in a single migration because entity classes cross-reference each other.

Output: One SQL migration file that transforms the entire schema to LOGH domain naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md (Domain Mapping tables)

Reference the V1 migration for original table/column definitions:
@backend/game-app/src/main/resources/db/migration/V1__core_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write V28 Flyway migration SQL</name>
  <files>backend/game-app/src/main/resources/db/migration/V28__logh_domain_rename.sql</files>
  <action>
Create a single Flyway migration file with the following operations IN ORDER:

**1. Rename tables (11 tables):**
- `general` -> `officer`
- `general_turn` -> `officer_turn`
- `city` -> `planet`
- `nation` -> `faction`
- `nation_turn` -> `faction_turn`
- `nation_flag` -> `faction_flag`
- `troop` -> `fleet`
- `emperor` -> `sovereign`
- `world_state` -> `session_state`
- `old_general` -> `old_officer`
- `old_nation` -> `old_faction`

Use `ALTER TABLE x RENAME TO y;` syntax.

**2. Rename FK columns (use new table names since tables already renamed):**
- officer: `nation_id` -> `faction_id`, `city_id` -> `planet_id`, `troop_id` -> `fleet_id`, `world_id` -> `session_id`
- planet: `nation_id` -> `faction_id`, `world_id` -> `session_id`
- fleet: `leader_general_id` -> `leader_officer_id`, `nation_id` -> `faction_id`, `world_id` -> `session_id`
- faction: `chief_general_id` -> `chief_officer_id`, `capital_city_id` -> `capital_planet_id`, `world_id` -> `session_id`
- faction_turn: `nation_id` -> `faction_id`, `world_id` -> `session_id`
- faction_flag: `nation_id` -> `faction_id`, `world_id` -> `session_id`
- officer_turn: `general_id` -> `officer_id`, `world_id` -> `session_id`
- sovereign: `world_id` -> `session_id`
- session_state: (world_id is already the primary concept, no rename needed for this table's own columns)
- diplomacy: `src_nation_id` -> `src_faction_id`, `dest_nation_id` -> `dest_faction_id`, `world_id` -> `session_id`
- event: `world_id` -> `session_id`
- message: `world_id` -> `session_id`
- old_officer: `world_id` -> `session_id`
- old_faction: `world_id` -> `session_id`
- rank_data: `nation_id` -> `faction_id`, `world_id` -> `session_id`
- board: `world_id` -> `session_id`
- board_comment: `world_id` -> `session_id`
- auction: `world_id` -> `session_id`
- betting: `world_id` -> `session_id`
- vote: `world_id` -> `session_id`
- vote_cast: `world_id` -> `session_id`
- tournament: `world_id` -> `session_id`
- game_history: `world_id` -> `session_id`
- world_history: `world_id` -> `session_id`
- yearbook_history: `world_id` -> `session_id`
- traffic_snapshot: `world_id` -> `session_id`
- hall_of_fame: `world_id` -> `session_id`
- general_record: `world_id` -> `session_id`
- select_pool: `world_id` -> `session_id`
- general_access_log: `world_id` -> `session_id`

Use `ALTER TABLE x RENAME COLUMN y TO z;` syntax.

**3. Rename stat columns on officer table:**
- `strength` -> `command`, `strength_exp` -> `command_exp`
- `intel` -> `intelligence`, `intel_exp` -> `intelligence_exp`
- `charm` -> `administration`, `charm_exp` -> `administration_exp`

**4. Add new stat columns on officer table (3 new stats with defaults):**
```sql
ALTER TABLE officer ADD COLUMN mobility smallint NOT NULL DEFAULT 50;
ALTER TABLE officer ADD COLUMN mobility_exp smallint NOT NULL DEFAULT 0;
ALTER TABLE officer ADD COLUMN attack smallint NOT NULL DEFAULT 50;
ALTER TABLE officer ADD COLUMN attack_exp smallint NOT NULL DEFAULT 0;
ALTER TABLE officer ADD COLUMN defense smallint NOT NULL DEFAULT 50;
ALTER TABLE officer ADD COLUMN defense_exp smallint NOT NULL DEFAULT 0;
```

Also add dex6, dex7, dex8 for the new stats:
```sql
ALTER TABLE officer ADD COLUMN dex_6 integer NOT NULL DEFAULT 0;
ALTER TABLE officer ADD COLUMN dex_7 integer NOT NULL DEFAULT 0;
ALTER TABLE officer ADD COLUMN dex_8 integer NOT NULL DEFAULT 0;
```

**5. Rename resource/item columns on officer table:**
- `gold` -> `funds`, `rice` -> `supplies`
- `crew` -> `ships`, `crew_type` -> `ship_class`
- `train` -> `training`, `atmos` -> `morale`
- `weapon_code` -> `flagship_code`, `book_code` -> `equip_code`
- `horse_code` -> `engine_code`, `item_code` -> `accessory_code`

**6. Rename planet (formerly city) resource columns:**
- `pop` -> `population`, `pop_max` -> `population_max`
- `agri` -> `production`, `agri_max` -> `production_max`
- `comm` -> `commerce`, `comm_max` -> `commerce_max`
- `secu` -> `security`, `secu_max` -> `security_max`
- `trust` -> `approval`
- `def` -> `orbital_defense`, `def_max` -> `orbital_defense_max`
- `wall` -> `fortress`, `wall_max` -> `fortress_max`
- `trade` -> `trade_route`

**7. Rename faction (formerly nation) columns:**
- `gold` -> `funds`, `rice` -> `supplies`
- `bill` -> `tax_rate`
- `rate` -> `conscription_rate`, `rate_tmp` -> `conscription_rate_tmp`
- `tech` -> `tech_level`
- `power` -> `military_power`
- `gennum` -> `officer_count`
- `level` -> `faction_rank`
- `type_code` -> `faction_type`

**8. Rename indexes:**
- `idx_general_world_id` -> `idx_officer_session_id`
- `idx_general_nation_id` -> `idx_officer_faction_id`
- `idx_general_city_id` -> `idx_officer_planet_id`
- `idx_general_user_id` -> `idx_officer_user_id`
- `idx_city_world_id` -> `idx_planet_session_id`
- `idx_city_nation_id` -> `idx_planet_faction_id`
- `idx_nation_world_id` -> `idx_faction_session_id`
- `idx_diplomacy_world_id` -> `idx_diplomacy_session_id`
- `idx_message_world_id` -> `idx_message_session_id`
- `idx_event_world_id` -> `idx_event_session_id`

Use `ALTER INDEX x RENAME TO y;` syntax.

**9. Rename enum type:**
```sql
ALTER TYPE nation_aux_key RENAME TO faction_aux_key;
```

**10. Rename officer_turn column:**
- In officer_turn table: `general_id` -> `officer_id` (already covered above, verify)

Add comments at the top of the file explaining the migration purpose and that all operations are metadata-only (no data rewrite). Include a note that this migration MUST be applied together with the corresponding Kotlin entity class renames.

IMPORTANT: Before writing, read V1__core_tables.sql and V4__auxiliary_tables.sql to verify exact column names and all tables that have `world_id` columns. Do NOT miss any table. Also check V7, V17, V22, V26, V27 for additional columns/tables that may have been added later.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh && docker-compose up -d postgres && sleep 3 && cd backend && ./gradlew :game-app:flywayInfo 2>&1 | tail -20</automated>
  </verify>
  <done>V28__logh_domain_rename.sql exists with all table renames, column renames, new stat columns, index renames, and enum rename. File is syntactically valid SQL.</done>
</task>

</tasks>

<verification>
- V28 migration file exists at the correct path
- SQL syntax is valid (no typos in table/column names)
- All 11 table renames present
- All FK column renames cover every table with world_id, nation_id, city_id, general_id, troop_id references
- 3 new stat columns (mobility, attack, defense) + their _exp columns added
- dex_6, dex_7, dex_8 columns added
- All resource/item renames match CLAUDE.md mapping exactly
</verification>

<success_criteria>
V28 migration file is complete and covers the full OpenSamguk-to-LOGH domain transformation at the database level.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-01-SUMMARY.md`
</output>
