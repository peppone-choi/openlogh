---
phase: 03-command-point-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - game-app/src/main/resources/db/migration/V31__add_pcp_mcp_columns.sql
  - game-app/src/main/kotlin/com/openlogh/entity/Officer.kt
  - game-app/src/main/kotlin/com/openlogh/model/CpPoolConfig.kt
autonomous: true
requirements: [CMD-02]

must_haves:
  truths:
    - "Officer entity has separate pcp, mcp, pcpMax, mcpMax fields"
    - "Database migration V31 adds pcp/mcp columns and migrates existing command_points data"
    - "CpPoolConfig provides rank-based max pool sizes"
  artifacts:
    - path: "game-app/src/main/resources/db/migration/V31__add_pcp_mcp_columns.sql"
      provides: "PCP/MCP columns on officer table"
      contains: "ADD COLUMN pcp"
    - path: "game-app/src/main/kotlin/com/openlogh/entity/Officer.kt"
      provides: "Dual CP pool entity fields"
      contains: "var pcp"
    - path: "game-app/src/main/kotlin/com/openlogh/model/CpPoolConfig.kt"
      provides: "Rank-based pool size lookup"
      exports: ["CpPoolConfig"]
  key_links:
    - from: "Officer.kt"
      to: "V31 migration"
      via: "JPA column mapping"
      pattern: "@Column.*pcp"
---

<objective>
Add dual PCP/MCP columns to the officer table via Flyway V31 migration, update the Officer entity with new fields, and create the CpPoolConfig rank-to-pool-size lookup.

Purpose: Foundation data model for the dual command point system. All subsequent plans depend on these fields existing.
Output: V31 migration, updated Officer entity, CpPoolConfig model
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-command-point-system/03-RESEARCH.md

@game-app/src/main/kotlin/com/openlogh/entity/Officer.kt
@game-app/src/main/kotlin/com/openlogh/model/OfficerStat.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: V31 Flyway migration + CpPoolConfig model</name>
  <files>
    game-app/src/main/resources/db/migration/V31__add_pcp_mcp_columns.sql,
    game-app/src/main/kotlin/com/openlogh/model/CpPoolConfig.kt
  </files>
  <action>
    Create V31 migration SQL that:
    1. Adds 4 columns to `officer` table:
       - `pcp INT NOT NULL DEFAULT 5` — current political command points
       - `mcp INT NOT NULL DEFAULT 5` — current military command points
       - `pcp_max INT NOT NULL DEFAULT 5` — maximum PCP (rank-based)
       - `mcp_max INT NOT NULL DEFAULT 5` — maximum MCP (rank-based)
    2. Migrates existing data: `UPDATE officer SET pcp = command_points / 2, mcp = command_points - command_points / 2` (integer division splits evenly, odd point goes to MCP)
    3. Sets pcp_max and mcp_max based on officer_level using a CASE statement mapping:
       rank 0->5, 1->6, 2->7, 3->8, 4->10, 5->12, 6->15, 7->18, 8->22, 9->27, 10->35
    4. Does NOT drop command_points column yet (backward compat during transition)

    Create `CpPoolConfig.kt` in `com.openlogh.model` package:
    - Object with a `maxPoolByRank` array: `intArrayOf(5, 6, 7, 8, 10, 12, 15, 18, 22, 27, 35)`
    - Function `getMaxPool(rankLevel: Int): Int` that returns the pool size for a given rank, clamping to valid range 0..10
    - Function `getMaxPool(rankLevel: Short): Int` overload accepting Short for convenience with Officer.officerLevel
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && JAVA_HOME=/Users/apple/Library/Java/JavaVirtualMachines/temurin-23.0.2/Contents/Home ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>V31 migration file exists with correct SQL, CpPoolConfig compiles and provides rank-based lookup</done>
</task>

<task type="auto">
  <name>Task 2: Add PCP/MCP fields to Officer entity</name>
  <files>game-app/src/main/kotlin/com/openlogh/entity/Officer.kt</files>
  <action>
    Add 4 new JPA-mapped fields to Officer entity, placed after the existing `commandPoints` field (line 236):

    ```kotlin
    @Column(nullable = false)
    var pcp: Int = 5,

    @Column(nullable = false)
    var mcp: Int = 5,

    @Column(name = "pcp_max", nullable = false)
    var pcpMax: Int = 5,

    @Column(name = "mcp_max", nullable = false)
    var mcpMax: Int = 5,
    ```

    Keep the existing `commandPoints` field as-is (deprecated but not removed, for backward compat).
    Add a KDoc comment above `commandPoints`: `/** @deprecated Use pcp/mcp instead. Kept for backward compatibility. */`
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && JAVA_HOME=/Users/apple/Library/Java/JavaVirtualMachines/temurin-23.0.2/Contents/Home ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Officer entity has pcp, mcp, pcpMax, mcpMax fields that map to V31 columns</done>
</task>

</tasks>

<verification>
- `V31__add_pcp_mcp_columns.sql` exists and is valid SQL
- `Officer.kt` has 4 new fields: pcp, mcp, pcpMax, mcpMax
- `CpPoolConfig.getMaxPool(0)` returns 5, `getMaxPool(10)` returns 35
- `game-app:compileKotlin` succeeds
</verification>

<success_criteria>
Officer entity and database support dual PCP/MCP pools with rank-based maximums. Compilation succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-command-point-system/03-01-SUMMARY.md`
</output>
