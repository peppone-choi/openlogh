---
phase: 01-deterministic-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/TurnDaemon.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/RealtimeService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/EventService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/EventActionService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/SpecialAssignmentService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/modifier/InheritBuffModifier.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt
autonomous: true
requirements: [FOUND-03, FOUND-04]

must_haves:
  truths:
    - "Every exception catch block in engine code logs the exception with context instead of silently swallowing it"
    - "Entity processing order within a turn tick is deterministic regardless of database query order"
    - "Two generals with identical turnTime are always processed in the same order (by ID)"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt"
      provides: "Logged catch blocks (3 silent blocks fixed) + deterministic sort tiebreaker at line 424"
      contains: "thenBy"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt"
      provides: "Deterministic sort tiebreaker at line 210"
      contains: "thenBy"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/modifier/InheritBuffModifier.kt"
      provides: "Logger declaration + logged catch block"
      contains: "LoggerFactory.getLogger"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt"
      provides: "Logger declaration + logged catch block"
      contains: "LoggerFactory.getLogger"
  key_links:
    - from: "TurnService.executeGeneralCommandsUntil()"
      to: "entity iteration"
      via: "sortedWith(compareBy<...> { it.turnTime }.thenBy { it.id })"
      pattern: "compareBy.*turnTime.*thenBy.*id"
    - from: "InMemoryTurnProcessor.executeGeneralCommandsUntil()"
      to: "entity iteration"
      via: "sortedWith(compareBy<...> { it.turnTime }.thenBy { it.id })"
      pattern: "compareBy.*turnTime.*thenBy.*id"
---

<objective>
Add exception logging to all 16 silent catch blocks across 11 engine files and add deterministic sort tiebreakers to both entity processing locations.

Purpose: Make engine failures observable for debugging parity issues, and ensure entity processing order is deterministic regardless of database query ordering.
Output: Zero silent catch blocks in engine code, deterministic sort in both TurnService and InMemoryTurnProcessor.

Note: This plan depends on Plan 01 because both modify TurnService.kt. Plan 01 must complete first to avoid merge conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deterministic-foundation/01-CONTEXT.md
@.planning/phases/01-deterministic-foundation/01-RESEARCH.md
@.planning/phases/01-deterministic-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Logger variable names per file (MUST match existing convention in each file) -->

| File | Logger Variable | Already Exists |
|------|----------------|----------------|
| TurnService.kt | `logger` | Yes |
| TurnDaemon.kt | `logger` | Yes |
| RealtimeService.kt | `logger` | Yes |
| EconomyService.kt | `log` | Yes |
| EventService.kt | `log` | Yes |
| EventActionService.kt | `log` | Yes |
| GeneralMaintenanceService.kt | `log` | Yes |
| SpecialAssignmentService.kt | `log` | Yes |
| GeneralAI.kt | `logger` | Yes |
| InheritBuffModifier.kt | N/A | NO -- must add |
| WarAftermath.kt | N/A | NO -- must add |

<!-- Silent catch blocks inventory (16 total) -->

| File | Line | Context | Fallback Value |
|------|------|---------|----------------|
| TurnService.kt | 545 | nationCmdResult realtime push | empty catch body |
| TurnService.kt | 776 | startYear from scenario | world.currentYear.toInt() |
| TurnService.kt | 879 | startYear from scenario | world.currentYear.toInt() |
| TurnDaemon.kt | 114 | startTime parse | null |
| TurnDaemon.kt | 120 | opentime parse | false |
| RealtimeService.kt | 133 | realtime push result | empty catch body |
| RealtimeService.kt | 193 | realtime push result | empty catch body |
| RealtimeService.kt | 244 | startYear from scenario | world.currentYear.toInt() |
| EconomyService.kt | 680 | startYear from config | world.currentYear.toInt() |
| EventService.kt | 79 | startYear from scenario | return false |
| EventActionService.kt | 841 | startYear from scenario | world.currentYear.toInt() |
| GeneralMaintenanceService.kt | 49 | retirementYear from config | 80 |
| SpecialAssignmentService.kt | 110 | startYear from config | world.currentYear.toInt() |
| GeneralAI.kt | 114 | city connections load | emptyMap() |
| InheritBuffModifier.kt | 81 | JSON parse buff map | emptyMap() |
| WarAftermath.kt | 455 | occupiedBy JSON parse | attackerNationId |

<!-- Sort tiebreaker locations -->

| File | Line | Current Sort | Fix |
|------|------|-------------|-----|
| TurnService.kt | 424 | `.sortedBy { it.turnTime }` | `.sortedWith(compareBy<General> { it.turnTime }.thenBy { it.id })` |
| InMemoryTurnProcessor.kt | 210 | `.sortedBy { it.turnTime }` | `.sortedWith(compareBy<GeneralSnapshot> { it.turnTime }.thenBy { it.id })` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging to all 16 silent catch blocks in engine code</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnDaemon.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/RealtimeService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EventService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EventActionService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/SpecialAssignmentService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/modifier/InheritBuffModifier.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnDaemon.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/RealtimeService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EventService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EventActionService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/SpecialAssignmentService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/modifier/InheritBuffModifier.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
  </read_first>
  <action>
    Per D-04: Add SLF4J logging to all 16 silent `catch (_: Exception)` blocks. Change `_` to `e` and add a log call. Per D-06: include exception message, stack trace ref, and contextual info. Per D-04: preserve existing behavior -- do NOT change control flow, return values, or fallback logic.

    **For files that lack a logger declaration (InheritBuffModifier.kt, WarAftermath.kt):**
    Add a companion object with logger at the top of the class:
    ```kotlin
    // InheritBuffModifier.kt -- add inside the class body, before first method:
    companion object {
        private val log = LoggerFactory.getLogger(InheritBuffModifier::class.java)
    }
    ```
    ```kotlin
    // WarAftermath.kt -- add inside the class body, before first method:
    companion object {
        private val log = LoggerFactory.getLogger(WarAftermath::class.java)
    }
    ```
    Also add `import org.slf4j.LoggerFactory` to both files if not present.

    **Exact transformations for each catch block (use the EXISTING logger variable name per file):**

    TurnService.kt (uses `logger`):
    - Line 545: `} catch (_: Exception) { }` -> `} catch (e: Exception) { logger.warn("Failed to push nationCmd realtime result: {}", e.message) }`
    - Line 776: `} catch (_: Exception) {` -> `} catch (e: Exception) { logger.warn("Failed to resolve startYear for scenario {}: {}", world.scenarioCode, e.message)`
    - Line 879: same pattern as 776, same log message format

    TurnDaemon.kt (uses `logger`):
    - Line 114: `try { OffsetDateTime.parse(it) } catch (_: Exception) { null }` -> `try { OffsetDateTime.parse(it) } catch (e: Exception) { logger.warn("Failed to parse startTime '{}': {}", it, e.message); null }`
    - Line 120: `} catch (_: Exception) {` -> `} catch (e: Exception) { logger.warn("Failed to parse opentime '{}': {}", opentime, e.message)`

    RealtimeService.kt (uses `logger`):
    - Line 133: `} catch (_: Exception) { }` -> `} catch (e: Exception) { logger.warn("Failed to push realtime result: {}", e.message) }`
    - Line 193: same pattern as 133
    - Line 244: `} catch (_: Exception) {` -> `} catch (e: Exception) { logger.warn("Failed to resolve startYear for scenario {}: {}", world.scenarioCode, e.message)`

    EconomyService.kt (uses `log`):
    - Line 680: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to resolve startYear from config: {}", e.message)`

    EventService.kt (uses `log`):
    - Line 79: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to resolve startYear for scenario {}: {}", world.scenarioCode, e.message)`

    EventActionService.kt (uses `log`):
    - Line 841: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to resolve startYear for scenario {}: {}", world.scenarioCode, e.message)`

    GeneralMaintenanceService.kt (uses `log`):
    - Line 49: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to resolve retirementYear from config: {}", e.message)`

    SpecialAssignmentService.kt (uses `log`):
    - Line 110: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to resolve startYear from config: {}", e.message)`

    GeneralAI.kt (uses `logger`):
    - Line 114: `} catch (_: Exception) {` -> `} catch (e: Exception) { logger.warn("Failed to load city connections for map {}: {}", world.mapCode, e.message)`

    InheritBuffModifier.kt (uses newly-added `log`):
    - Line 81: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to parse buff JSON: {}", e.message)`

    WarAftermath.kt (uses newly-added `log`):
    - Line 455: `} catch (_: Exception) {` -> `} catch (e: Exception) { log.warn("Failed to parse occupiedBy for city, using attacker nation {}: {}", attackerNationId, e.message)`

    IMPORTANT: Only change `_` to `e` and add log call. Do NOT alter the catch body's return/fallback value. Do NOT add try/catch where none exists. Do NOT change any other code in these files.
  </action>
  <verify>
    <automated>cd backend && grep -rn "catch (_:" game-app/src/main/kotlin/com/opensam/engine/ | grep -v "test/" | grep -v "build/" ; echo "Count: $(grep -rc 'catch (_:' game-app/src/main/kotlin/com/opensam/engine/ --include='*.kt' 2>/dev/null | awk -F: '{s+=$2}END{print s}')"</automated>
    The grep must return zero matches (Count: 0). If any `catch (_:` patterns remain, the task is not complete.
  </verify>
  <acceptance_criteria>
    - `grep -rn "catch (_:" backend/game-app/src/main/kotlin/com/opensam/engine/` returns 0 results
    - InheritBuffModifier.kt contains `LoggerFactory.getLogger(InheritBuffModifier::class.java)`
    - WarAftermath.kt contains `LoggerFactory.getLogger(WarAftermath::class.java)`
    - All 16 catch blocks now contain either `logger.warn` or `log.warn` calls
    - No catch block has altered control flow (same return/fallback values as before)
    - `cd backend && ./gradlew :game-app:test -x :gateway-app:test --no-daemon` compiles and all tests pass
  </acceptance_criteria>
  <done>All 16 silent catch blocks now log exception message and context via SLF4J. InheritBuffModifier and WarAftermath have logger declarations added. No control flow changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic sort tiebreakers to entity processing</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt
  </read_first>
  <action>
    Per D-07 and D-08: Add secondary sort by ID to both entity processing locations.

    **TurnService.kt line 424:**
    Change:
    ```kotlin
    val generals = ports.allGenerals().map { it.toEntity() }.sortedBy { it.turnTime }
    ```
    To:
    ```kotlin
    val generals = ports.allGenerals().map { it.toEntity() }
        .sortedWith(compareBy<com.opensam.entity.General> { it.turnTime }.thenBy { it.id })
    ```
    Note: Use the fully-qualified `com.opensam.entity.General` type parameter if the simple `General` is ambiguous in that scope. Check imports -- if `General` is already imported unambiguously, use just `General`.

    **InMemoryTurnProcessor.kt line 210:**
    Change:
    ```kotlin
    val generals = state.generals.values.sortedBy { it.turnTime }
    ```
    To:
    ```kotlin
    val generals = state.generals.values.sortedWith(
        compareBy<GeneralSnapshot> { it.turnTime }.thenBy { it.id }
    )
    ```
    Note: Check if `GeneralSnapshot` is the correct type name by reading the file's imports and the `state.generals` map type. Use the actual type used in the map values.

    Also run `grep -rn "sortedBy.*turnTime" backend/game-app/src/main/kotlin/com/opensam/engine/` to verify no other locations were missed. If additional locations are found, apply the same `.thenBy { it.id }` fix.

    Per D-08: These changes are at the processor/pipeline level, not at the repository/query level. Do NOT modify any repository or query code.
  </action>
  <verify>
    <automated>cd backend && grep -rn "sortedBy { it.turnTime }" game-app/src/main/kotlin/com/opensam/engine/ ; echo "Remaining single-field sorts: $(grep -rc 'sortedBy { it.turnTime }' game-app/src/main/kotlin/com/opensam/engine/ --include='*.kt' 2>/dev/null | awk -F: '{s+=$2}END{print s}')"</automated>
    The grep must return zero matches (Remaining single-field sorts: 0). All turnTime sorts should now use `sortedWith(compareBy...thenBy)`.
  </verify>
  <acceptance_criteria>
    - `grep -rn "sortedBy { it.turnTime }" backend/game-app/src/main/kotlin/com/opensam/engine/` returns 0 results
    - TurnService.kt contains `compareBy` and `thenBy { it.id }` near former line 424
    - InMemoryTurnProcessor.kt contains `compareBy` and `thenBy { it.id }` near former line 210
    - No repository or query-level code was modified
    - `cd backend && ./gradlew :game-app:test -x :gateway-app:test --no-daemon` compiles and all tests pass
  </acceptance_criteria>
  <done>Both TurnService and InMemoryTurnProcessor sort generals by turnTime with ID tiebreaker. Entity processing order is deterministic regardless of database query ordering.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -rn "catch (_:" backend/game-app/src/main/kotlin/com/opensam/engine/` returns 0 results
2. `grep -rn "sortedBy { it.turnTime }" backend/game-app/src/main/kotlin/com/opensam/engine/` returns 0 results
3. `cd backend && ./gradlew :game-app:test -x :gateway-app:test --no-daemon` -- full game-app test suite green
</verification>

<success_criteria>
- Zero silent catch blocks (`catch (_:`) in engine code
- All 16 catch blocks log with SLF4J including exception message and contextual info
- InheritBuffModifier and WarAftermath have logger declarations
- Both turnTime sort locations use thenBy { it.id } tiebreaker
- No control flow changes in catch blocks
- No repository/query-level sort changes
- Full game-app test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-deterministic-foundation/01-02-SUMMARY.md`
</output>
