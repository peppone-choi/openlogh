---
phase: 02-numeric-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/test/kotlin/com/opensam/engine/ShortOverflowGuardTest.kt
  - backend/game-app/src/test/kotlin/com/opensam/engine/IntegerDivisionParityTest.kt
  - backend/game-app/src/test/kotlin/com/opensam/engine/RoundingParityTest.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/NpcSpawnService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/StatChangeService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleEngine.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleTrigger.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleService.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/war/WarUnitGeneral.kt
autonomous: true
requirements: [TYPE-01, TYPE-03]

must_haves:
  truths:
    - "No Short field can silently wrap past 32767/-32768 -- every .toShort() in engine service files is preceded by a coerceIn with domain-appropriate bounds"
    - "Integer division on negative operands in Kotlin matches PHP intdiv() behavior for all tested combinations"
    - "All existing tests still pass after coerceIn guards are added (no behavioral regression)"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/ShortOverflowGuardTest.kt"
      provides: "Unit tests verifying coerceIn guards prevent overflow at domain boundaries"
      contains: "class ShortOverflowGuardTest"
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/IntegerDivisionParityTest.kt"
      provides: "Unit tests verifying Kotlin integer division matches PHP intdiv for positive and negative operands"
      contains: "class IntegerDivisionParityTest"
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/RoundingParityTest.kt"
      provides: "Wave 0 test scaffold for TYPE-02 rounding parity (used by Plan 02)"
      contains: "class RoundingParityTest"
  key_links:
    - from: "ShortOverflowGuardTest.kt"
      to: "EconomyService.kt, GeneralMaintenanceService.kt, NpcSpawnService.kt, TurnService.kt"
      via: "Tests call service methods with boundary-value inputs and verify Short fields stay within bounds"
      pattern: "coerceIn.*toShort"
    - from: "IntegerDivisionParityTest.kt"
      to: "engine service files"
      via: "Tests verify Kotlin a/b matches PHP intdiv(a,b) for negative operand combinations"
      pattern: "assertEquals.*intdiv"
---

<objective>
Add coerceIn overflow guards to all Short entity field assignments in engine service files (TYPE-01), verify integer division parity with PHP for negative operands (TYPE-03), and create Wave 0 test scaffolds for all three phase requirements.

Purpose: Prevent silent Short overflow wrap-around that causes entity field values to diverge from legacy PHP, and confirm integer division produces identical results for all operand sign combinations.

Output: 3 new test files (ShortOverflowGuardTest, IntegerDivisionParityTest, RoundingParityTest scaffold), coerceIn guards added to all unguarded .toShort() sites in 10+ engine files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-numeric-type-safety/02-CONTEXT.md
@.planning/phases/02-numeric-type-safety/02-RESEARCH.md

@backend/game-app/src/main/kotlin/com/opensam/service/ItemService.kt
@backend/game-app/src/main/kotlin/com/opensam/entity/General.kt
@backend/game-app/src/main/kotlin/com/opensam/entity/City.kt
@backend/game-app/src/main/kotlin/com/opensam/entity/Nation.kt
@backend/game-app/src/test/kotlin/com/opensam/engine/FormulaParityTest.kt

<interfaces>
<!-- Short field domain bounds (from RESEARCH.md, verified against entity source) -->
<!-- Every .toShort() assignment must use coerceIn(low, high) with these bounds per D-01/D-02/D-03 -->

General Short fields and bounds:
  npcState:       -1..9
  affinity:       0..150
  bornYear:       0..32767
  deadYear:       0..32767
  leadership:     0..100
  leadershipExp:  0..1000
  strength:       0..100
  strengthExp:    0..1000
  intel:          0..100
  intelExp:       0..1000
  politics:       0..100
  politicsExp:    0..1000
  charm:          0..100
  charmExp:       0..1000
  injury:         0..80
  officerLevel:   0..20
  crewType:       0..50
  train:          0..110
  atmos:          0..150
  newmsg:         0..1
  makeLimit:      0..32767
  killTurn:       -32768..32767  (nullable, can be negative during processing)
  blockState:     0..2
  dedLevel:       0..30
  expLevel:       0..255
  age:            0..120
  startAge:       0..120
  belong:         0..12
  betray:         0..10
  specAge:        0..100
  spec2Age:       0..100
  defenceTrain:   0..100
  tournamentState: 0..3

City Short fields and bounds:
  level:          0..5
  supplyState:    0..1
  frontState:     0..2
  state:          0..10
  region:         0..20
  term:           0..32767

Nation Short fields and bounds:
  bill:           0..200
  rate:           0..100
  rateTmp:        0..100
  secretLimit:    0..20
  scoutLevel:     0..5
  warState:       0..3
  strategicCmdLimit: 0..72
  surrenderLimit: 0..120
  level:          0..9

Diplomacy: term: 0..32767
Emperor: year: 0..32767, month: 1..12
GeneralTurn: turnIdx: 0..20
Event: priority: 0..100

<!-- Existing guard pattern (from ItemService.kt lines 127-137) -->
general.injury = (general.injury - item.value).coerceIn(0, 80).toShort()
general.atmos = (general.atmos + item.value).coerceIn(0, 150).toShort()
general.train = (general.train + item.value).coerceIn(0, 110).toShort()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffolds for Short overflow, integer division, and rounding parity</name>
  <files>
    backend/game-app/src/test/kotlin/com/opensam/engine/ShortOverflowGuardTest.kt
    backend/game-app/src/test/kotlin/com/opensam/engine/IntegerDivisionParityTest.kt
    backend/game-app/src/test/kotlin/com/opensam/engine/RoundingParityTest.kt
  </files>
  <read_first>
    backend/game-app/src/test/kotlin/com/opensam/engine/FormulaParityTest.kt
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/General.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/City.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/Nation.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
  </read_first>
  <behavior>
    ShortOverflowGuardTest:
    - Test: arithmetic that would produce value > 32767 on a Short field gets clamped to upper domain bound (e.g., leadership computed as 150 -> clamped to 100)
    - Test: arithmetic that would produce value < lower bound gets clamped to lower domain bound (e.g., injury computed as -5 -> clamped to 0)
    - Test: boundary values (exactly at min, exactly at max, one above max, one below min) for representative fields: leadership (0-100), train (0-110), atmos (0-150), injury (0-80), bill (0-200)
    - Test: normal-range values pass through unchanged

    IntegerDivisionParityTest:
    - Test: positive / positive -> same result in Kotlin and PHP intdiv (7/2=3, 10/3=3)
    - Test: negative / positive -> same truncation toward zero (-7/2=-3, -10/3=-3)
    - Test: positive / negative -> same truncation toward zero (7/-2=-3, 10/-3=-3)
    - Test: negative / negative -> same truncation toward zero (-7/-2=3, -10/-3=3)
    - Test: zero dividend -> 0
    - Test: dividend smaller than divisor -> 0
    - Test: game-relevant values: gold=-500 divided by 3, population=10000 divided by 6

    RoundingParityTest (scaffold only -- tests will initially FAIL, fixed in Plan 02):
    - Test: PHP (int) cast matches Kotlin .toInt() for 2.3, 2.5, 2.7, -2.3, -2.5, -2.7
    - Test: PHP round() golden values: round(2.5)=3, round(1.5)=2, round(-2.5)=-3, round(0.5)=1
    - Test: Math.round() vs roundToInt() divergence at .5 boundaries
  </behavior>
  <action>
    Create three test files following the existing FormulaParityTest pattern (package com.opensam.engine, JUnit Jupiter, assertEquals).

    **ShortOverflowGuardTest.kt**: Per D-03, test that service methods producing Short field assignments clamp to domain-appropriate bounds. Create helper functions that simulate the arithmetic pattern `(existingValue + delta).coerceIn(min, max).toShort()` for each field category:
    - Stats (leadership, strength, intel, politics, charm): bounds 0..100
    - Stat exp (leadershipExp, strengthExp, intelExp, politicsExp, charmExp): bounds 0..1000
    - Military (train 0..110, atmos 0..150, injury 0..80, defenceTrain 0..100)
    - Nation economy (bill 0..200, rate 0..100, rateTmp 0..100)
    - Test overflow: input=40000 -> clamped to upper bound, input=-5000 -> clamped to lower bound
    - Test passthrough: input within bounds -> unchanged

    **IntegerDivisionParityTest.kt**: Per D-08/D-09, verify Kotlin `/` operator matches PHP `intdiv()` for all sign combinations. Use a parameterized test with `@CsvSource`:
    ```
    dividend, divisor, expected
    7, 2, 3
    -7, 2, -3
    7, -2, -3
    -7, -2, 3
    10, 3, 3
    -10, 3, -3
    0, 5, 0
    3, 10, 0
    -500, 3, -166
    10000, 6, 1666
    ```
    Also verify that Kotlin truncation toward zero is confirmed: `(-7) / 2 == -3` (not -4 which would be floor division). Per D-09, if all pass, no `phpIntdiv()` utility is needed.

    **RoundingParityTest.kt**: Per D-05, create the PHP-to-Kotlin golden comparison table as tests. Mark the rounding normalization tests with `@Disabled("Fixed in Plan 02 -- TYPE-02 rounding normalization")` so they compile but don't fail the build. Include:
    - `phpIntCast` tests: verify `.toInt()` matches PHP `(int)` for positive/negative/fractional values
    - `phpRound` tests: golden values for PHP `round()` -- these will be enabled when Plan 02 normalizes the rounding calls
    - A comment block documenting the full rounding comparison table from RESEARCH.md

    All three files: use `package com.opensam.engine`, import `org.junit.jupiter.api.Assertions.assertEquals`, `org.junit.jupiter.api.Test`, `org.junit.jupiter.params.ParameterizedTest` where needed.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :game-app:test --tests "com.opensam.engine.ShortOverflowGuardTest" --tests "com.opensam.engine.IntegerDivisionParityTest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/game-app/src/test/kotlin/com/opensam/engine/ShortOverflowGuardTest.kt` exists and contains `class ShortOverflowGuardTest`
    - File `backend/game-app/src/test/kotlin/com/opensam/engine/IntegerDivisionParityTest.kt` exists and contains `class IntegerDivisionParityTest`
    - File `backend/game-app/src/test/kotlin/com/opensam/engine/RoundingParityTest.kt` exists and contains `class RoundingParityTest`
    - ShortOverflowGuardTest contains tests for at least 5 field categories (stats, stat-exp, military, nation-economy, boundary)
    - IntegerDivisionParityTest contains `@CsvSource` with at least 8 dividend/divisor/expected rows including negative operands
    - RoundingParityTest contains `@Disabled` annotation on PHP round() tests
    - `cd backend && ./gradlew :game-app:test --tests "com.opensam.engine.ShortOverflowGuardTest" --tests "com.opensam.engine.IntegerDivisionParityTest" -x :gateway-app:test` exits 0
  </acceptance_criteria>
  <done>All three test files compile and pass (ShortOverflow + IntegerDivision green, RoundingParity scaffold with @Disabled tests). Wave 0 test infrastructure ready for both Plan 01 Task 2 and Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Add coerceIn overflow guards to all unguarded .toShort() assignments in engine service files</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/NpcSpawnService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/StatChangeService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleEngine.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleTrigger.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarUnitGeneral.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/opensam/service/ItemService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/GeneralMaintenanceService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/NpcSpawnService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/StatChangeService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleEngine.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleTrigger.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/BattleService.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarAftermath.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/war/WarUnitGeneral.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/General.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/City.kt
    backend/game-app/src/main/kotlin/com/opensam/entity/Nation.kt
  </read_first>
  <action>
    Per D-01 (guards at domain boundaries), D-02 (follow ItemService.kt pattern), and D-03 (domain-appropriate bounds from legacy PHP):

    **Audit method:** For each file listed, search for every `.toShort()` call. For each occurrence:
    1. Identify which entity field is being assigned (e.g., `general.leadership = ...toShort()`)
    2. Look up the domain bound from the interfaces block above
    3. If no `coerceIn`/`coerceAtLeast`/`coerceAtMost` precedes the `.toShort()`, add `.coerceIn(low, high)` before `.toShort()`
    4. If a guard already exists (like in ItemService), verify the bounds match the domain table and leave unchanged

    **Pattern to apply (from ItemService.kt D-02):**
    ```kotlin
    // BEFORE (unguarded):
    general.leadership = (computedValue).toShort()
    // AFTER (guarded):
    general.leadership = (computedValue).coerceIn(0, 100).toShort()
    ```

    **File-specific guidance:**

    EconomyService.kt (~6 .toShort()): Guard city field assignments (city.pop, city.trust adjustments) and nation field assignments (nation.bill, nation.rate). City pop is Int not Short -- skip Int fields. Focus on Short fields only.

    GeneralMaintenanceService.kt (~7 .toShort()): Guard general stat assignments (age, belong, injury, stat fields). Some already use the `scaledStatWithFloor` pattern with coerce -- verify those bounds match the table, add guards where missing.

    NpcSpawnService.kt (~34 .toShort()): Heavy .toShort() usage for NPC general stat generation. Guard all stat fields (leadership 0..100, strength 0..100, intel 0..100, politics 0..100, charm 0..100), exp fields (0..1000), and military fields (train 0..110, atmos 0..150, crewType 0..50).

    StatChangeService.kt (~5 .toShort()): Guard stat change operations (stat increments/decrements).

    TurnService.kt (~13 .toShort()): Guard general field assignments during turn processing. Many are year/month (0..32767) and indices (turnIdx 0..20).

    GeneralAI.kt (~6 .toShort()): Guard AI-computed field assignments (crewType, train, atmos, stats).

    BattleEngine.kt (~0 .toShort() but check): Verify no unguarded Short assignments in damage calculations.

    BattleTrigger.kt (~0 .toShort() but check): Verify trigger result assignments.

    BattleService.kt (~5 .toShort()): Guard crew/train/atmos assignments after battle.

    WarAftermath.kt (~9 .toShort() via coerce): Already has coerce guards -- verify bounds match the domain table.

    WarUnitGeneral.kt (~6 .toShort() via coerce): Already has coerce guards -- verify bounds match the domain table.

    **CRITICAL rules:**
    - Do NOT add guards to Int fields (gold, rice, crew, experience, dedication, dex1-5) -- these are already Int for good reason (per RESEARCH anti-pattern)
    - Do NOT change entity setter definitions (per D-01 -- guards at service layer only)
    - Do NOT modify adjacent code, formatting, or imports beyond what is needed for the guard (per Surgical Changes principle)
    - Match existing style: `(expression).coerceIn(low, high).toShort()` -- no line breaks in the chain
  </action>
  <verify>
    <automated>cd backend && ./gradlew :game-app:test -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - Every `.toShort()` in EconomyService.kt is preceded by `coerceIn` or `coerceAtLeast` or `coerceAtMost`
    - Every `.toShort()` in GeneralMaintenanceService.kt is preceded by a coerce guard
    - Every `.toShort()` in NpcSpawnService.kt is preceded by a coerce guard
    - Every `.toShort()` in StatChangeService.kt is preceded by a coerce guard
    - Every `.toShort()` in TurnService.kt is preceded by a coerce guard
    - Every `.toShort()` in GeneralAI.kt is preceded by a coerce guard
    - Every `.toShort()` in BattleService.kt is preceded by a coerce guard
    - Grep command `cd backend && grep -rn "\.toShort()" game-app/src/main/kotlin/com/opensam/engine/ | grep -v "coerceIn\|coerceAtLeast\|coerceAtMost"` returns empty or only false positives (e.g., inside test utility methods, type conversions on constants)
    - `cd backend && ./gradlew :game-app:test -x :gateway-app:test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>All .toShort() assignments in engine service files have domain-appropriate coerceIn guards following the ItemService.kt pattern. No Short field can silently overflow. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `cd backend && grep -rn "\.toShort()" game-app/src/main/kotlin/com/opensam/engine/ | grep -v "coerceIn\|coerceAtLeast\|coerceAtMost" | grep -v "test/"` -- should return empty or only non-assignment usages
2. `cd backend && ./gradlew :game-app:test --tests "com.opensam.engine.ShortOverflowGuardTest" --tests "com.opensam.engine.IntegerDivisionParityTest" -x :gateway-app:test` -- both pass
3. `cd backend && ./gradlew :game-app:test -x :gateway-app:test` -- full game-app suite green
</verification>

<success_criteria>
- Zero unguarded .toShort() assignments in engine service files (TYPE-01 complete)
- Integer division parity confirmed for all sign combinations (TYPE-03 complete)
- Wave 0 test scaffolds created for all three requirements (ShortOverflowGuardTest, IntegerDivisionParityTest, RoundingParityTest)
- Full game-app test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-numeric-type-safety/02-01-SUMMARY.md`
</output>
