---
phase: 08-npc-ai-parity
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
autonomous: true
requirements: [AI-01, AI-04]

must_haves:
  truths:
    - "doSortie selects the same attack target as PHP do출병 for identical war state"
    - "doRecruit selects the same recruitment parameters as PHP do징병"
    - "doCombatPrep returns the same action as PHP do전투준비"
    - "doWarpToFront/doWarpToRear/doWarpToDomestic select the same warp destination as PHP"
    - "doRally returns the same rally action as PHP do집합"
    - "doDismiss returns the same dismiss action as PHP do소집해제"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt"
      provides: "Golden value tests for 7 military do*() methods"
      contains: "doSortie golden value"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt"
      provides: "Fixed military do*() methods where divergences found"
  key_links:
    - from: "GeneralAI.kt::doSortie()"
      to: "calcWarRoute / mapService adjacency"
      via: "war target selection"
      pattern: "doSortie.*attackable.*warTargetNations"
    - from: "GeneralAI.kt::doRecruit()"
      to: "general.crew / city.pop"
      via: "recruitment calculation"
      pattern: "doRecruit.*crew.*train"
---

<objective>
Verify 7 military AI do*() methods (do출병, do징병, do전투준비, do전방워프, do후방워프, do집합, do소집해제) produce identical decisions to PHP GeneralAI.php. Fix any divergences found during PHP manual tracing. Lock golden values.

Purpose: Military AI drives NPC attack/defense behavior -- the most visible NPC actions in gameplay.
Output: Golden value parity tests for all 7 military methods + fixes for any discovered divergences
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-npc-ai-parity/08-CONTEXT.md
@.planning/phases/08-npc-ai-parity/08-RESEARCH.md
@.planning/phases/08-npc-ai-parity/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: new categorizeNationGeneral and corrected DiplomacyState -->
<!-- Executor should read 08-01-SUMMARY.md for exact signatures after Plan 01 completes -->

From GeneralAI.kt (existing method signatures for military do*()):
```kotlin
private fun doSortie(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy,
                     attackable: Boolean, warTargetNations: Map<Long, Int>): String?  // L1824
private fun doRecruit(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy,
                      attackable: Boolean): String?  // L1647
private fun doCombatPrep(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?  // L1796
private fun doWarpToFront(ctx: AIContext, rng: Random): String?  // L1873
private fun doWarpToRear(ctx: AIContext, rng: Random): String?  // L1906
private fun doRally(general: General, rng: Random): String  // L2630
private fun doDismiss(ctx: AIContext, rng: Random, attackable: Boolean): String?  // L2137
```

From GeneralAITest.kt (existing test setup pattern):
```kotlin
private fun createWorld(year: Short = 200, month: Short = 3): WorldState
private fun createGeneral(id: Long = 1, nationId: Long = 1, cityId: Long = 1, ...): General
// Uses mock(GeneralRepository::class.java) etc. for JpaWorldPortFactory
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Military do*() golden value parity -- 출병/징병/전투준비/전방워프</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </read_first>
  <behavior>
    - Test: doSortie with war state + adjacent enemy city returns "출병" with correct destCityId
    - Test: doSortie without attackable returns null
    - Test: doSortie guard: crew < threshold returns null (PHP minCrew check)
    - Test: doRecruit in rear city with low crew returns "징병" with correct amount
    - Test: doRecruit guard: city.pop below threshold returns null
    - Test: doCombatPrep with low train/atmos returns "전투준비"
    - Test: doCombatPrep guard: train >= 100 AND atmos >= 100 returns null
    - Test: doWarpToFront with general in rear city + front city exists returns warp action
    - Test: doWarpToFront guard: already in front city returns null
  </behavior>
  <action>
    **Per D-02 and D-05:** For each of the 4 military methods, read the PHP source, manually trace with fixed inputs, and lock golden values.

    **do출병 (doSortie) -- PHP line 2706:**
    1. Read PHP do출병() at GeneralAI.php L2706-2860. Key PHP logic:
       - Guard: diplomacyState must be d직전(3) or d전쟁(4)
       - Guard: general must have crew >= minCrew (from policy, default 100)
       - Guard: train >= minTrain (default 80), atmos >= minAtmos (default 50)
       - Guard: frontState check on current city -- PHP checks `frontState > 0` for general's city
       - Target selection: iterates warTargetNations, finds adjacent enemy cities, picks by calcWarRoute distance
       - Dest stored in reserved command for multi-turn movement
    2. Compare with Kotlin doSortie() at L1824. Fix any guard or target selection divergences found.
    3. Create golden value test: fixed general (leadership=80, strength=70, intel=50, crew=3000, train=95, atmos=95) in war state with adjacent enemy city. PHP manual trace determines expected return value.

    **do징병 (doRecruit) -- PHP line 2483:**
    4. Read PHP do징병() at GeneralAI.php L2483-2650. Key PHP logic:
       - Guard: diplomacyState >= d선포(1) for war recruitment, else only if crew < minDefCrew
       - Guard: city.pop must be > threshold (usually pop > 1000)
       - Amount calculation: min(maxCrew - crew, city.pop * recruitRate)
       - recruitRate varies by npcType and diplomacyState
    5. Compare with Kotlin doRecruit() at L1647. Fix divergences.
    6. Create golden value test: general with crew=500 in city with pop=10000, diplomacyState=RECRUITING.

    **do전투준비 (doCombatPrep) -- PHP line 2653:**
    7. Read PHP do전투준비() at GeneralAI.php L2653-2683. Key PHP logic:
       - Guard: train < maxTrain (100) OR atmos < maxAtmos (100)
       - Priority: train first if train < atmos, else atmos first
    8. Compare with Kotlin doCombatPrep() at L1796. Fix divergences.
    9. Create golden value test: general with train=60, atmos=80.

    **do전방워프 (doWarpToFront) -- PHP line 2972:**
    10. Read PHP do전방워프() at GeneralAI.php L2972-3020. Key PHP logic:
        - Guard: general NOT in front city already
        - Guard: front cities exist
        - Target: front city with most need (lowest troop count or closest to enemy)
    11. Compare with Kotlin doWarpToFront() at L1873. Fix divergences.
    12. Create golden value test: general in rear city, 2 front cities with different troop counts.

    **Per D-06:** Add all tests to existing GeneralAITest.kt in a new `@Nested` class `MilitaryAIParityTests`.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.engine.ai.GeneralAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAITest.kt contains `@Nested` class or section with "MilitaryAI" or "military" in name
    - GeneralAITest.kt contains at least 2 test methods with "doSortie" or "출병" in name
    - GeneralAITest.kt contains at least 2 test methods with "doRecruit" or "징병" in name
    - GeneralAITest.kt contains at least 1 test method with "doCombatPrep" or "전투준비" in name
    - GeneralAITest.kt contains at least 1 test method with "doWarpToFront" or "전방워프" in name
    - All tests pass including existing tests
  </acceptance_criteria>
  <done>
    doSortie, doRecruit, doCombatPrep, doWarpToFront each have 1-2 golden value tests + 1 guard test. Any PHP divergences in these 4 methods are fixed. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Military do*() golden value parity -- 후방워프/집합/소집해제</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </read_first>
  <behavior>
    - Test: doWarpToRear with general in front city + rear city exists returns warp action
    - Test: doWarpToRear guard: already in rear city returns null
    - Test: doRally always returns "집합" for troop leader
    - Test: doRally with position data returns movement action matching PHP
    - Test: doDismiss with general having crew > 0 + not attackable returns "소집해제"
    - Test: doDismiss guard: attackable=true returns null (don't dismiss during war)
    - Test: doDismiss guard: crew=0 returns null (nothing to dismiss)
  </behavior>
  <action>
    **Per D-02 and D-05:** Continue military method verification for remaining 3 methods.

    **do후방워프 (doWarpToRear) -- PHP line 2866:**
    1. Read PHP do후방워프() at GeneralAI.php L2866-2970. Key PHP logic:
       - Guard: general in front city (frontState > 0)
       - Guard: rear cities exist
       - Condition: civil general type OR crew below minDefCrew OR injured
       - Target: rear city with best development potential
    2. Compare with Kotlin doWarpToRear() at L1906. Fix divergences.
    3. Create golden value test: civil-type general in front city with rear cities available.

    **do집합 (doRally) -- PHP line 3111:**
    4. Read PHP do집합() at GeneralAI.php L3111-3125. Key PHP logic:
       - For npcState=5 troop leaders: always returns "집합"
       - May include position-based movement toward rally point stored in meta
    5. Compare with Kotlin doRally() at L2630. Fix divergences.
    6. Create golden value test.

    **do소집해제 (doDismiss) -- PHP line 2684:**
    7. Read PHP do소집해제() at GeneralAI.php L2684-2705. Key PHP logic:
       - Guard: crew > 0 (has troops to dismiss)
       - Guard: NOT attackable (not during active war)
       - Guard: diplomacyState <= d선포(1) (peacetime or early declaration only)
       - May also check train/atmos thresholds
    8. Compare with Kotlin doDismiss() at L2137. Fix divergences.
    9. Create golden value test: general with crew=2000 in peacetime.

    **Per D-06:** Add all tests to existing GeneralAITest.kt in the same `@Nested` class as Task 1.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.engine.ai.GeneralAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAITest.kt contains at least 1 test method with "doWarpToRear" or "후방워프" in name
    - GeneralAITest.kt contains at least 1 test method with "doRally" or "집합" in name
    - GeneralAITest.kt contains at least 2 test methods with "doDismiss" or "소집해제" in name
    - All tests pass including tests from Task 1
  </acceptance_criteria>
  <done>
    doWarpToRear, doRally, doDismiss each have 1-2 golden value tests + guard tests. Any PHP divergences in these 3 methods are fixed. All 7 military methods verified. All tests pass.
  </done>
</task>

</tasks>

<verification>
- All tests in com.opensam.engine.ai.GeneralAITest pass
- 7 military do*() methods each have at least 1 golden value test
- Each golden value derived from PHP manual trace (not assumed)
- Guard conditions match PHP for all 7 methods
- Full game-app test suite passes
</verification>

<success_criteria>
- 14+ new test methods covering 7 military do*() methods
- Each method has golden value(s) + guard condition tests
- All divergences between PHP and Kotlin military methods fixed
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-npc-ai-parity/08-02-SUMMARY.md`
</output>
