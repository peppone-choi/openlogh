---
phase: 08-npc-ai-parity
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  - backend/game-app/src/test/kotlin/com/opensam/engine/ai/NationAITest.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
autonomous: true
requirements: [AI-01, AI-04]

must_haves:
  truths:
    - "doNormalDomestic selects the same city stat to develop as PHP do일반내정 for identical city stats"
    - "doUrgentDomestic selects the same stat as PHP do긴급내정"
    - "doWarDomestic selects the same stat as PHP do전쟁내정"
    - "doTradeResources buys the same resource type as PHP do금쌀구매"
    - "doDonate/doNpcDedicate donates the same amount as PHP doNPC헌납"
    - "doReturn returns to the same city as PHP do귀환"
    - "doWarpToDomestic warps to the same city as PHP do내정워프"
    - "chooseTexRate/chooseGoldBillRate/chooseRiceBillRate produce the same rates as PHP"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt"
      provides: "Golden value tests for 10 domestic/economy do*() methods"
      contains: "doNormalDomestic golden value"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt"
      provides: "Fixed domestic do*() methods where divergences found"
  key_links:
    - from: "GeneralAI.kt::doNormalDomestic()"
      to: "calcCityDevelRate / calcCityDevScore"
      via: "weighted stat selection"
      pattern: "doNormalDomestic.*calcCity"
    - from: "GeneralAI.kt::chooseTexRate()"
      to: "nation.rate / supplyCities income"
      via: "tax rate calculation"
      pattern: "chooseTexRate.*rate"
---

<objective>
Verify 10 domestic/economy AI do*() methods (do일반내정, do긴급내정, do전쟁내정, do금쌀구매, doNPC헌납, do귀환, do내정워프, chooseTexRate, chooseGoldBillRate, chooseRiceBillRate) produce identical decisions to PHP GeneralAI.php. Fix any divergences found during PHP manual tracing. Lock golden values.

Purpose: Domestic AI controls NPC city development and resource management -- the economic backbone of NPC nations.
Output: Golden value parity tests for all 10 domestic methods + fixes for any discovered divergences
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-npc-ai-parity/08-CONTEXT.md
@.planning/phases/08-npc-ai-parity/08-RESEARCH.md
@.planning/phases/08-npc-ai-parity/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: corrected DiplomacyState, categorizeNationGeneral, priority lists -->

From GeneralAI.kt (existing method signatures for domestic do*()):
```kotlin
private fun doNormalDomestic(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?  // L1424
private fun doUrgentDomestic(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?  // L1532
private fun doWarDomestic(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?     // L1557
private fun doTradeResources(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?  // L2032
private fun doDonate(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?          // L2079
private fun doNpcDedicate(ctx: AIContext, rng: Random, nationPolicy: NpcNationPolicy): String?     // L3561
private fun doReturn(ctx: AIContext, rng: Random): String?                                         // L2014
private fun doWarpToDomestic(ctx: AIContext, rng: Random): String?                                 // L1952
fun chooseTexRate(ctx: AIContext, supplyCities: List<City>): Int                                   // L2912
fun chooseGoldBillRate(ctx: AIContext, supplyCities: List<City>, policy: NpcNationPolicy): Int     // L2942
fun chooseRiceBillRate(ctx: AIContext, supplyCities: List<City>, policy: NpcNationPolicy): Int     // L2985

// Helper methods used by domestic AI
private fun calcCityDevelRate(city: City): Map<String, Pair<Double, Int>>  // L381
private fun calcCityDevScore(city: City): Double                           // L414
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domestic do*() golden value parity -- 일반내정/긴급내정/전쟁내정/금쌀구매/NPC헌납/귀환/내정워프</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </read_first>
  <behavior>
    - Test: doNormalDomestic with city agri=200, comm=300, secu=100, def=150 selects secu (lowest) via weighted random
    - Test: doNormalDomestic guard: all stats at max returns null
    - Test: doUrgentDomestic in war with city wall=50 selects wall repair
    - Test: doWarDomestic with front city + low def selects def improvement
    - Test: doTradeResources with low gold + adequate rice returns "금매매" (buy gold)
    - Test: doTradeResources with low rice + adequate gold returns "쌀매매" (buy rice)
    - Test: doTradeResources guard: both adequate returns null
    - Test: doDonate/doNpcDedicate with excess resources returns donation action
    - Test: doReturn with general not in home/assigned city returns "귀환"
    - Test: doReturn guard: already in assigned city returns null
    - Test: doWarpToDomestic with general in non-domestic city + domestic city exists returns warp
  </behavior>
  <action>
    **Per D-02 and D-05:** For each domestic method, read the PHP source, manually trace with fixed inputs, and lock golden values.

    **do일반내정 (doNormalDomestic) -- PHP line 2118:**
    1. Read PHP do일반내정() at GeneralAI.php L2118-2218. Key PHP logic:
       - Calculates development rates for each city stat (agri, comm, secu, def, wall)
       - Uses calcCityDevelRate() to get weighted priorities
       - Weighted random selection: lower stats get higher weight
       - PHP weight formula: `(maxVal - currentVal) / maxVal * weight`
       - Returns action string like "농지개간", "상업투자", "치안강화", "수비강화", "성벽보수"
    2. Compare with Kotlin doNormalDomestic() at L1424 and calcCityDevelRate() at L381. Focus on:
       - Weight calculation formula parity
       - Action string mapping (Korean names must match command action codes)
       - Edge case: all stats maxed → null
    3. Create golden value test with specific city stats → expected action string.

    **do긴급내정 (doUrgentDomestic) -- PHP line 2220:**
    4. Read PHP L2220-2252. Key PHP logic:
       - Only in war state (diplomacyState >= d징병)
       - Prioritizes wall if wall < threshold, else def
    5. Compare with Kotlin L1532. Fix divergences.
    6. Create golden value test.

    **do전쟁내정 (doWarDomestic) -- PHP line 2253:**
    7. Read PHP L2253-2365. Key PHP logic:
       - Front city domestic: def, wall priority
       - Rear city domestic: agri, comm for income
    8. Compare with Kotlin L1557. Fix divergences.
    9. Create golden value test.

    **do금쌀구매 (doTradeResources) -- PHP line 2367:**
    10. Read PHP L2367-2482. Key PHP logic:
        - Compares gold vs rice reserves relative to monthly consumption
        - Buys the resource that is more critically short
        - Amount calculation based on exchange rate and surplus of other resource
    11. Compare with Kotlin L2032. Fix divergences.
    12. Create golden value test: general with gold=100, rice=5000 → expect gold purchase.

    **doNPC헌납 (doDonate/doNpcDedicate) -- PHP line 2785:**
    13. Read PHP doNPC헌납() at L2785-2864. Key PHP logic:
        - Donates personal gold/rice to nation treasury
        - Amount: excess above personal reserve threshold
        - Guard: personal gold/rice must exceed threshold
    14. Compare with Kotlin doDonate() at L2079 and doNpcDedicate() at L3561.
    15. Create golden value test.

    **do귀환 (doReturn) -- PHP line 3095:**
    16. Read PHP L3095-3110. Key PHP logic:
        - Returns to capital or assigned city if not there
        - Guard: already in assigned city → null
    17. Compare with Kotlin L2014. Fix divergences.
    18. Create golden value test.

    **do내정워프 (doWarpToDomestic) -- PHP line 3022:**
    19. Read PHP L3022-3094. Key PHP logic:
        - Warps to underdeveloped rear city for domestic work
        - Target: city with lowest devScore
    20. Compare with Kotlin L1952. Fix divergences.
    21. Create golden value test.

    **Per D-06:** Add all tests to GeneralAITest.kt in a new `@Nested` class `DomesticAIParityTests`.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.engine.ai.GeneralAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAITest.kt contains `@Nested` class or section with "DomesticAI" or "domestic" in name
    - GeneralAITest.kt contains at least 2 test methods with "doNormalDomestic" or "일반내정" in name
    - GeneralAITest.kt contains at least 1 test method with "doUrgentDomestic" or "긴급내정" in name
    - GeneralAITest.kt contains at least 1 test method with "doWarDomestic" or "전쟁내정" in name
    - GeneralAITest.kt contains at least 2 test methods with "doTradeResources" or "금쌀구매" in name
    - GeneralAITest.kt contains at least 1 test method with "doDonate" or "doNpcDedicate" or "헌납" in name
    - GeneralAITest.kt contains at least 1 test method with "doReturn" or "귀환" in name
    - GeneralAITest.kt contains at least 1 test method with "doWarpToDomestic" or "내정워프" in name
    - All tests pass
  </acceptance_criteria>
  <done>
    7 domestic do*() methods each have 1-2 golden value tests + guard tests. Any divergences with PHP fixed. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Economy rate chooser golden value parity -- chooseTexRate/chooseGoldBillRate/chooseRiceBillRate</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/NationAITest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/NationAITest.kt
  </read_first>
  <behavior>
    - Test: chooseTexRate with 3 supply cities of varying income returns PHP-matching rate
    - Test: chooseTexRate with high military spending returns higher tax rate
    - Test: chooseGoldBillRate with adequate treasury returns low bill rate
    - Test: chooseGoldBillRate with depleted treasury returns max bill rate
    - Test: chooseRiceBillRate with adequate rice returns low bill rate
    - Test: chooseRiceBillRate with depleted rice returns max bill rate
  </behavior>
  <action>
    **Per D-02 and D-05:** Verify the 3 economy rate chooser methods against PHP.

    **chooseTexRate -- PHP line 4172:**
    1. Read PHP chooseTexRate() at GeneralAI.php L4172-4200. Key PHP logic:
       - Calculates optimal tax rate based on supply city income potential vs military expenses
       - Formula: considers nation.gold, nation.rice, total salary, troop upkeep
       - Bounds: min 10, max 50 (or policy-defined)
    2. Compare with Kotlin chooseTexRate() at L2912.
    3. Create golden value test: nation with 3 supply cities, gold=5000, salary=2000.

    **chooseGoldBillRate -- PHP line 4201:**
    4. Read PHP chooseGoldBillRate() at GeneralAI.php L4201-4247. Key PHP logic:
       - Bill rate for gold (military technology investment rate)
       - Based on gold reserves relative to expenses
       - Higher rate when gold surplus, lower when deficit
    5. Compare with Kotlin L2942. Fix divergences.
    6. Create golden value test.

    **chooseRiceBillRate -- PHP line 4248:**
    7. Read PHP chooseRiceBillRate() at GeneralAI.php L4248-4290. Key PHP logic:
       - Bill rate for rice (troop food supply investment rate)
       - Based on rice reserves relative to troop consumption
    8. Compare with Kotlin L2985. Fix divergences.
    9. Create golden value test.

    **Per D-06:** Add tests to GeneralAITest.kt in a `@Nested` class `EconomyRateParityTests`. If NationAITest.kt has existing rate tests, extend those instead.

    **NationAI.kt dead code check:** Verify whether NationAI.kt::adjustTaxAndBill() is called at runtime. If it is, it must match PHP. If GeneralAI.kt::chooseTexRate/chooseGoldBillRate/chooseRiceBillRate are the actual runtime path (called from chooseNationTurn), then NationAI.kt::adjustTaxAndBill() is dead code and should be documented with a TODO comment.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.engine.ai.GeneralAITest" --tests "com.opensam.engine.ai.NationAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAITest.kt contains at least 2 test methods with "chooseTexRate" or "texRate" in name
    - GeneralAITest.kt contains at least 2 test methods with "chooseGoldBillRate" or "goldBillRate" in name
    - GeneralAITest.kt contains at least 2 test methods with "chooseRiceBillRate" or "riceBillRate" in name
    - If NationAI.kt::adjustTaxAndBill() is dead code, it contains a TODO comment documenting this
    - All tests pass
  </acceptance_criteria>
  <done>
    chooseTexRate, chooseGoldBillRate, chooseRiceBillRate each have 2 golden value tests covering normal and edge cases. NationAI.kt dead code status documented. All 10 domestic/economy methods verified. All tests pass.
  </done>
</task>

</tasks>

<verification>
- All tests in com.opensam.engine.ai.GeneralAITest pass
- All tests in com.opensam.engine.ai.NationAITest pass
- 10 domestic/economy do*() methods each have at least 1 golden value test
- Economy rate choosers verified against PHP formulas
- NationAI.kt dead code status determined and documented
- Full game-app test suite passes
</verification>

<success_criteria>
- 16+ new test methods covering 10 domestic/economy methods
- Each method has golden value(s) from PHP manual trace
- All divergences between PHP and Kotlin domestic methods fixed
- NationAI.kt adjustTaxAndBill dead code status clarified
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-npc-ai-parity/08-03-SUMMARY.md`
</output>
