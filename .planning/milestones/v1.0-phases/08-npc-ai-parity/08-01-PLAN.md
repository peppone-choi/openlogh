---
phase: 08-npc-ai-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/ai/NpcPolicy.kt
  - backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt
  - backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
autonomous: true
requirements: [AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "calcDiplomacyState() returns the same 5-state value as PHP for identical diplomacy table inputs"
    - "do선전포고() selects the same war target as PHP for identical game state"
    - "do불가침제의() proposes NAP to the same nations as PHP for identical game state"
    - "categorizeNationGeneral() classifies generals into the same 7 categories as PHP"
    - "chooseGeneralTurn() follows the same branch order as PHP (do선양 before npcType=5, injury threshold, lord-without-capital)"
    - "chooseNationTurn() iterates nationPolicy.priority in the same order as PHP"
    - "Default priority lists match PHP AutorunGeneralPolicy/AutorunNationPolicy"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt"
      provides: "Rewritten calcDiplomacyState, do선전포고, do불가침제의, categorizeNationGeneral, chooseGeneralTurn flow"
      contains: "categorizeNationGeneral"
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/ai/NpcPolicy.kt"
      provides: "Corrected default priority lists"
      contains: "NPC사망대비"
    - path: "backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt"
      provides: "Golden value parity tests for diplomacy, personnel, wanderer, priorities"
      contains: "calcDiplomacyState golden value"
  key_links:
    - from: "GeneralAI.kt::calcDiplomacyState()"
      to: "All do*() methods via AIContext.diplomacyState"
      via: "AIContext construction"
      pattern: "calcDiplomacyState.*nation.*diplomacies"
    - from: "GeneralAI.kt::chooseNationTurn()"
      to: "doNationAction() dispatch"
      via: "nationPolicy.priority iteration"
      pattern: "for.*actionName.*in.*nationPolicy\\.priority"
---

<objective>
Fix structural divergences in NPC AI: rewrite calcDiplomacyState to PHP 5-state model, rewrite do선전포고/do불가침제의 to match PHP logic, add missing categorizeNationGeneral utility, fix chooseGeneralTurn/chooseNationTurn entry point flow, correct default priority lists. Lock golden values for all fixed methods.

Purpose: These are ROOT CAUSE divergences (Research Divergences 1-5) that affect every downstream AI decision. Must be fixed before verifying individual military/domestic do*() methods.
Output: Corrected GeneralAI.kt structural methods + NpcPolicy.kt priority lists + golden value parity tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-npc-ai-parity/08-CONTEXT.md
@.planning/phases/08-npc-ai-parity/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt:
```kotlin
// Current DiplomacyState enum (line ~278) -- MUST be rewritten to match PHP 5-state
enum class DiplomacyState { PEACE, DECLARED, IMMINENT, AT_WAR, RECRUITING }

// AIContext data class used by all do*() methods
data class AIContext(
    val world: WorldState, val general: General, val city: City, val nation: Nation?,
    val diplomacyState: DiplomacyState, val generalType: GeneralType,
    val allCities: List<City>, val allGenerals: List<General>, val allNations: List<Nation>,
    val frontCities: List<City>, val rearCities: List<City>, val nationGenerals: List<General>,
)

// Entry points called by TurnService
fun chooseGeneralTurn(general: General, world: WorldState): String  // line 3187
fun chooseNationTurn(general: General, world: WorldState): String   // line 3027
fun chooseInstantNationTurn(general: General, world: WorldState): String?  // line 3128
```

From backend/game-app/src/main/kotlin/com/opensam/engine/ai/NpcPolicy.kt:
```kotlin
data class NpcGeneralPolicy(
    val priority: List<String> = DEFAULT_GENERAL_PRIORITY,
    val minNPCWarLeadership: Int = 40,
    // ...
) {
    companion object {
        val DEFAULT_GENERAL_PRIORITY = listOf(
            "긴급내정", "전쟁내정", "징병", "전투준비", "출병", "전방워프", "후방워프",
            "내정워프", "귀환", "일반내정", "금쌀구매", "NPC헌납", "소집해제", "중립"
        )
    }
}

data class NpcNationPolicy(
    val priority: List<String> = DEFAULT_NATION_PRIORITY,
    // ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite calcDiplomacyState + categorizeNationGeneral + priority lists</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/NpcPolicy.kt,
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    legacy-core/hwe/sammo/AutorunGeneralPolicy.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/NpcPolicy.kt,
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt
  </read_first>
  <behavior>
    - Test: calcDiplomacyState with no war declarations returns PEACE (code 0)
    - Test: calcDiplomacyState early game (yearMonth <= startYearMonth + 29) with war returns DECLARED (code 1)
    - Test: calcDiplomacyState with minTerm > 8 returns DECLARED (code 1)
    - Test: calcDiplomacyState with minTerm = 7 returns RECRUITING (code 2, d징병)
    - Test: calcDiplomacyState with minTerm = 4 returns IMMINENT (code 3, d직전)
    - Test: calcDiplomacyState with attackable + onWar returns AT_WAR (code 4)
    - Test: categorizeNationGeneral classifies NPC with leadership >= minWarLeadership as npcWarGeneral
    - Test: categorizeNationGeneral classifies NPC with low leadership as npcCivilGeneral
    - Test: categorizeNationGeneral classifies player general with recent combat as userWarGeneral
    - Test: categorizeNationGeneral classifies player general without combat as userCivilGeneral
    - Test: DEFAULT_GENERAL_PRIORITY starts with NPC사망대비 (matching PHP)
    - Test: DEFAULT_GENERAL_PRIORITY has 귀환 at index 1 and 금쌀구매 at index 2 (matching PHP)
  </behavior>
  <action>
    **Per D-03 and Research Divergence 1:** Rewrite calcDiplomacyState() in GeneralAI.kt to match PHP 5-state model:

    1. Read PHP calcDiplomacyState() at GeneralAI.php line 206-270. The PHP logic:
       - d평화=0, d선포=1, d징병=2, d직전=3, d전쟁=4
       - Early game check: yearMonth <= joinYearMonth(startyear+2, 5) forces 평화 or 선포
       - Term-based: diplomacy rows with state=1 (선전포고), use min(term). term > 8 → 선포, term > 5 → 징병, else → 직전
       - AT_WAR: attackable AND onWar (state=0 in diplomacy means active war)
       - last_attackable KVStorage check: if was attackable within 5 months, keep 직전

    2. Rewrite Kotlin DiplomacyState enum to:
       ```kotlin
       enum class DiplomacyState(val code: Int) {
           PEACE(0),      // d평화
           DECLARED(1),   // d선포
           RECRUITING(2), // d징병
           IMMINENT(3),   // d직전
           AT_WAR(4),     // d전쟁
       }
       ```

    3. Rewrite calcDiplomacyState() to use term-based transitions matching PHP exactly. The method signature should accept world (for startyear/year/month) and the diplomacy list. Map Diplomacy entity stateCode "선전포고" to PHP state=1, stateCode "전쟁" to PHP state=0. Use Diplomacy.term field for countdown.

    **Per Research Divergence 4:** Add categorizeNationGeneral() method:

    4. Read PHP categorizeNationGeneral() at GeneralAI.php line 3516-3600. Implement as internal fun returning a data class with:
       - npcWarGenerals: List<General> (NPC with leadership >= minNPCWarLeadership, npcState >= 2, not dying)
       - npcCivilGenerals: List<General> (NPC with low leadership or killTurn <= 5)
       - userWarGenerals: List<General> (player generals who fought recently or have troops > threshold)
       - userCivilGenerals: List<General> (other player generals)
       - troopLeaders: List<General> (npcState == 5)
       - lostGenerals: List<General> (in non-supply cities during war)
       - chiefGenerals: List<General> (officerLevel > 4, mapped from PHP > 4 to Kotlin equivalent)

    **Per Research Divergence 5:** Fix NpcPolicy.kt default priority lists:

    5. Read PHP AutorunGeneralPolicy.php $default_priority. Update NpcGeneralPolicy.DEFAULT_GENERAL_PRIORITY to:
       ```kotlin
       val DEFAULT_GENERAL_PRIORITY = listOf(
           "NPC사망대비", "귀환", "금쌀구매", "출병", "긴급내정", "전투준비",
           "전방워프", "NPC헌납", "징병", "후방워프", "전쟁내정", "소집해제",
           "일반내정", "내정워프"
       )
       ```

    6. Read PHP AutorunNationPolicy.php $default_priority. Update NpcNationPolicy.DEFAULT_NATION_PRIORITY similarly.

    7. Add golden value parity tests in NpcAiParityTest.kt for calcDiplomacyState (6 state transitions), categorizeNationGeneral (4 classification tests), and priority list ordering (2 tests).

    **Note on officer level mapping:** PHP uses officer_level 12 for chief. Kotlin uses 20. When tracing PHP conditions like `officer_level < 12`, map to the equivalent Kotlin threshold per Pitfall 1 in Research.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.NpcAiParityTest" --tests "com.opensam.engine.ai.GeneralAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAI.kt contains `enum class DiplomacyState(val code: Int)` with values PEACE(0), DECLARED(1), RECRUITING(2), IMMINENT(3), AT_WAR(4)
    - GeneralAI.kt contains `fun categorizeNationGeneral(` returning a data class with npcWarGenerals, npcCivilGenerals, userWarGenerals, userCivilGenerals fields
    - NpcPolicy.kt DEFAULT_GENERAL_PRIORITY starts with "NPC사망대비" (first element)
    - NpcPolicy.kt DEFAULT_GENERAL_PRIORITY has "귀환" at index 1
    - NpcAiParityTest.kt contains at least 6 test methods with "calcDiplomacyState" in the name
    - NpcAiParityTest.kt contains at least 4 test methods with "categorizeNationGeneral" in the name
    - All existing tests in com.opensam.engine.ai.* and com.opensam.qa.parity.NpcAiParityTest pass
  </acceptance_criteria>
  <done>
    calcDiplomacyState returns PHP-matching 5-state values for all term ranges and early-game conditions. categorizeNationGeneral classifies generals into 7 PHP-matching categories. Default priority lists match PHP AutorunGeneralPolicy/AutorunNationPolicy ordering. All golden value tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite do선전포고 + do불가침제의 + fix chooseGeneralTurn/chooseNationTurn entry flow</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </files>
  <read_first>
    legacy-core/hwe/sammo/GeneralAI.php,
    backend/game-app/src/main/kotlin/com/opensam/engine/ai/GeneralAI.kt,
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/NpcAiParityTest.kt,
    backend/game-app/src/test/kotlin/com/opensam/engine/ai/GeneralAITest.kt
  </read_first>
  <behavior>
    - Test: do선전포고 with officerLevel < 20 (chief) returns null (guard: only chief can declare war)
    - Test: do선전포고 in peace state with no attackable targets selects correct war target via PHP resource calc (npcWar/npcCivil/userWar/userCivil separation)
    - Test: do선전포고 trial probability matches PHP formula ((avgGold/reqGold + avgRice/reqRice + devRate) / 4) ^ 6
    - Test: do불가침제의 proposes NAP to nation that provided assistance (PHP recv_assist/resp_assist pattern)
    - Test: do불가침제의 with cooldown (resp_assist_try within 8 months) returns null
    - Test: chooseGeneralTurn checks do선양 BEFORE npcType=5 (PHP order)
    - Test: chooseGeneralTurn injury threshold uses cureThreshold (PHP default 10), not injury > 0
    - Test: chooseGeneralTurn npcType=5 + nationId=0 sets killTurn=1 (already exists in Kotlin, verify)
    - Test: chooseGeneralTurn lord-without-capital uses PHP structured do건국/do방랑군이동/do해산 with relYearMonth
    - Test: chooseNationTurn RNG seed context uses "GeneralAI" (not "NationTurn") per Pitfall 5
  </behavior>
  <action>
    **Per D-03 and Research Divergence 2:** Rewrite do선전포고() (doDeclaration):

    1. Read PHP do선전포고() at GeneralAI.php line 1848-1975. Rewrite Kotlin doDeclaration() to match:
       - Guard: officerLevel check mapped from PHP `< 12` to Kotlin equivalent (>= 20 means chief in Kotlin)
       - Guard: dipState must be PEACE (d평화) -- only declare war in peacetime
       - Guard: attackable must be FALSE (no existing attack targets)
       - Guard: frontCities must be EMPTY (PHP requires no borders, Kotlin currently requires borders -- REVERSED!)
       - TechLimit guard: implement or skip with comment if opensamguk does not use tech system
       - Resource calculation: use categorizeNationGeneral() to separate npcWar/npcCivil/userWar/userCivil generals. User resources counted at 50% weight (PHP: `$userGold * 0.5`).
       - Trial probability: `((avgGold/reqGold + avgRice/reqRice + devRate) / 4).pow(6)`
       - Target selection: prefer nations NOT in any diplomacy. Use isNeighbor/adjacency. Fallback to war nations with probability.

    **Per D-03 and Research Divergence 3:** Rewrite do불가침제의() (doNonAggressionProposal):

    2. Read PHP do불가침제의() at GeneralAI.php line 1765-1847. Rewrite Kotlin doNonAggressionProposal() to match:
       - PHP bases NAP on recv_assist/resp_assist KVStorage (assistance tracking from war allies)
       - Candidate nations: those who provided assistance (recv_assist entries)
       - Amount filter: `amount * 4 < income` → skip
       - DiplomatMonth = `24 * amount / income`
       - Cooldown: resp_assist_try within 8 months → skip
       - If Kotlin does not have assistance tracking in nation.meta, implement minimal tracking or document as known divergence with TODO

    **Per Research entry point flow differences:** Fix chooseGeneralTurn():

    3. Read PHP chooseGeneralTurn() at GeneralAI.php line ~3400-3530. Fix Kotlin chooseGeneralTurn() at line 3187:
       - Add do선양 (abdication) check BEFORE npcType=5 check (PHP checks do선양 first)
       - Change injury threshold from `general.injury > 0` to `general.injury > cureThreshold` where cureThreshold comes from generalPolicy (PHP default: 10)
       - Fix lord-without-capital logic: PHP uses structured do건국(relYearMonth check)/do방랑군이동/do해산 sequence, not random probabilities
       - Add NPC message emission with probability (PHP emits npcmsg)
       - Ensure defence_train=80 set for npcType >= 2 (already exists)

    4. Fix chooseNationTurn() RNG seed context:
       - PHP uses "GeneralAI" for ALL AI RNG. Kotlin currently uses "NationTurn" for chooseNationTurn (line 3030) and "GeneralTurn" for chooseGeneralTurn (line 3190). Change both to "GeneralAI" per Pitfall 5.

    5. Add golden value parity tests in NpcAiParityTest.kt:
       - do선전포고: 2 golden values (peace+resources → war target, non-peace → null)
       - do불가침제의: 2 golden values (assistance exists → NAP proposal, cooldown → null)
       - chooseGeneralTurn: 2 golden values (injury=5 with cureThreshold=10 → not 요양, injury=15 → 요양)
       - chooseNationTurn: 1 golden value (priority dispatch order matches PHP)

    6. Add branch-point tests in GeneralAITest.kt:
       - chooseGeneralTurn do선양 ordering
       - chooseGeneralTurn lord-without-capital structured flow
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.NpcAiParityTest" --tests "com.opensam.engine.ai.GeneralAITest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - GeneralAI.kt doDeclaration method contains `frontCities.isEmpty()` guard matching PHP (not frontCities.isNotEmpty())
    - GeneralAI.kt doDeclaration method contains `categorizeNationGeneral` call for resource calculation
    - GeneralAI.kt doNonAggressionProposal method references assistance tracking (recv_assist or equivalent meta key)
    - GeneralAI.kt chooseGeneralTurn contains do선양/doAbdicate check BEFORE npcType==5 check
    - GeneralAI.kt chooseGeneralTurn injury check uses threshold comparison (not `> 0`)
    - GeneralAI.kt chooseNationTurn RNG seed context string is "GeneralAI" (not "NationTurn")
    - GeneralAI.kt chooseGeneralTurn RNG seed context string is "GeneralAI" (not "GeneralTurn")
    - NpcAiParityTest.kt contains at least 2 test methods with "do선전포고" or "doDeclaration" in the name
    - NpcAiParityTest.kt contains at least 2 test methods with "do불가침제의" or "doNonAggressionProposal" in the name
    - All existing tests pass
  </acceptance_criteria>
  <done>
    do선전포고 selects war targets using PHP resource calculation with war/civil general separation. do불가침제의 proposes NAP based on assistance tracking. chooseGeneralTurn follows PHP branch order (do선양 first, injury threshold, structured lord logic). RNG seed contexts match PHP. All golden value tests pass.
  </done>
</task>

</tasks>

<verification>
- All tests in com.opensam.engine.ai.* pass
- All tests in com.opensam.qa.parity.NpcAiParityTest pass
- calcDiplomacyState produces 5-state values matching PHP for all term ranges
- do선전포고 and do불가침제의 golden values match PHP manual trace
- chooseGeneralTurn entry flow matches PHP branch order
- Default priority lists match PHP AutorunGeneralPolicy ordering
</verification>

<success_criteria>
- DiplomacyState enum has 5 coded values matching PHP (0-4)
- categorizeNationGeneral utility exists and classifies 7 general types
- do선전포고 uses PHP resource calc with war/civil separation
- do불가침제의 uses PHP assistance-based logic
- chooseGeneralTurn/chooseNationTurn entry flow matches PHP
- RNG seed contexts all use "GeneralAI"
- Priority lists match PHP ordering
- 20+ new golden value and branch-point test assertions pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-npc-ai-parity/08-01-SUMMARY.md`
</output>
