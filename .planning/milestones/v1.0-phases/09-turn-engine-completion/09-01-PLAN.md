---
phase: 09-turn-engine-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
  - backend/game-app/src/test/kotlin/com/opensam/engine/TurnServiceTest.kt
autonomous: true
requirements:
  - TURN-01
  - TURN-02
  - TURN-03
  - TURN-04
  - TURN-05

must_haves:
  truths:
    - "checkWander dissolves wander nations (nation.level=0) whose chief general (officerLevel=20) exists and world.year >= startYear+2"
    - "updateOnline updates world.meta with online_user_cnt and online_nation per tick"
    - "checkOverhead calculates refreshLimit using round(turnterm^0.6 * 3) * refreshLimitCoef formula"
    - "updateGeneralNumber counts generals per nation (excluding npcState=5) and saves nation.gennum"
    - "postUpdateMonthly call order matches legacy: checkWander -> updateGeneralNumber -> triggerTournament -> registerAuction"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt"
      provides: "4 implemented stub methods + reordered postUpdateMonthly"
      contains: "commandRegistry.create"
    - path: "backend/game-app/src/test/kotlin/com/opensam/engine/TurnServiceTest.kt"
      provides: "Unit tests for all 4 stub methods"
      contains: "checkWander"
  key_links:
    - from: "TurnService.checkWander"
      to: "CommandRegistry + CommandExecutor"
      via: "commandRegistry.create('해산') then run()"
      pattern: "commandRegistry\\.create.*해산"
    - from: "TurnService.updateGeneralNumber"
      to: "NationRepository"
      via: "nationRepository.saveAll"
      pattern: "nationRepository\\.saveAll"
---

<objective>
Implement the 4 remaining stub methods in TurnService.kt (checkWander, updateOnline, checkOverhead, updateGeneralNumber) with legacy PHP parity logic, and fix the postUpdateMonthly call ordering to match legacy func_gamerule.php.

Purpose: These stubs are the last unimplemented turn pipeline methods. Without them, wander nations never dissolve, online counts are never tracked, overhead guards don't function, and nation general counts go stale.

Output: TurnService.kt with 4 working methods + corrected postUpdateMonthly order + unit tests in TurnServiceTest.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-turn-engine-completion/09-CONTEXT.md
@.planning/phases/09-turn-engine-completion/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement 4 stub methods and fix postUpdateMonthly ordering</name>
  <files>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
    backend/game-app/src/test/kotlin/com/opensam/engine/TurnServiceTest.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
    backend/game-app/src/test/kotlin/com/opensam/engine/TurnServiceTest.kt
    backend/game-app/src/main/kotlin/com/opensam/command/general/che_해산.kt
    legacy-core/hwe/func_gamerule.php
    legacy-core/hwe/func.php
  </read_first>
  <behavior>
    - Test: checkWander with year >= startYear+2, one wander nation (level=0) with chief (officerLevel=20) -> commandRegistry.create("해산") called, command.run() executed
    - Test: checkWander with year < startYear+2 -> no command created (year guard)
    - Test: checkWander with nation.level != 0 -> no command created (not wander)
    - Test: updateOnline -> world.meta["online_user_cnt"] set to count of recent access logs, world.meta["online_nation"] set to nation name string
    - Test: checkOverhead with turnterm=300, refreshLimitCoef=10 -> refreshLimit = round(300^0.6 * 3) * 10 = round(109.56) * 10 = 1100
    - Test: checkOverhead with default refreshLimitCoef (missing from config) -> uses default 10
    - Test: updateGeneralNumber -> nation.gennum updated to count of non-npcState5 generals in that nation; nation with 0 generals gets gennum=0
  </behavior>
  <action>
**A. Fix postUpdateMonthly call ordering (lines ~359-380 in TurnService.kt):**

Reorder the 4 post-pipeline try-catch blocks from current order:
```
checkWander -> triggerTournament -> registerAuction -> updateGeneralNumber
```
to legacy order (per D-03, func_gamerule.php:423-441):
```
checkWander -> updateGeneralNumber -> triggerTournament -> registerAuction
```
Move the `updateGeneralNumber(world)` try-catch block to immediately after `checkWander(world)`, before `triggerTournament(world)`.

**B. Implement checkWander (per D-02, func_gamerule.php:445-467):**

Replace the stub body of `private fun checkWander(world: WorldState)` with:
1. Read startYear from `world.config["startYear"]` (as Number?.toInt(), fallback: return early)
2. Year guard: `if (world.currentYear.toInt() < startYear + 2) return` (per Pitfall 2 -- guard at call site)
3. Query generals: `generalRepository.findByWorldId(world.id.toLong())`
4. For each general with `officerLevel.toInt() == 20`:
   - Load nation: `nationRepository.findById(general.nationId)`
   - If nation exists and `nation.level.toInt() == 0` (wander nation):
     - Create command: `commandRegistry.create("해산", general.id, world.id.toLong())`
     - If command is not null and `command.hasFullConditionMet()`:
       - Create a DeterministicRng seeded with `(world.config["hiddenSeed"] as? String) ?: world.id.toString()`, context `"checkWander"`, year, month
       - Call `command.run(RandUtil(rng))` 
       - Log: `"[Turn] checkWander: 방랑군 자동 해산 - generalId=${general.id}"`

Note: Check the actual `commandRegistry.create()` method signature. It may take different params than shown -- read the CommandRegistry class to confirm. The `해산` command class is `che_해산.kt`. The key legacy behavior is: find chief generals of wander nations and execute the dissolution command.

**C. Implement updateOnline (per func.php:1205-1248):**

Replace the stub body of `private fun updateOnline(world: WorldState)` with:
1. `val worldId = world.id.toLong()`
2. Query access logs: `generalAccessLogRepository.findByWorldId(worldId)` -- filter to those with `lastRefresh` >= `world.updatedAt` (i.e., accessed since last tick)
3. Count online: `val onlineCount = recentLogs.size`
4. Build nation name map: `nationRepository.findByWorldId(worldId)` -> map of `nationId to name`, add `0L to "재야"`
5. Group logs by nationId, sort by count descending
6. Build online nation string: `onlineByNation.map { "【${nationNames[it.key] ?: "unknown"}】" }.joinToString(", ")`
7. Set `world.meta["online_user_cnt"] = onlineCount`
8. Set `world.meta["online_nation"] = onlineNationStr`

Note: If `GeneralAccessLog` entity or `generalAccessLogRepository.findByWorldId()` doesn't have a `lastRefresh` field, check what field tracks last access time. Adapt the filter accordingly. The goal is: count generals who accessed the game recently.

**D. Implement checkOverhead (per func.php:1103-1116):**

Replace the stub body of `private fun checkOverhead(world: WorldState)` with:
1. `val turnterm = world.tickSeconds.toDouble()`
2. `val refreshLimitCoef = (world.config["refreshLimitCoef"] as? Number)?.toInt() ?: 10`
3. `val nextRefreshLimit = kotlin.math.round(Math.pow(turnterm, 0.6) * 3).toInt() * refreshLimitCoef`
4. `val currentRefreshLimit = (world.meta["refreshLimit"] as? Number)?.toInt() ?: 0`
5. `if (nextRefreshLimit != currentRefreshLimit) { world.meta["refreshLimit"] = nextRefreshLimit }`

**E. Implement updateGeneralNumber (per func_gamerule.php:174-186):**

Replace the stub body of `private fun updateGeneralNumber(world: WorldState)` with:
1. `val worldId = world.id.toLong()`
2. `val generals = generalRepository.findByWorldId(worldId)`
3. `val nations = nationRepository.findByWorldId(worldId)`
4. Count generals per nation excluding npcState=5 and nationId=0:
   ```kotlin
   val genCountByNation = generals
       .filter { it.npcState.toInt() != 5 && it.nationId > 0 }
       .groupingBy { it.nationId }
       .eachCount()
   ```
5. Update each nation:
   ```kotlin
   for (nation in nations) {
       if (nation.id == 0L) continue
       val newCount = (genCountByNation[nation.id] ?: 0).toShort()
       nation.gennum = newCount
   }
   ```
6. `nationRepository.saveAll(nations)`

Remove `@Suppress("UNUSED_PARAMETER")` from all 4 methods since `world` is now used.

**F. Write unit tests in TurnServiceTest.kt** -- add new test methods after the existing tests:

Tests for checkWander, updateOnline, checkOverhead, updateGeneralNumber as described in `<behavior>`. Use the existing mock setup. For checkWander, mock `commandRegistry.create(...)` to return a mock command, verify `command.run()` is called. For updateGeneralNumber, capture the saved nations and verify gennum values.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.engine.TurnServiceTest" -x :gateway-app:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TurnService.kt: `checkWander` method body contains `commandRegistry.create` (not just TODO comment)
    - TurnService.kt: `updateOnline` method body contains `world.meta["online_user_cnt"]`
    - TurnService.kt: `checkOverhead` method body contains `Math.pow(turnterm, 0.6)`
    - TurnService.kt: `updateGeneralNumber` method body contains `nationRepository.saveAll`
    - TurnService.kt: postUpdateMonthly section has updateGeneralNumber BEFORE triggerTournament (grep line order)
    - TurnService.kt: no `@Suppress("UNUSED_PARAMETER")` on the 4 stub methods
    - TurnServiceTest.kt: contains test methods with names matching `checkWander`, `updateOnline`, `checkOverhead`, `updateGeneralNumber`
    - `./gradlew :game-app:test --tests "com.opensam.engine.TurnServiceTest"` exits 0
  </acceptance_criteria>
  <done>All 4 stub methods implemented with legacy-matching logic. postUpdateMonthly order corrected. Unit tests pass for all 4 methods covering happy path and guard conditions.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./gradlew :game-app:test --tests "com.opensam.engine.TurnServiceTest" -x :gateway-app:test` -- all tests pass
2. grep -n "checkWander\|updateGeneralNumber\|triggerTournament\|registerAuction" TurnService.kt -- verify ordering: checkWander < updateGeneralNumber < triggerTournament < registerAuction by line number
3. grep "UNUSED_PARAMETER" TurnService.kt -- should NOT match any of the 4 method signatures
</verification>

<success_criteria>
- 4 stub methods have real implementation bodies (no TODO comments remain for these methods)
- postUpdateMonthly ordering matches legacy: checkWander -> updateGeneralNumber -> triggerTournament -> registerAuction
- All TurnServiceTest tests pass including new stub method tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-turn-engine-completion/09-01-SUMMARY.md`
</output>
