---
phase: 09-turn-engine-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - backend/game-app/src/test/kotlin/com/opensam/qa/parity/TurnPipelineParityTest.kt
  - backend/game-app/src/test/kotlin/com/opensam/qa/parity/DisasterParityTest.kt
autonomous: true
requirements:
  - TURN-05
  - TURN-06

must_haves:
  truths:
    - "postUpdateMonthly call order in TurnService matches legacy func_gamerule.php:423-441 exactly"
    - "Disaster trigger probabilities (boomRate, raiseProp, affectRatio) match legacy RaiseDisaster.php values for months 1,4,7,10"
    - "Disaster 3-year grace period (startYear+3 > year skips disasters) matches legacy"
    - "Disaster RNG seed string matches legacy PHP (verify 'disaster' vs 'disater' typo)"
    - "Disaster state codes and effect formulas match legacy for each disaster type"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/opensam/qa/parity/TurnPipelineParityTest.kt"
      provides: "postUpdateMonthly ordering assertion test"
      contains: "postUpdateMonthly"
    - path: "backend/game-app/src/test/kotlin/com/opensam/qa/parity/DisasterParityTest.kt"
      provides: "Disaster probability and effect golden value tests"
      contains: "processDisasterOrBoom"
  key_links:
    - from: "TurnPipelineParityTest"
      to: "TurnService.kt postUpdateMonthly section"
      via: "call order assertion"
      pattern: "checkWander.*updateGeneralNumber.*triggerTournament.*registerAuction"
    - from: "DisasterParityTest"
      to: "EconomyService.processDisasterOrBoom"
      via: "golden value comparison"
      pattern: "processDisasterOrBoom"
---

<objective>
Verify turn step ordering matches legacy daemon.ts/func_gamerule.php exactly, and verify disaster/event trigger probabilities and effects match legacy RaiseDisaster.php via golden value tests.

Purpose: Plan 01 implemented the stubs and fixed ordering. This plan locks the correctness with assertion tests: (1) postUpdateMonthly call order must match legacy, (2) disaster probabilities, RNG seed, grace period, and per-city effects must produce identical results to legacy PHP.

Output: Extended TurnPipelineParityTest.kt with ordering assertions + new DisasterParityTest.kt with golden value tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-turn-engine-completion/09-CONTEXT.md
@.planning/phases/09-turn-engine-completion/09-RESEARCH.md
@.planning/phases/09-turn-engine-completion/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: postUpdateMonthly ordering assertion + pipeline coverage</name>
  <files>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/TurnPipelineParityTest.kt
  </files>
  <read_first>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/TurnPipelineParityTest.kt
    backend/game-app/src/main/kotlin/com/opensam/engine/TurnService.kt
    legacy-core/hwe/func_gamerule.php
  </read_first>
  <behavior>
    - Test: postUpdateMonthly call order assertion -- verify TurnService source code (or a spy/mock trace) confirms the order: checkWander -> updateGeneralNumber -> triggerTournament -> registerAuction
    - Test: verify checkEmperior is covered by UnificationCheck step (1600) in the pipeline -- not a separate post-pipeline call
    - Test: verify SetNationFront is covered by WarFrontRecalc step (1300) in the pipeline -- not a separate post-pipeline call
  </behavior>
  <action>
Add new test methods to the existing `TurnPipelineParityTest.kt` file (per D-07: extend existing test files first).

**A. postUpdateMonthly ordering assertion test:**

Add a test that reads TurnService.kt source file (or uses reflection/source grep) to verify the post-pipeline call order. The simplest approach: use a source-code line-number assertion that verifies the 4 method calls appear in the correct order. Alternative: use a Mockito InOrder spy on a TurnService instance to verify call sequence during a processWorld invocation.

Recommended approach (source-code order check):
```kotlin
@Test
@DisplayName("postUpdateMonthly call order matches legacy func_gamerule.php:423-441")
fun `postUpdateMonthly order matches legacy`() {
    // Read TurnService source and find the post-pipeline section
    // Verify checkWander appears before updateGeneralNumber
    // Verify updateGeneralNumber appears before triggerTournament
    // Verify triggerTournament appears before registerAuction
    //
    // Legacy order (func_gamerule.php:423-441):
    //   1. checkWander
    //   2. updateGeneralNumber
    //   3. refreshNationStaticInfo (implicit in JPA save)
    //   4. checkEmperior (covered by UnificationCheck step 1600)
    //   5. triggerTournament
    //   6. registerAuction
    //   7. SetNationFront (covered by WarFrontRecalc step 1300)
}
```

The source-based approach: read the file as a classpath resource or use `File("src/main/kotlin/.../TurnService.kt").readText()`, find the line indices of each call, assert ordering. This is fragile but explicit.

Better approach: use a test that creates a TurnService with spied/mocked dependencies, calls processWorld on a world with elapsed tick, and uses Mockito `inOrder` to verify the call sequence of the 4 post-pipeline methods. Since checkWander/updateGeneralNumber are private, verify via their observable side effects:
- checkWander -> `commandRegistry.create("해산", ...)` call
- updateGeneralNumber -> `nationRepository.saveAll(...)` call
- triggerTournament -> `tournamentService.checkAndTriggerTournament(...)` call  
- registerAuction -> `auctionService.listActiveAuctions(...)` call

Use Mockito `inOrder(commandRegistry, nationRepository, tournamentService, auctionService)` to verify this exact ordering.

**B. Pipeline coverage assertion for checkEmperior and SetNationFront:**

Add tests verifying that:
1. The pipeline step list contains "UnificationCheck" at order 1600 (already tested, but add explicit assertion naming the legacy function it covers: `checkEmperior`)
2. The pipeline step list contains "WarFrontRecalc" at order 1300 (already tested, but add explicit assertion naming: `SetNationFront`)

```kotlin
@Test
@DisplayName("UnificationCheck step covers legacy checkEmperior()")
fun `unification check covers checkEmperior`() {
    assertThat(LEGACY_STEP_ORDER).contains("UnificationCheck")
    assertThat(LEGACY_STEP_ORDERS[LEGACY_STEP_ORDER.indexOf("UnificationCheck")]).isEqualTo(1600)
}

@Test
@DisplayName("WarFrontRecalc step covers legacy SetNationFront()")
fun `warFrontRecalc covers SetNationFront`() {
    assertThat(LEGACY_STEP_ORDER).contains("WarFrontRecalc")
    assertThat(LEGACY_STEP_ORDERS[LEGACY_STEP_ORDER.indexOf("WarFrontRecalc")]).isEqualTo(1300)
}
```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.TurnPipelineParityTest" -x :gateway-app:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TurnPipelineParityTest.kt contains a test method with name matching `postUpdateMonthly` or `post.*monthly.*order`
    - TurnPipelineParityTest.kt contains assertion verifying checkWander before updateGeneralNumber before triggerTournament before registerAuction
    - TurnPipelineParityTest.kt contains test asserting UnificationCheck at order 1600 covers checkEmperior
    - TurnPipelineParityTest.kt contains test asserting WarFrontRecalc at order 1300 covers SetNationFront
    - `./gradlew :game-app:test --tests "com.opensam.qa.parity.TurnPipelineParityTest"` exits 0
  </acceptance_criteria>
  <done>postUpdateMonthly ordering locked by assertion test. Pipeline step coverage for checkEmperior and SetNationFront documented and verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disaster probability and effect golden value parity tests</name>
  <files>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/DisasterParityTest.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
    legacy-core/hwe/sammo/Event/Action/RaiseDisaster.php
    legacy-core/hwe/func.php
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyIntegrationParityTest.kt
  </read_first>
  <behavior>
    - Test: 3-year grace period -- world with year < startYear+3 -> processDisasterOrBoom returns immediately, no city changes
    - Test: boomRate values -- month 1: 0.0, month 4: 0.25, month 7: 0.25, month 10: 0.0 (golden values)
    - Test: RNG seed string -- verify EconomyService uses correct seed string for DeterministicRng.create (check if "disaster" or "disater" -- if PHP uses "disater" typo, Kotlin must match for RNG parity)
    - Test: disaster raiseProp formula -- for isGood=false: 0.06 - secuRatio*0.05 (range 1-6%); for isGood=true: 0.02 + secuRatio*0.05 (range 2-7%)
    - Test: disaster effect on city -- golden value: given a city with known pop/agri/comm/secu/wall values, after disaster with known state code, verify exact resulting field values
    - Test: boom effect on city -- golden value: affectRatio = 1.01 + (secuRatio/0.8).coerceIn(0,1)*0.04, verify pop/agri/comm growth amounts
    - Test: disaster state codes per month -- month 1: [4,5,3,9], month 4: [7,5,6], month 7: [8,5,8], month 10: [3,5,3,9]
    - Test: SabotageInjury -- 30% chance, injury amount rng.nextInt(1,17), crew*=0.98, atmos*=0.98, train*=0.98, injury cap 80
  </behavior>
  <action>
Create a new test file `DisasterParityTest.kt` in `backend/game-app/src/test/kotlin/com/opensam/qa/parity/`. This is an exception to D-07 (extend existing files) because disaster parity is a distinct concern from economy integration and warrants its own test class.

Follow the pattern from `EconomyIntegrationParityTest.kt` for test setup (mock repositories, create EconomyService with mocks, build WorldState and City fixtures).

**A. 3-year grace period test:**
Create a world with `startYear=200`, `currentYear=202` (202 < 200+3=203). Call `processDisasterOrBoom(world)`. Assert no city field changes.

**B. boomRate golden value tests:**
For each month in [1, 4, 7, 10]:
- Create a world at that month with year well past grace period (e.g., year=210, startYear=200)
- Use a fixed hiddenSeed (e.g., "golden_parity_seed")
- The boomRate is deterministic by month: assert `when (month) { 4, 7 -> 0.25; else -> 0.0 }`
- Verify via: create DeterministicRng with same seed/context, check `rng.nextDouble() < boomRate` outcome matches what processDisasterOrBoom would compute

**C. RNG seed string verification (Pitfall 5):**
Read `EconomyService.kt` line ~707 to check the seed context string passed to `DeterministicRng.create()`. Legacy PHP uses `"disater"` (typo). If Kotlin uses `"disaster"`, this is a parity divergence.

Write a test that:
1. Creates two DeterministicRng instances: one with `"disaster"`, one with `"disater"`, same other params
2. Calls `nextDouble()` on both
3. Asserts they produce DIFFERENT values (proving the seed string matters)
4. Documents which string Kotlin currently uses vs which PHP uses

If the strings differ, the test should FAIL with a clear message indicating the fix needed. If they already match, the test passes.

**D. Disaster state codes per month golden value:**
Assert the disaster entry state codes for each month:
- Month 1: state codes [4, 5, 3, 9]
- Month 4: state codes [7, 5, 6]
- Month 7: state codes [8, 5, 8]
- Month 10: state codes [3, 5, 3, 9]
- Boom month 4: state code 2
- Boom month 7: state code 1

This can be verified by inspecting the `disasterEntries` map in EconomyService or by running processDisasterOrBoom with a controlled RNG and checking the resulting city.state values.

**E. Disaster/boom effect golden value test:**
Create a city fixture:
```
pop=50000, agri=800, comm=600, secu=400, secuMax=1000, wall=300, def=500
```

For a disaster (isGood=false) with state code 4 (역병):
- affectRatio = 0.99 - (secuRatio / 0.8).coerceIn(0,1) * 0.04
  where secuRatio = 400/1000 = 0.4
  affectRatio = 0.99 - (0.4/0.8).coerceIn(0,1) * 0.04 = 0.99 - 0.5*0.04 = 0.99 - 0.02 = 0.97
- pop = (50000 * 0.97).toInt() = 48500
- agri = (800 * 0.97).toInt().coerceAtLeast(0) = 776
- comm = (600 * 0.97).toInt().coerceAtLeast(0) = 582
- (exact values depend on implementation -- compute by hand from PHP source, lock as golden values)

For a boom (isGood=true) at month 4 (호황, state code 2):
- affectRatio = 1.01 + (secuRatio/0.8).coerceIn(0,1) * 0.04
  = 1.01 + 0.5 * 0.04 = 1.01 + 0.02 = 1.03
- pop = (50000 * 1.03).toInt() = 51500
- (compute exact values per PHP, lock as golden)

**F. SabotageInjury golden value test:**
Create a general in an affected city. With a fixed RNG seed:
- Verify 30% probability check: `rng.nextDouble() >= 0.3` means 70% chance of NO injury
- If injured: injury amount from `rng.nextInt(1, 17)` (1-16 inclusive)
- crew *= 0.98, atmos *= 0.98, train *= 0.98
- injury capped at 80

Use golden seed to produce deterministic outcomes. Lock the exact injury amount and resulting stat values.

All golden values must be computed by reading the legacy PHP source (per D-06), NOT by running the Kotlin code first.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.DisasterParityTest" -x :gateway-app:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DisasterParityTest.kt exists in `backend/game-app/src/test/kotlin/com/opensam/qa/parity/`
    - DisasterParityTest.kt contains test for 3-year grace period with assertion that city fields unchanged
    - DisasterParityTest.kt contains test verifying boomRate values: 0.0 for months 1,10 and 0.25 for months 4,7
    - DisasterParityTest.kt contains test for RNG seed string parity (documents "disaster" vs "disater")
    - DisasterParityTest.kt contains golden value assertions for disaster effect on city fields (pop, agri, comm)
    - DisasterParityTest.kt contains golden value assertions for boom effect on city fields
    - DisasterParityTest.kt contains test for disaster state codes per month matching legacy
    - `./gradlew :game-app:test --tests "com.opensam.qa.parity.DisasterParityTest"` exits 0
  </acceptance_criteria>
  <done>Disaster/event probabilities, RNG seed, grace period, state codes, and per-city effects all verified against legacy PHP values via golden value tests. Any parity divergences (like RNG seed typo) are either fixed or documented.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.TurnPipelineParityTest" -x :gateway-app:test` -- all tests pass including new ordering assertions
2. `cd backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.DisasterParityTest" -x :gateway-app:test` -- all disaster golden value tests pass
3. `cd backend && ./gradlew :game-app:test -x :gateway-app:test` -- full game-app test suite green (no regressions)
</verification>

<success_criteria>
- postUpdateMonthly call order locked by failing test if anyone reorders the calls
- Disaster probabilities match legacy RaiseDisaster.php (boomRate, raiseProp, affectRatio formulas)
- RNG seed string parity verified (disaster vs disater typo documented/fixed)
- 3-year grace period verified
- City state codes per month match legacy
- Full game-app test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-turn-engine-completion/09-02-SUMMARY.md`
</output>
