---
plan: 23-03-Gin7-processWarIncome
phase: 23-gin7-economy-port
milestone: v2.3
wave: 1
autonomous: true
requirements: [EC-03]
key_files:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/Gin7ProcessWarIncomeTest.kt
references:
  - upstream: a7a19cc3 — com.opensam.engine.EconomyService.processWarIncome
  - legacy_php: hwe/sammo/Event/Action/ProcessWarIncome.php
---

# Plan 23-03: Gin7EconomyService.processWarIncome

## Objective

Implement war-income processing: factions currently in war state receive a monthly bonus income. Unlike `processIncome` (month 1/7), this runs EVERY month via the pre_month event.

## Legacy reference extraction

```bash
git show a7a19cc3 -- backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt | awk '/fun processWarIncome/,/^\s*\}/'
```

Legacy behavior: only factions with `warState > 0` receive war bonus. Bonus is a flat funds payment proportional to military_power or number of officers.

## Tasks

### Task 1: TDD RED

Create `Gin7ProcessWarIncomeTest.kt` with:
1. Faction with `warState == 0` receives NO war income
2. Faction with `warState > 0` receives war income bonus
3. Multiple warring factions all receive independently
4. Empty world runs cleanly

Commit RED with `--no-verify`.

### Task 2: GREEN — port the method

1. Add `fun processWarIncome(world: SessionState)` (no resource parameter — always funds)
2. Filter factions by war state
3. Apply per-faction bonus formula matching upstream
4. Persist

Verify tests pass.

Commit GREEN with `--no-verify`.

## Acceptance criteria

- [ ] `Gin7EconomyService.processWarIncome(world)` public method
- [ ] War state filter works
- [ ] Bonus formula matches upstream constants
- [ ] 4 tests passing
- [ ] No regression in Phase 14 / 22 tests
- [ ] SUMMARY.md + STATE.md + ROADMAP.md updated
