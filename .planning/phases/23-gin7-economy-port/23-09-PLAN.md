---
plan: 23-09-Gin7-randomizePlanetTradeRate
phase: 23-gin7-economy-port
milestone: v2.3
wave: 3
autonomous: true
requirements: [EC-09]
key_files:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/Gin7RandomizePlanetTradeRateTest.kt
references:
  - legacy: EconomyService.randomizeCityTradeRate (line 603-628)
---

# Plan 23-09: Gin7EconomyService.randomizePlanetTradeRate

## Objective

Port legacy `randomizeCityTradeRate` — periodically shuffles each planet's `trade_route` / `trade_rate` to introduce market variance. Small method (~25 lines).

## Legacy reference

`EconomyService.kt:603-628`. Read via `sed -n '603,628p' backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt`.

## Tasks

### Task 1: TDD RED

Create `Gin7RandomizePlanetTradeRateTest.kt` with:
1. Seeded RNG → deterministic trade rate assignment
2. All supplied planets receive new trade rate; isolated planets unchanged
3. Value range within legacy min/max bounds
4. Empty world clean run
5. Multiple calls with same seed → same result (determinism)

Commit RED with `--no-verify`.

### Task 2: GREEN — port the method

1. Add `fun randomizePlanetTradeRate(world: SessionState)` to Gin7
2. Inject `Random` for testability
3. Apply per-planet randomization
4. Persist via planetRepository

Verify tests pass.

Commit GREEN with `--no-verify`.

## Acceptance criteria

- [ ] `Gin7EconomyService.randomizePlanetTradeRate(world)` implemented
- [ ] Deterministic with seeded RNG
- [ ] 5 tests passing
- [ ] SUMMARY.md + STATE.md + ROADMAP.md updated
