---
plan: 23-02-Gin7-processSemiAnnual-per-resource
phase: 23-gin7-economy-port
milestone: v2.3
wave: 1
autonomous: true
requirements: [EC-02]
key_files:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/Gin7ProcessSemiAnnualTest.kt
references:
  - upstream: a7a19cc3 — com.opensam.engine.EconomyService.processSemiAnnual (per-resource body)
  - legacy_php: hwe/sammo/Event/Action/ProcessSemiAnnual.php
---

# Plan 23-02: Gin7EconomyService.processSemiAnnual per-resource body

## Objective

Implement real per-resource semi-annual decay body in `Gin7EconomyService.processSemiAnnual(world, resource)`. Decays faction treasury AND officer personal stockpiles on a per-resource schedule (month 1 = funds, month 7 = supplies). Runs ONCE per year per resource.

## Legacy reference extraction

```bash
git show a7a19cc3 -- backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt | awk '/fun processSemiAnnual/,/^\s*\}/'
```

The legacy method applies:
1. Faction treasury decay (tax on stockpile above threshold — discourages hoarding)
2. Officer personal stockpile decay (upkeep cost for private reserves)
3. Decay rate scales with amount held (progressive bracket)

## Tasks

### Task 1: TDD RED

Create `Gin7ProcessSemiAnnualTest.kt` with:
1. `processSemiAnnual(world, "gold")` decays `faction.funds` AND `officer.funds`; untouched `supplies` fields verified
2. `processSemiAnnual(world, "rice")` decays `faction.supplies` AND `officer.supplies`; untouched `funds` fields verified
3. Decay rate anchor: faction with 10,000 funds loses X% per call (anchor to legacy bracket — read from upstream)
4. Officer with <1,000 funds: no decay (below threshold)
5. Invalid resource throws

Commit RED with `--no-verify`.

### Task 2: GREEN — port the method

1. Add `fun processSemiAnnual(world: SessionState, resource: String)` to Gin7
2. `require(resource in setOf("gold", "rice"))`
3. Apply progressive-bracket decay formula per resource
4. Persist faction + officer changes via repositories

Verify all 5 tests pass.

Commit GREEN with `--no-verify`.

## Acceptance criteria

- [ ] `Gin7EconomyService.processSemiAnnual(world, resource)` public method
- [ ] Per-resource isolation enforced
- [ ] Progressive decay bracket matches legacy constants
- [ ] 5 tests passing
- [ ] No regression in Phase 14 / Phase 22 tests
- [ ] SUMMARY.md + STATE.md + ROADMAP.md updated
