---
plan: 23-07-Gin7-processYearlyStatistics
phase: 23-gin7-economy-port
milestone: v2.3
wave: 3
autonomous: true
requirements: [EC-07]
key_files:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/Gin7ProcessYearlyStatisticsTest.kt
references:
  - legacy: EconomyService.processYearlyStatistics (line 362-438, ~76 lines)
---

# Plan 23-07: Gin7EconomyService.processYearlyStatistics

## Objective

Port legacy `processYearlyStatistics` (annual January 1 trigger) to Gin7. Refreshes faction `military_power` and `officer_count` based on current roster, resources, tech_level, and planet aggregates.

## Legacy reference

```kotlin
// From EconomyService.kt:362-438
// 국력 = (자원/100 + 기술 + 도시파워 + 장수능력 + 숙련/1000 + 경험공헌/100) / 10
// Applied per-faction annually
```

Read the full method body from `EconomyService.kt:362-438`.

## Tasks

### Task 1: TDD RED

Create `Gin7ProcessYearlyStatisticsTest.kt` with:
1. Faction with known (resources, tech, planets, officers) → expected military_power matches formula
2. Empty faction (no planets, no officers) → military_power = 0
3. `officer_count` field updated to match roster size
4. Neutral faction (id=0) skipped
5. Anchor: formula constants match legacy (document in SUMMARY if any deviate)

Commit RED with `--no-verify`.

### Task 2: GREEN — port the method

1. Add `fun processYearlyStatistics(world: SessionState)` to Gin7
2. Port the per-faction calculation loop
3. LOGH domain mapping: `power → militaryPower`, `gennum → officerCount`
4. Persist via factionRepository

Verify tests pass.

Commit GREEN with `--no-verify`.

## Acceptance criteria

- [ ] `Gin7EconomyService.processYearlyStatistics(world)` implemented
- [ ] Formula matches legacy constants (or deviations documented)
- [ ] 5 tests passing
- [ ] SUMMARY.md + STATE.md + ROADMAP.md updated
