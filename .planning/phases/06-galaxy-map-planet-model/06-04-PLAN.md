---
phase: 06-galaxy-map-planet-model
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - frontend/src/app/(game)/galaxy/page.tsx
  - backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
autonomous: false
requirements: [GAL-01, GAL-02, GAL-03, GAL-04]

must_haves:
  truths:
    - "Galaxy map page is accessible and renders 80 star systems"
    - "Star systems show correct faction ownership colors"
    - "Fortress systems are visually distinct and show gun stats in detail panel"
    - "Route connections are navigable on the map"
    - "ScenarioService integrates star system initialization into world creation"
  artifacts:
    - path: "frontend/src/app/(game)/galaxy/page.tsx"
      provides: "Galaxy map page route"
      contains: "GalaxyMap"
  key_links:
    - from: "galaxy/page.tsx"
      to: "GalaxyMap component"
      via: "React component import"
      pattern: "GalaxyMap"
    - from: "ScenarioService"
      to: "StarSystemService"
      via: "initializeStarSystems call during world creation"
      pattern: "starSystemService.initializeStarSystems"
---

<objective>
Wire the galaxy map into the game UI, integrate star system initialization into scenario/world creation, and verify the end-to-end flow.

Purpose: Connects all Phase 6 components into a working feature accessible to players.
Output: Galaxy map page, ScenarioService integration, human-verified visual result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-galaxy-map-planet-model/06-02-SUMMARY.md
@.planning/phases/06-galaxy-map-planet-model/06-03-SUMMARY.md
@backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
@frontend/src/app/(game)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Galaxy page route + ScenarioService star system integration</name>
  <files>
    frontend/src/app/(game)/galaxy/page.tsx
    backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
  </files>
  <action>
    1. **Create `frontend/src/app/(game)/galaxy/page.tsx`**:
       - Next.js page component at the `/galaxy` route inside the game layout
       - Import GalaxyMap from `@/components/galaxy/GalaxyMap`
       - Get sessionId from the game context/store (follow existing pattern in other game pages -- check how other pages under `(game)/` obtain the session ID)
       - Render GalaxyMap with sessionId prop
       - Add page title "Galaxy Map" or "은하 지도"
       - Full-height layout: the map should fill the available viewport space

    2. **Update ScenarioService** to integrate star system initialization:
       - Find the method that creates/initializes a new game world (look for where `planetService.initializeAllCities()` is called)
       - After planet initialization, add a call to `starSystemService.initializeStarSystems(sessionId, "logh", factionMap)` where factionMap maps faction name strings ("제국", "동맹", "페잔") to their DB faction IDs
       - After star systems are initialized, update planets with their `starSystemId` foreign keys by matching planet.mapPlanetId to star system mapStarId ranges or by using the star system's planet list
       - Inject StarSystemService into ScenarioService constructor

    Note: The exact integration point depends on the current ScenarioService structure. Read it first, find where world initialization happens, and add star system initialization alongside planet initialization. If ScenarioService delegates to WorldService, follow the delegation chain.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin --quiet 2>&1 | tail -5 && cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Galaxy map page exists at /galaxy route. ScenarioService initializes 80 star systems with routes when creating a new game world. Both backend and frontend compile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of galaxy map</name>
  <files>none</files>
  <action>
    Human verifies the complete galaxy map system visually. All automation is complete from prior tasks. This checkpoint confirms visual quality and interactivity.
  </action>
  <verify>Human visual inspection per how-to-verify steps below</verify>
  <done>Human confirms galaxy map renders correctly with all 80 systems, faction colors, fortress indicators, and interactive features</done>
  <what-built>Complete galaxy map system: 80 star systems with route connections, faction territory coloring (Empire blue, Alliance green, Fezzan yellow), fortress system indicators (Iserlohn, Geiersburg, Rentenberg, Garmisch), interactive detail panel, pan/zoom navigation</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && ./gradlew :game-app:bootRun`
    2. Start frontend: `cd frontend && pnpm dev`
    3. Navigate to the galaxy map page in the browser
    4. Verify:
       - 80 star systems visible on the map
       - Empire systems are blue, Alliance systems are green, Fezzan (1 system) is yellow
       - Route lines connect star systems
       - 4 fortress systems have distinctive hexagonal indicators (Iserlohn, Geiersburg/Eisenherz, Rentenberg/Freya, Garmisch/Kiphoiser)
       - Click a star system -- detail panel shows name, faction, level, planet count
       - Click a fortress system -- detail panel shows fortress type, gun power, gun range, garrison capacity
       - Pan (drag) and zoom (scroll wheel) work on the map
       - Connected systems highlight when a system is selected
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Backend compiles and boots without migration errors
- Frontend compiles and galaxy page renders
- All 80 star systems visible on galaxy map
- 4 fortress systems have distinctive visual treatment
- Detail panel shows complete star system information
</verification>

<success_criteria>
- Galaxy map page is accessible at /galaxy
- 80 star systems rendered with correct faction colors
- Route connections visible
- Fortress systems visually distinct with gun stats in detail
- Pan/zoom navigation works
- Human verification confirms visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/06-galaxy-map-planet-model/06-04-SUMMARY.md`
</output>
