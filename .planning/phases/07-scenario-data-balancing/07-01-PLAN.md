---
phase: 07-scenario-data-balancing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/src/main/resources/data/scenarios/scenario_logh_10.json
  - backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
  - backend/game-app/src/test/kotlin/com/openlogh/service/ScenarioServiceTest.kt
autonomous: true
requirements:
  - SCEN-01
  - SCEN-02

must_haves:
  truths:
    - "10개 시나리오 모두 게임 시작 시 Officer 엔티티가 DB에 적재된다"
    - "S10(UC799.4) 시나리오가 43개 이상의 장교 데이터를 포함한다"
    - "라인하르트, 양웬리 등 원작 캐릭터의 8스탯이 gin7 기준으로 확인 가능하다"
    - "ScenarioService.initializeWorld()가 LOGH 시나리오에서 함대를 생성한다"
  artifacts:
    - path: "backend/shared/src/main/resources/data/scenarios/scenario_logh_10.json"
      provides: "S10 장교 데이터 (43명 이상, 8-stat 형식)"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt"
      provides: "LOGH 시나리오용 초기 함대 생성 로직"
  key_links:
    - from: "scenario_logh_10.json general[]"
      to: "ScenarioService.parseOfficer()"
      via: "hasEightStatTuple 감지 (row.size>=16, bornYear UC 700-900 range)"
      pattern: "collectGenerals.*scenario.general"
    - from: "ScenarioService.initializeWorld()"
      to: "Fleet entity"
      via: "LOGH 시나리오 초기 함대 생성"
      pattern: "Fleet.*save|fleetRepository"
---

<objective>
S10 시나리오 장교 데이터 완성 + LOGH 시나리오 초기 함대 생성

Purpose: S9까지는 43-46명 장교 데이터가 있으나 S10(UC799.4 황제 만세!)은 general[] 배열이 비어 있다. 또한 LOGH 시나리오 초기화 시 함대가 생성되지 않아 게임 시작 후 함대 편성이 불가능하다.

Output: S10 장교 JSON 데이터(43명+), LOGH 시나리오용 초기 함대 생성 코드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/shared/src/main/resources/data/scenarios/scenario_logh_09.json
@backend/shared/src/main/resources/data/scenarios/scenario_logh_10.json
@backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Fleet.kt
@backend/game-app/src/main/kotlin/com/openlogh/repository/FleetRepository.kt

<interfaces>
<!-- general[] row format (8-stat LOGH, row.size=18): -->
<!-- [affinity, name, picture|null, nationIdx, planetName|null, leadership, command, intelligence, politics, administration, mobility, attack, defense, officerLevel, bornYear, deadYear, personality1, personality2] -->
<!-- nationIdx: 1=제국, 2=동맹, 3=페잔 -->
<!-- officerLevel: 10=원수, 9=상급대장, 8=대장, 7=중장, 6=소장, 5=준장, 4=대령, 3=중령, 2=소령, 1=대위, 0=소위 -->
<!-- personality codes: "정복","의협","유지","신중","야심","충실","용맹","외교","냉철","모략","위압","천재" -->
<!-- hasEightStatTuple detection: row.size>=16 AND row[10..12] are Number AND row[8..9] are Number AND row[14] in 700..900 -->

<!-- ScenarioService.initializeWorld() saves Officers via officerRepository.saveAll(generalsToSave) -->
<!-- Fleet entity fields: sessionId, name, leaderOfficerId, factionId, planetId, meta -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: S10 장교 데이터 완성 (scenario_logh_10.json)</name>
  <files>backend/shared/src/main/resources/data/scenarios/scenario_logh_10.json</files>
  <behavior>
    - S09와 동일한 장교 명단 기반으로 S10 맥락(UC799.4)에 맞는 계급/배치 반영
    - S10 scenarios.json 캡 목록: empire_cap=[18,33,41,50,44,55,71,68,64,24,78,49,43,61,62,69,46,75,77] — 제국이 다수 성계 보유
    - 모든 행은 18-element 8-stat 형식: [affinity, name, null, nationIdx, planetName|null, leadership, command, intelligence, politics, administration, mobility, attack, defense, officerLevel, bornYear, deadYear, personality1, personality2]
    - 라인하르트: leadership=98,command=96,intelligence=92,politics=85,administration=78,mobility=95,attack=97,defense=90, officerLevel=10(원수), bornYear=767
    - 양웬리: leadership=92,command=95,intelligence=98,politics=72,administration=68,mobility=88,attack=90,defense=95, officerLevel=8(대장), bornYear=767
    - S09 대비 변경점: 로이엔탈 사망(제외), 비텐펠트 계급 상승, 양웬리 사망 이전 배치
    - 최소 40명 이상 장교 포함 (S09=43명 기준)
  </behavior>
  <action>
    Read scenario_logh_09.json general[] as reference. S10(UC799.4 황제만세)은 S9 란테마리오 직후로 제국이 사실상 승리 직전 상황이다.

    S10 general[] 구성 지침:
    1. 제국(nationIdx=1): 라인하르트 원수(officerLevel=10), 미터마이어 상급대장(9), 뮐러 대장(8), 비텐펠트 대장(8), 켐프 중장(7), 루츠 중장(7), 바그다슈 중장(7), 슈탕게 준장(5) 등 주요 제독 포함. 로이엔탈 제외(S10 전 사망).
    2. 동맹(nationIdx=2): 양웬리 대장(8), 이슈르 중장(7), 파트리체프 소장(6), 카스텔라노 소장(6) 등 잔존 함대 지휘관.
    3. 페잔(nationIdx=3): 루빈스키, 도밀로 중립 인물 포함.

    JSON "general" 배열을 완전히 채운다. "general_ex"와 "general_neutral"은 빈 배열로 유지.

    이미 존재하는 S10 JSON의 다른 필드(title, startYear, stat, const, nation, diplomacy 등)는 건드리지 않는다.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('backend/shared/src/main/resources/data/scenarios/scenario_logh_10.json') as f:
    d = json.load(f)
gen = d['general']
assert len(gen) >= 40, f'Expected >=40 generals, got {len(gen)}'
# Verify 8-stat format
for g in gen:
    assert len(g) == 18, f'Row length must be 18, got {len(g)} for {g[1]}'
    assert isinstance(g[5], (int, float)), f'leadership must be number for {g[1]}'
    assert g[14] in range(700, 900), f'bornYear must be 700-900 for {g[1]}'
print(f'S10 general count: {len(gen)} - OK')
print('Sample:', gen[0][1], gen[1][1])
"
    </automated>
  </verify>
  <done>scenario_logh_10.json의 general[] 배열이 40명 이상의 18-element 8-stat 형식 장교 데이터를 포함하며 검증 스크립트가 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LOGH 시나리오 초기 함대 생성 로직 추가</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
    backend/game-app/src/test/kotlin/com/openlogh/service/ScenarioServiceTest.kt
  </files>
  <behavior>
    - initializeWorld()가 "logh" 맵 시나리오에서 Officer 저장 후 각 faction별 초기 함대를 1개씩 생성한다
    - 함대명: "{factionName} 제1함대" 형식
    - Fleet.leaderOfficerId = 해당 진영의 최고 officerLevel을 가진 Officer.id
    - Fleet.factionId, Fleet.planetId = 해당 진영의 capitalPlanetId
    - Fleet.meta["initial"] = true 플래그로 초기 생성 표시
    - Test: ScenarioService.initializeWorld("logh_01") 후 Fleet 엔티티가 진영 수(3)만큼 존재한다
  </behavior>
  <action>
    ScenarioService.initializeWorld() 내에서 officerRepository.saveAll() 직후에 아래 코드 블록을 추가한다:

    ```kotlin
    // 3. Create initial fleets for LOGH scenarios (one per faction)
    if (mapName == "logh") {
        val savedOfficersByFaction = savedOfficers.groupBy { it.factionId }
        val fleetsToCreate = savedNations.mapNotNull { faction ->
            val factionOfficers = savedOfficersByFaction[faction.id] ?: return@mapNotNull null
            val leader = factionOfficers.maxByOrNull { it.officerLevel } ?: return@mapNotNull null
            Fleet(
                sessionId = worldId,
                name = "${faction.name} 제1함대",
                leaderOfficerId = leader.id,
                factionId = faction.id,
                planetId = faction.capitalPlanetId ?: allCityIds.firstOrNull() ?: 0L,
                meta = mutableMapOf("initial" to true),
            )
        }
        if (fleetsToCreate.isNotEmpty()) {
            fleetRepository.saveAll(fleetsToCreate)
            log.info("[World {}] Created {} initial fleets for LOGH scenario", worldId, fleetsToCreate.size)
        }
    }
    ```

    FleetRepository가 ScenarioService 생성자에 없으면 `@Autowired` constructor 파라미터로 추가한다 (Phase 02 결정: FleetRepository added as nullable field to CommandServices — 여기서는 nullable 없이 직접 주입).

    Test: ScenarioServiceTest에서 logh_01 시나리오 초기화 후 fleetRepository.findBySessionId(worldId).size >= 2 검증 (제국+동맹 최소).

    ScenarioServiceTest가 이미 존재하므로 기존 테스트를 깨지 않도록 새 test function만 추가한다. Mock이 필요한 경우 @MockitoBean 패턴 사용 (Phase 02 결정: Spring MVC 프로젝트, runBlocking 패턴 유지).
  </action>
  <verify>
    <automated>cd backend && ./gradlew :game-app:test --tests "com.openlogh.service.ScenarioServiceTest" -x compileTestKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ScenarioService가 LOGH 시나리오 초기화 시 Fleet 엔티티를 진영별로 생성하며 테스트가 통과한다</done>
</task>

</tasks>

<verification>
- python3 으로 S10 JSON 검증: general[] >= 40 entries, 모두 18-element 8-stat 형식
- 전체 시나리오 S1-S10 all have general[] data: `for i in 1..10: python3 -c "...len(general)..."`
- Gradle test: ScenarioServiceTest passes
</verification>

<success_criteria>
1. S10 scenario_logh_10.json의 general[] 배열이 40명 이상의 올바른 8-stat 형식 데이터를 포함한다
2. ScenarioService.initializeWorld("logh_01")이 Officer + Fleet 엔티티를 DB에 저장한다
3. 모든 10개 LOGH 시나리오 JSON이 유효한 general[] 데이터를 가진다
4. ScenarioServiceTest가 전부 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/07-scenario-data-balancing/07-01-SUMMARY.md`
</output>
