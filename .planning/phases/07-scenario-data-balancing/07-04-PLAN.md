---
phase: 07-scenario-data-balancing
plan: "04"
type: tdd
wave: 2
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - backend/game-app/src/test/kotlin/com/openlogh/integration/ScenarioPlayableIntegrationTest.kt
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "LOGH 시나리오를 초기화하면 Officer, Fleet, Planet 엔티티가 모두 DB에 존재한다"
    - "초기화된 Officer가 커맨드를 실행할 수 있다 (CP 차감 → 대기시간 → 결과)"
    - "경제 파이프라인이 한 사이클 동안 오류 없이 실행된다"
    - "커스텀 캐릭터(8-stat, 합계 400)가 시나리오에 합류하고 진영에 귀속된다"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/openlogh/integration/ScenarioPlayableIntegrationTest.kt"
      provides: "전체 플로우 통합 테스트 (시나리오 시작→커맨드→경제→커스텀 캐릭터)"
  key_links:
    - from: "ScenarioService.initializeWorld()"
      to: "Officer + Fleet + Planet 엔티티"
      via: "H2 in-memory DB, @SpringBootTest(webEnvironment=NONE)"
      pattern: "officerRepository.findBySessionId|fleetRepository.findBySessionId"
    - from: "CommandExecutor.executeGeneralCommand()"
      to: "CommandResult"
      via: "대기(ALWAYS_ALLOWED) 커맨드로 CP 흐름 검증"
      pattern: "CommandResult.*success"
    - from: "OfficerService.createOfficer()"
      to: "Officer with statMode=8stat"
      via: "CreateGeneralRequest(statMode=8stat, valid 8 stats summing to 400)"
      pattern: "isEightStat.*validateEightStats"
---

<objective>
전체 게임 시작 플로우 통합 테스트 (시나리오 초기화 → 커맨드 → 경제 → 커스텀 캐릭터)

Purpose: Phase 1-6의 모든 시스템이 LOGH 시나리오 기반으로 함께 동작하는지 검증한다. 단위 테스트가 각 시스템을 개별 검증했다면, 이 통합 테스트는 실제 게임 시작 흐름 전체를 E2E로 확인한다.

Output: ScenarioPlayableIntegrationTest.kt — @SpringBootTest H2 기반 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-scenario-data-balancing/07-01-SUMMARY.md
@.planning/phases/07-scenario-data-balancing/07-02-SUMMARY.md
@.planning/phases/07-scenario-data-balancing/07-03-SUMMARY.md

@backend/game-app/src/main/kotlin/com/openlogh/service/ScenarioService.kt
@backend/game-app/src/main/kotlin/com/openlogh/service/OfficerService.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/RealtimeService.kt
@backend/game-app/src/main/resources/application-test.yml

<interfaces>
<!-- ScenarioService.initializeWorld(scenarioCode: String): SessionState -->
<!-- Returns the SessionState (world) with id set -->

<!-- OfficerService.createOfficer(worldId: Long, loginId: String, request: CreateGeneralRequest): Officer? -->
<!-- For 8-stat: request.statMode="8stat", sum(8 stats)=400, each in 20..95 -->

<!-- CommandExecutor.executeGeneralCommand(general, actionCode, arg, env): CommandResult -->
<!-- "대기" is ALWAYS_ALLOWED — no position card needed, minimal CP cost -->

<!-- Gin7EconomyService (injected via Spring): -->
<!-- fun collectTax(world: SessionState, factions: List<Faction>, planets: List<Planet>): Map<Long, Long> -->

<!-- application-test.yml uses H2 in-memory DB with Flyway disabled -->
<!-- Use @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.NONE) -->
<!-- Use @ActiveProfiles("test") -->
</interfaces>
</context>

<feature>
  <name>시나리오 플레이어블 통합 테스트</name>
  <files>backend/game-app/src/test/kotlin/com/openlogh/integration/ScenarioPlayableIntegrationTest.kt</files>
  <behavior>
    **Test 1: 시나리오 초기화 완전성**
    - ScenarioService.initializeWorld("logh_01") 호출
    - officerRepository.findBySessionId(worldId).size >= 40 (S1은 46명)
    - planetRepository.findBySessionId(worldId).isNotEmpty()
    - factionRepository.findBySessionId(worldId).size == 3 (제국/동맹/페잔)
    - (07-01 구현 후) fleetRepository.findBySessionId(worldId).isNotEmpty()

    **Test 2: 커맨드 실행 파이프라인**
    - 위 worldId의 첫 번째 Officer를 가져온다
    - CommandExecutor.executeGeneralCommand(officer, "대기", null, commandEnv) 실행
    - result.success == true
    - (대기 커맨드는 ALWAYS_ALLOWED이므로 직무권한카드 불필요)

    **Test 3: 경제 사이클 1회전**
    - 같은 SessionState, Faction, Planet 목록으로 Gin7EconomyService.collectTax() 호출
    - 반환된 Map의 값이 최소 하나 > 0 (세금이 걷힘)
    - 예외 발생하지 않음

    **Test 4: 커스텀 캐릭터 생성**
    - OfficerService.createOfficer(worldId, "test-user", CreateGeneralRequest(
        name="테스트제독",
        nationId=1,
        statMode="8stat",
        leadership=70, command=50, intelligence=50, politics=50,
        administration=30, mobility=50, attack=50, defense=50,
        // sum = 400
      ))
    - 반환된 Officer != null
    - officer.leadership == 70.toShort()
    - officer.factionId == 제국 factionId

    각 테스트는 @Transactional로 롤백되어 상호 격리된다.
  </behavior>
  <implementation>
    1. integration/ 패키지 생성 (test/kotlin/com/openlogh/integration/)
    2. @SpringBootTest(webEnvironment = NONE) + @ActiveProfiles("test") + @Transactional
    3. @Autowired: ScenarioService, OfficerService, CommandExecutor, Gin7EconomyService,
       officerRepository, planetRepository, factionRepository, fleetRepository
    4. CommandEnv 생성 시 SessionState의 currentYear/currentMonth 사용
    5. Phase 02 결정: CommandExecutor는 suspend fun → runBlocking{} 래퍼 사용
    6. RED→GREEN: 먼저 failing test 작성, 그 다음 실행하여 실패 확인, 그 다음 필요한 경우 코드 조정
    7. 기존 통합 테스트가 있으면 참고하되 새 파일로 분리
  </implementation>
</feature>

<verification>
  <automated>cd backend && ./gradlew :game-app:test --tests "com.openlogh.integration.ScenarioPlayableIntegrationTest" 2>&1 | tail -30</automated>
</verification>

<success_criteria>
1. 4개 통합 테스트 모두 GREEN (시나리오 초기화, 커맨드 실행, 경제 사이클, 커스텀 캐릭터)
2. @Transactional 롤백으로 테스트 간 DB 상태 격리 확인
3. H2 in-memory DB에서 실행 완료 (외부 DB 불필요)
4. 전체 game-app 테스트 suite: `./gradlew :game-app:test` GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/07-scenario-data-balancing/07-04-SUMMARY.md`
</output>
