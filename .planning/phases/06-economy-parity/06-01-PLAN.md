---
phase: 06-economy-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt
  - backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyEventParityTest.kt
  - backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
autonomous: true
requirements: [ECON-01, ECON-03, ECON-04, ECON-05, ECON-06]

must_haves:
  truths:
    - "Tax collection (gold/rice/wall) for fixed inputs matches PHP golden values within 0 tolerance"
    - "Supply route BFS and unsupplied penalty matches PHP behavior"
    - "Population growth/decline over semi-annual cycle matches PHP golden values"
    - "City infrastructure decay (0.99) + growth via genericRatio matches PHP"
    - "Salary distribution (getBill/getDedLevel) matches PHP for all dedication tiers"
    - "Disaster/boom probability and effect scaling matches PHP"
    - "Nation level thresholds and level-up rewards match PHP"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt"
      provides: "PHP-verified golden value tests for gold/rice/wall income, salary, tax rate"
      contains: "legacyCityGoldIncome"
    - path: "backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyEventParityTest.kt"
      provides: "PHP-verified golden value tests for semi-annual, disaster/boom, supply, nation level"
      contains: "processSemiAnnual"
  key_links:
    - from: "EconomyFormulaParityTest.kt"
      to: "EconomyService.kt"
      via: "direct method calls (processIncome, getBill)"
      pattern: "service\\.(processIncome|calcCity|getBill)"
    - from: "EconomyEventParityTest.kt"
      to: "EconomyService.kt"
      via: "direct method calls (processSemiAnnual, processDisasterOrBoom, updateCitySupply)"
      pattern: "service\\.(processSemiAnnual|processDisasterOrBoom|updateCitySupply|updateNationLevel)"
---

<objective>
Verify all core economy formulas against legacy PHP source and fix any mismatches immediately.

Purpose: Ensure tax collection, supply routing, population growth, city development, salary distribution, disaster/boom, and nation level calculations produce identical values to legacy PHP. This is the foundation for command-level and integration testing in Plan 02.

Output: Extended EconomyFormulaParityTest.kt and EconomyEventParityTest.kt with PHP-verified golden values; any EconomyService.kt fixes for discovered mismatches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-economy-parity/06-CONTEXT.md
@.planning/phases/06-economy-parity/06-RESEARCH.md

@backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
@backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt
@backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyEventParityTest.kt
@backend/game-app/src/main/kotlin/com/opensam/engine/modifier/NationTypeModifiers.kt
@backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/EconomyPreUpdateStep.kt
@backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/EconomyPostUpdateStep.kt
@backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/DisasterAndTradeStep.kt
@backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/YearlyStatisticsStep.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone legacy-core and verify/extend income + salary golden values (ECON-01, ECON-06)</name>
  <files>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
  </files>
  <read_first>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyFormulaParityTest.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/modifier/NationTypeModifiers.kt
  </read_first>
  <behavior>
    - Test: calcCityGoldIncome with pop=10000, comm=500, commMax=1000, trust=80, secu=500, secuMax=1000, officerCount=0, isCapital=false, nationTypeMod=1.0 produces exact PHP golden value from func_time_event.php:88-104
    - Test: calcCityRiceIncome with matching parameters produces exact PHP golden value from func_time_event.php:106-122
    - Test: calcCityWallIncome with def=800, wall=600, wallMax=1000 produces exact PHP golden value from func_time_event.php:124-140
    - Test: calcCityGoldIncome with nationTypeMod=1.2 (merchant nation type like 상인) produces correctly modified value
    - Test: getBill with dedication=0 returns 400 (floor tier), dedication=100 returns ceil(sqrt(100)/10)*200+400=600, dedication=900 returns ceil(sqrt(900)/10)*200+400=1000
    - Test: getDedLevel maps dedication ranges to correct level tiers (0->0, 100->1, ..., edge values at thresholds)
    - Test: processIncome with empty treasury and salary > available gold handles partial payment correctly
    - Test: processIncome with nation taxRate=10 vs taxRate=20 produces correct gold/rice proportional difference
  </behavior>
  <action>
    **Step 0: Clone legacy-core** (per D-02).
    Run: `cd /Users/apple/Desktop/개인프로젝트/opensamguk && git clone https://storage.hided.net/gitea/devsam/core.git legacy-core`
    If clone fails (network/auth), use existing Kotlin comments and formula annotations in EconomyFormulaParityTest.kt as fallback (mark tests with LOW confidence comment).

    **Step 1: Read legacy PHP formulas.**
    Read these files from legacy-core/:
    - `hwe/func_time_event.php` lines ~88-161 (calcCityGoldIncome, calcCityRiceIncome, calcCityWallRiceIncome)
    - `hwe/func_converter.php` line ~668 (getBill, getDedLevel)
    - `hwe/sammo/Event/Action/ProcessIncome.php` (salary distribution, taxRate logic)
    - `hwe/sammo/GameConstBase.php` (basegold=0, baserice=2000)

    For each formula, trace the PHP execution with concrete inputs and record the exact output. These become golden values.

    **Step 2: Extend EconomyFormulaParityTest.kt.**
    For each existing test class (`CityGoldIncome`, `CityRiceIncome`, `CityWallIncome`, `Salary`, `TaxRate`):
    - Add @CsvSource rows with PHP-traced golden values (at least 3-4 distinct input combinations per formula)
    - Include nation type modifier variants: 상인 (goldMultiplier=1.2), 농업국 (riceMultiplier=1.2, popGrowthMultiplier=1.05), 도적 (goldMultiplier=0.9), default (1.0)
    - Include edge cases: zero population, zero commMax, zero officerCount, max trust=100, min trust=0

    For getBill/getDedLevel:
    - Add @CsvSource parameterized test with at least 6 dedication values: 0, 50, 100, 400, 900, 2500 (covers floor, mid, high tiers)
    - PHP formula: `ceil(sqrt($dedication) / 10) * 200 + 400`

    For processIncome salary distribution:
    - Test with nation gold=500 and total salary=1000 (partial payment scenario)
    - Test with nation gold=5000 and total salary=1000 (full payment)
    - Verify general gold/rice changes and nation treasury deduction

    **Step 3: Fix mismatches immediately** (per D-03).
    If any golden value test fails, read the corresponding EconomyService.kt method, compare with PHP, fix the Kotlin code. Add a comment like `// Fixed: PHP uses intval() here, not round()` at the fix site.

    Run: `cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.EconomyFormulaParityTest" -x :gateway-app:test`
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.EconomyFormulaParityTest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - EconomyFormulaParityTest.kt contains at least 3 @CsvSource rows per income formula (gold, rice, wall) with PHP-traced golden values
    - EconomyFormulaParityTest.kt contains getBill parameterized test with at least 6 dedication values
    - EconomyFormulaParityTest.kt contains processIncome salary distribution tests (partial and full payment)
    - EconomyFormulaParityTest.kt contains nation type modifier variant tests (at least 2 nation types)
    - All EconomyFormulaParityTest tests pass (exit code 0)
    - Any EconomyService.kt fixes have inline comments explaining the PHP divergence
  </acceptance_criteria>
  <done>All income formulas (gold/rice/wall), salary (getBill/getDedLevel), and processIncome salary distribution verified against PHP golden values. Mismatches fixed inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify/extend semi-annual, supply, disaster/boom, nation level, and yearly statistics golden values (ECON-03, ECON-04, ECON-05)</name>
  <files>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyEventParityTest.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt
  </files>
  <read_first>
    backend/game-app/src/test/kotlin/com/opensam/qa/parity/EconomyEventParityTest.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/EconomyService.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/EconomyPostUpdateStep.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/DisasterAndTradeStep.kt,
    backend/game-app/src/main/kotlin/com/opensam/engine/turn/steps/YearlyStatisticsStep.kt
  </read_first>
  <behavior>
    - Test: processSemiAnnual applies 0.99 decay to all city infrastructure fields BEFORE growth (ordering critical per Research Pitfall 3)
    - Test: popIncrease with pop=5000, popMax=10000, supplied=true, nationTypeMod popGrowthMultiplier=1.0 produces exact PHP golden value
    - Test: popIncrease with 농업국 popGrowthMultiplier=1.05 produces correctly modified value
    - Test: infrastructure genericRatio growth for agri, comm, secu, def, wall each produce PHP golden values
    - Test: updateCitySupply BFS from capital marks connected cities as supplied, disconnected as unsupplied with 0.9 trust decay
    - Test: unsupplied city with trust<30 converts to neutral (belong=0)
    - Test: processDisasterOrBoom disaster probability 0.06-secuRatio*0.05 matches PHP per month (1,4,7,10)
    - Test: processDisasterOrBoom boom probability 25% for months 4,7 matches PHP
    - Test: disaster effect scaling based on secuRatio matches PHP (pop/agri/comm reduction)
    - Test: updateNationLevel threshold-based level-up with gold/rice reward matches PHP NATION_LEVEL_THRESHOLDS
    - Test: randomizeCityTradeRate produces values in range 95-105 with level-based probability
    - Test: processYearlyStatistics power formula = (resource+tech+cityPower+statPower+dexPower+expDed)/10 matches PHP
  </behavior>
  <action>
    **Step 1: Read legacy PHP semi-annual and event formulas.**
    Read from legacy-core/:
    - `hwe/sammo/Event/Action/ProcessSemiAnnual.php` (0.99 decay ordering, popIncrease formula, genericRatio formula, trust adjustment, resource decay)
    - `hwe/func_time_event.php` (popIncrease function, supply route logic)
    - `hwe/sammo/Event/Action/RaiseDisaster.php` (disaster probability `0.06 - secuRatio * 0.05`, boom rate, effect scaling by secuRatio)
    - `hwe/sammo/Event/Action/RandomizeCityTradeRate.php` (level-based probability, range 95-105)
    - `hwe/sammo/Event/Action/UpdateNationLevel.php` (thresholds: 0,1,2,4,6,9,12,16,20,25; gold/rice rewards)
    - `hwe/sammo/Event/Action/checkStatistic` or equivalent (power formula components)

    For each formula, trace execution with concrete inputs. Record exact output as golden values.

    **Step 2: Extend EconomyEventParityTest.kt.**

    For processSemiAnnual:
    - Add test verifying decay-before-growth ordering: city with agri=500, agriMax=1000 -> after semi-annual, agri = round(500 * 0.99 * (1 + genericRatio)). Golden value from PHP trace.
    - Add popIncrease parameterized test with at least 4 rows: low pop (1000), mid pop (5000), high pop near cap (9000/10000), overpop (11000/10000)
    - Add infrastructure growth parameterized test for agri, comm, secu, def, wall with distinct initial values
    - Add nation type modifier variants (농업국 popGrowthMultiplier=1.05)
    - Add trust adjustment test: trust<50 increases by 1, trust>50 decreases by 1, trust=50 stays

    For updateCitySupply:
    - Add BFS test: capital at city 1, adjacent cities 2,3 connected, city 4 disconnected -> cities 1,2,3 supplied, city 4 unsupplied
    - Add unsupplied penalty test: unsupplied city trust decays by 0.9 multiplier each turn
    - Add neutral conversion test: unsupplied city with trust=25 (< 30) -> belong set to 0

    For processDisasterOrBoom:
    - Add month-specific probability test: month=1 (disaster possible), month=2 (no disaster), month=4 (disaster+boom possible)
    - Add disaster effect test with known secuRatio -> exact pop/agri/comm reduction golden values
    - Add boom effect test with known conditions -> exact infrastructure increase golden values

    For updateNationLevel:
    - Add threshold test: nation with 3 cities -> level should be 2 (threshold 2), with 6 cities -> level 4 (threshold 6)
    - Add level-up reward test: level increase from 2->3 grants specific gold/rice amount

    For randomizeCityTradeRate:
    - Add test with DeterministicRng seed verifying output in 95-105 range

    For processYearlyStatistics:
    - Add power formula test with known nation state -> exact power value

    **Step 3: Fix mismatches immediately** (per D-03).
    Same pattern as Task 1: compare failing test with PHP source, fix EconomyService.kt, add inline comment.

    Run: `cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.EconomyEventParityTest" -x :gateway-app:test`
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.EconomyEventParityTest" -x :gateway-app:test</automated>
  </verify>
  <acceptance_criteria>
    - EconomyEventParityTest.kt contains processSemiAnnual decay-before-growth ordering test
    - EconomyEventParityTest.kt contains popIncrease parameterized test with at least 4 rows
    - EconomyEventParityTest.kt contains updateCitySupply BFS test with supplied/unsupplied verification
    - EconomyEventParityTest.kt contains processDisasterOrBoom probability and effect tests
    - EconomyEventParityTest.kt contains updateNationLevel threshold test
    - EconomyEventParityTest.kt contains randomizeCityTradeRate range test
    - EconomyEventParityTest.kt contains processYearlyStatistics power formula test
    - All EconomyEventParityTest tests pass (exit code 0)
    - Any EconomyService.kt fixes have inline comments explaining the PHP divergence
  </acceptance_criteria>
  <done>Semi-annual processing, supply routing, disaster/boom, nation level, trade rate, and yearly statistics all verified against PHP golden values. Mismatches fixed inline.</done>
</task>

</tasks>

<verification>
Run full economy parity test suite:
```bash
cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test --tests "com.opensam.qa.parity.Economy*" -x :gateway-app:test
```
All tests must pass. Then run full game-app suite to verify no regressions:
```bash
cd /Users/apple/Desktop/개인프로젝트/opensamguk/backend && ./gradlew :game-app:test -x :gateway-app:test
```
</verification>

<success_criteria>
- All EconomyFormulaParityTest tests green with PHP-verified golden values (ECON-01, ECON-06)
- All EconomyEventParityTest tests green with PHP-verified golden values (ECON-03, ECON-04, ECON-05)
- Any discovered mismatches in EconomyService.kt are fixed with inline PHP-reference comments
- Full game-app test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-economy-parity/06-01-SUMMARY.md`
</output>
