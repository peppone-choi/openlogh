---
phase: 04-economy-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
autonomous: true
requirements:
  - ECON-02

must_haves:
  truths:
    - "조병창(hasShipyard=true)이 있는 행성이 매 tick마다 자동으로 함선/물자를 생산한다"
    - "생산된 함선은 PlanetWarehouse에 누적되며 플레이어 조작 없이 지속된다"
    - "TickEngine.processTick()이 ShipyardProductionService.runProduction()을 호출한다"
    - "행성 소유권이 변경되면(factionId 변경) 생산이 중단되지 않고 새 진영으로 귀속된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt"
      provides: "조병창 자동생산 — production stat 기반 함선/물자 생산"
      exports: ["ShipyardProductionService", "runProduction"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt"
      provides: "ShipyardProductionService를 매 N틱마다 호출"
  key_links:
    - from: "TickEngine.processTick()"
      to: "ShipyardProductionService.runProduction(sessionId)"
      via: "tickCount % SHIPYARD_INTERVAL_TICKS == 0L 조건"
    - from: "ShipyardProductionService.runProduction()"
      to: "PlanetWarehouse (battleship/cruiser/supplies 필드)"
      via: "planetWarehouseRepository.save(pw)"
---

<objective>
ShipyardProductionService를 gin7 규칙에 맞게 확장하고 TickEngine에 연결하여 조병창 자동생산 루프를 완성한다.

Purpose: 기존 ShipyardProductionService는 기본 로직이 있으나 TickEngine과 연결되지 않았다. gin7 규칙(production stat 기반 생산량, 조병창 행성별 품목 설정, GREEN 승조원만 생산)을 보강하고 TickEngine에서 주기적으로 호출한다.
Output: ShipyardProductionService.kt (확장) + TickEngine.kt (호출 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-economy-system/04-CONTEXT.md

@backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/PlanetWarehouse.kt

<interfaces>
<!-- Key contracts the executor needs. -->

From entity/PlanetWarehouse.kt:
```kotlin
var hasShipyard: Boolean = false
var battleship: Int = 0
var cruiser: Int = 0
var destroyer: Int = 0
var supplies: Int = 0
fun addShips(shipClass: ShipClassType, amount: Int)
fun addCrew(proficiency: CrewProficiency, amount: Int)
fun touch()
```

From entity/Planet.kt:
```kotlin
var production: Int = 0       // 현재 생산력 (0~productionMax)
var productionMax: Int = 0
var factionId: Long = 0       // 0 = 중립
```

From model/ShipClassType.kt (existing enum):
```kotlin
enum class ShipClassType { BATTLESHIP, CRUISER, DESTROYER, CARRIER, TRANSPORT, HOSPITAL }
```

From engine/GameTimeConstants.kt (check and use existing):
```kotlin
// 1 tick = 1 real second
// CP_REGEN_INTERVAL_TICKS = 300 (5분)
// GAME_SECONDS_PER_MONTH = 108000 (30 real hours)
```

From PlanetWarehouse.meta:
// planet.meta["shipyardClass"] = "CRUISER" — 조병창별 생산 함종 설정 (없으면 BATTLESHIP 기본값)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShipyardProductionService gin7 확장</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/PlanetWarehouse.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Planet.kt
  </read_first>
  <behavior>
    - Test 1: production=200이면 baseOutput=1 (200/200, coerceAtLeast(1))
    - Test 2: production=1000이면 baseOutput=5
    - Test 3: production=0이면 baseOutput=1 (최소 1 보장)
    - Test 4: hasShipyard=false인 행성은 생산 결과 목록에 포함되지 않는다
    - Test 5: planet.factionId=0(중립)이면 생산하지 않는다
    - Test 6: planet.meta["shipyardClass"]="CRUISER"이면 CRUISER 생산, 없으면 BATTLESHIP
    - Test 7: supplyOutput = production / 100, coerceAtLeast(1)
  </behavior>
  <action>
    기존 ShipyardProductionService.runProduction()을 다음과 같이 개선한다:

    1. factionId=0(중립) 행성은 건너뛴다:
       ```kotlin
       val planet = planetRepository.findById(pw.planetId).orElse(null) ?: continue
       if (planet.factionId == 0L) continue  // 중립 행성 생산 없음
       ```

    2. 생산 함종을 planet.meta["shipyardClass"]에서 읽는다 (없으면 BATTLESHIP 기본값):
       ```kotlin
       val shipClassStr = (planet.meta["shipyardClass"] as? String) ?: "BATTLESHIP"
       val shipClass = try {
           ShipClassType.valueOf(shipClassStr)
       } catch (e: IllegalArgumentException) {
           ShipClassType.BATTLESHIP
       }
       ```

    3. 기존 생산 공식 유지 (production / 200, coerceAtLeast(1)) — 이미 올바름.

    4. ProductionReport에 factionId 필드를 추가한다:
       ```kotlin
       data class ProductionReport(
           val planetId: Long,
           val planetName: String,
           val factionId: Long,         // 추가
           val shipsProduced: Int,
           val shipClass: ShipClassType,
           val crewProduced: Int,
           val suppliesProduced: Int,
       )
       ```
       report 생성 시 factionId = planet.factionId 포함.

    기존 동작(pw.addShips, pw.addCrew GREEN, pw.supplies += supplyOutput, pw.touch(), planetWarehouseRepository.save)은 그대로 유지.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.service.ShipyardProductionServiceTest" -x processResources 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 7개 테스트 통과
    - factionId=0 중립 행성은 ProductionReport에 포함되지 않음
    - planet.meta["shipyardClass"]로 생산 함종 설정 가능
  </done>
</task>

<task type="auto">
  <name>Task 2: TickEngine — ShipyardProductionService 주기적 호출</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
    - backend/game-app/src/main/kotlin/com/openlogh/engine/GameTimeConstants.kt (존재하면)
  </read_first>
  <action>
    TickEngine 생성자에 ShipyardProductionService를 추가한다.

    GameTimeConstants에 SHIPYARD_INTERVAL_TICKS = 3600L 상수를 추가한다 (1 게임일 = 3600틱). GameTimeConstants.kt가 없으면 TickEngine companion object에 private const val로 정의한다.

    processTick() 내부에 다음을 추가한다 (tacticalBattleService.processSessionBattles 호출 이후):
    ```kotlin
    // 조병창 자동생산: 매 3600틱(1 게임일)마다
    if (world.tickCount % SHIPYARD_INTERVAL_TICKS == 0L) {
        try {
            shipyardProductionService.runProduction(world.id.toLong())
        } catch (e: Exception) {
            logger.warn("Shipyard production error for world {}: {}", world.id, e.message)
        }
    }
    ```

    기존 processTick()의 다른 호출들(realtimeService, gameEventService, sessionStateRepository.save 등)은 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 컴파일 성공
    - TickEngine이 ShipyardProductionService를 생성자 주입받음:
      grep -n "shipyardProductionService" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
    - processTick()에서 주기적 호출 확인 (SHIPYARD_INTERVAL_TICKS 사용)
  </done>
</task>

</tasks>

<verification>
1. 컴파일: cd backend && ./gradlew :game-app:compileKotlin → BUILD SUCCESSFUL
2. 테스트: ./gradlew :game-app:test --tests "com.openlogh.service.ShipyardProductionServiceTest" → 7 tests passed
3. 중립 방어: grep -n "factionId == 0" backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt → 존재
4. TickEngine 연결: grep -n "shipyardProductionService.runProduction" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt → 존재
</verification>

<success_criteria>
- ShipyardProductionService가 factionId=0 중립 행성을 건너뜀
- planet.meta["shipyardClass"]로 생산 함종 설정 가능
- TickEngine이 매 3600틱마다 runProduction() 호출
- 7개 단위 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-economy-system/04-02-SUMMARY.md`
</output>
