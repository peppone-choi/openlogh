---
phase: 04-economy-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
autonomous: true
requirements:
  - ECON-01
  - ECON-03

must_haves:
  truths:
    - "TickEngine.runMonthlyPipeline()이 Gin7EconomyService를 호출한다"
    - "매 90일(전략 30턴) 주기(month=1,4,7,10)에 진영 자금이 세수만큼 증가한다"
    - "행성 인구/생산/교역 수치가 월별 성장률로 상한값까지 증가한다"
    - "세율(taxRate)이 높을수록 지지율(approval)이 감소하고 낮을수록 증가한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt"
      provides: "gin7 세율/납입 + 행성자원 성장 처리"
      exports: ["Gin7EconomyService", "processMonthly"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt"
      provides: "runMonthlyPipeline에 Gin7EconomyService 연결"
  key_links:
    - from: "TickEngine.runMonthlyPipeline()"
      to: "Gin7EconomyService.processMonthly()"
      via: "직접 호출"
    - from: "Gin7EconomyService.processMonthly()"
      to: "Faction.funds"
      via: "factionRepository.save()"
    - from: "Gin7EconomyService.processMonthly()"
      to: "Planet.approval"
      via: "planetRepository.save()"
---

<objective>
Gin7EconomyService를 새로 작성하고 TickEngine의 runMonthlyPipeline()에 연결하여 gin7 경제 루프를 완성한다.

Purpose: Phase 1에서 삼국지 로직 제거 후 stub 상태인 EconomyService.processMonthly()를 gin7 규칙으로 재구현한다. TickEngine.runMonthlyPipeline()의 TODO를 실제 경제 처리로 교체한다.
Output: Gin7EconomyService.kt (새 파일) + TickEngine.kt (wiring 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-economy-system/04-CONTEXT.md

@backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Faction.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Planet.kt

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From entity/Faction.kt:
```kotlin
var funds: Int = 0
var supplies: Int = 0
var taxRate: Short = 100          // 100 = 100%, 즉 세율 100분의 taxRate
var factionType: String = "che_neutral"  // "empire" | "alliance" | "fezzan" | "rebel"
```

From entity/Planet.kt:
```kotlin
var factionId: Long = 0
var population: Int = 0
var populationMax: Int = 0
var production: Int = 0
var productionMax: Int = 0
var commerce: Int = 0
var commerceMax: Int = 0
var approval: Float = 0f          // 0~100
var supplyState: Short = 1        // 1=공급중, 0=고립
```

From engine/TickEngine.kt (lines 125-142):
```kotlin
private fun runMonthlyPipeline(world: SessionState) {
    logger.info("Monthly pipeline triggered ...")
    gameEventService.broadcastTurnAdvance(...)
}
// ← TODO: Gin7EconomyService 호출 삽입 지점
```

From repository layer (existing patterns):
```kotlin
// PlanetRepository.findBySessionId(sessionId: Long): List<Planet>
// FactionRepository.findBySessionId(sessionId: Long): List<Faction>
// factionRepository.saveAll(factions)
// planetRepository.saveAll(planets)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gin7EconomyService — 세율 징수 + 행성자원 성장</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Planet.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Faction.kt
    - backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt (참조용, 같은 패키지)
  </read_first>
  <behavior>
    - Test 1: taxRate=30일 때 planet.commerce=10000이면 세수 = 10000 * 0.30 = 3000
    - Test 2: taxRate=50일 때 approval이 감소한다 (approval -= (taxRate - 30) * 0.5f per month)
    - Test 3: taxRate=20일 때 approval이 증가한다 (approval += (30 - taxRate) * 0.3f per month)
    - Test 4: population &lt; populationMax일 때 매달 0.5% 성장한다
    - Test 5: month=1,4,7,10일 때만 세금 징수 실행된다 (90일 주기)
    - Test 6: 행성 supplyState=0(고립)이면 자원 성장 없음
  </behavior>
  <action>
    package com.openlogh.engine에 Gin7EconomyService.kt를 생성한다.

    @Service 클래스로 factionRepository, planetRepository를 생성자 주입받는다.

    processMonthly(world: SessionState) @Transactional:
    1. 세금 징수 (isTaxMonth 체크 — month=1,4,7,10):
       - 진영별 소속 행성(factionId != 0, supplyState=1)을 조회
       - 세수 계산: sum(planet.commerce * faction.taxRate / 100)
       - faction.funds += taxRevenue
       - approval 조정: taxRate > 30이면 approval -= (taxRate - 30) * 0.5f, taxRate < 30이면 approval += (30 - taxRate) * 0.3f
       - approval.coerceIn(0f, 100f)
    2. 행성 자원 성장 (매월, supplyState=1인 행성만):
       - population: (population * 1.005).toInt().coerceAtMost(populationMax)
       - production: (production * 1.003).toInt().coerceAtMost(productionMax)
       - commerce: (commerce * 1.003).toInt().coerceAtMost(commerceMax)
    3. factionRepository.saveAll(factions), planetRepository.saveAll(planets)
    4. logger.info("[World {}] 세수 처리: {} 진영, 총 {} 자금 징수", ...)

    fun isTaxMonth(month: Short): Boolean = month.toInt() in setOf(1, 4, 7, 10)

    기존 EconomyService.processMonthly()에 "// gin7EconomyService.processMonthly()로 교체됨" 주석을 추가하여 stub임을 명시한다(삭제하지 말 것 — 기존 코드 구조 보존).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.Gin7EconomyServiceTest" -x processResources 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Gin7EconomyService.kt 컴파일 성공
    - 6개 테스트 모두 통과
    - month=1,4,7,10에만 세금 징수, 그 외에는 자원 성장만 처리
  </done>
</task>

<task type="auto">
  <name>Task 2: TickEngine.runMonthlyPipeline() — Gin7EconomyService 연결</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
    - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt (Task 1에서 생성)
  </read_first>
  <action>
    TickEngine 생성자에 Gin7EconomyService를 추가한다 (기존 생성자 파라미터 끝에 추가).

    runMonthlyPipeline(world: SessionState) 메서드 내부에 아래를 추가한다:
    ```kotlin
    try {
        gin7EconomyService.processMonthly(world)
    } catch (e: Exception) {
        logger.warn("Economy pipeline error for world {}: {}", world.id, e.message)
    }
    ```
    기존 logger.info()와 gameEventService.broadcastTurnAdvance() 호출은 그대로 유지한다.

    TODO 주석(lines 128-131)을 제거하고, 주석을 "// Gin7 경제 파이프라인: 세율/납입 + 행성자원 성장"으로 교체한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    - TickEngine이 Gin7EconomyService를 주입받아 컴파일 성공
    - runMonthlyPipeline()에서 gin7EconomyService.processMonthly(world) 호출 확인:
      grep -n "gin7EconomyService.processMonthly" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt
    - TODO 주석이 0건:
      grep -n "TODO Phase 4.*gin7EconomyService" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt | wc -l 결과 0
  </done>
</task>

</tasks>

<verification>
1. 컴파일: cd backend && ./gradlew :game-app:compileKotlin → BUILD SUCCESSFUL
2. 테스트: ./gradlew :game-app:test --tests "com.openlogh.engine.Gin7EconomyServiceTest" → 6 tests passed
3. 연결 확인: grep -n "gin7EconomyService" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt → 2건 이상 (생성자 + 호출)
4. TODO 제거: grep "TODO Phase 4" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt | wc -l → 0
</verification>

<success_criteria>
- Gin7EconomyService.kt 파일 존재 및 @Service 어노테이션 확인
- TickEngine이 Gin7EconomyService를 생성자 주입받음
- runMonthlyPipeline()에서 gin7EconomyService.processMonthly() 호출
- month=1,4,7,10 세금 징수 로직 테스트 통과
- 행성 자원 매월 0.3~0.5% 성장 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-economy-system/04-01-SUMMARY.md`
</output>
