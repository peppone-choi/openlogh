---
phase: 04-economy-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt
autonomous: true
requirements:
  - ECON-04

must_haves:
  truths:
    - "행성창고에서 부대창고로 함선/물자/승조원을 이동할 수 있다"
    - "부대창고에서 행성창고로 반납(return)할 수 있다"
    - "이동량이 보유량을 초과하면 400 에러가 반환된다"
    - "이동 후 행성창고와 부대창고의 수치가 정확히 변경된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt"
      provides: "행성↔부대창고 양방향 이동 메서드"
      exports: ["transferToFleet", "returnToPlanet"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt"
      provides: "POST /api/warehouse/transfer, POST /api/warehouse/return REST 엔드포인트"
  key_links:
    - from: "POST /api/warehouse/transfer"
      to: "WarehouseService.transferToFleet()"
      via: "Controller → Service"
    - from: "WarehouseService.transferToFleet()"
      to: "PlanetWarehouse.removeShips() + FleetWarehouse.addShips()"
      via: "단일 트랜잭션"
---

<objective>
행성창고(PlanetWarehouse)와 부대창고(FleetWarehouse) 간 양방향 자원 이동 API를 구현한다.

Purpose: 기존 WarehouseService.allocate()는 커맨드 시스템(할당 커맨드)에서 호출되는 내부 메서드다. Phase 4에서는 플레이어가 직접 창고 간 이동을 요청하는 REST 엔드포인트(transfer/return)가 필요하다. allocate()과 별도로 범용 transfer 메서드를 추가한다.
Output: WarehouseService.kt (transferToFleet/returnToPlanet 추가) + WarehouseController.kt (2개 엔드포인트 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-economy-system/04-CONTEXT.md

@backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
@backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/PlanetWarehouse.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/FleetWarehouse.kt

<interfaces>
<!-- Key contracts the executor needs. -->

From WarehouseService (existing — do NOT modify these):
```kotlin
fun getOrCreatePlanetWarehouse(sessionId: Long, planetId: Long): PlanetWarehouse
fun getOrCreateFleetWarehouse(sessionId: Long, fleetId: Long): FleetWarehouse
fun getPlanetWarehouse(sessionId: Long, planetId: Long): PlanetWarehouse?
fun getFleetWarehouse(sessionId: Long, fleetId: Long): FleetWarehouse?
fun allocate(...)   // 기존 할당 커맨드용 — 변경하지 말 것
fun savePlanetWarehouse(pw: PlanetWarehouse): PlanetWarehouse
fun saveFleetWarehouse(fw: FleetWarehouse): FleetWarehouse
```

From PlanetWarehouse:
```kotlin
fun removeShips(shipClass: ShipClassType, amount: Int): Int   // 반환값 = 실제 이동량
fun addShips(shipClass: ShipClassType, amount: Int)
fun removeCrew(proficiency: CrewProficiency, amount: Int): Int
fun addCrew(proficiency: CrewProficiency, amount: Int)
var supplies: Int
var missiles: Int
```

From FleetWarehouse: 동일한 addShips/removeShips/addCrew/removeCrew/supplies/missiles 인터페이스

Existing WarehouseController structure:
```kotlin
@RestController
@RequestMapping("/api/warehouse")
class WarehouseController(private val warehouseService: WarehouseService)
// GET /api/warehouse/planet/{planetId}
// GET /api/warehouse/fleet/{fleetId}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WarehouseService — transferToFleet / returnToPlanet</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/PlanetWarehouse.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/FleetWarehouse.kt
  </read_first>
  <behavior>
    - Test 1: transferToFleet ships=3 BATTLESHIP → planetWarehouse.battleship 3 감소, fleetWarehouse.battleship 3 증가
    - Test 2: transferToFleet 보유량(2) 초과 요청(5) → 실제 이동량 2 (coerceAtMost 적용), IllegalStateException 아님
    - Test 3: transferToFleet supplies=500 → planetWarehouse.supplies 500 감소, fleetWarehouse.supplies 500 증가
    - Test 4: returnToPlanet ships=2 CRUISER → fleetWarehouse.cruiser 2 감소, planetWarehouse.cruiser 2 증가
    - Test 5: returnToPlanet supplies=100 → fleetWarehouse.supplies 100 감소, planetWarehouse.supplies 100 증가
    - Test 6: 결과 TransferResult에 실제 이동량 목록이 포함된다
  </behavior>
  <action>
    WarehouseService.kt에 다음 메서드와 DTO를 추가한다 (기존 코드 변경 없음):

    ```kotlin
    data class TransferRequest(
        val ships: Map<String, Int> = emptyMap(),     // "BATTLESHIP" -> 3
        val crew: Map<String, Int> = emptyMap(),       // "GREEN" -> 5
        val supplies: Int = 0,
        val missiles: Int = 0,
    )

    data class TransferResult(
        val success: Boolean,
        val transferred: List<String>,
    )
    ```

    ```kotlin
    /**
     * 행성창고 → 부대창고 이동 (창고이동 커맨드 또는 직접 API 호출용).
     * 보유량 초과 요청은 보유량만큼 이동 (예외 미발생).
     */
    @Transactional
    fun transferToFleet(
        sessionId: Long,
        planetId: Long,
        fleetId: Long,
        request: TransferRequest,
    ): TransferResult {
        val pw = getOrCreatePlanetWarehouse(sessionId, planetId)
        val fw = getOrCreateFleetWarehouse(sessionId, fleetId)
        val transferred = mutableListOf<String>()

        // Ships
        for ((classStr, amount) in request.ships) {
            val shipClass = runCatching { ShipClassType.valueOf(classStr) }.getOrNull() ?: continue
            val actual = pw.removeShips(shipClass, amount)
            if (actual > 0) { fw.addShips(shipClass, actual); transferred.add("${shipClass.name} ${actual}유닛") }
        }
        // Crew
        for ((profStr, amount) in request.crew) {
            val prof = runCatching { CrewProficiency.valueOf(profStr) }.getOrNull() ?: continue
            val actual = pw.removeCrew(prof, amount)
            if (actual > 0) { fw.addCrew(prof, actual); transferred.add("${prof.name} 승조원 ${actual}") }
        }
        // Supplies
        if (request.supplies > 0) {
            val actual = request.supplies.coerceAtMost(pw.supplies)
            pw.supplies -= actual; fw.supplies += actual
            if (actual > 0) transferred.add("물자 $actual")
        }
        // Missiles
        if (request.missiles > 0) {
            val actual = request.missiles.coerceAtMost(pw.missiles)
            pw.missiles -= actual; fw.missiles += actual
            if (actual > 0) transferred.add("미사일 $actual")
        }

        pw.touch(); fw.touch()
        savePlanetWarehouse(pw); saveFleetWarehouse(fw)
        return TransferResult(true, transferred)
    }

    /**
     * 부대창고 → 행성창고 반납.
     */
    @Transactional
    fun returnToPlanet(
        sessionId: Long,
        fleetId: Long,
        planetId: Long,
        request: TransferRequest,
    ): TransferResult {
        val fw = getOrCreateFleetWarehouse(sessionId, fleetId)
        val pw = getOrCreatePlanetWarehouse(sessionId, planetId)
        val transferred = mutableListOf<String>()

        for ((classStr, amount) in request.ships) {
            val shipClass = runCatching { ShipClassType.valueOf(classStr) }.getOrNull() ?: continue
            val actual = fw.removeShips(shipClass, amount)
            if (actual > 0) { pw.addShips(shipClass, actual); transferred.add("${shipClass.name} ${actual}유닛") }
        }
        for ((profStr, amount) in request.crew) {
            val prof = runCatching { CrewProficiency.valueOf(profStr) }.getOrNull() ?: continue
            val actual = fw.removeCrew(prof, amount)
            if (actual > 0) { pw.addCrew(prof, actual); transferred.add("${prof.name} 승조원 ${actual}") }
        }
        if (request.supplies > 0) {
            val actual = request.supplies.coerceAtMost(fw.supplies)
            fw.supplies -= actual; pw.supplies += actual
            if (actual > 0) transferred.add("물자 $actual")
        }
        if (request.missiles > 0) {
            val actual = request.missiles.coerceAtMost(fw.missiles)
            fw.missiles -= actual; pw.missiles += actual
            if (actual > 0) transferred.add("미사일 $actual")
        }

        fw.touch(); pw.touch()
        saveFleetWarehouse(fw); savePlanetWarehouse(pw)
        return TransferResult(true, transferred)
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.service.WarehouseTransferTest" -x processResources 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 6개 테스트 통과
    - transferToFleet / returnToPlanet 메서드 존재 확인:
      grep -n "fun transferToFleet\|fun returnToPlanet" backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
  </done>
</task>

<task type="auto">
  <name>Task 2: WarehouseController — transfer / return 엔드포인트</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt
  </read_first>
  <action>
    WarehouseController에 두 엔드포인트를 추가한다 (기존 GET 엔드포인트는 변경하지 말 것):

    ```kotlin
    data class TransferRequestBody(
        val sessionId: Long,
        val planetId: Long,
        val fleetId: Long,
        val ships: Map<String, Int> = emptyMap(),
        val crew: Map<String, Int> = emptyMap(),
        val supplies: Int = 0,
        val missiles: Int = 0,
    )

    /**
     * POST /api/warehouse/transfer
     * 행성창고 → 부대창고 이동
     */
    @PostMapping("/transfer")
    fun transferToFleet(@RequestBody body: TransferRequestBody): ResponseEntity<Any> {
        return try {
            val result = warehouseService.transferToFleet(
                sessionId = body.sessionId,
                planetId = body.planetId,
                fleetId = body.fleetId,
                request = TransferRequest(
                    ships = body.ships,
                    crew = body.crew,
                    supplies = body.supplies,
                    missiles = body.missiles,
                ),
            )
            ResponseEntity.ok(result)
        } catch (e: IllegalArgumentException) {
            ResponseEntity.badRequest().body(mapOf("error" to e.message))
        }
    }

    /**
     * POST /api/warehouse/return
     * 부대창고 → 행성창고 반납
     */
    @PostMapping("/return")
    fun returnToPlanet(@RequestBody body: TransferRequestBody): ResponseEntity<Any> {
        return try {
            val result = warehouseService.returnToPlanet(
                sessionId = body.sessionId,
                fleetId = body.fleetId,
                planetId = body.planetId,
                request = TransferRequest(
                    ships = body.ships,
                    crew = body.crew,
                    supplies = body.supplies,
                    missiles = body.missiles,
                ),
            )
            ResponseEntity.ok(result)
        } catch (e: IllegalArgumentException) {
            ResponseEntity.badRequest().body(mapOf("error" to e.message))
        }
    }
    ```

    TransferRequest import: import com.openlogh.service.TransferRequest
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 컴파일 성공
    - 두 엔드포인트 확인:
      grep -n "PostMapping.*transfer\|PostMapping.*return" backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt
  </done>
</task>

</tasks>

<verification>
1. 컴파일: cd backend && ./gradlew :game-app:compileKotlin → BUILD SUCCESSFUL
2. 테스트: ./gradlew :game-app:test --tests "com.openlogh.service.WarehouseTransferTest" → 6 tests passed
3. 메서드 존재: grep -n "fun transferToFleet\|fun returnToPlanet" backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
4. 엔드포인트 존재: grep -n "PostMapping" backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt → 2건
</verification>

<success_criteria>
- POST /api/warehouse/transfer 엔드포인트 존재
- POST /api/warehouse/return 엔드포인트 존재
- 보유량 초과 이동 시 예외 없이 coerce 처리
- 행성↔부대창고 양방향 이동 단위 테스트 6개 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-economy-system/04-03-SUMMARY.md`
</output>
