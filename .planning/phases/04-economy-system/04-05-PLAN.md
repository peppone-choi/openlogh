---
phase: 04-economy-system
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - backend/game-app/src/test/kotlin/com/openlogh/engine/EconomyIntegrationTest.kt
autonomous: true
requirements:
  - ECON-01
  - ECON-02
  - ECON-03
  - ECON-04
  - ECON-05
  - ECON-06

must_haves:
  truths:
    - "모든 ECON 요구사항이 단일 통합 테스트 스위트로 검증된다"
    - "TickEngine.runMonthlyPipeline()이 Gin7EconomyService를 호출하여 세수가 진영 자금에 반영된다"
    - "ShipyardProductionService.runProduction()이 PlanetWarehouse에 함선을 추가한다"
    - "행성↔부대창고 transfer/return API가 200 응답과 올바른 수치 변경을 반환한다"
    - "페잔 차관 3건 연체 시 페잔 엔딩이 트리거된다"
    - "출격 중 함대가 있으면 진영 자금이 소모된다"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/openlogh/engine/EconomyIntegrationTest.kt"
      provides: "Phase 4 경제 시스템 통합 테스트"
      min_lines: 80
  key_links:
    - from: "EconomyIntegrationTest"
      to: "Gin7EconomyService + ShipyardProductionService + WarehouseService + FezzanEndingService + FleetSortieCostService"
      via: "단위 모의(mock) 또는 H2 인메모리 DB 슬라이스 테스트"
---

<objective>
Phase 4 경제 시스템 6개 요구사항(ECON-01 ~ ECON-06)을 모두 검증하는 통합 테스트를 작성한다.

Purpose: Plan 01~04에서 개별 단위 테스트가 작성되었으나 파이프라인 전체가 올바르게 연결되었는지 확인하는 통합 테스트가 없다. EconomyIntegrationTest가 각 서비스의 핵심 계약을 grep-verifiable한 방식으로 검증한다.
Output: EconomyIntegrationTest.kt (6개 시나리오)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-economy-system/04-CONTEXT.md

@backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
@backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt
@backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
@backend/game-app/src/main/kotlin/com/openlogh/service/FezzanEndingService.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/FleetSortieCostService.kt

<interfaces>
<!-- Contracts verified by this test suite. -->

ECON-01 (행성자원): Gin7EconomyService.processMonthly() → planet.population 증가
ECON-02 (조병창): ShipyardProductionService.runProduction() → PlanetWarehouse.battleship 증가
ECON-03 (세율/납입): Gin7EconomyService.processMonthly() month=1 → faction.funds 증가
ECON-04 (창고이동): WarehouseService.transferToFleet() → planetWarehouse 감소, fleetWarehouse 증가
ECON-05 (페잔차관): FezzanEndingService.checkAndTrigger() with 3 defaulted loans → ending triggered
ECON-06 (출격비용): FleetSortieCostService.processSortieCost() → faction.funds 감소
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EconomyIntegrationTest 작성 (6개 ECON 시나리오)</name>
  <files>backend/game-app/src/test/kotlin/com/openlogh/engine/EconomyIntegrationTest.kt</files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/engine/Gin7EconomyService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/service/ShipyardProductionService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/service/WarehouseService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/service/FezzanEndingService.kt
    - backend/game-app/src/main/kotlin/com/openlogh/engine/FleetSortieCostService.kt
    - backend/game-app/src/test/kotlin/com/openlogh (기존 테스트 패턴 파악용, 디렉토리 확인)
  </read_first>
  <action>
    package com.openlogh.engine에 EconomyIntegrationTest.kt를 작성한다.
    JUnit 5 + Mockito (기존 테스트 패턴 따름). Spring 컨텍스트 없이 단위 mock 방식 사용.

    각 @Test 메서드마다 // ECON-XX 주석을 달아 추적 가능하게 한다.

    **ECON-01: 행성 자원 성장**
    ```kotlin
    @Test fun `ECON-01 processMonthly increases planet population each month`() {
        // Given: planet.population=10000, populationMax=20000, supplyState=1, month=2 (비세금달)
        // When: gin7EconomyService.processMonthly(world)
        // Then: planet.population >= 10050 (0.5% 성장)
        // verify: planetRepository.saveAll() 호출됨
    }
    ```

    **ECON-02: 조병창 자동생산**
    ```kotlin
    @Test fun `ECON-02 runProduction adds ships to PlanetWarehouse`() {
        // Given: PlanetWarehouse(hasShipyard=true), planet.production=400, factionId=1
        // When: shipyardProductionService.runProduction(sessionId)
        // Then: ProductionReport 1건 반환, shipsProduced=2 (400/200)
        // verify: planetWarehouseRepository.save() 호출됨
    }
    ```

    **ECON-03: 세율 납입**
    ```kotlin
    @Test fun `ECON-03 processMonthly on tax month adds funds to faction`() {
        // Given: faction.taxRate=30, planet.commerce=10000, supplyState=1, month=1
        // When: gin7EconomyService.processMonthly(world)
        // Then: faction.funds 증가량 = 3000 (10000 * 30/100)
        // verify: factionRepository.saveAll() 호출됨
    }
    ```

    **ECON-04: 창고 이동**
    ```kotlin
    @Test fun `ECON-04 transferToFleet moves ships from planet to fleet warehouse`() {
        // Given: PlanetWarehouse(battleship=5), FleetWarehouse(battleship=0)
        // When: warehouseService.transferToFleet(sessionId, planetId, fleetId, TransferRequest(ships={"BATTLESHIP":3}))
        // Then: planetWarehouse.battleship=2, fleetWarehouse.battleship=3
        // verify: TransferResult.transferred 포함 "BATTLESHIP 3유닛"
    }
    ```

    **ECON-05: 페잔 차관 엔딩**
    ```kotlin
    @Test fun `ECON-05 checkAndTrigger fires ending when 3 loans defaulted`() {
        // Given: 3건의 FezzanLoan(isDefaulted=true)이 존재, fezzanEndingTriggered not set
        // When: fezzanEndingService.checkAndTrigger(sessionId)
        // Then: triggerFezzanEnding 호출됨 (verify mock), world.meta["fezzanEndingTriggered"]=true
    }
    ```

    **ECON-06: 함대 출격비용**
    ```kotlin
    @Test fun `ECON-06 processSortieCost deducts funds proportional to active fleets`() {
        // Given: 출격 중 함대 1개(currentUnits=10), faction.funds=10000, commander.administration=50
        // When: fleetSortieCostService.processSortieCost(sessionId)
        // Then: faction.funds < 10000 (비용 차감됨)
        // Then: faction.funds >= 0 (마이너스 없음)
    }
    ```

    각 테스트는 독립적으로 mock을 설정하고 검증한다. ArgumentCaptor 또는 직접 반환값으로 검증.
    기존 테스트 파일 패턴(import, annotation, mock 방식)을 반드시 확인하고 동일하게 따른다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.EconomyIntegrationTest" -x processResources 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 6개 테스트 모두 통과 (ECON-01 through ECON-06)
    - 각 테스트에 // ECON-XX 주석 존재:
      grep -n "ECON-0" backend/game-app/src/test/kotlin/com/openlogh/engine/EconomyIntegrationTest.kt | wc -l → 6건 이상
  </done>
</task>

<task type="auto">
  <name>Task 2: 전체 game-app 테스트 + 컴파일 검증</name>
  <files></files>
  <read_first>
    없음 — 검증만 수행.
  </read_first>
  <action>
    다음 명령을 순서대로 실행하여 Phase 4 전체 구현이 기존 테스트를 깨뜨리지 않음을 확인한다:

    1. 전체 컴파일:
       cd backend && ./gradlew :game-app:compileKotlin :game-app:compileTestKotlin

    2. 경제 관련 테스트 전체 실행:
       ./gradlew :game-app:test --tests "com.openlogh.engine.Gin7EconomyServiceTest" --tests "com.openlogh.service.ShipyardProductionServiceTest" --tests "com.openlogh.service.WarehouseTransferTest" --tests "com.openlogh.service.FezzanEndingServiceTest" --tests "com.openlogh.engine.FleetSortieCostServiceTest" --tests "com.openlogh.engine.EconomyIntegrationTest" -x processResources

    3. 기존 테스트 회귀 확인:
       ./gradlew :game-app:test -x processResources 2>&1 | grep -E "BUILD|FAILED|ERROR|tests"

    실패한 테스트가 있으면 원인을 찾아 수정한다. Phase 4 신규 코드가 기존 테스트를 깨뜨렸다면 해당 파일을 수정한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test -x processResources 2>&1 | grep -E "BUILD|FAILED|tests" | tail -10</automated>
  </verify>
  <done>
    - BUILD SUCCESSFUL
    - 0 FAILED
    - Phase 4 신규 테스트 29개 이상 통과 (6+7+6+5+5 from plans 01-04 + 6 integration)
  </done>
</task>

</tasks>

<verification>
1. 통합 테스트: ./gradlew :game-app:test --tests "com.openlogh.engine.EconomyIntegrationTest" → 6 tests passed
2. Phase 4 단위 테스트: Plans 01-04의 모든 신규 테스트 통과
3. 회귀 없음: ./gradlew :game-app:test → BUILD SUCCESSFUL, 0 FAILED
4. ECON 주석 추적: grep "ECON-0" backend/game-app/src/test/kotlin/com/openlogh/engine/EconomyIntegrationTest.kt | wc -l → 6

Phase 4 success criteria (ROADMAP):
1. TickEngine.runMonthlyPipeline() → Gin7EconomyService 호출 (Plan 01):
   grep -n "gin7EconomyService.processMonthly" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt → 존재
2. 조병창 자동생산 (Plan 02):
   grep -n "shipyardProductionService.runProduction" backend/game-app/src/main/kotlin/com/openlogh/engine/TickEngine.kt → 존재
3. 창고 이동 API (Plan 03):
   grep -n "PostMapping.*transfer\|PostMapping.*return" backend/game-app/src/main/kotlin/com/openlogh/controller/WarehouseController.kt → 2건
4. 페잔 엔딩 트리거 (Plan 04):
   grep -n "fezzanEndingTriggered" backend/game-app/src/main/kotlin/com/openlogh/service/FezzanEndingService.kt → 존재
5. 출격비용 (Plan 04):
   grep -n "SORTIE_COST_INTERVAL_TICKS" backend/game-app/src/main/kotlin/com/openlogh/engine/FleetSortieCostService.kt → 존재
</verification>

<success_criteria>
- EconomyIntegrationTest 6개 시나리오 모두 통과 (ECON-01 through ECON-06)
- 전체 game-app 테스트 BUILD SUCCESSFUL (0 FAILED)
- Phase 4 ROADMAP success criteria 1~4 모두 grep-verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/04-economy-system/04-05-SUMMARY.md`
</output>
