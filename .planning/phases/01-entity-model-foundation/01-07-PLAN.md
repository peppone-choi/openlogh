---
phase: 01-entity-model-foundation
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/command/BaseCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/GeneralCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/NationCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandResultApplicator.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandServices.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandEnv.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/constraint/ConstraintHelper.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/war/WarUnit.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/war/WarUnitGeneral.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/war/WarFormula.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/war/BattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/TurnService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/StatChangeService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/DiplomacyService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/EventService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/EventActionService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/GeneralMaintenanceService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/UnificationService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/TurnDaemon.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/RealtimeService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/ai/GeneralAI.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/ai/NationAI.kt
  - backend/game-app/src/main/kotlin/com/openlogh/dto/ResponseDtos.kt
  - backend/game-app/src/main/kotlin/com/openlogh/dto/GeneralDtos.kt
  - backend/game-app/src/main/kotlin/com/openlogh/dto/TroopDtos.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/GeneralController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/CityController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/NationController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/TroopController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/WorldController.kt
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
must_haves:
  truths:
    - "Command infrastructure (BaseCommand, GeneralCommand, NationCommand, CommandExecutor) references Officer/Planet/Faction/Fleet"
    - "All 55 general commands and 38 nation commands reference new entity and field names"
    - "WarUnit.strength renamed to WarUnit.command, WarUnit.intel renamed to WarUnit.intelligence"
    - "WarUnitGeneral references Officer entity with 8 stat fields"
    - "Battle formulas use command/intelligence/administration instead of strength/intel/charm"
    - "Turn engine services (TurnService, EconomyService, etc.) reference new entity names"
    - "AI services reference new entity names"
    - "All DTOs (ResponseDtos, GeneralDtos, TroopDtos) use LOGH field names"
    - "All controllers reference new service/entity names"
    - "game-app compiles without errors"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/war/WarUnitOfficer.kt"
      provides: "WarUnitOfficer with 8-stat combat fields"
      contains: "class WarUnitOfficer"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/dto/ResponseDtos.kt"
      provides: "OfficerResponse, PlanetResponse, FactionResponse, FleetResponse DTOs"
      contains: "OfficerResponse"
  key_links:
    - from: "CommandExecutor"
      to: "OfficerRepository, PlanetRepository, FactionRepository"
      via: "Constructor injection for entity lookup"
      pattern: "officerRepository"
    - from: "WarUnitOfficer"
      to: "Officer entity"
      via: "Reads 8 stat fields for combat"
      pattern: "officer\\.command|officer\\.intelligence|officer\\.attack|officer\\.defense"
---

<objective>
Propagate LOGH domain renames through the entire command system (93 commands), war/battle engine, turn engine services, AI system, DTOs, and controllers. This is the largest plan and covers the "long tail" of 150+ files.

Purpose: With entities, repositories, services, and CQRS all renamed (Plans 03-06), the command layer, engine, DTOs, and controllers are the remaining backend files. After this plan, the game-app should compile cleanly.

Output: All backend Kotlin files use LOGH domain names. `./gradlew :game-app:compileKotlin` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md

Previous plan summaries (for entity/field name reference):
@.planning/phases/01-entity-model-foundation/01-03-SUMMARY.md
@.planning/phases/01-entity-model-foundation/01-05-SUMMARY.md
@.planning/phases/01-entity-model-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command infrastructure, all 93 commands, war engine, and turn engine</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/ (all files),
    backend/game-app/src/main/kotlin/com/openlogh/engine/ (all files except turn/cqrs/ which is done in Plan 05)
  </files>
  <action>
This is a systematic find-and-replace across 150+ files. Use a methodical approach:

**Phase A: Command Infrastructure (7 files)**

Read and update each:
1. `BaseCommand.kt` -- Replace entity type references: `General` -> `Officer`, `City` -> `Planet`, `Nation` -> `Faction`, `Troop` -> `Fleet`. Replace field access: `.strength` -> `.command`, `.intel` -> `.intelligence`, `.charm` -> `.administration`, `.gold` -> `.funds`, `.rice` -> `.supplies`, `.crew` -> `.ships`, `.crewType` -> `.shipClass`, `.train` -> `.training`, `.atmos` -> `.morale`, `.nationId` -> `.factionId`, `.cityId` -> `.planetId`, `.troopId` -> `.fleetId`, `.worldId` -> `.sessionId`. Replace helper method names if they reference old entity names (e.g., `getGeneral()` -> `getOfficer()`).
2. `GeneralCommand.kt` -- Rename to `OfficerCommand.kt`. Class `GeneralCommand` -> `OfficerCommand`. Update all internal references.
3. `NationCommand.kt` -- Rename to `FactionCommand.kt`. Class `NationCommand` -> `FactionCommand`. Update all internal references.
4. `CommandExecutor.kt` -- Update all entity/repository/service references.
5. `CommandRegistry.kt` -- Update command class references.
6. `CommandResultApplicator.kt` -- Update entity field access.
7. `CommandServices.kt` / `CommandEnv.kt` -- Update injected service/repository names.
8. `constraint/Constraint.kt`, `constraint/ConstraintChain.kt`, `constraint/ConstraintHelper.kt` -- Update entity/field references.

**Phase B: General Commands -> Officer Commands (55 files in command/general/)**

For ALL files in `command/general/`:
- Update class hierarchy: `extends GeneralCommand` -> `extends OfficerCommand`
- Update entity references: `General` -> `Officer`, `City` -> `Planet`, `Nation` -> `Faction`, `Troop` -> `Fleet`
- Update field access on all entities using the same mapping as Phase A
- Update method calls on services/repositories
- Update import statements
- Korean file names (e.g., `출병.kt`, `che_농지개간.kt`) stay the same -- only internal references change

**Phase C: Nation Commands -> Faction Commands (38 files in command/nation/)**

Same approach as Phase B but for `command/nation/` files:
- Update class hierarchy: `extends NationCommand` -> `extends FactionCommand`
- All entity/field renames same as above
- Also rename `DomesticCommand.kt` references if it exists

**Phase D: War/Battle Engine (15+ files in engine/war/)**

1. `WarUnit.kt` -- Rename fields: `strength` -> `command`, `intel` -> `intelligence`. Keep `leadership` unchanged.
2. `WarUnitGeneral.kt` -> `WarUnitOfficer.kt` -- Rename class. Update to read from Officer entity: `officer.command`, `officer.intelligence`, `officer.administration`, `officer.mobility`, `officer.attack`, `officer.defense` (add new stat reads). Delete old file.
3. `WarUnitCity.kt` -> `WarUnitPlanet.kt` -- Rename class. Update Planet field access. Delete old file.
4. `BattleEngine.kt`, `BattleService.kt`, `FieldBattleService.kt`, `WarFormula.kt`, `WarAftermath.kt` -- Update all entity/field references.
5. All files in `engine/war/trigger/` -- Update entity references.
6. `engine/trigger/GeneralTrigger.kt` -> `engine/trigger/OfficerTrigger.kt` -- Rename class. Update references.

**Phase E: Turn Engine Services (20+ files)**

Update ALL files in `engine/` (excluding `turn/cqrs/` which is done):
- `TurnDaemon.kt`, `TurnService.kt`, `RealtimeService.kt`
- `EconomyService.kt`, `DiplomacyService.kt`, `StatChangeService.kt`
- `EventService.kt`, `EventActionService.kt`, `GeneralMaintenanceService.kt` -> `OfficerMaintenanceService.kt`
- `UnificationService.kt`, `NpcSpawnService.kt`, `DistanceService.kt`
- `SpecialAssignmentService.kt`, `TournamentBattle.kt`
- All files in `engine/event/actions/` subdirectories
- All files in `engine/modifier/` subdirectories
- All files in `engine/map/`
- `engine/turn/TurnPipeline.kt`, `engine/turn/TurnStep.kt`
- All files in `engine/turn/steps/`

Same field/entity rename mapping throughout.

**Phase F: Shared module**

Check `backend/shared/src/main/kotlin/com/openlogh/` for any entity references:
- `shared/GameConstants.kt` -- Update if it references General/City/Nation
- `shared/model/ScenarioData.kt` -- Update if it references old entity names
- `shared/dto/WorldDtos.kt` -- Update WorldState references

**Methodology:**
1. Use `grep -rl` to find all files referencing each old name
2. Process files systematically by directory
3. For each file: read fully, apply all renames, write replacement
4. Verify no old names remain with grep after each directory

CRITICAL: The `CommandRegistry.kt` registers commands by Korean action code strings (e.g., "출병", "농지개간"). These Korean keys must NOT change. Only the internal class references and field access change.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin -x :gateway-app:compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>All command files, engine files, and shared module files use LOGH domain names. game-app Kotlin compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update all DTOs and controllers</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/dto/ (all 23 files),
    backend/game-app/src/main/kotlin/com/openlogh/controller/ (all 34 files)
  </files>
  <action>
**DTOs (23 files in dto/):**

Read each DTO file and update:

1. `ResponseDtos.kt` -- This is the key file with response classes:
   - `GeneralResponse` -> `OfficerResponse`: rename all fields (strength->command, intel->intelligence, charm->administration, gold->funds, rice->supplies, crew->ships, crewType->shipClass, train->training, atmos->morale, weaponCode->flagshipCode, bookCode->equipCode, horseCode->engineCode, itemCode->accessoryCode, nationId->factionId, cityId->planetId, troopId->fleetId, worldId->sessionId). Add new fields: mobility, mobilityExp, attack, attackExp, defense, defenseExp, dex6, dex7, dex8.
   - `CityResponse` -> `PlanetResponse`: rename all resource fields per mapping
   - `NationResponse` -> `FactionResponse`: rename all fields per mapping
   - `TroopResponse` -> `FleetResponse`: rename FK fields
   - `BestGeneralResponse` -> `BestOfficerResponse`: rename fields
   - `WorldStateResponse` -> `SessionStateResponse` (if exists in game-app dto/)

2. `GeneralDtos.kt` -> `OfficerDtos.kt`: Update all DTO classes and field names. Delete old file.
3. `TroopDtos.kt` -> `FleetDtos.kt`: Update. Delete old file.
4. `NationManagementDtos.kt` -> `FactionManagementDtos.kt`: Update. Delete old file.
5. All other DTO files (CommandDtos, DiplomacyDtos, FrontInfoDtos, etc.): Update entity/field references where they appear. These files may reference General/City/Nation types or fields.

Also update the response-building code in services/controllers that constructs these DTOs.

**Controllers (34 files in controller/):**

1. `GeneralController.kt` -> `OfficerController.kt`: Rename class, update service injection (`generalService` -> `officerService`), update return types, update endpoint paths if they include entity names. Delete old file.
2. `CityController.kt` -> `PlanetController.kt`: Same approach. Delete old file.
3. `NationController.kt` -> `FactionController.kt`: Same approach. Delete old file.
4. `TroopController.kt` -> `FleetController.kt`: Same approach. Delete old file.
5. `WorldController.kt` -> `SessionController.kt` (or keep if it manages "worlds" conceptually).
6. All other controllers: Update entity/service/DTO references.

**REST endpoint paths:**
- Check if any `@RequestMapping` or `@GetMapping` paths include old entity names (e.g., `/api/generals` -> keep as-is or rename to `/api/officers`). Since the frontend will be updated in Plan 08, rename paths to use LOGH terms:
  - `/api/general/**` -> `/api/officer/**`
  - `/api/city/**` -> `/api/planet/**`
  - `/api/nation/**` -> `/api/faction/**`
  - `/api/troop/**` -> `/api/fleet/**`
  - `/api/world/**` -> `/api/session/**` (or keep `/api/world/` if it's a lobby concept)

NOTE: If renaming REST paths, document the mapping clearly in the summary so Plan 08 (frontend) can update API client URLs.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin :gateway-app:compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>All DTOs use LOGH field names. All controllers reference new services and return new DTO types. Both game-app and gateway-app compile successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files and verify full test suite</name>
  <files>backend/game-app/src/test/kotlin/com/openlogh/ (all test files)</files>
  <action>
Run `find backend/game-app/src/test/ -name "*.kt" | wc -l` to count test files.
Then systematically update ALL test files:

1. Use `grep -rl "General\|City\|Nation\|Troop\|Emperor\|WorldState" backend/game-app/src/test/` to find all affected test files.

2. For each test file, apply the same entity/field rename mapping:
   - Entity names: General->Officer, City->Planet, Nation->Faction, Troop->Fleet, Emperor->Sovereign, WorldState->SessionState
   - Stat fields: strength->command, intel->intelligence, charm->administration
   - Resource fields: gold->funds, rice->supplies, crew->ships, crewType->shipClass, train->training, atmos->morale
   - Item fields: weaponCode->flagshipCode, bookCode->equipCode, horseCode->engineCode, itemCode->accessoryCode
   - FK fields: nationId->factionId, cityId->planetId, troopId->fleetId, worldId->sessionId
   - City fields: pop->population, agri->production, comm->commerce, secu->security, trust->approval, def->orbitalDefense, wall->fortress, trade->tradeRoute
   - Nation fields: gold->funds, rice->supplies, bill->taxRate, rate->conscriptionRate, tech->techLevel, power->militaryPower, gennum->officerCount, level->factionRank, typeCode->factionType

3. Rename test files if they reference old class names:
   - Any test named `*GeneralTest*` consider renaming to `*OfficerTest*`
   - Keep Korean-named command test files as-is

4. Update import statements in all test files.

5. Run the full test suite and fix any remaining compilation or runtime errors.

After updating tests, run: `cd backend && ./gradlew test`

If tests fail, analyze failures and fix. Common issues:
- Missed field renames in test fixtures
- JPQL queries still using old entity names
- Spring context not finding renamed beans
- H2 in-memory DB not getting V28 migration (check if tests use Flyway or manual schema)
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew test 2>&1 | tail -30</automated>
  </verify>
  <done>Full backend test suite passes. No test files reference old entity or field names. Both game-app and gateway-app tests are green.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test` passes (all tests green)
- `grep -r "class General\b\|class City\b\|class Nation\b\|class Troop\b" backend/game-app/src/main/kotlin/` returns 0 matches
- `grep -r "\.strength\b\|\.intel\b\|\.charm\b" backend/game-app/src/main/kotlin/ | grep -v "//"` returns 0 matches
- `grep -r "GeneralResponse\|CityResponse\|NationResponse\|TroopResponse" backend/game-app/src/main/kotlin/com/openlogh/dto/` returns 0 matches
</verification>

<success_criteria>
The entire backend (game-app + gateway-app) compiles and all tests pass with LOGH domain naming throughout. No references to old OpenSamguk entity names, stat names, or resource names remain in production code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-07-SUMMARY.md`
Document any REST endpoint path changes for the frontend plan to reference.
</output>
