---
phase: 01-entity-model-foundation
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldState.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldPorts.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPorts.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/CachingWorldPorts.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldReadPort.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldWritePort.kt
autonomous: true
requirements:
  - CHAR-01
must_haves:
  truths:
    - "GeneralSnapshot renamed to OfficerSnapshot with 8 stat fields + LOGH resource/item names"
    - "CitySnapshot renamed to PlanetSnapshot with LOGH resource field names"
    - "NationSnapshot renamed to FactionSnapshot with LOGH field names"
    - "TroopSnapshot renamed to FleetSnapshot with LOGH FK names"
    - "SnapshotEntityMapper converts between new entity names and new snapshot names"
    - "WorldStateLoader loads entities into correctly named snapshots"
    - "WorldStatePersister writes snapshots back to correctly named entities"
    - "Port interfaces reference new entity/snapshot names"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldState.kt"
      provides: "Renamed snapshot data classes (OfficerSnapshot, PlanetSnapshot, FactionSnapshot, FleetSnapshot)"
      contains: "OfficerSnapshot"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt"
      provides: "Entity<->Snapshot mapper using new names"
      contains: "Officer"
  key_links:
    - from: "InMemoryWorldState.kt (OfficerSnapshot)"
      to: "Officer.kt entity"
      via: "SnapshotEntityMapper converts between them"
      pattern: "OfficerSnapshot.*Officer"
    - from: "WorldStateLoader.kt"
      to: "Repository interfaces"
      via: "Loads entities and converts to snapshots"
      pattern: "officerRepository\\.findAll"
    - from: "WorldStatePersister.kt"
      to: "JpaBulkWriter"
      via: "Writes dirty snapshots back as entities"
      pattern: "bulkWriter.*officer"
---

<objective>
Update the entire CQRS snapshot layer to use LOGH domain names: rename all snapshot data classes, update the entity-snapshot mapper, loader, persister, and port interfaces.

Purpose: This is the CRITICAL layer identified in research. The CQRS snapshots must mirror entity fields exactly. Any desync causes data loss on persist. This plan ensures the snapshot layer matches the renamed entities from Plans 03/04.

Output: All CQRS files updated with LOGH naming, ready for service layer integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md

Key source files (READ ALL before modifying):
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldState.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldPorts.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPorts.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/CachingWorldPorts.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldReadPort.kt
@backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldWritePort.kt

Entity files created by Plan 03 (for field reference):
@backend/game-app/src/main/kotlin/com/openlogh/entity/Officer.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Planet.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Faction.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/Fleet.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update InMemoryWorldState snapshot data classes</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldState.kt</files>
  <action>
Read InMemoryWorldState.kt fully (197 lines). This file contains snapshot data classes that mirror every entity field.

**Rename snapshot classes:**
- `GeneralSnapshot` -> `OfficerSnapshot`
- `CitySnapshot` -> `PlanetSnapshot`
- `NationSnapshot` -> `FactionSnapshot`
- `TroopSnapshot` -> `FleetSnapshot`
- `GeneralTurnSnapshot` -> `OfficerTurnSnapshot`
- `NationTurnSnapshot` -> `FactionTurnSnapshot`
- `DiplomacySnapshot` -- keep name (diplomacy entity not renamed), but rename FK fields

**In OfficerSnapshot (was GeneralSnapshot), rename fields to match Officer entity:**
- FK: `worldId`->`sessionId`, `nationId`->`factionId`, `cityId`->`planetId`, `troopId`->`fleetId`
- Stats: `strength`->`command`, `strengthExp`->`commandExp`, `intel`->`intelligence`, `intelExp`->`intelligenceExp`, `charm`->`administration`, `charmExp`->`administrationExp`
- Add new fields: `mobility: Short = 50`, `mobilityExp: Short = 0`, `attack: Short = 50`, `attackExp: Short = 0`, `defense: Short = 50`, `defenseExp: Short = 0`
- Add: `dex6: Int = 0`, `dex7: Int = 0`, `dex8: Int = 0`
- Resources: `gold`->`funds`, `rice`->`supplies`, `crew`->`ships`, `crewType`->`shipClass`, `train`->`training`, `atmos`->`morale`
- Items: `weaponCode`->`flagshipCode`, `bookCode`->`equipCode`, `horseCode`->`engineCode`, `itemCode`->`accessoryCode`

**In PlanetSnapshot (was CitySnapshot), rename fields to match Planet entity:**
- FK: `worldId`->`sessionId`, `nationId`->`factionId`
- Fields: `pop`->`population`, `popMax`->`populationMax`, `agri`->`production`, `agriMax`->`productionMax`, `comm`->`commerce`, `commMax`->`commerceMax`, `secu`->`security`, `secuMax`->`securityMax`, `trust`->`approval`, `def`->`orbitalDefense`, `defMax`->`orbitalDefenseMax`, `wall`->`fortress`, `wallMax`->`fortressMax`, `trade`->`tradeRoute`

**In FactionSnapshot (was NationSnapshot), rename fields to match Faction entity:**
- FK: `worldId`->`sessionId`, `capitalCityId`->`capitalPlanetId`, `chiefGeneralId`->`chiefOfficerId`
- Fields: `gold`->`funds`, `rice`->`supplies`, `bill`->`taxRate`, `rate`->`conscriptionRate`, `rateTmp`->`conscriptionRateTmp`, `tech`->`techLevel`, `power`->`militaryPower`, `gennum`->`officerCount`, `level`->`factionRank`, `typeCode`->`factionType`

**In FleetSnapshot (was TroopSnapshot), rename fields:**
- FK: `worldId`->`sessionId`, `leaderGeneralId`->`leaderOfficerId`, `nationId`->`factionId`

**In OfficerTurnSnapshot (was GeneralTurnSnapshot):**
- FK: `generalId`->`officerId`, `worldId`->`sessionId`

**In FactionTurnSnapshot (was NationTurnSnapshot):**
- FK: `nationId`->`factionId`, `worldId`->`sessionId`

**In DiplomacySnapshot:**
- FK: `worldId`->`sessionId`, `srcNationId`->`srcFactionId`, `destNationId`->`destFactionId`

**Update the InMemoryWorldState class itself:**
- `generals: MutableMap<Long, GeneralSnapshot>` -> `officers: MutableMap<Long, OfficerSnapshot>`
- `cities: MutableMap<Long, CitySnapshot>` -> `planets: MutableMap<Long, PlanetSnapshot>`
- `nations: MutableMap<Long, NationSnapshot>` -> `factions: MutableMap<Long, FactionSnapshot>`
- `troops: MutableMap<Long, TroopSnapshot>` -> `fleets: MutableMap<Long, FleetSnapshot>`
- `generalTurns: ...` -> `officerTurns: ...`
- `nationTurns: ...` -> `factionTurns: ...`

CRITICAL: Every field in every snapshot MUST exactly match its corresponding entity class. Cross-reference with the entity files created in Plan 03.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh && grep -c "OfficerSnapshot\|PlanetSnapshot\|FactionSnapshot\|FleetSnapshot" backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldState.kt</automated>
  </verify>
  <done>All snapshot data classes renamed with fields matching their corresponding entities. InMemoryWorldState maps use new names (officers, planets, factions, fleets).</done>
</task>

<task type="auto">
  <name>Task 2: Update SnapshotEntityMapper, Loader, Persister, and Port interfaces</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldPorts.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPorts.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPortFactory.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/CachingWorldPorts.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldReadPort.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldWritePort.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/DirtyTracker.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaBulkWriter.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/TurnCoordinator.kt
  </files>
  <action>
Read ALL files listed above before making changes. This is a synchronized update -- all files must reference the new names consistently.

**SnapshotEntityMapper.kt (300 lines -- largest file):**
This is the critical bridge between entities and snapshots. Every field mapping must be updated:
- All `General` <-> `GeneralSnapshot` methods -> `Officer` <-> `OfficerSnapshot`
- All `City` <-> `CitySnapshot` methods -> `Planet` <-> `PlanetSnapshot`
- All `Nation` <-> `NationSnapshot` methods -> `Faction` <-> `FactionSnapshot`
- All `Troop` <-> `TroopSnapshot` methods -> `Fleet` <-> `FleetSnapshot`
- All `GeneralTurn` <-> `GeneralTurnSnapshot` methods -> `OfficerTurn` <-> `OfficerTurnSnapshot`
- All `NationTurn` <-> `NationTurnSnapshot` methods -> `FactionTurn` <-> `FactionTurnSnapshot`
- Update every field reference in the mapping methods to use new names (e.g., `general.strength` -> `officer.command`, `city.pop` -> `planet.population`, `nation.gold` -> `faction.funds`)
- Add mappings for new fields: `mobility`, `mobilityExp`, `attack`, `attackExp`, `defense`, `defenseExp`, `dex6`, `dex7`, `dex8`
- Update import statements

**WorldStateLoader.kt:**
- Update repository references: `generalRepository` -> `officerRepository`, `cityRepository` -> `planetRepository`, `nationRepository` -> `factionRepository`, `troopRepository` -> `fleetRepository`
- Update snapshot map names: `generals` -> `officers`, `cities` -> `planets`, etc.
- Update mapper calls to use new method names

**WorldStatePersister.kt:**
- Update snapshot map names in dirty entity extraction
- Update mapper calls for snapshot-to-entity conversion
- Update bulk writer calls

**InMemoryWorldPorts.kt:**
- Update map references: `state.generals` -> `state.officers`, `state.cities` -> `state.planets`, etc.
- Update method signatures if they reference old types

**JpaWorldPorts.kt, JpaWorldPortFactory.kt, CachingWorldPorts.kt:**
- Update entity type references in all read/write port implementations
- Update repository injection names

**WorldReadPort.kt, WorldWritePort.kt:**
- Update interface method signatures to use new entity/snapshot type names
- `getGeneral()` -> `getOfficer()`, `getCity()` -> `getPlanet()`, `getNation()` -> `getFaction()`, `getTroop()` -> `getFleet()`
- Similarly for write methods

**DirtyTracker.kt:**
- Update set names: `dirtyGenerals` -> `dirtyOfficers`, `dirtyCities` -> `dirtyPlanets`, etc.

**JpaBulkWriter.kt:**
- Update entity type references in bulk persistence methods

**InMemoryTurnProcessor.kt:**
- Update any references to old snapshot/entity names

**TurnCoordinator.kt:**
- Update references to InMemoryWorldState field names

Update all import statements in all files to reference new entity class names.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh && grep -r "GeneralSnapshot\|CitySnapshot\|NationSnapshot\|TroopSnapshot" backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/ | grep -v "//" | wc -l</automated>
  </verify>
  <done>All CQRS files reference new names (OfficerSnapshot, PlanetSnapshot, FactionSnapshot, FleetSnapshot). Zero references to old snapshot class names remain (excluding comments). Mapper handles all 8 stat fields including 3 new ones.</done>
</task>

</tasks>

<verification>
- `grep -r "GeneralSnapshot\|CitySnapshot\|NationSnapshot\|TroopSnapshot" backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/` returns 0 non-comment matches
- `grep -r "generals\b" backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/` returns 0 non-comment matches (should be `officers`)
- SnapshotEntityMapper maps all 8 stat fields (including mobility, attack, defense) in both directions
- All port interfaces use new method names (getOfficer, getPlanet, getFaction, getFleet)
</verification>

<success_criteria>
The entire CQRS snapshot layer consistently uses LOGH naming. Snapshot data classes mirror their entity counterparts field-for-field. The mapper correctly handles all renamed and new fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-05-SUMMARY.md`
</output>
