---
phase: 01-entity-model-foundation
plan: 08
type: execute
wave: 4
depends_on: [07]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/gameApi.ts
  - frontend/src/stores/generalStore.ts
  - frontend/src/stores/gameStore.ts
  - frontend/src/stores/worldStore.ts
  - frontend/src/components/game/general-basic-card.tsx
  - frontend/src/components/game/city-basic-card.tsx
  - frontend/src/components/game/nation-basic-card.tsx
  - frontend/src/components/game/command-panel.tsx
  - frontend/src/components/game/game-dashboard.tsx
  - frontend/src/components/app-sidebar.tsx
  - frontend/src/app/(game)/layout.tsx
  - frontend/src/app/(game)/general/page.tsx
  - frontend/src/app/(game)/generals/page.tsx
  - frontend/src/app/(game)/city/page.tsx
  - frontend/src/app/(game)/nation/page.tsx
  - frontend/src/app/(game)/troop/page.tsx
  - frontend/src/app/(game)/diplomacy/page.tsx
autonomous: false
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
must_haves:
  truths:
    - "TypeScript General interface renamed to Officer with 8 stat fields"
    - "TypeScript City interface renamed to Planet with LOGH resource fields"
    - "TypeScript Nation interface renamed to Faction with LOGH fields"
    - "TypeScript Troop interface renamed to Fleet"
    - "gameApi.ts endpoints updated to match new backend REST paths"
    - "Zustand stores use new type names and field accessors"
    - "All game pages/components render using new field names"
    - "Frontend builds without TypeScript errors"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Officer, Planet, Faction, Fleet TypeScript interfaces with LOGH fields"
      contains: "interface Officer"
    - path: "frontend/src/lib/gameApi.ts"
      provides: "API client with updated endpoint URLs"
      contains: "officer"
    - path: "frontend/src/stores/officerStore.ts"
      provides: "Renamed officer store"
      contains: "useOfficerStore"
  key_links:
    - from: "types/index.ts (Officer interface)"
      to: "backend OfficerResponse DTO"
      via: "Field names must match JSON serialization"
      pattern: "command.*intelligence.*administration.*mobility.*attack.*defense"
    - from: "gameApi.ts endpoints"
      to: "backend controller REST paths"
      via: "URL paths match @RequestMapping"
      pattern: "/api/officer|/api/planet|/api/faction"
---

<objective>
Update all frontend TypeScript types, API client, Zustand stores, and game components to use LOGH domain names matching the renamed backend API.

Purpose: The backend API now returns LOGH field names (Officer, Planet, Faction, Fleet with new stat/resource names). The frontend must match to avoid runtime errors. This is the final plan that makes the full stack consistent.

Output: Frontend builds cleanly with `pnpm build`. All types, stores, and components use LOGH naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md

Previous plan summary (for REST endpoint path mapping):
@.planning/phases/01-entity-model-foundation/01-07-SUMMARY.md

Key source files:
@frontend/src/types/index.ts
@frontend/src/lib/gameApi.ts
@frontend/src/stores/generalStore.ts
@frontend/src/stores/gameStore.ts
@frontend/src/stores/worldStore.ts
@frontend/src/app/(game)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeScript types, API client, and Zustand stores</name>
  <files>
    frontend/src/types/index.ts,
    frontend/src/lib/gameApi.ts,
    frontend/src/stores/officerStore.ts,
    frontend/src/stores/gameStore.ts,
    frontend/src/stores/worldStore.ts
  </files>
  <action>
**Step 1: Update types/index.ts (1346 lines -- the master type file)**

Read the entire file. Apply these renames:

Interface renames:
- `General` -> `Officer`
- `City` -> `Planet`
- `Nation` -> `Faction`
- `Troop` -> `Fleet`
- `WorldState` -> `SessionState` (if frontend uses this type)
- `GeneralResponse` -> `OfficerResponse` (if separate from entity type)
- `BestGeneral` -> `BestOfficer`

Field renames in Officer (was General):
- `strength`/`strengthExp` -> `command`/`commandExp`
- `intel`/`intelExp` -> `intelligence`/`intelligenceExp`
- `charm`/`charmExp` -> `administration`/`administrationExp`
- Add: `mobility: number`, `mobilityExp: number`, `attack: number`, `attackExp: number`, `defense: number`, `defenseExp: number`
- Add: `dex6: number`, `dex7: number`, `dex8: number`
- `gold` -> `funds`, `rice` -> `supplies`
- `crew` -> `ships`, `crewType` -> `shipClass`
- `train` -> `training`, `atmos` -> `morale`
- `weaponCode` -> `flagshipCode`, `bookCode` -> `equipCode`, `horseCode` -> `engineCode`, `itemCode` -> `accessoryCode`
- `nationId` -> `factionId`, `cityId` -> `planetId`, `troopId` -> `fleetId`, `worldId` -> `sessionId`

Field renames in Planet (was City):
- `pop`/`popMax` -> `population`/`populationMax`
- `agri`/`agriMax` -> `production`/`productionMax`
- `comm`/`commMax` -> `commerce`/`commerceMax`
- `secu`/`secuMax` -> `security`/`securityMax`
- `trust` -> `approval`
- `def`/`defMax` -> `orbitalDefense`/`orbitalDefenseMax`
- `wall`/`wallMax` -> `fortress`/`fortressMax`
- `trade` -> `tradeRoute`
- `nationId` -> `factionId`, `worldId` -> `sessionId`

Field renames in Faction (was Nation):
- `gold` -> `funds`, `rice` -> `supplies`
- `bill` -> `taxRate`, `rate` -> `conscriptionRate`, `rateTmp` -> `conscriptionRateTmp`
- `tech` -> `techLevel`, `power` -> `militaryPower`
- `gennum` -> `officerCount`, `level` -> `factionRank`, `typeCode` -> `factionType`
- `chiefGeneralId` -> `chiefOfficerId`, `capitalCityId` -> `capitalPlanetId`
- `worldId` -> `sessionId`

Field renames in Fleet (was Troop):
- `leaderGeneralId` -> `leaderOfficerId`, `nationId` -> `factionId`, `worldId` -> `sessionId`

**Step 2: Update gameApi.ts**

Read the file. Update:
- Endpoint URLs to match renamed backend paths (reference 01-07-SUMMARY.md for exact path mapping)
- Type references: `General` -> `Officer`, `City` -> `Planet`, `Nation` -> `Faction`, `Troop` -> `Fleet`
- Function names: `getGenerals` -> `getOfficers`, `getGeneral` -> `getOfficer`, `getCities` -> `getPlanets`, `getCity` -> `getPlanet`, `getNations` -> `getFactions`, `getNation` -> `getFaction`, `getTroops` -> `getFleets`, `getTroop` -> `getFleet`
- Any WorldState-related endpoints

**Step 3: Update Zustand stores**

`generalStore.ts` -> `officerStore.ts`:
- Rename file
- `useGeneralStore` -> `useOfficerStore`
- `myGeneral` -> `myOfficer`
- `fetchMyGeneral` -> `fetchMyOfficer`
- Update API calls to use renamed gameApi functions
- Update type references
- Delete old file

`gameStore.ts`:
- `generals` -> `officers`, `cities` -> `planets`, `nations` -> `factions`
- Update fetch function names
- Update type references
- Update any field access (e.g., `general.strength` -> `officer.command`)

`worldStore.ts`:
- Update WorldState references to SessionState if applicable
- Update field access

`authStore.ts`:
- Check for any entity references, update if present

**Step 4: Update utility files**

Check and update:
- `frontend/src/lib/game-utils.ts` -- entity/field references
- `frontend/src/lib/formatLog.ts` -- field name formatting
- `frontend/src/lib/formatBattleLog.ts` -- stat name references
- `frontend/src/lib/income-calc.ts` -- resource field references
- `frontend/src/lib/interception-utils.ts` -- stat references
- `frontend/src/hooks/useWebSocket.ts` -- topic URLs if they include old names

Use `grep -rl "General\|\.strength\|\.intel\|\.charm\|\.gold\|\.rice\|\.crew\|nationId\|cityId\|troopId\|worldId" frontend/src/lib/ frontend/src/hooks/ frontend/src/stores/` to find all affected files.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>All TypeScript types, API client, and stores use LOGH domain names. TypeScript compilation passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update all game pages and components</name>
  <files>
    frontend/src/components/game/ (all components),
    frontend/src/app/(game)/ (all pages),
    frontend/src/components/app-sidebar.tsx,
    frontend/src/components/top-bar.tsx,
    frontend/src/app/(game)/layout.tsx,
    frontend/src/data/tutorial/ (tutorial mock data)
  </files>
  <action>
**Step 1: Find all affected component and page files**

```bash
grep -rl "General\|City\|Nation\|Troop\|\.strength\|\.intel\|\.charm\|\.gold\|\.rice\|\.crew\|useGeneralStore\|useGameStore" frontend/src/components/ frontend/src/app/
```

**Step 2: Update game components (40+ files in components/game/)**

For each component:
- Update import: `useGeneralStore` -> `useOfficerStore`, type imports
- Update store access: `generalStore.myGeneral` -> `officerStore.myOfficer`
- Update field rendering: `general.strength` -> `officer.command`, `city.pop` -> `planet.population`, etc.
- Update displayed stat names in UI text (if hardcoded Korean):
  - "무력" -> "지휘" (strength -> command)
  - "지력" -> "정보" (intel -> intelligence)
  - "매력" -> "운영" (charm -> administration)
  - Add labels for new stats: "기동" (mobility), "공격" (attack), "방어" (defense)

Key components to update:
- `general-basic-card.tsx` -> Consider renaming to `officer-basic-card.tsx`: Shows officer stats -- add 3 new stat displays
- `city-basic-card.tsx` -> Consider renaming to `planet-basic-card.tsx`: Shows planet resources
- `nation-basic-card.tsx` -> Consider renaming to `faction-basic-card.tsx`: Shows faction info
- `command-panel.tsx`: References general/nation commands
- `game-dashboard.tsx`: Shows overview with entity data
- `stat-bar.tsx` / `sammo-bar.tsx`: May render stat values
- `map-viewer.tsx`, `map-canvas.tsx`: May reference city/nation data
- All 3D map components in `map-3d/`: Update CityModel -> PlanetModel if it references entity types
- `equipment-browser.tsx`: Item field names (weapon->flagship, book->equip, horse->engine, item->accessory)
- `crew-type-browser.tsx`: crew/crewType references

**Step 3: Update game pages (40+ pages in app/(game)/)**

- `general/page.tsx` -> update to show Officer data with 8 stats
- `generals/page.tsx` and `generals/[id]/page.tsx` -> update type refs and field access
- `city/page.tsx` -> update to Planet fields
- `nation/page.tsx` and `nations/page.tsx` -> update to Faction fields
- `troop/page.tsx` -> update to Fleet
- `diplomacy/page.tsx`, `global-diplomacy/page.tsx` -> update nation->faction refs
- `battle/page.tsx`, `battle-center/page.tsx`, `battle-simulator/page.tsx` -> update stat refs
- `chief/page.tsx` -> update nation->faction leadership
- All other pages: update any entity/field references

**Step 4: Update layout and navigation**

- `app/(game)/layout.tsx`: Update store imports, WebSocket topic subscriptions, sidebar navigation labels
- `components/app-sidebar.tsx`: Update navigation item labels if they say "장수" -> "제독/장교", "도시" -> "행성", "국가" -> "진영"

**Step 5: Update tutorial mock data**

- `frontend/src/data/tutorial/` -- Update mock entity data to use new field names

**Step 6: Final build check**

Run `pnpm build` to verify:
- Zero TypeScript errors
- Zero Next.js build errors
- All pages compile successfully
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>All frontend components and pages use LOGH domain names. `pnpm build` succeeds with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete LOGH domain model transformation across full stack: V28 migration, 8-stat system, all entity/DTO/controller/frontend renames</what-built>
  <how-to-verify>
    1. Start services: `docker-compose up -d` then `cd backend && ./gradlew :gateway-app:bootRun` and `./gradlew :game-app:bootRun`
    2. Verify V28 migration applied: Check gateway logs for "Successfully applied 1 migration" or similar Flyway output
    3. Open frontend: `cd frontend && pnpm dev` then visit http://localhost:3000
    4. Verify game loads without console errors
    5. If a world exists, check that officer stats show 8 values (including mobility, attack, defense)
    6. Check that entity labels in UI use LOGH terms (장교/제독, 행성, 진영, 함대)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm build` succeeds
- `cd backend && ./gradlew test` passes (from Plan 07)
- `grep -r "General\|\.strength\b\|\.intel\b\|\.charm\b" frontend/src/types/index.ts` returns 0 matches
- `grep -r "useGeneralStore" frontend/src/` returns 0 matches
- Frontend renders with LOGH terminology
</verification>

<success_criteria>
The entire frontend uses LOGH domain terminology. TypeScript types match backend DTOs field-for-field. The application loads and displays data with the new 8-stat system and LOGH entity names.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-08-SUMMARY.md`
</output>
