---
phase: 01-entity-model-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/model/OfficerStat.kt
  - backend/game-app/src/main/kotlin/com/openlogh/model/RankTitle.kt
autonomous: true
requirements:
  - CHAR-02
  - CHAR-03
must_haves:
  truths:
    - "OfficerStat enum defines all 8 stats with PCP/MCP category"
    - "StatCategory.PCP contains leadership, politics, administration, intelligence"
    - "StatCategory.MCP contains command, mobility, attack, defense"
    - "RankTitle resolves 11 tiers (0-10) to faction-specific titles"
    - "Empire titles differ from Alliance titles at every tier"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/model/OfficerStat.kt"
      provides: "8-stat enum with PCP/MCP grouping"
      exports: ["OfficerStat", "StatCategory"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/model/RankTitle.kt"
      provides: "11-tier rank title resolution by faction"
      exports: ["RankTitle", "RankTitleResolver"]
  key_links: []
---

<objective>
Create the OfficerStat enum with PCP/MCP stat categorization and the RankTitle utility for 11-tier faction-specific title resolution.

Purpose: These are pure data models with no dependencies on existing code. They define the contracts that subsequent plans will wire into entities and services. Building them first (Wave 1) means entity renames in Wave 2 can immediately reference them.

Output: Two new Kotlin files in the model package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md (Stat Mapping table, Rank System tables)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OfficerStat enum with PCP/MCP categorization</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/model/OfficerStat.kt, backend/game-app/src/test/kotlin/com/openlogh/model/OfficerStatTest.kt</files>
  <behavior>
    - OfficerStat.entries has exactly 8 values
    - OfficerStat.LEADERSHIP.category == StatCategory.PCP
    - OfficerStat.POLITICS.category == StatCategory.PCP
    - OfficerStat.ADMINISTRATION.category == StatCategory.PCP
    - OfficerStat.INTELLIGENCE.category == StatCategory.PCP
    - OfficerStat.COMMAND.category == StatCategory.MCP
    - OfficerStat.MOBILITY.category == StatCategory.MCP
    - OfficerStat.ATTACK.category == StatCategory.MCP
    - OfficerStat.DEFENSE.category == StatCategory.MCP
    - OfficerStat.pcpStats() returns exactly 4 stats
    - OfficerStat.mcpStats() returns exactly 4 stats
  </behavior>
  <action>
Create `backend/game-app/src/main/kotlin/com/openlogh/model/OfficerStat.kt` with:

```kotlin
package com.openlogh.model

enum class StatCategory { PCP, MCP }

enum class OfficerStat(val category: StatCategory, val koreanName: String) {
    LEADERSHIP(StatCategory.PCP, "통솔"),
    POLITICS(StatCategory.PCP, "정치"),
    ADMINISTRATION(StatCategory.PCP, "운영"),
    INTELLIGENCE(StatCategory.PCP, "정보"),
    COMMAND(StatCategory.MCP, "지휘"),
    MOBILITY(StatCategory.MCP, "기동"),
    ATTACK(StatCategory.MCP, "공격"),
    DEFENSE(StatCategory.MCP, "방어");

    companion object {
        fun pcpStats(): List<OfficerStat> = entries.filter { it.category == StatCategory.PCP }
        fun mcpStats(): List<OfficerStat> = entries.filter { it.category == StatCategory.MCP }
    }
}
```

Write tests first (RED), then create the enum (GREEN).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.model.OfficerStatTest" -x :gateway-app:test</automated>
  </verify>
  <done>OfficerStat enum has 8 values with correct PCP/MCP grouping, pcpStats() and mcpStats() each return 4 stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RankTitle resolver for 11-tier faction-specific titles</name>
  <files>backend/game-app/src/main/kotlin/com/openlogh/model/RankTitle.kt, backend/game-app/src/test/kotlin/com/openlogh/model/RankTitleTest.kt</files>
  <behavior>
    - RankTitleResolver.resolve(0, "empire") returns RankTitle(tier=0, title="Sub-Lieutenant", korean="소위")
    - RankTitleResolver.resolve(10, "empire") returns RankTitle(tier=10, title="Reichsmarschall", korean="원수")
    - RankTitleResolver.resolve(0, "alliance") returns RankTitle(tier=0, title="Sub-Lieutenant", korean="소위")
    - RankTitleResolver.resolve(10, "alliance") returns RankTitle(tier=10, title="Fleet Admiral", korean="원수")
    - RankTitleResolver.resolve(9, "empire") returns title="Fleet Admiral" (Empire), korean="상급대장"
    - RankTitleResolver.resolve(9, "alliance") returns title="Admiral of the Fleet", korean="상급대장"
    - Tier out of range (11, -1) throws IllegalArgumentException
    - Unknown faction type falls back to Empire titles
  </behavior>
  <action>
Create `backend/game-app/src/main/kotlin/com/openlogh/model/RankTitle.kt` with:

A `RankTitle` data class with `tier: Int`, `title: String`, `korean: String` fields.

A `RankTitleResolver` object with a `resolve(tier: Int, factionType: String): RankTitle` function.

Empire ranks (per CLAUDE.md Rank System table):
| Tier | Title | Korean |
|------|-------|--------|
| 0 | Sub-Lieutenant | 소위 |
| 1 | Lieutenant | 대위 |
| 2 | Lieutenant Commander | 소령 |
| 3 | Commander | 중령 |
| 4 | Captain | 대령 |
| 5 | Commodore | 준장 |
| 6 | Rear Admiral | 소장 |
| 7 | Vice Admiral | 중장 |
| 8 | Admiral | 대장 |
| 9 | Fleet Admiral | 상급대장 |
| 10 | Reichsmarschall | 원수 |

Alliance ranks (same Korean names, some English titles differ):
| Tier | Title | Korean |
|------|-------|--------|
| 0 | Sub-Lieutenant | 소위 |
| 1 | Lieutenant | 대위 |
| 2 | Lieutenant Commander | 소령 |
| 3 | Commander | 중령 |
| 4 | Captain | 대령 |
| 5 | Commodore | 준장 |
| 6 | Rear Admiral | 소장 |
| 7 | Vice Admiral | 중장 |
| 8 | Admiral | 대장 |
| 9 | Admiral of the Fleet | 상급대장 |
| 10 | Fleet Admiral | 원수 |

Store as two private lists indexed by tier. Validate tier is 0-10.

Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.model.RankTitleTest" -x :gateway-app:test</automated>
  </verify>
  <done>RankTitle resolves all 11 tiers for both Empire and Alliance with correct English and Korean titles. Invalid tiers throw IllegalArgumentException.</done>
</task>

</tasks>

<verification>
- Both test files pass: `./gradlew :game-app:test --tests "com.openlogh.model.*" -x :gateway-app:test`
- OfficerStat.pcpStats() returns exactly [LEADERSHIP, POLITICS, ADMINISTRATION, INTELLIGENCE]
- OfficerStat.mcpStats() returns exactly [COMMAND, MOBILITY, ATTACK, DEFENSE]
- RankTitleResolver handles all 11 tiers for both faction types
</verification>

<success_criteria>
Two new model files exist with full test coverage. The 8-stat PCP/MCP grouping and 11-tier rank resolution are implemented as standalone utilities ready for integration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-02-SUMMARY.md`
</output>
