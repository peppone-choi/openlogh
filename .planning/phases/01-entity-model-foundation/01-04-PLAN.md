---
phase: 01-entity-model-foundation
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/entity/WorldState.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/repository/WorldStateRepository.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/dto/WorldStateResponse.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/service/WorldService.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/controller/WorldController.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/GameProcessOrchestrator.kt
  - backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/WorldActivationBootstrap.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/GeneralTurn.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/NationTurn.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/NationFlag.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/OldGeneral.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/OldNation.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/GeneralRecord.kt
  - backend/game-app/src/main/kotlin/com/openlogh/entity/GeneralAccessLog.kt
autonomous: true
requirements:
  - CHAR-01
must_haves:
  truths:
    - "Gateway WorldState.kt is renamed to SessionState.kt with @Table(name = 'session_state')"
    - "Gateway repository, DTO, service, controller all reference SessionState"
    - "GeneralTurn -> OfficerTurn with @Table(name = 'officer_turn'), FK renamed"
    - "NationTurn -> FactionTurn with @Table(name = 'faction_turn'), FK renamed"
    - "NationFlag -> FactionFlag with @Table(name = 'faction_flag'), FK renamed"
    - "OldGeneral -> OldOfficer with @Table(name = 'old_officer'), FK renamed"
    - "OldNation -> OldFaction with @Table(name = 'old_faction'), FK renamed"
    - "GeneralRecord -> OfficerRecord with FK renamed"
    - "GeneralAccessLog -> OfficerAccessLog with FK renamed"
  artifacts:
    - path: "backend/gateway-app/src/main/kotlin/com/openlogh/gateway/entity/SessionState.kt"
      provides: "Gateway SessionState entity"
      contains: "class SessionState"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/entity/OfficerTurn.kt"
      provides: "OfficerTurn entity"
      contains: "class OfficerTurn"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/entity/FactionTurn.kt"
      provides: "FactionTurn entity"
      contains: "class FactionTurn"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/entity/FactionFlag.kt"
      provides: "FactionFlag entity"
      contains: "class FactionFlag"
  key_links:
    - from: "gateway SessionState.kt"
      to: "V28 migration"
      via: "@Table(name = 'session_state') matches renamed table"
      pattern: "@Table\\(name = \"session_state\"\\)"
---

<objective>
Rename the gateway-app WorldState entity and all supporting/auxiliary game-app entities (turns, flags, old records, access logs) to LOGH domain names, and propagate the renames through gateway service/controller/DTO layers.

Purpose: The gateway module has its own WorldState entity mapping to the same table. This must be renamed simultaneously with game-app entities. Auxiliary entities (GeneralTurn, NationTurn, NationFlag, OldGeneral, OldNation, GeneralRecord, GeneralAccessLog) must also be renamed to match V28 table names. This plan runs in parallel with Plan 03 (core entities) since they touch different files.

Output: Renamed gateway entity + supporting entities, updated gateway service layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-entity-model-foundation/01-RESEARCH.md
@CLAUDE.md

Key source files:
@backend/gateway-app/src/main/kotlin/com/openlogh/gateway/entity/WorldState.kt
@backend/gateway-app/src/main/kotlin/com/openlogh/gateway/repository/WorldStateRepository.kt
@backend/gateway-app/src/main/kotlin/com/openlogh/gateway/service/WorldService.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/GeneralTurn.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/NationTurn.kt
@backend/game-app/src/main/kotlin/com/openlogh/entity/NationFlag.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename gateway WorldState to SessionState + update gateway layer</name>
  <files>
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/entity/SessionState.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/repository/SessionStateRepository.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/dto/SessionStateResponse.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/service/WorldService.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/controller/WorldController.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/GameProcessOrchestrator.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/GameContainerOrchestrator.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/WorldActivationBootstrap.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/orchestrator/GameOrchestrator.kt,
    backend/gateway-app/src/main/kotlin/com/openlogh/gateway/service/WorldRouteRegistry.kt
  </files>
  <action>
Read ALL gateway files in the entity/, repository/, dto/, service/, controller/, orchestrator/ directories before making changes.

**Step 1 - Rename entity:**
- `gateway/entity/WorldState.kt` -> `gateway/entity/SessionState.kt`
- Class: `WorldState` -> `SessionState`
- Table: `@Table(name = "world_state")` -> `@Table(name = "session_state")`
- Delete old file

**Step 2 - Rename repository:**
- `WorldStateRepository.kt` -> `SessionStateRepository.kt`
- Interface: `WorldStateRepository` -> `SessionStateRepository`
- Update entity type reference: `JpaRepository<WorldState, Long>` -> `JpaRepository<SessionState, Long>`
- Update any `@Query` annotations to reference `SessionState` entity name
- Delete old file

**Step 3 - Rename DTO:**
- `WorldStateResponse.kt` -> `SessionStateResponse.kt`
- Class: `WorldStateResponse` -> `SessionStateResponse`
- Delete old file

**Step 4 - Update service layer:**
In `WorldService.kt`: Replace all `WorldState` references with `SessionState`, `WorldStateRepository` with `SessionStateRepository`, `WorldStateResponse` with `SessionStateResponse`. Keep the service class name as `WorldService` (it manages "worlds" conceptually, the entity is the implementation detail).

**Step 5 - Update controllers:**
In `WorldController.kt` and any other controllers: Replace `WorldState`/`WorldStateResponse`/`WorldStateRepository` references.

**Step 6 - Update orchestrators:**
In `GameProcessOrchestrator.kt`, `GameContainerOrchestrator.kt`, `WorldActivationBootstrap.kt`, `GameOrchestrator.kt`: Replace `WorldState` with `SessionState`, `WorldStateRepository` with `SessionStateRepository`.

**Step 7 - Update WorldRouteRegistry:**
Replace any `WorldState` type references.

**Step 8 - Check shared module:**
Read `backend/shared/src/main/kotlin/com/openlogh/shared/dto/WorldDtos.kt` -- if it contains WorldState references, update those too.

Use grep to find ALL files in gateway-app that reference `WorldState` and ensure none are missed.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh && grep -r "class WorldState\|WorldStateRepository\b" backend/gateway-app/src/main/kotlin/ | grep -v "//"; echo "---"; test -f backend/gateway-app/src/main/kotlin/com/openlogh/gateway/entity/SessionState.kt && echo "SessionState.kt exists"</automated>
  </verify>
  <done>Gateway module uses SessionState everywhere. No references to old WorldState class remain in gateway-app source (excluding comments).</done>
</task>

<task type="auto">
  <name>Task 2: Rename auxiliary game-app entities (turns, flags, old records, logs)</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/entity/OfficerTurn.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/FactionTurn.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/FactionFlag.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/OldOfficer.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/OldFaction.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/OfficerRecord.kt,
    backend/game-app/src/main/kotlin/com/openlogh/entity/OfficerAccessLog.kt
  </files>
  <action>
Read each entity file fully before modifying.

**GeneralTurn.kt -> OfficerTurn.kt:**
- Class: `GeneralTurn` -> `OfficerTurn`
- Table: `@Table(name = "general_turn")` -> `@Table(name = "officer_turn")`
- FK: `generalId`/`general_id` -> `officerId`/`officer_id`, `worldId`/`world_id` -> `sessionId`/`session_id`
- Delete old file

**NationTurn.kt -> FactionTurn.kt:**
- Class: `NationTurn` -> `FactionTurn`
- Table: `@Table(name = "nation_turn")` -> `@Table(name = "faction_turn")`
- FK: `nationId`/`nation_id` -> `factionId`/`faction_id`, `worldId`/`world_id` -> `sessionId`/`session_id`
- Delete old file

**NationFlag.kt -> FactionFlag.kt:**
- Class: `NationFlag` -> `FactionFlag`
- Table: `@Table(name = "nation_flag")` -> `@Table(name = "faction_flag")`
- FK: `nationId`/`nation_id` -> `factionId`/`faction_id`, `worldId`/`world_id` -> `sessionId`/`session_id`
- Note: The `@Enumerated` type annotation for `nation_aux_key` should be updated to `faction_aux_key` if referenced
- Delete old file

**OldGeneral.kt -> OldOfficer.kt:**
- Class: `OldGeneral` -> `OldOfficer`
- Table: `@Table(name = "old_general")` -> `@Table(name = "old_officer")`
- FK: `worldId`/`world_id` -> `sessionId`/`session_id`
- Delete old file

**OldNation.kt -> OldFaction.kt:**
- Class: `OldNation` -> `OldFaction`
- Table: `@Table(name = "old_nation")` -> `@Table(name = "old_faction")`
- FK: `worldId`/`world_id` -> `sessionId`/`session_id`
- Delete old file

**GeneralRecord.kt -> OfficerRecord.kt:**
- Class: `GeneralRecord` -> `OfficerRecord`
- Table: update @Table name if it has one (check existing file)
- FK: `worldId`/`world_id` -> `sessionId`/`session_id`, any `generalId` -> `officerId`
- Delete old file

**GeneralAccessLog.kt -> OfficerAccessLog.kt:**
- Class: `GeneralAccessLog` -> `OfficerAccessLog`
- Table: update @Table name if it has one
- FK: `worldId`/`world_id` -> `sessionId`/`session_id`, any `generalId` -> `officerId`
- Delete old file

Also update any other entity files that have `worldId` FK:
- `Diplomacy.kt`: rename `worldId`->`sessionId`, `srcNationId`->`srcFactionId`, `destNationId`->`destFactionId`
- `Event.kt`: rename `worldId`->`sessionId`
- `Message.kt`: rename `worldId`->`sessionId`
- `RankData.kt`: rename `worldId`->`sessionId`, `nationId`->`factionId`
- `Board.kt`, `BoardComment.kt`: rename `worldId`->`sessionId`
- `Auction.kt`, `AuctionBid.kt`: rename `worldId`->`sessionId`
- `Betting.kt`, `BetEntry.kt`: rename `worldId`->`sessionId`
- `Vote.kt`, `VoteCast.kt`: rename `worldId`->`sessionId`
- `Tournament.kt`: rename `worldId`->`sessionId`
- `GameHistory.kt`, `WorldHistory.kt`, `YearbookHistory.kt`: rename `worldId`->`sessionId`
- `TrafficSnapshot.kt`: rename `worldId`->`sessionId`
- `HallOfFame.kt`: rename `worldId`->`sessionId`
- `SelectPool.kt`: rename `worldId`->`sessionId`

Use `grep -rl "worldId\|world_id" backend/game-app/src/main/kotlin/com/openlogh/entity/` to find ALL entity files with worldId and rename them all. Do NOT miss any.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh && grep -rl "worldId" backend/game-app/src/main/kotlin/com/openlogh/entity/ | head -5; echo "---"; for f in OfficerTurn FactionTurn FactionFlag OldOfficer OldFaction; do test -f "backend/game-app/src/main/kotlin/com/openlogh/entity/${f}.kt" && echo "${f}.kt exists"; done</automated>
  </verify>
  <done>All auxiliary entities renamed. No entity file in game-app/entity/ contains `worldId` or `nationId` or `cityId` or `generalId` as field names (only sessionId, factionId, planetId, officerId). Old files deleted.</done>
</task>

</tasks>

<verification>
- `grep -rl "worldId" backend/gateway-app/src/main/kotlin/` returns 0 results
- `grep -rl "worldId" backend/game-app/src/main/kotlin/com/openlogh/entity/` returns 0 results
- All old entity files deleted (GeneralTurn, NationTurn, NationFlag, OldGeneral, OldNation, GeneralRecord, GeneralAccessLog)
- Gateway boots: `cd backend && ./gradlew :gateway-app:compileKotlin` succeeds (may have errors from other layers referencing old names -- that's expected, those are fixed in later plans)
</verification>

<success_criteria>
All entity classes across both gateway-app and game-app use LOGH domain names. Every `worldId` FK is renamed to `sessionId`, every `nationId` to `factionId`, etc.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-model-foundation/01-04-SUMMARY.md`
</output>
