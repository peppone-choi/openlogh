---
phase: 05-organization-fleet-structure
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPorts.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldPorts.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldReadPort.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldWritePort.kt
autonomous: false
requirements: [ORG-01, ORG-02, ORG-03, ORG-04, ORG-05, ORG-06]

must_haves:
  truths:
    - "CQRS layer loads and persists UnitCrew snapshots alongside fleets"
    - "World state loader hydrates unitCrews map from database"
    - "All 6 unit types can be created, crewed, and persisted through a full load/save cycle"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt"
      provides: "UnitCrew loading into InMemoryWorldState"
      contains: "unitCrew"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt"
      provides: "UnitCrew persistence from InMemoryWorldState"
      contains: "unitCrew"
  key_links:
    - from: "WorldStateLoader"
      to: "UnitCrewRepository"
      via: "Loads unit_crew rows into InMemoryWorldState.unitCrews"
      pattern: "unitCrewRepository"
    - from: "WorldStatePersister"
      to: "UnitCrewRepository"
      via: "Persists dirty UnitCrewSnapshots back to database"
      pattern: "unitCrewRepository"
---

<objective>
Wire UnitCrew into the CQRS snapshot layer and verify the full integration end-to-end.

Purpose: The CQRS snapshot layer is the backbone of game state management. Without wiring UnitCrew into it, crew assignments would be invisible to the turn engine and would not survive load/save cycles.
Output: Updated CQRS ports, loader, persister with UnitCrew support. Human-verified integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-organization-fleet-structure/05-01-SUMMARY.md
@.planning/phases/05-organization-fleet-structure/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire UnitCrew into CQRS snapshot layer</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/SnapshotEntityMapper.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/WorldStatePersister.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/WorldStateLoader.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/persist/JpaWorldPorts.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryWorldPorts.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldReadPort.kt,
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/port/WorldWritePort.kt
  </files>
  <action>
    Follow the exact pattern used for other entities (Fleet, Officer, Planet) in the CQRS layer. Read each file to understand the existing pattern before modifying.

    1. **WorldReadPort.kt** -- Add method:
       - `fun findUnitCrewsBySessionId(sessionId: Long): List<UnitCrewSnapshot>` (or matching port pattern)

    2. **WorldWritePort.kt** -- Add method:
       - `fun saveUnitCrew(snapshot: UnitCrewSnapshot)` (or matching port pattern)
       - `fun deleteUnitCrew(id: Long)`

    3. **JpaWorldPorts.kt** -- Implement the new port methods:
       - Inject UnitCrewRepository
       - findUnitCrewsBySessionId: query repo, map to snapshots via SnapshotEntityMapper
       - saveUnitCrew: map snapshot to entity, save via repo
       - deleteUnitCrew: delete by id

    4. **InMemoryWorldPorts.kt** -- Implement in-memory versions:
       - Read from InMemoryWorldState.unitCrews map
       - Write to InMemoryWorldState.unitCrews map + mark dirty

    5. **WorldStateLoader.kt** -- Add UnitCrew loading:
       - After loading fleets, load unitCrews via port and populate state.unitCrews map
       - Follow exact pattern of how fleets are loaded (findBySessionId -> map to snapshots -> put in MutableMap)

    6. **WorldStatePersister.kt** -- Add UnitCrew persistence:
       - Check dirty tracker for unitCrew changes
       - Persist changed UnitCrewSnapshots via write port
       - Follow exact pattern of how other entities are persisted

    7. **SnapshotEntityMapper.kt** -- Add bidirectional mapping (if not done in Plan 01):
       - `fun toUnitCrewSnapshot(entity: UnitCrew): UnitCrewSnapshot`
       - `fun toUnitCrewEntity(snapshot: UnitCrewSnapshot): UnitCrew`
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>CQRS layer loads UnitCrew rows into InMemoryWorldState on world load. Dirty UnitCrew snapshots persist back to database on save. Compile succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete organization and fleet structure system:
    - 6 unit types (Fleet/Patrol/Transport/Ground/Garrison/Solo) with correct compositions
    - Crew slot system (10/3/3/1/1/0 slots per type with role-based assignment)
    - Population-based formation caps (1 billion = 1 fleet or 6 patrols)
    - CQRS snapshot integration for crew persistence
    - Frontend unit management UI with formation cap display
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && ./gradlew :game-app:bootRun`
       - Verify V34 migration applies cleanly (check logs for "Successfully applied migration V34")
       - If DB error, check that previous migrations are applied

    2. Test unit creation API:
       ```bash
       # Create a fleet (replace IDs with valid session/officer/faction)
       curl -X POST http://localhost:9001/api/units \
         -H "Content-Type: application/json" \
         -d '{"sessionId":1,"commanderOfficerId":1,"factionId":1,"name":"Test Fleet","unitType":"FLEET"}'

       # Create a patrol
       curl -X POST http://localhost:9001/api/units \
         -H "Content-Type: application/json" \
         -d '{"sessionId":1,"commanderOfficerId":2,"factionId":1,"name":"Test Patrol","unitType":"PATROL"}'
       ```

    3. Test formation caps:
       ```bash
       curl http://localhost:9001/api/factions/1/formation-caps
       ```
       - Should show current/max for each unit type based on faction population

    4. Test crew assignment:
       ```bash
       curl -X POST http://localhost:9001/api/units/{unitId}/crew \
         -H "Content-Type: application/json" \
         -d '{"officerId":3,"slotRole":"VICE_COMMANDER"}'
       ```

    5. Start frontend: `cd frontend && pnpm dev`
       - Navigate to game UI and check that unit panel components render
       - Verify formation cap bars show for each unit type
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Backend compiles and boots with V34 migration
- All 6 unit types can be created via API
- Crew slots enforce limits per unit type
- Formation caps reflect faction population
- CQRS load/save cycle preserves crew assignments
- Frontend components render unit list and crew roster
</verification>

<success_criteria>
The complete organization and fleet structure is operational: 6 unit types with gin7-accurate compositions, role-based crew slots, population-linked formation caps, full CQRS integration, and a frontend UI showing the military organization. A player can see their faction's units, crew rosters, and remaining formation capacity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-organization-fleet-structure/05-04-SUMMARY.md`
</output>
