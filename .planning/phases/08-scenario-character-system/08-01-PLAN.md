---
phase: 08-scenario-character-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/CommandHierarchy.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandBufferTest.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandHierarchyTest.kt
autonomous: true
requirements:
  - ENGINE-02
  - ENGINE-03

must_haves:
  truths:
    - "TacticalCommand sealed class exists with exhaustive subtypes for all 7 WebSocket command types"
    - "CommandHierarchy data class exists with fleetCommander, subCommanders, successionQueue, crcRadius, commJammed fields"
    - "Test scaffolds exist for command buffer drain and CommandHierarchy initialization"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalCommand.kt"
      provides: "Sealed class for command buffer pattern"
      contains: "sealed class TacticalCommand"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/CommandHierarchy.kt"
      provides: "Data model for command hierarchy (Phase 9-10 ready)"
      contains: "data class CommandHierarchy"
    - path: "backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandBufferTest.kt"
      provides: "Tests for ENGINE-02 command buffer behavior"
      contains: "class CommandBufferTest"
    - path: "backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandHierarchyTest.kt"
      provides: "Tests for ENGINE-03 hierarchy initialization"
      contains: "class CommandHierarchyTest"
  key_links:
    - from: "TacticalCommand.kt"
      to: "EnergyAllocation, Formation, UnitStance, ConquestRequest"
      via: "import from com.openlogh.model and com.openlogh.engine.tactical"
      pattern: "import com\\.openlogh\\.model\\."
---

<objective>
Create the TacticalCommand sealed class and CommandHierarchy data model as interface contracts for Phase 8, plus Wave 0 test scaffolds.

Purpose: These are the new types that Plan 02 (engine merge) and Plan 03 (buffer integration) depend on. Creating them first prevents scavenger hunts and enables parallel work on Plan 02.
Output: 2 new Kotlin source files + 2 new test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scenario-character-system/08-CONTEXT.md
@.planning/phases/08-scenario-character-system/08-RESEARCH.md

<interfaces>
<!-- Existing types the new files must import -->

From com.openlogh.model.EnergyAllocation:
```kotlin
data class EnergyAllocation(
    val beam: Int, val gun: Int, val shield: Int,
    val engine: Int, val warp: Int, val sensor: Int,
)
```

From com.openlogh.model.Formation:
```kotlin
enum class Formation { WEDGE, BY_CLASS, MIXED, THREE_COLUMN }
```

From com.openlogh.model.UnitStance:
```kotlin
enum class UnitStance { NAVIGATION, ANCHORING, STATIONED, COMBAT }
```

From com.openlogh.engine.tactical.ConquestRequest:
```kotlin
data class ConquestRequest(val command: ConquestCommand, val attackerOfficerId: Long, ...)
```

From com.openlogh.service.UnitCommandRequest:
```kotlin
data class UnitCommandRequest(val officerId: Long, val command: String, val dirX: Double, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TacticalCommand sealed class and CommandHierarchy data model</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalCommand.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/CommandHierarchy.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
    backend/game-app/src/main/kotlin/com/openlogh/model/EnergyAllocation.kt
    backend/game-app/src/main/kotlin/com/openlogh/model/Formation.kt
    backend/game-app/src/main/kotlin/com/openlogh/model/UnitStance.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
  </read_first>
  <action>
Create two new Kotlin files in `engine/tactical/` package:

**TacticalCommand.kt** (per D-03, D-04):
- Package: `com.openlogh.engine.tactical`
- `sealed class TacticalCommand` with abstract fields `battleId: Long` and `officerId: Long`
- 7 subclasses matching all WebSocket @MessageMapping handlers in BattleWebSocketController:
  1. `SetEnergy(battleId, officerId, allocation: EnergyAllocation)` -- energy endpoint
  2. `SetStance(battleId, officerId, stance: UnitStance)` -- stance endpoint
  3. `SetFormation(battleId, officerId, formation: Formation)` -- formation endpoint (currently inside unit-command but deserves own type)
  4. `Retreat(battleId, officerId)` -- retreat endpoint
  5. `SetAttackTarget(battleId, officerId, targetFleetId: Long)` -- attack-target endpoint
  6. `UnitCommand(battleId, officerId, command: String, dirX: Double = 0.0, dirY: Double = 0.0, speed: Double = 1.0, targetFleetId: Long? = null, formation: String? = null)` -- unit-command endpoint
  7. `PlanetConquest(battleId, officerId, request: ConquestRequest)` -- planet-conquest endpoint
- Import: `com.openlogh.model.EnergyAllocation`, `com.openlogh.model.Formation`, `com.openlogh.model.UnitStance`, `com.openlogh.engine.tactical.ConquestRequest`

**CommandHierarchy.kt** (per D-05, D-06 -- data model only, no logic):
- Package: `com.openlogh.engine.tactical`
- `data class CommandHierarchy`:
  - `val fleetCommander: Long` -- fleet commander officer ID
  - `val subCommanders: MutableMap<Long, SubFleet> = mutableMapOf()` -- officerId to SubFleet
  - `val successionQueue: MutableList<Long> = mutableListOf()` -- ordered by rank desc
  - `val crcRadius: MutableMap<Long, Double> = mutableMapOf()` -- officerId to CRC radius
  - `var commJammed: Boolean = false` -- communication jammed flag
- `data class SubFleet`:
  - `val commanderId: Long`
  - `val commanderName: String`
  - `val unitFleetIds: List<Long>`
  - `val commanderRank: Int`
- NO logic methods, NO Spring dependencies -- pure data model for Phase 9-10
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin -x bootJar 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TacticalCommand.kt contains `sealed class TacticalCommand`
    - TacticalCommand.kt contains `data class SetEnergy` with `allocation: EnergyAllocation`
    - TacticalCommand.kt contains `data class SetStance` with `stance: UnitStance`
    - TacticalCommand.kt contains `data class Retreat`
    - TacticalCommand.kt contains `data class SetAttackTarget` with `targetFleetId: Long`
    - TacticalCommand.kt contains `data class UnitCommand` with `command: String`
    - TacticalCommand.kt contains `data class PlanetConquest` with `request: ConquestRequest`
    - CommandHierarchy.kt contains `data class CommandHierarchy`
    - CommandHierarchy.kt contains `val fleetCommander: Long`
    - CommandHierarchy.kt contains `val subCommanders: MutableMap<Long, SubFleet>`
    - CommandHierarchy.kt contains `val successionQueue: MutableList<Long>`
    - CommandHierarchy.kt contains `val crcRadius: MutableMap<Long, Double>`
    - CommandHierarchy.kt contains `var commJammed: Boolean`
    - CommandHierarchy.kt contains `data class SubFleet`
    - compileKotlin passes with exit code 0
  </acceptance_criteria>
  <done>TacticalCommand sealed class with 7 subtypes compiles. CommandHierarchy + SubFleet data classes compile. No logic implemented -- data models only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 test scaffolds for CommandBuffer and CommandHierarchy</name>
  <files>
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandBufferTest.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/CommandHierarchyTest.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalCommand.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/CommandHierarchy.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleEngineTest.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  </read_first>
  <behavior>
    CommandBufferTest (RED -- these will fail until Plan 03 implements buffer logic):
    - Test 1: enqueued SetEnergy command is drained and applied to unit's energy allocation
    - Test 2: enqueued SetStance command is drained and applied to unit's stance
    - Test 3: enqueued Retreat command sets unit isRetreating=true
    - Test 4: multiple commands enqueued before tick are all applied in order
    - Test 5: command for dead unit (isAlive=false) is silently ignored

    CommandHierarchyTest (RED -- these will fail until Plan 03 implements init logic):
    - Test 1: buildCommandHierarchy creates hierarchy with correct fleetCommander
    - Test 2: successionQueue is ordered by rank descending
    - Test 3: crcRadius is initialized for the fleet commander
    - Test 4: commJammed defaults to false
  </behavior>
  <action>
Create two JUnit 5 test files following existing test patterns (see TacticalBattleEngineTest.kt for style).

**CommandBufferTest.kt:**
- Package: `com.openlogh.engine.tactical`
- Use `@Test` annotations, `assertEquals`, `assertTrue`, `assertFalse` from JUnit 5
- Create helper function to build a minimal TacticalBattleState with a commandBuffer (ConcurrentLinkedQueue) and 2 units
- Tests should enqueue TacticalCommand instances into the buffer, then call a `drainCommandBuffer(state)` function (which does not exist yet -- tests will fail RED)
- Each test verifies the state change AFTER drain
- Mark tests with `@Disabled("Plan 03: command buffer drain not yet implemented")` so they compile but are skipped until Plan 03

**CommandHierarchyTest.kt:**
- Package: `com.openlogh.engine.tactical`
- Test a `buildCommandHierarchy(fleetId, leaderOfficerId, units, officerRanks)` top-level function (which does not exist yet)
- Create test data: 3 units with different ranks (10, 7, 5)
- Verify fleetCommander matches leaderOfficerId
- Verify successionQueue order is [rank10, rank7, rank5]
- Verify crcRadius has an entry for the commander
- Mark tests with `@Disabled("Plan 03: CommandHierarchy initialization not yet implemented")` so they compile but are skipped

Both files should compile and be skippable via `@Disabled`. They will be un-disabled in Plan 03.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileTestKotlin -x bootJar 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CommandBufferTest.kt contains `class CommandBufferTest`
    - CommandBufferTest.kt contains `@Disabled` annotations
    - CommandBufferTest.kt contains at least 5 `@Test` functions
    - CommandBufferTest.kt contains `ConcurrentLinkedQueue`
    - CommandHierarchyTest.kt contains `class CommandHierarchyTest`
    - CommandHierarchyTest.kt contains `@Disabled` annotations
    - CommandHierarchyTest.kt contains at least 4 `@Test` functions
    - compileTestKotlin passes with exit code 0
  </acceptance_criteria>
  <done>Wave 0 test scaffolds compile, are marked @Disabled, and define the behavioral contracts for ENGINE-02 (buffer drain) and ENGINE-03 (hierarchy init). Plan 03 will enable and make them pass.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew :game-app:compileKotlin -x bootJar` exits 0
- `cd backend && ./gradlew :game-app:compileTestKotlin -x bootJar` exits 0
- `grep -r "sealed class TacticalCommand" backend/game-app/src/main/` finds 1 match
- `grep -r "data class CommandHierarchy" backend/game-app/src/main/` finds 1 match
</verification>

<success_criteria>
TacticalCommand sealed class and CommandHierarchy data model exist as compilable Kotlin files. Test scaffolds exist and compile (disabled). No runtime behavior changed -- purely additive.
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario-character-system/08-01-SUMMARY.md`
</output>
