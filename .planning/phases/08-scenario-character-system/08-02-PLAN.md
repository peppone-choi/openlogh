---
phase: 08-scenario-character-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetCaptureProcessor.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/ItemModifiers.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleRestController.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/DetectionServiceTest.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/PlanetConquestServiceTest.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleEngineTest.kt
autonomous: true
requirements:
  - ENGINE-01

must_haves:
  truths:
    - "Only TacticalBattleEngine exists as the single tactical engine -- TacticalCombatEngine is deleted"
    - "TacticalUnit has all merged fields: supplies, weaponCooldowns, debuffs, detectionCapability, CommandRange object, isStopped, stanceChangeTicksRemaining"
    - "engine/war/ package is completely empty or deleted"
    - "Duplicate controllers (BattleRestController, TacticalBattleController, TacticalBattleRestController) are deleted"
    - "DetectionEngine and PlanetCaptureProcessor live in engine/tactical/ package"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt"
      provides: "Unified tactical engine with merged TacticalUnit fields"
      contains: "var weaponCooldowns"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionEngine.kt"
      provides: "Detection engine moved from war/ to tactical/"
      contains: "package com.openlogh.engine.tactical"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetCaptureProcessor.kt"
      provides: "Planet capture processor moved from war/ to tactical/"
      contains: "package com.openlogh.engine.tactical"
  key_links:
    - from: "DetectionService.kt"
      to: "DetectionEngine.kt"
      via: "import within same tactical package"
      pattern: "import com\\.openlogh\\.engine\\.tactical\\.Detection"
    - from: "PlanetConquestService.kt"
      to: "PlanetCaptureProcessor.kt"
      via: "import within same tactical package"
      pattern: "import com\\.openlogh\\.engine\\.tactical\\.(CaptureProcessing|PlanetCaptureProcessor)"
---

<objective>
Merge TacticalCombatEngine fields into TacticalBattleEngine, move war/ services to tactical/, delete war/ package and duplicate controllers.

Purpose: Eliminate the dual-engine architecture (ENGINE-01). After this plan, there is exactly one tactical engine with one TacticalUnit type containing all fields from both engines.
Output: Unified TacticalBattleEngine.kt with merged fields, moved DetectionEngine/PlanetCaptureProcessor, deleted war/ package and 3 duplicate controllers
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scenario-character-system/08-CONTEXT.md
@.planning/phases/08-scenario-character-system/08-RESEARCH.md

<interfaces>
<!-- TacticalUnit field diff from RESEARCH.md -- executor must merge these -->

Fields to ADD to tactical/ TacticalUnit (from war/ TacticalUnit):
```kotlin
var supplies: Int = 0
var weaponCooldowns: MutableMap<TacticalWeaponType, Int> = mutableMapOf()
var debuffs: MutableMap<String, Int> = mutableMapOf()
var detectionCapability: DetectionCapability = DetectionCapability(baseRange = 5.0, basePrecision = 0.5, evasionRating = 0.2)
var stanceChangeTicksRemaining: Int = 0  // replaces ticksSinceStanceChange
```

Fields to REPLACE in tactical/ TacticalUnit:
```kotlin
// REMOVE these 3 fields:
var commandRange: Double = 0.0
var commandRangeMax: Double = 100.0
var ticksSinceLastOrder: Int = 0

// REPLACE with CommandRange object:
var commandRange: CommandRange = CommandRange()
```

Computed property to ADD:
```kotlin
val isStopped: Boolean get() = velX == 0.0 && velY == 0.0
```

CommandRange class (from war/ TacticalCombatEngine.kt):
```kotlin
data class CommandRange(
    var currentRange: Double = 0.0,
    val maxRange: Double = 100.0,
    val expansionRate: Double = 1.0,
    val hasCommandRange: Boolean = true,
) {
    fun tick() { if (hasCommandRange && currentRange < maxRange) currentRange += expansionRate }
    fun resetOnCommand() = copy(currentRange = 0.0)
}
```

DetectionCapability class (from war/ DetectionEngine.kt):
```kotlin
data class DetectionCapability(
    val baseRange: Double,
    val basePrecision: Double,
    val evasionRating: Double,
)
```

ShipSubtype enum (from war/ TacticalCombatEngine.kt):
```kotlin
enum class ShipSubtype { BATTLESHIP, CRUISER, DESTROYER, CARRIER, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge TacticalUnit fields and move war/ services to tactical/</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetCaptureProcessor.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionService.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/ItemModifiers.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/TacticalCombatEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/DetectionEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/PlanetCaptureProcessor.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/BattleTrigger.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/DetectionService.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/ItemModifiers.kt
  </read_first>
  <action>
**Step 1: Merge TacticalUnit fields in TacticalBattleEngine.kt** (per D-01):

In `TacticalUnit` data class, make these changes:
- REMOVE fields: `commandRange: Double`, `commandRangeMax: Double`, `ticksSinceLastOrder: Int`
- ADD field: `var commandRange: CommandRange = CommandRange()` (replaces the 3 removed fields)
- REMOVE field: `ticksSinceStanceChange: Int` (count-UP approach)
- ADD field: `var stanceChangeTicksRemaining: Int = 0` (count-DOWN approach from war/, clearer semantics)
- ADD field: `var supplies: Int = 0`
- ADD field: `var weaponCooldowns: MutableMap<TacticalWeaponType, Int> = mutableMapOf()`
- ADD field: `var debuffs: MutableMap<String, Int> = mutableMapOf()`
- ADD field: `var detectionCapability: DetectionCapability = DetectionCapability(baseRange = 5.0, basePrecision = 0.5, evasionRating = 0.2)`
- CHANGE field: `var shipSubtype: String = ""` to `var shipSubtype: ShipSubtype? = null` (richer type)
- ADD computed property: `val isStopped: Boolean get() = velX == 0.0 && velY == 0.0`

Add the `CommandRange` data class (copy from war/TacticalCombatEngine.kt) to TacticalBattleEngine.kt (or a separate file -- executor's discretion). It has: `currentRange`, `maxRange`, `expansionRate`, `hasCommandRange`, `fun tick()`, `fun resetOnCommand()`.

If `ShipSubtype` enum and `DetectionCapability` data class are defined in war/ files, move/copy their definitions into tactical/ package files.

**Step 2: Update all references to removed/changed fields:**

Search entire codebase for:
- `commandRangeMax` -- replace with `commandRange.maxRange`
- `ticksSinceLastOrder` -- replace with CommandRange tick logic
- `ticksSinceStanceChange` -- replace with `stanceChangeTicksRemaining` (invert count direction: old code `ticksSinceStanceChange >= COOLDOWN` becomes `stanceChangeTicksRemaining <= 0`)
- `shipSubtype` string comparisons -- update to ShipSubtype enum comparisons
- `.commandRange` Double usage -- update to `.commandRange.currentRange` for reads, `.commandRange = commandRange.resetOnCommand()` for resets

**Step 3: Move DetectionEngine.kt from war/ to tactical/** (per D-02):
- Copy `engine/war/DetectionEngine.kt` to `engine/tactical/DetectionEngine.kt`
- Change package declaration from `com.openlogh.engine.war` to `com.openlogh.engine.tactical`
- Keep all classes inside (DetectionEngine, DetectorUnit, DetectionTarget, DetectionInfo, DetectionCapability)

**Step 4: Move PlanetCaptureProcessor.kt from war/ to tactical/** (per D-02):
- Copy `engine/war/PlanetCaptureProcessor.kt` to `engine/tactical/PlanetCaptureProcessor.kt`
- Change package declaration from `com.openlogh.engine.war` to `com.openlogh.engine.tactical`
- Keep all classes inside (PlanetCaptureProcessor, CaptureProcessingInput, CaptureProcessingResult)

**Step 5: Update imports in DetectionService.kt:**
- Change `import com.openlogh.engine.war.DetectionEngine` to `import com.openlogh.engine.war` lines are no longer needed (same package)
- Remove all `import com.openlogh.engine.war.*` lines since DetectionEngine, DetectorUnit, DetectionTarget are now in the same `engine.tactical` package

**Step 6: Update imports in PlanetConquestService.kt:**
- Remove `import com.openlogh.engine.war.CaptureProcessingInput`
- Remove `import com.openlogh.engine.war.CaptureProcessingResult`
- Remove `import com.openlogh.engine.war.PlanetCaptureProcessor`
- These classes are now in the same `engine.tactical` package -- no import needed

**Step 7: Fix ItemModifiers.kt** (per Pitfall 5):
- Remove `import com.openlogh.engine.war.BattleTrigger`
- Remove `import com.openlogh.engine.war.BattleTriggerRegistry`
- Remove or stub any code referencing BattleTrigger/BattleTriggerRegistry (they are empty stubs)
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin -x bootJar 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TacticalBattleEngine.kt TacticalUnit contains `var commandRange: CommandRange = CommandRange()`
    - TacticalBattleEngine.kt TacticalUnit contains `var supplies: Int = 0`
    - TacticalBattleEngine.kt TacticalUnit contains `var weaponCooldowns: MutableMap<TacticalWeaponType, Int>`
    - TacticalBattleEngine.kt TacticalUnit contains `var debuffs: MutableMap<String, Int>`
    - TacticalBattleEngine.kt TacticalUnit contains `var detectionCapability: DetectionCapability`
    - TacticalBattleEngine.kt TacticalUnit contains `var stanceChangeTicksRemaining: Int`
    - TacticalBattleEngine.kt TacticalUnit contains `val isStopped: Boolean get()`
    - TacticalBattleEngine.kt TacticalUnit does NOT contain `var commandRangeMax: Double`
    - TacticalBattleEngine.kt TacticalUnit does NOT contain `var ticksSinceLastOrder: Int`
    - TacticalBattleEngine.kt TacticalUnit does NOT contain `var ticksSinceStanceChange: Int`
    - DetectionEngine.kt exists in engine/tactical/ with `package com.openlogh.engine.tactical`
    - PlanetCaptureProcessor.kt exists in engine/tactical/ with `package com.openlogh.engine.tactical`
    - DetectionService.kt does NOT contain `import com.openlogh.engine.war`
    - PlanetConquestService.kt does NOT contain `import com.openlogh.engine.war`
    - ItemModifiers.kt does NOT contain `import com.openlogh.engine.war`
    - compileKotlin passes with exit code 0
  </acceptance_criteria>
  <done>TacticalUnit has all merged fields from both engines. DetectionEngine and PlanetCaptureProcessor are in tactical/ package. All war/ imports removed from tactical/ and modifier/ packages. Compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete war/ package, duplicate controllers, and clean up war/ tests</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/TacticalCombatEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/DetectionEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/PlanetCaptureProcessor.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/war/BattleTrigger.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleController.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleRestController.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/war/
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/DetectionServiceTest.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/PlanetConquestServiceTest.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleEngineTest.kt
  </files>
  <read_first>
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleController.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/TacticalBattleRestController.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/DetectionServiceTest.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/PlanetConquestServiceTest.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleEngineTest.kt
  </read_first>
  <action>
**Step 1: Delete war/ source files** (per D-07):
- Delete `engine/war/TacticalCombatEngine.kt`
- Delete `engine/war/DetectionEngine.kt` (already moved to tactical/ in Task 1)
- Delete `engine/war/PlanetCaptureProcessor.kt` (already moved to tactical/ in Task 1)
- Delete `engine/war/BattleTrigger.kt`
- If the war/ directory is now empty, remove it entirely

**Step 2: Delete duplicate controllers** (per D-08):
- Delete `controller/BattleRestController.kt`
- Delete `controller/TacticalBattleController.kt`
- Delete `controller/TacticalBattleRestController.kt`
- Only BattleWebSocketController.kt and BattleSimController.kt remain

**Step 3: Clean up war/ test files:**
- Delete `engine/war/TacticalCombatEngineTest.kt` (engine being removed)
- Delete `engine/war/BattleTriggerTest.kt` (stub being removed)
- Update `engine/war/DetectionEngineTest.kt`: move to `engine/tactical/` and update package+imports to `com.openlogh.engine.tactical`, OR delete if DetectionServiceTest already covers the same behavior
- For remaining 22 war/ test files: audit each file. If it imports from `com.openlogh.engine.war.TacticalCombatEngine` or `BattleTrigger`, delete it. If it tests legacy OpenSamguk battle engine behavior (BattleEngine, not TacticalCombatEngine), keep but verify it compiles (these test the legacy `engine/war/BattleEngine.kt` if it exists, not the tactical engine)
- If any war/ test imports `DetectionEngine` or `PlanetCaptureProcessor` from war/, update to tactical/ import

**Step 4: Update tactical/ test files:**
- `DetectionServiceTest.kt`: remove any `import com.openlogh.engine.war.*` -- classes are now in same package
- `PlanetConquestServiceTest.kt`: remove any `import com.openlogh.engine.war.*`
- `TacticalBattleEngineTest.kt`: update any references to old field names (`commandRangeMax` -> `commandRange.maxRange`, `ticksSinceLastOrder` -> CommandRange tick, `ticksSinceStanceChange` -> `stanceChangeTicksRemaining`, `shipSubtype` String -> ShipSubtype enum)

**Step 5: Verify no remaining war/ imports in production code:**
Run `grep -r "import com.openlogh.engine.war" backend/game-app/src/main/` -- should return 0 results
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test -x bootJar 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `engine/war/TacticalCombatEngine.kt` does not exist
    - `engine/war/DetectionEngine.kt` does not exist
    - `engine/war/PlanetCaptureProcessor.kt` does not exist
    - `engine/war/BattleTrigger.kt` does not exist
    - `controller/BattleRestController.kt` does not exist
    - `controller/TacticalBattleController.kt` does not exist
    - `controller/TacticalBattleRestController.kt` does not exist
    - `grep -r "import com.openlogh.engine.war" backend/game-app/src/main/` returns 0 results
    - `./gradlew :game-app:test -x bootJar` passes with exit code 0
  </acceptance_criteria>
  <done>engine/war/ package deleted. Duplicate controllers deleted. All test files updated or removed. Full test suite passes. Single tactical engine architecture achieved (ENGINE-01 complete).</done>
</task>

</tasks>

<verification>
- `ls backend/game-app/src/main/kotlin/com/openlogh/engine/war/` returns error (directory deleted)
- `ls backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRest*` returns error (file deleted)
- `grep -r "import com.openlogh.engine.war" backend/game-app/src/main/` returns 0 results
- `cd backend && ./gradlew :game-app:test -x bootJar` passes
- `grep "var weaponCooldowns" backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt` finds 1 match
</verification>

<success_criteria>
TacticalCombatEngine and its war/ package no longer exist. All its unique fields are merged into TacticalBattleEngine.TacticalUnit. DetectionEngine and PlanetCaptureProcessor live in tactical/. Duplicate controllers deleted. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario-character-system/08-02-SUMMARY.md`
</output>
