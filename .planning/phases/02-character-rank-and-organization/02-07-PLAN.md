---
phase: 02-character-rank-and-organization
plan: 07
type: execute
wave: 4
depends_on: [02-05, 02-06]
files_modified: []
autonomous: false
requirements:
    - CHAR-01
    - CHAR-02
    - CHAR-03
    - ORG-02
    - ORG-03

must_haves:
    truths:
        - 'Character selection flow works end to end: select original or generate custom'
        - 'Officer profile displays all 4 sections correctly'
        - 'Org chart renders with live holder data and faction colors'
    artifacts: []
    key_links: []
---

<objective>
Visual and functional verification of all Phase 2 frontend work.

Purpose: Character selection, officer profile, and org chart are highly visual features that require human verification. This checkpoint ensures the UI matches the design contract before the phase is complete.

Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-character-rank-and-organization/02-UI-SPEC.md
@.planning/phases/02-character-rank-and-organization/02-05-SUMMARY.md
@.planning/phases/02-character-rank-and-organization/02-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of character selection, officer profile, and org chart</name>
  <action>
    Start the frontend dev server (`cd frontend && pnpm dev`) and backend game-app if needed for live data. Then present the three screens for human verification per the verification steps below.
  </action>
  <what-built>
    Phase 2 complete frontend: character selection/creation with 8-stat allocator, officer profile with 4-section layout (D-10), and org chart with live holder data and faction-differentiated styling (D-07/D-08).
  </what-built>
  <how-to-verify>
    **Prerequisites:** Start the frontend dev server (`cd frontend && pnpm dev`). If backend is needed for live data, start game-app as well.

    **1. Character Selection (select-pool page):**
    1. Navigate to the character selection page (join a session first)
    2. Verify two tabs: `오리지널 캐릭터` and `제네레이트`
    3. Original tab: verify character cards show portrait, 8 stat bars, age, and `제독 선택하기` button
    4. Generate tab: verify 8-stat allocator with +/- buttons, budget badge (`남은 포인트: {N}`), name input, origin selector
    5. Test: adjust stats until budget = 0, verify badge shows `배분 완료` in green
    6. Test: go over budget, verify badge shows `초과: {N}` in red
    7. Test `균형형` and `랜덤` preset buttons
    8. For Empire faction: verify origin selector shows radio group (귀족/제국기사/평민)
    9. For Alliance faction: verify origin shows fixed `시민` label

    **2. Officer Profile:**
    1. Navigate to an officer's profile page
    2. Verify Section 1: portrait (80x80), name, rank badge, faction badge, origin badge, career badge, age
    3. Verify Section 2: 8 horizontal stat bars with colors matching design spec, numeric values, exp indicators
    4. Verify Section 3: position card chips in game font (DungGeunMo), hover tooltip shows granted commands
    5. Verify Section 4: location, fleet, injury status, PCP/MCP values

    **3. Organization Chart:**
    1. Navigate to /org-chart page
    2. Verify page title `조직도` and description
    3. Verify two tabs: `은하제국` and `자유행성동맹`
    4. Empire tab: verify tree uses gold (#c9a84c) for branch lines and node titles
    5. Alliance tab: verify tree uses blue (#1e4a8a) for branch lines and node titles
    6. Verify tree expands to depth 3 by default
    7. Click a node with children: verify it toggles expand/collapse
    8. Verify filled positions show officer name; vacant positions show `[□ 공석]`
    9. Verify legend text at bottom

  </how-to-verify>
  <verify>Human visual inspection of all three screens</verify>
  <acceptance_criteria>
    - Character selection page renders with two tabs and correct Korean labels
    - 8-stat allocator enforces budget with visual feedback
    - Officer profile shows 4 sections per D-10
    - Org chart shows live holders with faction-differentiated colors
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues for gap closure</resume-signal>
  <done>User approved all three screens match UI-SPEC design contract</done>
</task>

</tasks>

<verification>
Human visual verification of all three screens against UI-SPEC design contract.
</verification>

<success_criteria>

- All three screens (character selection, officer profile, org chart) render correctly
- Korean copy matches UI-SPEC copywriting contract
- Faction colors differentiate Empire and Alliance org charts
- 8-stat allocator enforces budget constraints with visual feedback
- User approves the visual output
  </success_criteria>

<output>
After completion, create `.planning/phases/02-character-rank-and-organization/02-07-SUMMARY.md`
</output>
