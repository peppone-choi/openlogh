---
phase: 02-character-rank-and-organization
plan: 06
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
    - frontend/src/app/(game)/officer/page.tsx
    - frontend/src/app/(game)/org-chart/page.tsx
    - frontend/src/components/game/officer-profile-card.tsx
    - frontend/src/components/game/position-card-grid.tsx
    - frontend/src/components/game/position-card-chip.tsx
    - frontend/src/components/game/org-tree-node-live.tsx
autonomous: true
requirements:
    - CHAR-03
    - CHAR-06
    - CHAR-09
    - CHAR-10
    - CHAR-11
    - CHAR-12
    - CHAR-13
    - CHAR-14
    - RANK-01
    - RANK-03
    - RANK-04
    - RANK-13
    - RANK-14
    - ORG-01
    - ORG-02
    - ORG-03
    - ORG-06
    - ORG-08

must_haves:
    truths:
        - 'Officer profile displays 4 sections per D-10: basic info, 8 stats + exp, position cards, location/status'
        - '8 stats render as horizontal bars with stat-specific colors and numeric values'
        - 'Position cards display as grid of chips (max 16) with game font, tooltip on hover shows granted commands'
        - 'Org chart fetches live holder data from /api/org-chart/{sessionId} and renders expandable tree'
        - 'Empire org chart uses --empire-gold for branch lines; Alliance uses --alliance-blue (D-08)'
        - 'Vacant positions show [vacant marker] in muted-foreground color per D-07'
    artifacts:
        - path: 'frontend/src/components/game/officer-profile-card.tsx'
          provides: 'D-10 4-section officer profile layout'
          exports: ['OfficerProfileCard']
        - path: 'frontend/src/components/game/position-card-grid.tsx'
          provides: 'Grid of position card chips'
          exports: ['PositionCardGrid']
        - path: 'frontend/src/components/game/position-card-chip.tsx'
          provides: 'Single position card badge with tooltip'
          exports: ['PositionCardChip']
        - path: 'frontend/src/components/game/org-tree-node-live.tsx'
          provides: 'Enhanced org tree node with live holder data'
          exports: ['OrgTreeNodeLive']
        - path: 'frontend/src/app/(game)/org-chart/page.tsx'
          provides: 'Org chart page with live data binding and faction tabs'
        - path: 'frontend/src/app/(game)/officer/page.tsx'
          provides: 'Officer profile page with 4-section layout'
    key_links:
        - from: 'org-chart/page.tsx'
          to: '/api/org-chart/{sessionId}'
          via: 'fetch for all position holders'
          pattern: 'api/org-chart'
        - from: 'officer/page.tsx'
          to: '/api/position-cards/{sessionId}/{officerId}'
          via: "fetch for officer's position cards"
          pattern: 'api/position-cards'
        - from: 'officer-profile-card.tsx'
          to: 'position-card-grid.tsx'
          via: 'Section 3 renders PositionCardGrid'
          pattern: 'PositionCardGrid'
---

<objective>
Build the officer profile page (4-section layout per D-10) and rewrite the org chart page with live holder data from the backend API.

Purpose: Players need to view officer details (stats, position cards, location) and navigate the faction organization hierarchy to see who holds which positions. These are core UI surfaces for the organizational simulation.

Output: OfficerProfileCard component, PositionCardGrid/Chip components, live org chart page with faction-differentiated styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-rank-and-organization/02-CONTEXT.md
@.planning/phases/02-character-rank-and-organization/02-UI-SPEC.md
@.planning/phases/02-character-rank-and-organization/02-03-SUMMARY.md
@.planning/phases/02-character-rank-and-organization/02-04-SUMMARY.md

<interfaces>
From Plan 04 — OrgChart API response:
```typescript
interface OrgChartHolder {
    positionType: string;
    positionNameKo: string;
    category: string;
    officerId: number | null;
    officerName: string | null;
    officerPicture: string | null;
    officerRank: number | null;
    officerFactionId: number | null;
}

interface OrgChartResponse {
sessionId: number;
holders: OrgChartHolder[];
allPositionTypes: PositionTypeInfo[];
}

interface PositionTypeInfo {
code: string;
displayName: string;
category: string;
minRank: number;
grantedCommands: string[];
}

interface OfficerPositionCard {
id: number;
positionType: string;
positionNameKo: string;
category: string;
grantedCommands: string[];
}

```

From Plan 03 — Rank ladder API:
```

GET /api/rank/ladder/{sessionId}?rank=N -> RankLadderEntry[]
GET /api/rank/limits -> Map<number, number>

````

From UI-SPEC — D-10 Officer Profile 4 sections:
1. Basic info: portrait (80x80), name (display size, semibold), rank badge, faction badge, origin badge, career badge, age
2. 8 stats: horizontal bars with LoghBar, stat label (colored), numeric value, exp indicator (+{N} in yellow-500)
3. Position cards: grid of PositionCardChip (max 16), game font, tooltip with commands. Empty: `보유한 직무카드가 없습니다`
4. Location/status: key-value pairs — Location (planet), Fleet (name or -), Injury (color-coded), PCP/MCP

From UI-SPEC — D-07/D-08 Org Chart:
- Expandable/collapsible tree, default expand to depth 3
- Node: Korean title (faction color), English title (muted), description, required rank badge
- Live data: each node shows current holder name; vacant = `[□ 공석]` in muted-foreground
- Empire: --empire-gold (#c9a84c) for branch lines, node titles, chevrons
- Alliance: --alliance-blue (#1e4a8a) for branch lines, node titles, chevrons
- Tab toggle: `은하제국` / `자유행성동맹`, preserves scroll position

From UI-SPEC — Faction colors:
```css
--empire-gold: #c9a84c;
--empire-gold-bright: #ffd700;
--alliance-blue: #1e4a8a;
--alliance-blue-bright: #4a9eff;
````

From existing org-chart/page.tsx:

- Already has EMPIRE_ORG and ALLIANCE_ORG static tree structures (OrgNode interface)
- Already has OrgTreeNode component rendering recursive tree
- Needs: live holder data binding, faction color differentiation
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: OfficerProfileCard + PositionCardGrid/Chip components</name>
  <files>
    frontend/src/components/game/officer-profile-card.tsx,
    frontend/src/components/game/position-card-grid.tsx,
    frontend/src/components/game/position-card-chip.tsx,
    frontend/src/app/(game)/officer/page.tsx
  </files>
  <read_first>
    frontend/src/app/(game)/officer/page.tsx,
    frontend/src/components/game/stat-bar.tsx,
    frontend/src/components/game/logh-bar.tsx,
    frontend/src/components/game/officer-portrait.tsx,
    frontend/src/components/game/faction-badge.tsx,
    frontend/src/components/game/rank-ladder.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/tooltip.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/types/index.ts,
    frontend/src/stores/officerStore.ts,
    .planning/phases/02-character-rank-and-organization/02-UI-SPEC.md
  </read_first>
  <action>
    1. **Create `position-card-chip.tsx`:**
    - Props: `card: OfficerPositionCard`, `variant: 'default' | 'vacant'`
    - Render: Badge component with game font (font-game / DungGeunMo)
      - Default variant: card.positionNameKo text, subtle background
      - Vacant variant: `[□ 공석]` text in muted-foreground color
    - Wrap in Tooltip: on hover show card description (positionType) and granted commands list
    - Sharp corners (border-radius 0px)
    - Spacing: xs (4px) internal padding

    2. **Create `position-card-grid.tsx`:**
    - Props: `cards: OfficerPositionCard[]`, `maxCards: number` (default 16)
    - Render: flex-wrap grid of PositionCardChip components
    - If cards is empty: show inline text `보유한 직무카드가 없습니다` in muted-foreground
    - Gap: sm (8px) between chips

    3. **Create `officer-profile-card.tsx`** — D-10 4-section layout:
    - Props: `officer: Officer`, `positionCards: OfficerPositionCard[]`, `faction: { type: string; name: string }`

    **Section 1 — Basic Info (no heading):**
    - OfficerPortrait 80x80
    - Name: text-lg font-semibold (display role)
    - Row of Badges: rank (formatOfficerLevelText or manual mapping), faction, origin (Korean label), career (Korean label), age
    - Use existing FactionBadge component for faction
    - Origin label mapping: noble=`귀족`, knight=`제국기사`, commoner=`평민`, citizen=`시민`
    - Career label mapping: military=`군인`, politician=`정치가`

    **Section 2 — heading `능력치`:**
    - For each of 8 stats (STAT_KEYS_8 order):
      - Row: stat label (STAT_LABELS_KO, colored with STAT_COLORS), numeric value (tabular-nums), LoghBar or StatBar showing value/100, exp indicator `+{exp} exp` in yellow-500 if exp > 0
    - Gap: sm (8px) between rows

    **Section 3 — heading `직무권한카드`:**
    - PositionCardGrid with the officer's cards

    **Section 4 — heading `위치 / 상태`:**
    - Key-value pairs using a simple grid:
      - `위치`: planet name (from officer's planetId — may need lookup or show ID for now)
      - `소속 함대`: fleet name or `-`
      - `부상`: color-coded health text (injury === 0: `건강` green, > 0: `부상 ({injury})` red)
      - `PCP`: officer.pcp value
      - `MCP`: officer.mcp value

    Use Separator between sections. Use Card as the outer container.

    4. **Update `officer/page.tsx`:**
    - Fetch position cards from `GET /api/position-cards/{sessionId}/{officerId}`
    - Render OfficerProfileCard with officer data from officerStore + fetched position cards
    - Handle loading and error states

  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/openlogh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/game/officer-profile-card.tsx exists and contains `OfficerProfileCard`, `능력치`, `직무권한카드`, `위치 / 상태`
    - frontend/src/components/game/position-card-grid.tsx exists and contains `PositionCardGrid`, `보유한 직무카드가 없습니다`
    - frontend/src/components/game/position-card-chip.tsx exists and contains `PositionCardChip`, game font class (font-game)
    - officer/page.tsx fetches from `/api/position-cards/` and renders OfficerProfileCard
    - All section headings match UI-SPEC: `능력치`, `직무권한카드`, `위치 / 상태`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Officer profile page shows 4-section layout with 8-stat bars, position card grid, and location/status per D-10</done>
</task>

<task type="auto">
  <name>Task 2: Org chart page rewrite with live data and faction differentiation</name>
  <files>
    frontend/src/components/game/org-tree-node-live.tsx,
    frontend/src/app/(game)/org-chart/page.tsx,
    frontend/src/app/globals.css
  </files>
  <read_first>
    frontend/src/app/(game)/org-chart/page.tsx,
    frontend/src/app/globals.css,
    frontend/src/components/ui/collapsible.tsx,
    frontend/src/components/game/page-header.tsx,
    frontend/src/stores/gameStore.ts,
    .planning/phases/02-character-rank-and-organization/02-UI-SPEC.md
  </read_first>
  <action>
    1. **Add faction CSS custom properties** to `globals.css` (in the :root or dark theme section):
    ```css
    --empire-gold: #c9a84c;
    --empire-gold-bright: #ffd700;
    --alliance-blue: #1e4a8a;
    --alliance-blue-bright: #4a9eff;
    ```

    2. **Create `org-tree-node-live.tsx`:**
    - Props:
      ```typescript
      interface OrgNodeWithHolder extends OrgNode {
          positionType?: string;
          holder?: { officerName: string; officerPicture: string; officerRank: number } | null;
          children?: OrgNodeWithHolder[];
      }
      interface OrgTreeNodeLiveProps {
          node: OrgNodeWithHolder;
          depth: number;
          factionColor: 'empire' | 'alliance';
          defaultExpanded?: boolean;
      }
      ```
    - Render using Collapsible (shadcn):
      - Toggle trigger: ChevronDown/ChevronRight icon (colored with faction color), Korean title (faction color), English title (muted)
      - Leaf nodes: small dot marker instead of chevron
      - Indentation: 16px per depth level (`paddingLeft: depth * 16`)
      - Holder display:
        - If holder exists: show `{officerName}` text after position title
        - If no holder: show `[□ 공석]` in muted-foreground color (--muted-foreground)
      - Description text (text-[10px] muted)
      - Required rank Badge if present
    - Faction coloring:
      - Empire: branch lines and titles use `var(--empire-gold)` / `#c9a84c`
      - Alliance: branch lines and titles use `var(--alliance-blue)` / `#1e4a8a`
    - Children rendered recursively

    3. **Rewrite `org-chart/page.tsx`:**
    - Page title: `조직도` via PageHeader
    - Description: `은하제국 / 자유행성동맹 조직 계층 구조`
    - Tabs: `은하제국` and `자유행성동맹`
    - On mount: fetch `GET /api/org-chart/{sessionId}` for holder data
    - Merge holder data with existing static EMPIRE_ORG and ALLIANCE_ORG trees:
      - For each node in the tree, look up if any holder in the API response matches the node's positionType
      - Attach holder info (name, picture, rank) to the node
    - Render OrgTreeNodeLive recursively starting from root
    - Default expand to depth 3
    - Tab toggle preserves scroll position (use a ref to store scrollTop before tab change, restore after)
    - Legend card at bottom: `조직 단위를 클릭하면 하위 조직을 펼치거나 접을 수 있습니다.`

    **Important:** Keep the existing static EMPIRE_ORG and ALLIANCE_ORG tree definitions (they define the structure). Add `positionType` field to each OrgNode to enable matching with API data. Map each node's `rankRequired` to the corresponding PositionCardType code. Read the existing OrgNode interface and tree data to understand the mapping.

  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/openlogh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/game/org-tree-node-live.tsx exists and contains `OrgTreeNodeLive`, `공석`, `factionColor`
    - frontend/src/app/(game)/org-chart/page.tsx contains `은하제국`, `자유행성동맹` tab labels and `조직도` page title
    - org-chart/page.tsx fetches from `/api/org-chart/`
    - org-chart/page.tsx contains `조직 단위를 클릭하면 하위 조직을 펼치거나 접을 수 있습니다` legend text
    - globals.css contains `--empire-gold` and `--alliance-blue` custom properties
    - OrgTreeNodeLive uses different colors based on factionColor prop (empire-gold vs alliance-blue)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Org chart page fetches live holder data, renders expandable tree with faction-differentiated colors, shows current holders or vacant markers per D-07/D-08</done>
</task>

</tasks>

<verification>
1. `cd /Users/apple/Desktop/openlogh/frontend && npx tsc --noEmit` — no type errors
2. Officer profile has all 4 sections with correct Korean headings
3. Org chart has two faction tabs with different color schemes
4. Vacant positions show `[□ 공석]` marker
</verification>

<success_criteria>

- Officer profile shows 4-section layout matching D-10
- Position cards render as game-font chips with tooltip
- Org chart displays live holders from API with faction colors
- Vacant positions clearly marked
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/02-character-rank-and-organization/02-06-SUMMARY.md`
</output>
