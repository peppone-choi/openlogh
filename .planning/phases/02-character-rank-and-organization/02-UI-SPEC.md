---
phase: 2
slug: character-rank-and-organization
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-03-28
---

# Phase 2 -- UI Design Contract

> Visual and interaction contract for Character, Rank, and Organization frontend work. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property          | Value                                                                                             |
| ----------------- | ------------------------------------------------------------------------------------------------- |
| Tool              | shadcn (new-york style)                                                                           |
| Preset            | new-york, baseColor: neutral, cssVariables: true                                                  |
| Component library | Radix UI 1.4.3 (headless) + shadcn components                                                     |
| Icon library      | Lucide React 0.564.0                                                                              |
| Font (body)       | Pretendard, Inter, Apple SD Gothic Neo, Noto Sans KR, Malgun Gothic, sans-serif (`--font-legacy`) |
| Font (game)       | DungGeunMo, Pretendard, Apple SD Gothic Neo, Noto Sans KR, monospace (`--font-game`)              |
| Border radius     | 0px globally (8-bit pixel aesthetic, all corners sharp)                                           |

**Source:** `frontend/components.json`, `frontend/src/app/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage                                                                |
| ----- | ----- | -------------------------------------------------------------------- |
| xs    | 4px   | Icon gaps, inline badge padding, stat cell internal margins          |
| sm    | 8px   | Compact element spacing, grid gap-px patterns, stat bar gaps         |
| md    | 16px  | Default element spacing, card padding (p-4), page horizontal padding |
| lg    | 24px  | Section padding (space-y-6), card content vertical spacing           |
| xl    | 32px  | Layout gaps between major card groups                                |
| 2xl   | 48px  | Empty state vertical padding (py-12)                                 |
| 3xl   | 64px  | Page-level top/bottom margins (portrait images)                      |

Exceptions:

- Portrait images: 64x64px (existing OfficerBasicCard), 48x48px (sm), 80x80px (lg profile view)
- OrgTreeNode indent: 16px per depth level (depth \* 16)
- StatCell: 6px internal padding (p-1.5 = 6px, existing pattern)

**Source:** Existing component patterns in `officer-basic-card.tsx`, `org-chart/page.tsx`, `empty-state.tsx`

---

## Typography

| Role        | Size               | Weight         | Line Height | Font                                |
| ----------- | ------------------ | -------------- | ----------- | ----------------------------------- |
| Body        | 14px (text-sm)     | 400 (normal)   | 1.5         | --font-legacy (Pretendard)          |
| Label       | 10px (text-[10px]) | 400 (normal)   | 1.4         | --font-legacy                       |
| Heading     | 14px (text-sm)     | 600 (semibold) | 1.4         | --font-legacy                       |
| Display     | 18px (text-lg)     | 700 (bold)     | 1.2         | --font-legacy                       |
| Numeric     | 14px (text-sm)     | 500 (medium)   | 1.0         | tabular-nums (font-variant-numeric) |
| Game accent | 14px               | 400            | 1.5         | --font-game (DungGeunMo)            |

**Rationale:** This phase is data-dense (8 stats, 16 position cards, org tree). The existing codebase uses 10px labels, 14px body, and 18px display consistently. Two weights (400 normal + 600 semibold) cover all existing components. Game accent font (DungGeunMo) reserved for position card names and rank titles to reinforce the 8-bit LOGH aesthetic.

**Source:** Existing patterns in `officer-basic-card.tsx` (text-[10px], text-sm, font-bold), `org-chart/page.tsx` (text-sm font-semibold)

---

## Color

| Role            | Value     | CSS Variable         | Usage                                                            |
| --------------- | --------- | -------------------- | ---------------------------------------------------------------- |
| Dominant (60%)  | `#0a0a1a` | `--background`       | Page background, surfaces                                        |
| Secondary (30%) | `#1a1a2e` | `--card`             | Cards, panels, popover surfaces                                  |
| Accent (10%)    | `#c9a84c` | `--primary`          | See reserved-for list below                                      |
| Destructive     | `#c41e3a` | `--destructive`      | Destructive actions, injury indicators, enemy faction highlights |
| Muted text      | `#7a7a9e` | `--muted-foreground` | Labels, secondary text, descriptions                             |
| Border          | `#2a2a4e` | `--border`           | Card borders, grid separators, tree branch lines                 |

### Accent reserved for:

1. Primary CTA buttons (character selection, stat confirmation)
2. Active tab indicator on Empire/Alliance org chart toggle
3. Rank crown icon for promotion candidate (#1 in ladder)
4. Officer name highlight when viewing own profile
5. Ring/focus indicator on interactive elements

### Faction differentiation colors (D-08):

| Faction  | Primary                     | Bright                             | Usage                                                 |
| -------- | --------------------------- | ---------------------------------- | ----------------------------------------------------- |
| Empire   | `#c9a84c` (--empire-gold)   | `#ffd700` (--empire-gold-bright)   | Org chart branch lines, node titles, tab active state |
| Alliance | `#1e4a8a` (--alliance-blue) | `#4a9eff` (--alliance-blue-bright) | Org chart branch lines, node titles, tab active state |
| Fezzan   | `#c4a01e` (--fezzan-gold)   | `#d4a574` (--fezzan-warm)          | Not used in Phase 2 org charts                        |

### Stat colors (existing, locked):

| Stat                  | Color        | Hex       |
| --------------------- | ------------ | --------- |
| leadership (통솔)     | red          | `#ef4444` |
| command (지휘)        | orange       | `#f97316` |
| intelligence (정보)   | dodgerblue   | `#1e90ff` |
| politics (정치)       | limegreen    | `#32cd32` |
| administration (운영) | mediumpurple | `#9370db` |
| mobility (기동)       | cyan         | `#06b6d4` |
| attack (공격)         | salmon       | `#ff6b6b` |
| defense (방어)        | lightblue    | `#74c0fc` |

**Source:** `globals.css` (CSS custom properties), `officer/page.tsx` (stat color assignments)

---

## Copywriting Contract

All copy is in Korean (per project convention and user preference).

### Character Selection / Creation

| Element                                       | Copy                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------ |
| Primary CTA (original character)              | `선택하기` (Select)                                                                  |
| Primary CTA (generate character)              | `제독 생성` (Create Officer)                                                         |
| Empty state heading (no characters available) | `선택 가능한 캐릭터가 없습니다`                                                      |
| Empty state body                              | `모든 오리지널 캐릭터가 선택되었습니다. 제네레이트 탭에서 새 캐릭터를 생성해주세요.` |
| Error state                                   | `캐릭터 목록을 불러오지 못했습니다. 네트워크 연결을 확인하고 다시 시도해주세요.`     |
| Tab: Original pick                            | `오리지널 캐릭터`                                                                    |
| Tab: Generate                                 | `제네레이트`                                                                         |
| Stat budget indicator (remaining > 0)         | `남은 포인트: {N}`                                                                   |
| Stat budget indicator (exact)                 | `배분 완료`                                                                          |
| Stat budget indicator (over)                  | `초과: {N}`                                                                          |
| Origin selector: Empire                       | `귀족 / 제국기사 / 평민` (radio group)                                               |
| Origin selector: Alliance                     | `시민` (fixed, non-selectable label)                                                 |

### Officer Profile (D-10 four sections)

| Element               | Copy                                      |
| --------------------- | ----------------------------------------- |
| Section 1 heading     | (none -- portrait + name serve as header) |
| Section 2 heading     | `능력치`                                  |
| Section 3 heading     | `직무권한카드`                            |
| Section 3 empty state | `보유한 직무카드가 없습니다`              |
| Section 4 heading     | `위치 / 상태`                             |
| Exp progress label    | `+{N} exp` (inline after bar)             |

### Organization Chart

| Element             | Copy                                                          |
| ------------------- | ------------------------------------------------------------- |
| Page title          | `조직도`                                                      |
| Page description    | `은하제국 / 자유행성동맹 조직 계층 구조`                      |
| Tab: Empire         | `은하제국`                                                    |
| Tab: Alliance       | `자유행성동맹`                                                |
| Vacant position     | `공석` (displayed as `[□ 공석]` per user specification)       |
| Holder display      | `{officerName}` (in node, after position title)               |
| Legend: expand hint | `조직 단위를 클릭하면 하위 조직을 펼치거나 접을 수 있습니다.` |

### Rank Ladder

| Element             | Copy                           |
| ------------------- | ------------------------------ |
| Top candidate badge | `승진 후보`                    |
| My position badge   | `나`                           |
| Rank empty state    | `해당 계급의 제독이 없습니다.` |
| My standing         | `내 서열: {N}위 / {total}명`   |

### Destructive Actions

| Action                       | Confirmation Copy                                               | Approach                                                                      |
| ---------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Character deletion (CHAR-10) | `{name} 제독을 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` | Dialog with explicit confirmation button labeled `삭제` (destructive variant) |

**Source:** CONTEXT.md D-07, D-09, D-10; existing patterns in `select-pool/page.tsx`, `empty-state.tsx`, `error-state.tsx`

---

## Component Inventory

### Existing (reuse as-is)

| Component            | Location                                 | Phase 2 Usage                                   |
| -------------------- | ---------------------------------------- | ----------------------------------------------- |
| `StatBar`            | `components/game/stat-bar.tsx`           | 8-stat display in profile and selection         |
| `LoghBar`            | `components/game/logh-bar.tsx`           | Exp progress bars in profile section 2          |
| `OfficerPortrait`    | `components/game/officer-portrait.tsx`   | Profile section 1, character selection cards    |
| `FactionBadge`       | `components/game/faction-badge.tsx`      | Profile section 1 faction indicator             |
| `RankLadder`         | `components/game/rank-ladder.tsx`        | Rank standing display                           |
| `EmptyState`         | `components/game/empty-state.tsx`        | No characters available, no position cards      |
| `ErrorState`         | `components/game/error-state.tsx`        | API failure states                              |
| `LoadingState`       | `components/game/loading-state.tsx`      | Data fetch in progress                          |
| `PageHeader`         | `components/game/page-header.tsx`        | All page titles                                 |
| `OfficerBasicCard`   | `components/game/officer-basic-card.tsx` | Compact officer display in lists                |
| shadcn `Card`        | `components/ui/card.tsx`                 | All section containers                          |
| shadcn `Badge`       | `components/ui/badge.tsx`                | Rank labels, origin labels, position card chips |
| shadcn `Button`      | `components/ui/button.tsx`               | CTAs, stat +/- controls                         |
| shadcn `Tabs`        | `components/ui/tabs.tsx`                 | Character selection mode toggle                 |
| shadcn `Input`       | `components/ui/input.tsx`                | Character name, stat numeric input              |
| shadcn `Select`      | `components/ui/select.tsx`               | Origin selector (Empire)                        |
| shadcn `Progress`    | `components/ui/progress.tsx`             | Used by StatBar                                 |
| shadcn `Collapsible` | `components/ui/collapsible.tsx`          | Org tree expand/collapse                        |
| shadcn `Table`       | `components/ui/table.tsx`                | Faction officers list                           |
| shadcn `Tooltip`     | `components/ui/tooltip.tsx`              | Position card detail on hover                   |
| shadcn `Separator`   | `components/ui/separator.tsx`            | Section dividers in profile                     |
| shadcn `Skeleton`    | `components/ui/skeleton.tsx`             | Loading placeholders                            |

### New (to be built in Phase 2)

| Component            | Purpose                                                              | Key Props                                                                     |
| -------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `StatAllocator`      | 8-stat point distribution for generate character                     | `budget: number`, `stats: Record<StatKey8, number>`, `onChange`, `min`, `max` |
| `PositionCardGrid`   | Grid of position card chips (max 16)                                 | `cards: PositionCard[]`, `maxCards: 16`                                       |
| `PositionCardChip`   | Single position card badge with tooltip                              | `card: PositionCard`, `variant: 'default' \| 'vacant'`                        |
| `OrgTreeNodeLive`    | Enhanced OrgTreeNode with live holder data                           | `node: OrgNodeWithHolder`, `depth`, `factionColor`                            |
| `OfficerProfileCard` | D-10 4-section profile layout                                        | `officer: Officer`, `positionCards: PositionCard[]`, `faction: Faction`       |
| `OriginSelector`     | Empire origin picker (noble/knight/commoner) or Alliance fixed label | `factionType: string`, `value`, `onChange`                                    |
| `CharacterPickGrid`  | Grid of selectable original characters with stats                    | `characters: Officer[]`, `onSelect`, `selecting`                              |

### Component relationships:

```
CharacterSelectionPage
  +-- Tabs (오리지널 캐릭터 | 제네레이트)
  |   +-- CharacterPickGrid
  |   |   +-- Card > OfficerPortrait + StatBar (x8) + Button
  |   +-- StatAllocator
  |       +-- StatBar (x8) + Button (+/-) + Input
  |       +-- OriginSelector
  +-- EmptyState / ErrorState / LoadingState

OfficerProfileCard (D-10 layout)
  +-- Section 1: OfficerPortrait + name + FactionBadge + rank + origin + career + age
  +-- Section 2: StatBar (x8) + LoghBar (exp x8)
  +-- Section 3: PositionCardGrid
  |   +-- PositionCardChip (x max 16)
  +-- Section 4: location + fleet + injury + CP display

OrgChartPage
  +-- Tabs (은하제국 | 자유행성동맹)
  +-- Card > OrgTreeNodeLive (recursive)
  |   +-- holder name or [□ 공석]
  +-- Legend Card
```

---

## Interaction Contracts

### Character Selection (original)

1. Page loads with list of available (unselected) original characters
2. Each character card shows: portrait, name, 8 stats as horizontal bars, age
3. Click `선택하기` on a card triggers selection API call
4. During API call: button shows `선택 중...`, all other select buttons disabled
5. On success: toast `제독을 선택했습니다.`, redirect to game home
6. On failure: toast error, re-enable buttons
7. If all characters taken: show EmptyState with redirect to generate tab

### Character Creation (generate)

1. Name input (max 20 chars, required)
2. Origin selector: Empire shows radio group (귀족/제국기사/평민), Alliance shows fixed `시민` label
3. 8-stat allocator with total point budget badge
4. Each stat has -/+ buttons (step 5) and numeric input
5. Budget badge: green when remaining=0, secondary when remaining>0, destructive when over
6. Preset buttons: `균형형` (even distribution) and `랜덤` (random within limits)
7. Submit disabled until: name filled AND remaining=0
8. On submit: `생성 중...` loading state, then toast + redirect

### Organization Chart

1. Default: expand tree to depth 3
2. Click node with children: toggle expand/collapse with ChevronDown/ChevronRight icon
3. Leaf nodes: show small dot marker instead of chevron
4. Each node shows: Korean title (faction color), English title (muted), description, required rank badge
5. **Live data binding**: each node queries position_card records to show current holder name. Vacant positions display `[□ 공석]` in muted-foreground color
6. **Faction differentiation (D-08)**: Empire tree uses `--empire-gold` for branch lines, node titles, and chevrons. Alliance tree uses `--alliance-blue`. This is the sole visual differentiation -- structure and layout are identical between factions
7. Tab toggle between Empire and Alliance preserves scroll position

### Officer Profile (D-10)

1. Section 1 (basic info): portrait (80x80), name (display size, bold), rank badge, faction badge, origin badge, career badge, age with color coding
2. Section 2 (stats): 8 horizontal bars using existing LoghBar component. Each row: stat label (colored), numeric value, bar, exp indicator (`+{N}` in yellow-500 if exp > 0)
3. Section 3 (position cards): grid of PositionCardChip components. Each chip shows card name in game font. Tooltip on hover shows card description and granted commands. Empty slots not shown (just the grid of held cards). If no cards: show inline `보유한 직무카드가 없습니다`
4. Section 4 (location/status): key-value pairs. Location (planet name), Fleet (fleet name or `-`), Injury (color-coded health text), PCP/MCP remaining values

---

## Registry Safety

| Registry        | Blocks Used                                                                                                      | Safety Gate  |
| --------------- | ---------------------------------------------------------------------------------------------------------------- | ------------ |
| shadcn official | Card, Badge, Button, Tabs, Input, Select, Progress, Collapsible, Table, Tooltip, Separator, Skeleton, ScrollArea | not required |

No third-party registries declared. The `registries` field in `components.json` is empty (`{}`).

**Source:** `frontend/components.json` -- `"registries": {}`

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
