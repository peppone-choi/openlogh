---
phase: 02-gin7-command-system
plan: "07"
type: execute
wave: 4
depends_on:
  - "02-06"
files_modified:
  - backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandRegistryTest.kt
  - backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandPipelineTest.kt
  - backend/game-app/src/test/kotlin/com/openlogh/service/CommandProposalServiceTest.kt
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
  - CMD-06
  - CMD-07
  - CMD-08
  - CMD-09

must_haves:
  truths:
    - "Gin7CommandRegistryTest: 81종 커맨드 코드 전체가 등록되어 있고 stub 구현이 0종임을 확인한다"
    - "Gin7CommandPipelineTest: PositionCard 없는 장교가 작전커맨드를 실행하면 PERMISSION DENIED 반환된다"
    - "Gin7CommandPipelineTest: 적절한 PositionCard가 있는 장교가 '워프항행'을 실행하면 planetId가 변경된다"
    - "Gin7CommandPipelineTest: MCP 커맨드 실행 후 officer.mcp(또는 faction.mcp)가 cpCost만큼 감소한다"
    - "CommandProposalServiceTest: 하급자가 상급자에게 제안하면 PENDING 상태로 저장된다"
    - "CommandProposalServiceTest: 상급자가 승인하면 APPROVED로 상태 전환되고 커맨드가 실행된다"
    - "CommandProposalServiceTest: 동급자가 제안 시 IllegalArgumentException이 발생한다"
    - "grep -r 'officerLevel >= 5' 결과가 0건이다"
  artifacts:
    - path: "backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandRegistryTest.kt"
      provides: "81종 커맨드 등록 검증 테스트"
      contains: "class Gin7CommandRegistryTest"
    - path: "backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandPipelineTest.kt"
      provides: "PositionCard 게이팅 + MCP 차감 + 커맨드 효과 통합 테스트"
      contains: "class Gin7CommandPipelineTest"
    - path: "backend/game-app/src/test/kotlin/com/openlogh/service/CommandProposalServiceTest.kt"
      provides: "제안 시스템 단위 테스트"
      contains: "class CommandProposalServiceTest"
  key_links:
    - from: "Gin7CommandPipelineTest"
      to: "CommandExecutor.executeOfficerCommand()"
      via: "commandExecutor.executeOfficerCommand(\"워프항행\", officer, env, arg)"
      pattern: "executeOfficerCommand.*워프항행"
    - from: "CommandProposalServiceTest"
      to: "CommandProposalService.approveProposal()"
      via: "proposalService.approveProposal(sessionId, proposalId, approverId)"
      pattern: "approveProposal"
---

<objective>
Phase 2 통합 검증 — 81종 커맨드 등록 + 파이프라인 + 제안 시스템 테스트.

Purpose: Plan 02~06에서 구현된 81종 커맨드와 제안 시스템이 실제로 동작함을 자동화 테스트로 검증한다. officerLevel >= 5 우회 0건 확인, PositionCard 게이팅 동작, MCP/PCP 풀 분리, 제안 생성/승인/거부 흐름을 테스트한다.

Output: 3개 테스트 파일, 모든 테스트 통과
</objective>

<execution_context>
@/Users/apple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-gin7-command-system/02-CONTEXT.md

<interfaces>
<!-- 기존 테스트 패턴 확인용 -->
<!-- backend/game-app/src/test/kotlin/com/openlogh/qa/parity/CommandParityTest.kt 참고 -->

<!-- CommandExecutor 테스트 세팅 패턴 -->
```kotlin
// 최소 CommandEnv 구성
val env = CommandEnv(
    sessionId = 1L,
    year = 796,
    month = 1,
    startYear = 796,
    realtimeMode = true,
    gameStor = emptyMap(),
)

// 테스트용 Officer 생성
val officer = Officer(
    id = 1L,
    sessionId = 1L,
    officerLevel = 5,
    factionId = 1L,
    planetId = 10L,
    positionCards = "FLEET_COMMANDER",  // PositionCard 코드
)

// Gin7CommandRegistry 직접 인스턴스화 (Spring 불필요)
val registry = Gin7CommandRegistry()
val command = registry.createOfficerCommand("워프항행", officer, env, mapOf("destPlanetId" to 20L))
val result = runBlocking { command.run(Random) }
```

<!-- PositionCard 코드 — 작전커맨드 접근에 필요한 카드 -->
// PositionCardRegistry에서 CommandGroup.OPERATIONS에 속하는 PositionCard 확인 필요
// 일반적으로 FLEET_COMMANDER 카드가 OPERATIONS 그룹 접근 허용

<!-- JUnit 5 + Kotlin Coroutines Test -->
```kotlin
import kotlinx.coroutines.test.runTest
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Assertions.*
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gin7CommandRegistryTest + Gin7CommandPipelineTest 작성</name>
  <files>
    backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandRegistryTest.kt,
    backend/game-app/src/test/kotlin/com/openlogh/command/Gin7CommandPipelineTest.kt
  </files>
  <read_first>
    - backend/game-app/src/test/kotlin/com/openlogh/qa/parity/CommandParityTest.kt (테스트 패턴 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt (등록된 커맨드 목록)
    - backend/game-app/src/main/kotlin/com/openlogh/model/PositionCardRegistry.kt (canExecute 로직)
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandEnv.kt (생성자 파라미터)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Officer.kt (테스트용 필드)
  </read_first>
  <behavior>
    Gin7CommandRegistryTest:
    - Test 1: registry.getGeneralCommandNames().size == 82 (81종 + 대기)
    - Test 2: gin7 81종 코드 전체가 registry에 존재함 (hardcoded list assertion)
    - Test 3: createOfficerCommand("대기", officer, env) 반환값이 non-null

    Gin7CommandPipelineTest:
    - Test 4: PositionCard 없는 officer가 "워프항행" 실행 → CommandExecutor returns success=false, logs contains "직무권한카드"
    - Test 5: OPERATIONS 카드 가진 officer가 "워프항행" + arg(destPlanetId=20L) 실행 → success=true, officer.planetId==20L
    - Test 6: "완전수리" 실행 (MCP 커맨드) → command.getCommandPoolType() == StatCategory.MCP
    - Test 7: "승진" 실행 (PCP 커맨드) → command.getCommandPoolType() == StatCategory.PCP
  </behavior>
  <action>
**Gin7CommandRegistryTest.kt** 위치: `src/test/kotlin/com/openlogh/command/`
패키지: `com.openlogh.command`

테스트는 Spring context 없이 순수 단위 테스트로 작성한다.
Gin7CommandRegistry()를 직접 인스턴스화하여 등록 수, 커맨드 코드 존재 여부 검증.

gin7 81종 코드 목록을 테스트 내부에 상수로 정의 (commands.json의 한국어 코드 전체):
```
"워프항행", "연료보급", "성계내항행", "군기유지", "항공훈련", "육전훈련",
"공전훈련", "육전전술훈련", "공전전술훈련", "경계출동", "무력진압",
"분열행진", "징발", "특별경비", "육전대출격", "육전대철수",
"원거리이동", "근거리이동", "퇴역", "지원", "망명", "회견", "수강",
"병기연습", "반의", "모의", "설득", "반란", "참가", "자금투입", "기함구매",
"작전계획", "작전철회", "발령", "부대결성", "부대해산", "강의", "수송계획", "수송중지",
"완전수리", "완전보급", "재편성", "보충", "반출입", "할당",
"승진", "발탁", "강등", "서작", "서훈", "임명", "파면", "사임", "봉토수여", "봉토직할",
"야회", "수렵", "회담", "담화", "연설", "국가목표", "납입률변경", "관세율변경",
"분배", "처단", "외교", "통치목표",
"일제수색", "체포허가", "집행명령", "체포명령", "사열", "습격",
"감시", "잠입공작", "탈출공작", "정보공작", "파괴공작", "선동공작",
"침입공작", "귀환공작"
```

**Gin7CommandPipelineTest.kt** 위치: `src/test/kotlin/com/openlogh/command/`

PositionCard 체크는 CommandExecutor를 통해야 하지만, CommandExecutor는 Spring bean이므로
Mockito를 사용하거나, PositionCardRegistry.canExecute()를 직접 테스트하는 방식으로 작성.

Test 4 (PositionCard 없는 장교):
```kotlin
val officer = Officer(id=1L, sessionId=1L, officerLevel=3, factionId=1L, planetId=10L, positionCards="")
val cards = officer.getPositionCardEnums()  // empty set
val canExecute = PositionCardRegistry.canExecute(cards, "워프항행")
assertFalse(canExecute)
```

Test 5 (올바른 카드 보유):
```kotlin
// OPERATIONS CommandGroup에 접근 가능한 카드를 보유한 officer
// PositionCard enum에서 commandGroups.contains(CommandGroup.OPERATIONS)인 카드 찾기
val registry = Gin7CommandRegistry()
val operationsCard = PositionCard.entries.first { it.commandGroups.contains(CommandGroup.OPERATIONS) }
val officer = Officer(..., positionCards = operationsCard.name)
val cards = officer.getPositionCardEnums()
assertTrue(PositionCardRegistry.canExecute(cards, "워프항행"))
```

Test 6, 7: 커맨드 인스턴스 생성 후 getCommandPoolType() 검증 (Spring 불필요):
```kotlin
val registry = Gin7CommandRegistry()
val repairCmd = registry.createOfficerCommand("완전수리", mockOfficer, mockEnv, null)
assertEquals(StatCategory.MCP, repairCmd.getCommandPoolType())

val promoteCmd = registry.createOfficerCommand("승진", mockOfficer, mockEnv, null)
assertEquals(StatCategory.PCP, promoteCmd.getCommandPoolType())
```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.command.Gin7CommandRegistryTest" --tests "com.openlogh.command.Gin7CommandPipelineTest" 2>&1 | tail -20</automated>
  </verify>
  <done>두 테스트 파일 모두 통과 (7개 테스트 케이스 GREEN)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommandProposalServiceTest + officerLevel 우회 0건 검증</name>
  <files>
    backend/game-app/src/test/kotlin/com/openlogh/service/CommandProposalServiceTest.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt (전체)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/CommandProposal.kt
  </read_first>
  <behavior>
    CommandProposalServiceTest:
    - Test 1: createProposal(proposerLevel=3, approverLevel=5) → PENDING 상태 저장됨
    - Test 2: createProposal(proposerLevel=5, approverLevel=5) → IllegalArgumentException 발생
    - Test 3: createProposal(proposerLevel=6, approverLevel=5) → IllegalArgumentException 발생 (상급자가 하급자에게 제안 불가)
    - Test 4: approveProposal() → proposal.status = "APPROVED", commandExecutor 호출됨
    - Test 5: rejectProposal() → proposal.status = "REJECTED"
    - Test 6: 이미 APPROVED인 proposal에 rejectProposal() → IllegalArgumentException
  </behavior>
  <action>
**CommandProposalServiceTest.kt** 위치: `src/test/kotlin/com/openlogh/service/`
패키지: `com.openlogh.service`

Mockito + JUnit 5 사용. CommandProposalRepository, OfficerRepository, CommandExecutor를 mock.

```kotlin
@ExtendWith(MockitoExtension::class)
class CommandProposalServiceTest {
    @Mock lateinit var proposalRepository: CommandProposalRepository
    @Mock lateinit var officerRepository: OfficerRepository
    @Mock lateinit var commandExecutor: CommandExecutor
    @Mock lateinit var gameEventService: GameEventService
    @Mock lateinit var sessionStateRepository: SessionStateRepository

    @InjectMocks lateinit var proposalService: CommandProposalService

    private fun makeOfficer(id: Long, level: Short) = Officer(
        id = id, sessionId = 1L, officerLevel = level,
        factionId = 1L, planetId = 10L, positionCards = "",
    )
}
```

Test 1: mock officerRepository.findById(1L) → proposer(level=3), findById(2L) → approver(level=5).
mock proposalRepository.save(any()) → proposal with status="PENDING".
val result = proposalService.createProposal(1L, 1L, 2L, "워프항행", emptyMap())
assertEquals("PENDING", result.status)

Test 2, 3: 동급/역방향 제안 → assertThrows<IllegalArgumentException>

Test 4: mock proposal(status="PENDING"), mock commandExecutor.executeOfficerCommand() → CommandResult(success=true, logs=listOf("완료"))
runTest { proposalService.approveProposal(1L, proposalId, approverId) }
verify(commandExecutor).executeOfficerCommand(any(), any(), any(), any(), any(), any(), any())

Test 5, 6: rejectProposal 정상/오류 케이스.

---

**officerLevel >= 5 우회 0건 검증** (Task 2 마지막 단계):
```bash
grep -r "officerLevel >= 5" \
  /Users/apple/Desktop/개인프로젝트/openlogh/backend/game-app/src/main/kotlin/
```
결과가 0줄이어야 한다. 만약 발견되면 즉시 제거한다 (Phase 1에서 이미 제거됐어야 하나 재확인).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.service.CommandProposalServiceTest" 2>&1 | tail -20</automated>
  </verify>
  <done>CommandProposalServiceTest 6개 케이스 통과, grep officerLevel >= 5 결과 0건</done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend

# 전체 Phase 2 테스트 실행
./gradlew :game-app:test --tests "com.openlogh.command.Gin7*" \
  --tests "com.openlogh.service.CommandProposalServiceTest" 2>&1 | tail -30

# Phase 2 성공 기준 1: officerLevel >= 5 우회 0건
grep -r "officerLevel >= 5" game-app/src/main/kotlin/ && echo "FAIL: bypass found" || echo "PASS: no bypass"

# Phase 2 성공 기준 2: 81종 커맨드 그룹 디렉토리 존재
ls game-app/src/main/kotlin/com/openlogh/command/gin7/

# Phase 2 성공 기준 3: 전체 컴파일
./gradlew :game-app:compileKotlin 2>&1 | grep BUILD
```
</verification>

<success_criteria>
- Gin7CommandRegistryTest: 82개 등록(81+대기), 81종 코드 전체 존재 검증 PASS
- Gin7CommandPipelineTest: PositionCard 게이팅 + MCP/PCP 분리 테스트 PASS
- CommandProposalServiceTest: 6개 케이스 (생성/승인/거부/계급체크) PASS
- grep -r "officerLevel >= 5" 결과 0건
- command/gin7/ 하위 7개 패키지 (operations/personal/commander/logistics/personnel/politics/intelligence) 전부 존재
- ./gradlew :game-app:compileKotlin BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/02-gin7-command-system/02-07-SUMMARY.md`
</output>
