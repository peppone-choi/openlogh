---
phase: 02-gin7-command-system
plan: "06"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/entity/CommandProposal.kt
  - backend/game-app/src/main/resources/db/migration/V46__add_command_proposal.sql
  - backend/game-app/src/main/kotlin/com/openlogh/repository/CommandProposalRepository.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/CommandProposalController.kt
autonomous: true
requirements:
  - CMD-08
  - CMD-09

must_haves:
  truths:
    - "장교가 POST /api/{sessionId}/proposals로 제안을 발행하면 CommandProposal 행이 DB에 저장된다"
    - "상급자가 PUT /api/{sessionId}/proposals/{id}/approve를 호출하면 제안된 커맨드가 CommandExecutor를 통해 실행된다"
    - "상급자가 PUT /api/{sessionId}/proposals/{id}/reject를 호출하면 proposal.status = REJECTED로 변경된다"
    - "제안자의 officerLevel이 승인자의 officerLevel보다 낮아야 제안이 수락된다"
    - "승인 시 CommandExecutor.executeOfficerCommand()가 제안 커맨드를 실행한 결과가 WebSocket으로 브로드캐스트된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/entity/CommandProposal.kt"
      provides: "CommandProposal JPA 엔티티 — proposerId, approverId, commandCode, args, status, sessionId"
      contains: "class CommandProposal"
    - path: "backend/game-app/src/main/resources/db/migration/V46__add_command_proposal.sql"
      provides: "command_proposal 테이블 Flyway 마이그레이션"
      contains: "CREATE TABLE command_proposal"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt"
      provides: "제안 생성/승인/거부 로직"
      exports: ["createProposal", "approveProposal", "rejectProposal"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/CommandProposalController.kt"
      provides: "REST API — POST /proposals, PUT /proposals/{id}/approve, PUT /proposals/{id}/reject"
  key_links:
    - from: "CommandProposalService.approveProposal()"
      to: "CommandExecutor.executeOfficerCommand()"
      via: "commandExecutor.executeOfficerCommand(commandCode, proposer, env, args)"
      pattern: "commandExecutor\\.executeOfficerCommand"
    - from: "CommandProposalController"
      to: "CommandProposalService"
      via: "@PostMapping(\"/proposals\") → proposalService.createProposal()"
      pattern: "proposalService\\.createProposal"
---

<objective>
제안 시스템 (하급자 → 상급자 커맨드 승인 파이프라인) 구현.

Purpose: gin7 조직 시뮬레이션의 핵심 메커니즘 — 하급 장교가 상급자에게 커맨드 실행을 제안하고 상급자가 승인/거부한다. 승인 시 CommandExecutor가 제안된 커맨드를 실제 실행한다. DB 엔티티(command_proposal), REST API, 서비스 계층 구현.

Output: CommandProposal 엔티티 + V46 마이그레이션 + Repository + Service + Controller
</objective>

<execution_context>
@/Users/apple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-gin7-command-system/02-CONTEXT.md

<interfaces>
<!-- CommandExecutor 시그니처 (재사용) -->
```kotlin
suspend fun executeOfficerCommand(
    actionCode: String,
    general: Officer,
    env: CommandEnv,
    arg: Map<String, Any>? = null,
    city: Planet? = null,
    nation: Faction? = null,
    rng: Random = Random.Default
): CommandResult
```

<!-- CommandEnv 구조 -->
```kotlin
data class CommandEnv(
    val sessionId: Long,
    val year: Int,
    val month: Int,
    val startYear: Int,
    val realtimeMode: Boolean,
    val gameStor: Map<String, Any>,
)
```

<!-- 최근 Flyway 버전 확인 -->
-- V45까지 완료 (Phase 1), 다음은 V46

<!-- 기존 Officer 엔티티 -->
Officer: id, sessionId, officerLevel: Short, factionId, planetId

<!-- GameEventService 브로드캐스트 -->
```kotlin
// 기존 WebSocket 브로드캐스트 패턴
gameEventService.broadcastCommandResult(sessionId, officerId, result)
// 또는
gameEventService.broadcast(sessionId, eventType, payload)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommandProposal 엔티티 + V46 마이그레이션 + Repository</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/entity/CommandProposal.kt,
    backend/game-app/src/main/resources/db/migration/V46__add_command_proposal.sql,
    backend/game-app/src/main/kotlin/com/openlogh/repository/CommandProposalRepository.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Officer.kt (엔티티 패턴 참고)
    - backend/game-app/src/main/resources/db/migration/V45__add_ship_unit.sql (최신 마이그레이션 패턴 확인)
  </read_first>
  <action>
**CommandProposal.kt**:
```kotlin
package com.openlogh.entity

import jakarta.persistence.*
import org.hibernate.annotations.JdbcTypeCode
import org.hibernate.type.SqlTypes
import java.time.OffsetDateTime

@Entity
@Table(name = "command_proposal")
class CommandProposal(
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    var id: Long = 0,

    @Column(name = "session_id", nullable = false)
    var sessionId: Long = 0,

    @Column(name = "proposer_id", nullable = false)
    var proposerId: Long = 0,           // 제안한 장교 ID

    @Column(name = "approver_id")
    var approverId: Long? = null,       // 승인/거부한 상급자 ID (null = 미처리)

    @Column(name = "command_code", nullable = false, length = 64)
    var commandCode: String = "",       // 한국어 커맨드 코드 (예: "작전계획")

    @JdbcTypeCode(SqlTypes.JSON)
    @Column(columnDefinition = "jsonb")
    var args: MutableMap<String, Any> = mutableMapOf(),  // 커맨드 인자

    @Column(nullable = false, length = 16)
    var status: String = "PENDING",     // PENDING / APPROVED / REJECTED

    @Column(name = "created_at", nullable = false)
    var createdAt: OffsetDateTime = OffsetDateTime.now(),

    @Column(name = "resolved_at")
    var resolvedAt: OffsetDateTime? = null,

    @Column(name = "result_log", length = 2048)
    var resultLog: String? = null,      // 승인 실행 결과 로그 (JSON)
)
```

**V46__add_command_proposal.sql**:
```sql
CREATE TABLE command_proposal (
    id              BIGSERIAL PRIMARY KEY,
    session_id      BIGINT NOT NULL,
    proposer_id     BIGINT NOT NULL,
    approver_id     BIGINT,
    command_code    VARCHAR(64) NOT NULL,
    args            JSONB NOT NULL DEFAULT '{}',
    status          VARCHAR(16) NOT NULL DEFAULT 'PENDING',
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    resolved_at     TIMESTAMPTZ,
    result_log      VARCHAR(2048),

    CONSTRAINT fk_proposal_session FOREIGN KEY (session_id)
        REFERENCES session_state(id) ON DELETE CASCADE
);

CREATE INDEX idx_command_proposal_session_status
    ON command_proposal(session_id, status);

CREATE INDEX idx_command_proposal_approver
    ON command_proposal(approver_id, status)
    WHERE status = 'PENDING';
```

**CommandProposalRepository.kt**:
```kotlin
package com.openlogh.repository

import com.openlogh.entity.CommandProposal
import org.springframework.data.jpa.repository.JpaRepository

interface CommandProposalRepository : JpaRepository<CommandProposal, Long> {
    fun findBySessionIdAndStatus(sessionId: Long, status: String): List<CommandProposal>
    fun findBySessionIdAndApproverIdAndStatus(
        sessionId: Long, approverId: Long, status: String
    ): List<CommandProposal>
    fun findBySessionIdAndProposerId(sessionId: Long, proposerId: Long): List<CommandProposal>
}
```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>CommandProposal.kt, V46 SQL, CommandProposalRepository.kt 생성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: CommandProposalService + CommandProposalController (REST API)</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt,
    backend/game-app/src/main/kotlin/com/openlogh/controller/CommandProposalController.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt (executeOfficerCommand 시그니처)
    - backend/game-app/src/main/kotlin/com/openlogh/controller/CommandController.kt (컨트롤러 패턴 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/engine/event/GameEventService.kt (브로드캐스트 패턴)
    - backend/game-app/src/main/kotlin/com/openlogh/repository/OfficerRepository.kt (findById 패턴)
  </read_first>
  <action>
**CommandProposalService.kt**:
```kotlin
package com.openlogh.service

@Service
class CommandProposalService(
    private val proposalRepository: CommandProposalRepository,
    private val officerRepository: OfficerRepository,
    private val commandExecutor: CommandExecutor,
    private val gameEventService: GameEventService,
    private val sessionStateRepository: SessionStateRepository,
) {
    /**
     * 하급자가 상급자에게 커맨드 실행을 제안한다.
     * approverId는 proposer의 officerLevel보다 높아야 한다.
     */
    fun createProposal(
        sessionId: Long,
        proposerId: Long,
        approverId: Long,
        commandCode: String,
        args: Map<String, Any>,
    ): CommandProposal {
        val proposer = officerRepository.findById(proposerId).orElseThrow {
            IllegalArgumentException("제안자 장교 미존재: $proposerId")
        }
        val approver = officerRepository.findById(approverId).orElseThrow {
            IllegalArgumentException("승인자 장교 미존재: $approverId")
        }
        require(proposer.officerLevel < approver.officerLevel) {
            "제안자(${proposer.officerLevel}계급)는 승인자(${approver.officerLevel}계급)보다 하급이어야 합니다."
        }
        val proposal = CommandProposal(
            sessionId = sessionId,
            proposerId = proposerId,
            approverId = approverId,
            commandCode = commandCode,
            args = args.toMutableMap(),
            status = "PENDING",
        )
        return proposalRepository.save(proposal)
    }

    /**
     * 상급자가 제안을 승인하고 커맨드를 실행한다.
     */
    suspend fun approveProposal(sessionId: Long, proposalId: Long, approverId: Long): CommandProposal {
        val proposal = proposalRepository.findById(proposalId).orElseThrow {
            IllegalArgumentException("제안 미존재: $proposalId")
        }
        require(proposal.sessionId == sessionId) { "세션 불일치" }
        require(proposal.approverId == approverId) { "승인 권한 없음" }
        require(proposal.status == "PENDING") { "이미 처리된 제안입니다." }

        val proposer = officerRepository.findById(proposal.proposerId).orElseThrow {
            IllegalArgumentException("제안자 미존재")
        }
        val session = sessionStateRepository.findById(sessionId).orElseThrow {
            IllegalArgumentException("세션 미존재: $sessionId")
        }
        val env = CommandEnv(
            sessionId = sessionId,
            year = session.currentYear.toInt(),
            month = session.currentMonth.toInt(),
            startYear = session.startYear.toInt(),
            realtimeMode = true,
            gameStor = emptyMap(),
        )

        val result = commandExecutor.executeOfficerCommand(
            actionCode = proposal.commandCode,
            general = proposer,
            env = env,
            arg = proposal.args,
        )

        proposal.status = if (result.success) "APPROVED" else "REJECTED"
        proposal.resolvedAt = java.time.OffsetDateTime.now()
        proposal.resultLog = result.logs.joinToString("\n")

        val saved = proposalRepository.save(proposal)

        // WebSocket 브로드캐스트
        gameEventService.broadcastProposalResult(sessionId, saved, result)

        return saved
    }

    /**
     * 상급자가 제안을 거부한다.
     */
    fun rejectProposal(sessionId: Long, proposalId: Long, approverId: Long): CommandProposal {
        val proposal = proposalRepository.findById(proposalId).orElseThrow {
            IllegalArgumentException("제안 미존재: $proposalId")
        }
        require(proposal.sessionId == sessionId) { "세션 불일치" }
        require(proposal.approverId == approverId) { "거부 권한 없음" }
        require(proposal.status == "PENDING") { "이미 처리된 제안입니다." }

        proposal.status = "REJECTED"
        proposal.resolvedAt = java.time.OffsetDateTime.now()
        return proposalRepository.save(proposal)
    }

    fun getPendingProposals(sessionId: Long, approverId: Long): List<CommandProposal> =
        proposalRepository.findBySessionIdAndApproverIdAndStatus(sessionId, approverId, "PENDING")

    fun getMyProposals(sessionId: Long, proposerId: Long): List<CommandProposal> =
        proposalRepository.findBySessionIdAndProposerId(sessionId, proposerId)
}
```

**CommandProposalController.kt**:
```kotlin
package com.openlogh.controller

@RestController
@RequestMapping("/api/{sessionId}/proposals")
class CommandProposalController(
    private val proposalService: CommandProposalService,
) {
    data class CreateProposalRequest(
        val proposerId: Long,
        val approverId: Long,
        val commandCode: String,
        val args: Map<String, Any> = emptyMap(),
    )

    @PostMapping
    fun createProposal(
        @PathVariable sessionId: Long,
        @RequestBody request: CreateProposalRequest,
    ): ResponseEntity<CommandProposal> {
        val proposal = proposalService.createProposal(
            sessionId, request.proposerId, request.approverId,
            request.commandCode, request.args,
        )
        return ResponseEntity.status(HttpStatus.CREATED).body(proposal)
    }

    @PutMapping("/{proposalId}/approve")
    suspend fun approveProposal(
        @PathVariable sessionId: Long,
        @PathVariable proposalId: Long,
        @RequestParam approverId: Long,
    ): ResponseEntity<CommandProposal> {
        val result = proposalService.approveProposal(sessionId, proposalId, approverId)
        return ResponseEntity.ok(result)
    }

    @PutMapping("/{proposalId}/reject")
    fun rejectProposal(
        @PathVariable sessionId: Long,
        @PathVariable proposalId: Long,
        @RequestParam approverId: Long,
    ): ResponseEntity<CommandProposal> {
        val result = proposalService.rejectProposal(sessionId, proposalId, approverId)
        return ResponseEntity.ok(result)
    }

    @GetMapping("/pending")
    fun getPendingProposals(
        @PathVariable sessionId: Long,
        @RequestParam approverId: Long,
    ): ResponseEntity<List<CommandProposal>> =
        ResponseEntity.ok(proposalService.getPendingProposals(sessionId, approverId))

    @GetMapping("/mine")
    fun getMyProposals(
        @PathVariable sessionId: Long,
        @RequestParam proposerId: Long,
    ): ResponseEntity<List<CommandProposal>> =
        ResponseEntity.ok(proposalService.getMyProposals(sessionId, proposerId))
}
```

GameEventService에 broadcastProposalResult 메서드가 없다면, 기존
gameEventService.broadcast(sessionId, "PROPOSAL_RESULT", mapOf("proposalId" to saved.id, "status" to saved.status, "logs" to result.logs))
패턴으로 대체한다. 컴파일 오류 발생 시 기존 broadcast 메서드 시그니처에 맞게 조정.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>CommandProposalService + Controller 생성, 제안/승인/거부 API 엔드포인트 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:compileKotlin 2>&1 | grep -E "error|BUILD"

# 엔티티/마이그레이션 확인
ls game-app/src/main/kotlin/com/openlogh/entity/CommandProposal.kt
ls game-app/src/main/resources/db/migration/V46__add_command_proposal.sql

# 서비스/컨트롤러 확인
ls game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt
ls game-app/src/main/kotlin/com/openlogh/controller/CommandProposalController.kt

# commandExecutor 연결 확인
grep "commandExecutor.executeOfficerCommand" \
  game-app/src/main/kotlin/com/openlogh/service/CommandProposalService.kt
```
</verification>

<success_criteria>
- CommandProposal.kt JPA 엔티티 존재 (session_id, proposer_id, approver_id, command_code, args JSONB, status)
- V46__add_command_proposal.sql 존재 (command_proposal 테이블 + 인덱스)
- CommandProposalRepository.kt 존재 (findBySessionIdAndApproverIdAndStatus 메서드)
- CommandProposalService.createProposal() 계급 체크 로직 포함
- CommandProposalService.approveProposal()이 CommandExecutor.executeOfficerCommand() 호출
- POST /api/{sessionId}/proposals, PUT /{id}/approve, PUT /{id}/reject 엔드포인트 존재
- ./gradlew :game-app:compileKotlin BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/02-gin7-command-system/02-06-SUMMARY.md`
</output>
