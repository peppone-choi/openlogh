---
phase: 02-gin7-command-system
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/politics/PoliticsCommands.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/intelligence/IntelligenceCommands.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
autonomous: true
requirements:
  - CMD-03
  - CMD-06

must_haves:
  truths:
    - "납입률변경 커맨드가 Faction.taxRate를 arg['taxRate']로 변경한다"
    - "국가목표/통치목표 커맨드가 Faction.meta['goal']에 목표값을 저장한다"
    - "야회/수렵/회담/담화/연설이 Faction.meta['approval'] 또는 Planet.approval을 증가시킨다"
    - "첩보 커맨드(잠입공작/탈출공작/정보공작 등)가 MCP를 소모하고 destOfficer에 효과를 낸다"
    - "일제수색/체포명령/체포허가가 대상 장교의 상태(meta)를 변경한다"
    - "Gin7CommandRegistry에서 정치(12종)+첩보(14종)=26종이 실제 클래스로 등록된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/command/gin7/politics/PoliticsCommands.kt"
      provides: "정치커맨드 12종 구현체 (PCP)"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/command/gin7/intelligence/IntelligenceCommands.kt"
      provides: "첩보커맨드 14종 구현체 (MCP)"
  key_links:
    - from: "Gin7CommandRegistry.init"
      to: "PoliticsCommands, IntelligenceCommands"
      via: "registerOfficerCommand(\"야회\") { g, e, a -> BanquetCommand(g, e, a) }"
      pattern: "BanquetCommand|IntelligenceOpCommand|TaxRateChangeCommand"
---

<objective>
정치커맨드(12종, PCP) + 첩보커맨드(14종, MCP) 실제 구현.

Purpose: 진영 통치 및 첩보 활동을 엔티티 변경으로 구현한다. 정치커맨드는 세율 변경, 지지도 조작, 외교 상태를 담당하고, 첩보커맨드는 잠입/탈출/파괴/선동 등 상대방 엔티티에 대한 효과를 낸다. 모든 MCP 첩보커맨드는 getCommandPoolType()=MCP override 필요.

Output: command/gin7/politics/ (12종) + command/gin7/intelligence/ (14종), Gin7CommandRegistry 26종 갱신
</objective>

<execution_context>
@/Users/apple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-gin7-command-system/02-CONTEXT.md

<interfaces>
<!-- commands.json 핵심 값 (정치/첩보) -->
정치커맨드 PCP (모두 cpCost=320, waitTime=0, duration=0, 통치목표만 cpCost=80):
- 야회(banquet), 수렵(hunt), 회담(conference), 담화(address), 연설(speech)
  → 사회적 활동, 지지도/사기 증가
- 국가목표(national_goal), 납입률변경(tax_rate_change), 관세율변경(tariff_rate_change)
  → 진영 정책 변경
- 분배(distribution): 자원 분배
- 처단(execution): 반역자 제거
- 외교(diplomacy): 외교 상태 변경
- 통치목표(governance_goal): cpCost=80, 행성 통치 목표 설정

첩보커맨드 MCP (모두 cpCost=160~800, waitTime=0, duration=0):
- 일제수색: cpCost=160
- 체포허가: cpCost=800
- 집행명령: cpCost=800
- 체포명령: cpCost=160
- 사열: cpCost=160
- 습격: cpCost=160
- 감시: cpCost=160
- 잠입공작/탈출공작/정보공작/파괴공작/선동공작: cpCost=160
- 침입공작/귀환공작: cpCost=320

<!-- Faction 엔티티 관련 필드 -->
Faction: id, sessionId, name, funds, supplies, taxRate, conscriptionRate, techLevel, militaryPower, factionRank, factionType, meta

<!-- Planet 관련 필드 -->
Planet: id, approval (지지도), security (치안), population, production, commerce, orbitalDefense, fortress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정치커맨드 12종 구현체 (command/gin7/politics/)</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/politics/PoliticsCommands.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/OfficerCommand.kt
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Faction.kt (taxRate, funds, meta 필드)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Planet.kt (approval, security 필드)
  </read_first>
  <action>
패키지: `com.openlogh.command.gin7.politics`
단일 파일 PoliticsCommands.kt에 12개 클래스 정의.
모든 정치커맨드는 PCP (override 불필요). getPreReqTurn()=0, getPostReqTurn()=0.

**BanquetCommand** ("야회"): run() → nation null이면 fail. nation.meta["approvalBonus"] = (nation.meta["approvalBonus"] as? Int ?: 0) + 5. log: "야회를 개최했다."

**HuntCommand** ("수렵"): run() → general.morale = minOf(100, general.morale + 10). log.

**ConferenceCommand** ("회담"): run() → destFaction null이면 fail. destFaction.meta["diplomacyStatus"] = "TALK". log.

**AddressCommand** ("담화"): run() → city(현재 행성).approval = minOf(100, city.approval + 3). log.

**SpeechCommand** ("연설"): run() → city.approval = minOf(100, city.approval + 5). nation.meta["speechBonus"] = true. log.

**NationalGoalCommand** ("국가목표"): run() → nation null이면 fail. nation.meta["goal"] = arg["goal"] ?: "승리". log.

**TaxRateChangeCommand** ("납입률변경"): run() → nation null이면 fail.
  val rate = (arg["taxRate"] as? Number)?.toInt() ?: return fail("세율 미지정")
  if (rate !in 0..100) return fail("세율은 0~100 사이여야 합니다")
  nation.taxRate = rate.toShort()
  log: "납입률을 ${rate}%로 변경했다."

**TariffRateChangeCommand** ("관세율변경"): run() → 동일 패턴, nation.meta["tariffRate"] = rate. log.

**DistributionCommand** ("분배"): run() → arg["targetFactionId"]의 진영에 arg["funds"]만큼 이전.
  nation.funds -= amount. destFaction.funds += amount. log.

**ExecutionCommand** ("처단"): run() → destOfficer null이면 fail.
  destOfficer.meta["executed"] = true. destOfficer.factionId = 0. log: "${destOfficer.name}을(를) 처단했다."
  pushGlobalHistoryLog 포함.

**DiplomacyCommand** ("외교"): run() → destFaction null이면 fail.
  val action = arg["action"] as? String ?: "교섭"
  nation.meta["diplomacyAction_${destFaction.id}"] = action. log.

**GovernanceGoalCommand** ("통치목표"): run() → city null이면 fail.
  city.meta["governanceGoal"] = arg["goal"] ?: "발전". log.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>PoliticsCommands.kt 생성, 12종 정치커맨드 클래스 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: 첩보커맨드 14종 구현체 + Gin7CommandRegistry 갱신</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/intelligence/IntelligenceCommands.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt (현재 전체)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Officer.kt (meta 필드)
  </read_first>
  <action>
패키지: `com.openlogh.command.gin7.intelligence`
단일 파일 IntelligenceCommands.kt에 14개 클래스 정의.
모든 첩보커맨드는 MCP: `override fun getCommandPoolType() = StatCategory.MCP`

**GeneralSearchCommand** ("일제수색"): run() → destPlanet null이면 fail.
  destPlanetOfficers?.forEach { it.meta["searched"] = true }. log: "일제 수색을 실시했다."

**ArrestAuthorizationCommand** ("체포허가", cpCost=800): run() → destOfficer null이면 fail.
  destOfficer.meta["arrestWarrant"] = true. log.

**ExecutionOrderCommand** ("집행명령", cpCost=800): run() → destOfficer null이면 fail.
  destOfficer.meta["executionOrder"] = true. log.

**ArrestOrderCommand** ("체포명령"): run() → destOfficer null이면 fail.
  destOfficer.meta["arrested"] = true. log: "${destOfficer.name}에 대한 체포명령을 내렸다."

**InspectionCommand** ("사열"): run() → city null이면 fail. city.security = minOf(100, city.security + 5). log.

**RaidCommand** ("습격"): run() → destPlanet null이면 fail.
  val damage = rng.nextInt(100, 500)
  destPlanet.orbitalDefense = maxOf(0, destPlanet.orbitalDefense - damage). log.

**SurveillanceCommand** ("감시"): run() → destOfficer null이면 fail.
  destOfficer.meta["underSurveillance"] = true. log.

**InfiltrationOpCommand** ("잠입공작"): run() → destPlanet null이면 fail.
  general.meta["infiltratedPlanet"] = destPlanet.id. log.

**EscapeOpCommand** ("탈출공작"): run() → general.meta.remove("infiltratedPlanet").
  general.planetId = city?.id ?: general.planetId. log.

**IntelligenceOpCommand** ("정보공작"): run() → destFaction null이면 fail.
  val intel = mapOf("funds" to destFaction.funds, "techLevel" to destFaction.techLevel)
  general.meta["intelOn_${destFaction.id}"] = intel. log.

**SabotageOpCommand** ("파괴공작"): run() → destPlanet null이면 fail.
  destPlanet.production = maxOf(0, destPlanet.production - rng.nextInt(1, 5)). log.

**AgitationOpCommand** ("선동공작"): run() → destPlanet null이면 fail.
  destPlanet.approval = maxOf(0, destPlanet.approval - rng.nextInt(5, 20)). log.

**IncursionOpCommand** ("침입공작", cpCost=320): run() → destFaction null이면 fail.
  general.meta["incursionTarget"] = destFaction.id. log.

**ReturnOpCommand** ("귀환공작", cpCost=320): run() → general.meta.remove("incursionTarget").
  log: "귀환 공작을 완료했다."

**Gin7CommandRegistry.kt 갱신**:
정치커맨드 12종 + 첩보커맨드 14종 = 26종을 실제 클래스로 교체:
```kotlin
// 정치커맨드
registerOfficerCommand("야회") { g, e, a -> BanquetCommand(g, e, a) }
registerOfficerCommand("수렵") { g, e, a -> HuntCommand(g, e, a) }
registerOfficerCommand("회담") { g, e, a -> ConferenceCommand(g, e, a) }
registerOfficerCommand("담화") { g, e, a -> AddressCommand(g, e, a) }
registerOfficerCommand("연설") { g, e, a -> SpeechCommand(g, e, a) }
registerOfficerCommand("국가목표") { g, e, a -> NationalGoalCommand(g, e, a) }
registerOfficerCommand("납입률변경") { g, e, a -> TaxRateChangeCommand(g, e, a) }
registerOfficerCommand("관세율변경") { g, e, a -> TariffRateChangeCommand(g, e, a) }
registerOfficerCommand("분배") { g, e, a -> DistributionCommand(g, e, a) }
registerOfficerCommand("처단") { g, e, a -> ExecutionCommand(g, e, a) }
registerOfficerCommand("외교") { g, e, a -> DiplomacyCommand(g, e, a) }
registerOfficerCommand("통치목표") { g, e, a -> GovernanceGoalCommand(g, e, a) }
// 첩보커맨드
registerOfficerCommand("일제수색") { g, e, a -> GeneralSearchCommand(g, e, a) }
registerOfficerCommand("체포허가") { g, e, a -> ArrestAuthorizationCommand(g, e, a) }
registerOfficerCommand("집행명령") { g, e, a -> ExecutionOrderCommand(g, e, a) }
registerOfficerCommand("체포명령") { g, e, a -> ArrestOrderCommand(g, e, a) }
registerOfficerCommand("사열") { g, e, a -> InspectionCommand(g, e, a) }
registerOfficerCommand("습격") { g, e, a -> RaidCommand(g, e, a) }
registerOfficerCommand("감시") { g, e, a -> SurveillanceCommand(g, e, a) }
registerOfficerCommand("잠입공작") { g, e, a -> InfiltrationOpCommand(g, e, a) }
registerOfficerCommand("탈출공작") { g, e, a -> EscapeOpCommand(g, e, a) }
registerOfficerCommand("정보공작") { g, e, a -> IntelligenceOpCommand(g, e, a) }
registerOfficerCommand("파괴공작") { g, e, a -> SabotageOpCommand(g, e, a) }
registerOfficerCommand("선동공작") { g, e, a -> AgitationOpCommand(g, e, a) }
registerOfficerCommand("침입공작") { g, e, a -> IncursionOpCommand(g, e, a) }
registerOfficerCommand("귀환공작") { g, e, a -> ReturnOpCommand(g, e, a) }
```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>IntelligenceCommands.kt 생성, 정치+첩보 26종 실제 클래스 등록, 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:compileKotlin 2>&1 | grep -E "error|BUILD"

# 정치/첩보 파일 확인
ls game-app/src/main/kotlin/com/openlogh/command/gin7/politics/
ls game-app/src/main/kotlin/com/openlogh/command/gin7/intelligence/

# Registry 등록 확인
grep -c "BanquetCommand\|IntelligenceOpCommand\|TaxRateChangeCommand" \
  game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
```
</verification>

<success_criteria>
- command/gin7/politics/PoliticsCommands.kt 존재, 12종 클래스
- command/gin7/intelligence/IntelligenceCommands.kt 존재, 14종 클래스
- Gin7CommandRegistry에서 정치+첩보 26종이 실제 클래스로 등록
- 첩보 14종 모두 getCommandPoolType() = StatCategory.MCP
- ./gradlew :game-app:compileKotlin BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/02-gin7-command-system/02-04-SUMMARY.md`
</output>
