---
phase: 02-gin7-command-system
plan: "05"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/OperationPlanCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/OperationCancelCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/AssignmentCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/FormFleetCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/DisbandFleetCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/GiveLectureCommand.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/TransportCommands.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
autonomous: true
requirements:
  - CMD-03
  - CMD-07

must_haves:
  truths:
    - "작전계획 커맨드가 Faction.meta['operationPlan']에 작전 데이터를 저장한다"
    - "발령 커맨드가 destOfficer에게 arg['positionCard'] 추가 및 arg['fleetId'] 배속을 처리한다"
    - "부대결성 커맨드가 Fleet 엔티티를 신규 생성하고 general을 leaderOfficerId로 설정한다"
    - "부대해산 커맨드가 Fleet을 삭제하고 소속 장교들의 fleetId를 0으로 초기화한다"
    - "Gin7CommandRegistry에서 지휘커맨드 8종이 실제 클래스로 등록된다"
    - "지휘커맨드 8종 모두 getCommandPoolType() = StatCategory.MCP"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/"
      provides: "지휘커맨드 8종 구현체 (MCP)"
  key_links:
    - from: "FormFleetCommand.run()"
      to: "FleetRepository.save(Fleet(...))"
      via: "services?.factionRepository or FleetRepository through context"
      pattern: "FormFleetCommand|Fleet\\(|fleetRepository"
---

<objective>
지휘커맨드(8종, MCP) 실제 구현.

Purpose: 지휘커맨드는 작전 수립/철회, 부대 결성/해산, 발령을 담당한다. 특히 부대결성(form_fleet)은 Fleet 엔티티를 실제로 생성해야 하고, 작전계획(operation_plan)은 규모에 따라 가변 CP(10~1280)를 지불한다. 발령(assignment)은 장교 배속과 PositionCard 부여를 결합한 복합 커맨드다.

Output: command/gin7/commander/ 패키지 8종, Gin7CommandRegistry 등록 완료 → stub 0종 달성
</objective>

<execution_context>
@/Users/apple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-gin7-command-system/02-CONTEXT.md

<interfaces>
<!-- commands.json 핵심 값 (지휘) -->
지휘커맨드 MCP:
- 작전계획(operation_plan): cpCost=10~1280 (variable by scale), waitTime=0, duration=0
- 작전철회(operation_cancel): cpCost=5~320 (variable by scale)
- 발령(assignment): cpCost=1~320 (variable by rank)
- 부대결성(form_fleet): cpCost=320
- 부대해산(disband_fleet): cpCost=160
- 강의(give_lecture): cpCost=160, restriction=사관학교
- 수송계획(transport_plan): cpCost=80
- 수송중지(transport_cancel): cpCost=80

<!-- Fleet 엔티티 구조 -->
```kotlin
@Entity @Table(name = "fleet")
class Fleet(
    var id: Long = 0,
    var sessionId: Long = 0,
    var leaderOfficerId: Long = 0,
    var factionId: Long = 0,
    var name: String = "",
    var unitType: String = "FLEET",  // FLEET / PATROL / TRANSPORT / GROUND
    var maxUnits: Int = 60,
    var currentUnits: Int = 0,
    var maxCrew: Int = 10,
    var planetId: Long = 0,
    var meta: MutableMap<String, Any> = mutableMapOf(),
)
```

<!-- FleetRepository 접근 방법 -->
// CommandServices에는 FleetRepository가 없음 — services.officerRepository를 통해 간접적으로
// 또는 FormFleetCommand에서 직접 FleetRepository를 주입할 수 없으므로:
// Fleet 생성은 run()의 반환값 message JSON에 "createFleet" 지시를 포함해
// CommandResultApplicator 또는 CommandExecutor가 처리하도록 한다.
// 대안: services에 FleetRepository를 추가하거나 context에 미리 생성된 Fleet을 전달.
// → 이 plan에서는 직접 Fleet 엔티티를 생성하여 CommandServices.factionRepository 대신
//   general.fleetId 설정 + CommandResult.message에 createFleet 지시를 담는 패턴 사용.
// 실용적 해법: CommandServices에 FleetRepository를 추가하고 FormFleetCommand에서 직접 save.

<!-- CommandServices 현재 구조 -->
```kotlin
class CommandServices(
    val officerRepository: OfficerRepository,
    val planetRepository: PlanetRepository,
    val factionRepository: FactionRepository,
    val diplomacyService: DiplomacyService,
    val messageService: MessageService,
    val modifierService: ModifierService,
)
```
FleetRepository가 없음 → CommandServices에 추가 필요 (이 plan Task 1에서 추가).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommandServices에 FleetRepository 추가 + 지휘커맨드 8종 구현체 생성</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/CommandServices.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/OperationPlanCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/OperationCancelCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/AssignmentCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/FormFleetCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/DisbandFleetCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/GiveLectureCommand.kt,
    backend/game-app/src/main/kotlin/com/openlogh/command/gin7/commander/TransportCommands.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandServices.kt (전체)
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt (CommandServices 생성 부분, ~line 108)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Fleet.kt (전체)
    - backend/game-app/src/main/kotlin/com/openlogh/repository/FleetRepository.kt (인터페이스 확인)
  </read_first>
  <action>
**CommandServices.kt 수정**:
FleetRepository를 nullable 파라미터로 추가:
```kotlin
class CommandServices(
    val officerRepository: OfficerRepository,
    val planetRepository: PlanetRepository,
    val factionRepository: FactionRepository,
    val diplomacyService: DiplomacyService,
    val messageService: MessageService,
    val modifierService: ModifierService,
    val fleetRepository: FleetRepository? = null,  // 추가
)
```

CommandExecutor.kt에서 CommandServices 생성 시 fleetRepository를 주입한다:
CommandExecutor 생성자에 FleetRepository 추가 (Autowired), CommandServices 생성 시 fleetRepository 전달.

---

패키지: `com.openlogh.command.gin7.commander`
모든 지휘커맨드는 MCP: `override fun getCommandPoolType() = StatCategory.MCP`

**OperationPlanCommand.kt** ("작전계획"):
- getPreReqTurn()=0, getPostReqTurn()=0
- run() → nation null이면 fail.
  val scale = (arg["scale"] as? Number)?.toInt() ?: 1  // 1~128
  val planName = arg["planName"] as? String ?: "작전계획-${env.year}-${env.month}"
  nation.meta["operationPlan"] = mapOf("name" to planName, "scale" to scale, "year" to env.year, "month" to env.month)
  log: "${general.name}이(가) '${planName}' 작전계획을 수립했다."
  pushNationalHistoryLog("작전계획 수립: $planName")

**OperationCancelCommand.kt** ("작전철회"):
- run() → nation null이면 fail.
  val planName = (nation.meta["operationPlan"] as? Map<*,*>)?.get("name") ?: "진행 중인 작전"
  nation.meta.remove("operationPlan")
  log: "${general.name}이(가) '${planName}' 작전을 철회했다."

**AssignmentCommand.kt** ("발령"):
- run() → destOfficer null이면 fail("대상 장교 미지정").
  val positionCard = arg["positionCard"] as? String
  val fleetId = (arg["fleetId"] as? Number)?.toLong()
  if (positionCard != null) {
      val cards = destOfficer.positionCards.split(",").filter { it.isNotBlank() }.toMutableSet()
      cards.add(positionCard)
      destOfficer.positionCards = cards.joinToString(",")
  }
  if (fleetId != null) destOfficer.fleetId = fleetId
  log: "${destOfficer.name}에게 발령을 내렸다."
  pushGlobalHistoryLog("${destOfficer.name} 발령 완료")

**FormFleetCommand.kt** ("부대결성"):
- run() → services?.fleetRepository null이면 fail("서비스 미초기화").
  val fleetName = arg["name"] as? String ?: "${general.name}의 함대"
  val unitType = arg["unitType"] as? String ?: "FLEET"
  val newFleet = Fleet(
      sessionId = general.sessionId,
      leaderOfficerId = general.id,
      factionId = general.factionId,
      name = fleetName,
      unitType = unitType,
      maxUnits = 60,
      planetId = general.planetId,
  )
  val saved = services!!.fleetRepository!!.save(newFleet)
  general.fleetId = saved.id
  log: "${general.name}이(가) '${fleetName}'을(를) 결성했다."
  pushGlobalHistoryLog("부대 결성: $fleetName")

**DisbandFleetCommand.kt** ("부대해산"):
- run() → services?.fleetRepository null이면 fail.
  val fleetId = general.fleetId
  if (fleetId == 0L) return fail("소속 함대가 없습니다.")
  // 함대 소속 장교들 fleetId 초기화
  services!!.officerRepository.findBySessionIdAndFleetId(general.sessionId, fleetId).forEach { 
      it.fleetId = 0L
      services!!.officerRepository.save(it)
  }
  services!!.fleetRepository!!.deleteById(fleetId)
  general.fleetId = 0L
  log: "부대를 해산했다."

**GiveLectureCommand.kt** ("강의"):
- run() → log: "${general.name}이(가) 사관학교에서 강의했다."
  // 수강생 훈련도 증가는 AttendLectureCommand에서 처리
  general.meta["lastLectureDate"] = "${env.year}-${env.month}"

**TransportCommands.kt** — 2종:
`TransportPlanCommand` ("수송계획"):
run() → nation null이면 fail.
  val source = (arg["sourcePlanetId"] as? Number)?.toLong() ?: fail("출발 행성 미지정")
  val dest = (arg["destPlanetId"] as? Number)?.toLong() ?: fail("목표 행성 미지정")
  nation.meta["transportPlan"] = mapOf("from" to source, "to" to dest, "amount" to (arg["amount"] ?: 1000))
  log: "수송 계획을 수립했다."

`TransportCancelCommand` ("수송중지"):
run() → nation.meta.remove("transportPlan"). log: "수송을 중지했다."
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>CommandServices에 fleetRepository 추가, 지휘 8종 파일 생성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: Gin7CommandRegistry — 지휘커맨드 8종 등록 + 전체 stub 제거 확인</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt (현재 전체)
  </read_first>
  <action>
Gin7CommandRegistry.kt에서 지휘커맨드 8종을 실제 클래스로 교체:
```kotlin
registerOfficerCommand("작전계획") { g, e, a -> OperationPlanCommand(g, e, a) }
registerOfficerCommand("작전철회") { g, e, a -> OperationCancelCommand(g, e, a) }
registerOfficerCommand("발령") { g, e, a -> AssignmentCommand(g, e, a) }
registerOfficerCommand("부대결성") { g, e, a -> FormFleetCommand(g, e, a) }
registerOfficerCommand("부대해산") { g, e, a -> DisbandFleetCommand(g, e, a) }
registerOfficerCommand("강의") { g, e, a -> GiveLectureCommand(g, e, a) }
registerOfficerCommand("수송계획") { g, e, a -> TransportPlanCommand(g, e, a) }
registerOfficerCommand("수송중지") { g, e, a -> TransportCancelCommand(g, e, a) }
```

이 시점에서 Gin7CommandRegistry에 남은 stub: "대기" 1종만 남아야 한다.
(작전16+병참6+개인15+인사10+정치12+첩보14+지휘8 = 81종 전부 실제 클래스로 교체됨)

Gin7StubCommand inner class와 registerMcpStub/registerPcpStub 헬퍼 메서드는 대기 커맨드에만 사용되므로 유지.
대기 커맨드는 stub 유지 (ALWAYS_ALLOWED, 특수 처리 불필요).

파일 상단 import 정리:
```kotlin
import com.openlogh.command.gin7.operations.*
import com.openlogh.command.gin7.logistics.*
import com.openlogh.command.gin7.personal.*
import com.openlogh.command.gin7.personnel.*
import com.openlogh.command.gin7.politics.*
import com.openlogh.command.gin7.intelligence.*
import com.openlogh.command.gin7.commander.*
```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>지휘 8종 등록 완료, gin7 81종 stub 없이 실제 클래스로 등록, 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:compileKotlin 2>&1 | grep -E "error|BUILD"

# stub 잔존 확인 — 대기만 남아야 함
grep "Gin7StubCommand\|Gin7McpStubCommand" \
  game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt | grep -v "대기\|class Gin7"

# 지휘커맨드 파일 수
ls game-app/src/main/kotlin/com/openlogh/command/gin7/commander/*.kt | wc -l

# 전체 gin7 커맨드 그룹 디렉토리 확인
ls game-app/src/main/kotlin/com/openlogh/command/gin7/
```
</verification>

<success_criteria>
- command/gin7/commander/ 에 8개 파일 (8종 클래스)
- Gin7CommandRegistry에서 "대기" 1종 제외 81종 전부 실제 클래스로 등록
- CommandServices에 fleetRepository 필드 추가됨
- ./gradlew :game-app:compileKotlin BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/02-gin7-command-system/02-05-SUMMARY.md`
</output>
