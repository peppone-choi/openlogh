---
phase: 06-frontend-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/types/galaxy.ts
  - frontend/src/types/tactical.ts
  - frontend/src/types/officer.ts
  - frontend/src/types/fleet.ts
  - frontend/src/types/planet.ts
  - frontend/src/types/command.ts
autonomous: true
requirements:
  - FE-08
must_haves:
  truths:
    - "TypeScript types use gin7 field names (funds/supplies/ships/officer) with no samguk aliases as primary fields"
    - "Officer type has 8 stats (leadership/command/intelligence/politics/administration/mobility/attack/defense)"
    - "Fleet type has shipClass, morale, training fields"
    - "Planet type has population, production, commerce, security, approval, orbital_defense, fortress fields"
    - "Faction type has funds, supplies, tax_rate, conscription_rate, tech_level, military_power, faction_type"
    - "Command types include positionCards, cpCost, cpType (PCP/MCP), commandGroup"
  artifacts:
    - path: "frontend/src/types/officer.ts"
      provides: "Officer, OfficerStat, RankTitle, PositionCard interfaces"
      exports: ["Officer", "OfficerStat", "RankTitle"]
    - path: "frontend/src/types/fleet.ts"
      provides: "Fleet, FleetUnit, ShipClass, Formation interfaces"
      exports: ["Fleet", "FleetUnit", "ShipClass"]
    - path: "frontend/src/types/planet.ts"
      provides: "Planet, Faction, StarSystem interfaces"
      exports: ["Planet", "Faction"]
    - path: "frontend/src/types/command.ts"
      provides: "CommandEntry, CommandGroup, PositionCard, CpPool interfaces"
      exports: ["CommandEntry", "CommandGroup", "CpPool"]
  key_links:
    - from: "frontend/src/types/index.ts"
      to: "frontend/src/types/officer.ts"
      via: "re-export"
      pattern: "export.*from.*officer"
    - from: "frontend/src/types/index.ts"
      to: "frontend/src/types/fleet.ts"
      via: "re-export"
      pattern: "export.*from.*fleet"
---

<objective>
Rewrite all frontend TypeScript types to use gin7 field names, replacing samguk-era aliases.
Split the monolithic types/index.ts into domain files and establish the type contracts all Phase 6 UI plans depend on.

Purpose: All subsequent plans in this phase build against these types. Getting them right first prevents cascading type errors across 7 other plan files.
Output: types/officer.ts, types/fleet.ts, types/planet.ts, types/command.ts — all re-exported from types/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- Current types/index.ts has samguk-era Nation (gold/rice/bill/rate), City (agri/comm/pop), General-style officer -->
<!-- gin7 domain mapping from CLAUDE.md: -->

Faction (replaces Nation):
- funds (was gold), supplies (was rice), tax_rate (was bill), conscription_rate (was rate)
- tech_level (was tech), military_power (was power), faction_rank (was level)
- faction_type: 'empire' | 'alliance' | 'fezzan' | 'rebel'

Planet (replaces City):
- population (was pop), production (was agri), commerce (was comm), security (was secu)
- approval (was trust), orbital_defense (was def), fortress (was wall), trade_route (was trade)

Officer (replaces General):
- 8 stats: leadership, command, intelligence, politics, administration, mobility, attack, defense
- (was: leadership, strength→command, intel→intelligence, politics, charm→administration, + 3 new)
- positionCards: string[] (JSONB list of card codes)
- rankLevel: number (0-10), rankTitle: string
- pcpPool, mcpPool: number (CP pools)
- factionId, planetId, fleetId: number | null

Fleet (replaces Troop):
- shipClass: 'battleship'|'cruiser'|'destroyer'|'carrier'|'transport'|'hospital'|'fortress'
- ships: number (count), morale: number, training: number
- leaderOfficerId: number | null
- formation: 'SPINDLE'|'BY_CLASS'|'MIXED'|'THREE_COLUMN'

ShipClass codes: battleship, cruiser, destroyer, carrier, transport, hospital, fortress
CommandGroup codes: OPERATION, PERSONAL, COMMAND, LOGISTICS, PERSONNEL, POLITICS, INTELLIGENCE
CP types: PCP (정략), MCP (군사)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain type files (officer, fleet, planet, command)</name>
  <files>
    frontend/src/types/officer.ts
    frontend/src/types/fleet.ts
    frontend/src/types/planet.ts
    frontend/src/types/command.ts
  </files>
  <read_first>
    frontend/src/types/index.ts (full file — understand existing types before replacing)
    frontend/src/stores/officerStore.ts (see what fields are consumed)
    frontend/src/stores/galaxyStore.ts (see galaxy/planet types used)
  </read_first>
  <behavior>
    - Officer has id, worldId, name, factionId, planetId, fleetId, officerLevel, rankLevel, rankTitle
    - Officer has 8 gin7 stats as separate fields, NOT as a nested object
    - Officer has pcpPool, mcpPool, pcpMax, mcpMax
    - Officer has positionCards: string[] (list of position card codes)
    - Fleet has id, worldId, name, factionId, leaderOfficerId, ships, shipClass, morale, training, formation, meta
    - Planet has id, worldId, name, factionId, population, production, commerce, security, approval, orbital_defense, fortress, trade_route, supplyState
    - Faction has id, worldId, name, abbreviation, color, faction_type, funds, supplies, capitalPlanetId, tax_rate, conscription_rate, tech_level, military_power, faction_rank
    - CommandEntry has code, name, group (CommandGroup), cpType ('PCP'|'MCP'), cpCost, cooldown, requiredCards: string[]
    - CpPool has pcpCurrent, pcpMax, mcpCurrent, mcpMax, regenRate
  </behavior>
  <action>
    Create frontend/src/types/officer.ts with: RankTitle enum (0-10), OfficerStat type (8 stat fields), PositionCard type (code string), Officer interface, OfficerSummary interface (lite version for lists).

    Create frontend/src/types/fleet.ts with: ShipClass union type, Formation union type, UnitStance type, Fleet interface, FleetUnit interface (individual unit in a fleet with shipClass, ships, morale, training, shipSubtype).

    Create frontend/src/types/planet.ts with: Planet interface (all gin7 fields), Faction interface (gin7 fields), StarSystem interface (already partially exists in types/galaxy.ts — keep there but re-export from planet.ts for convenience).

    Create frontend/src/types/command.ts with: CommandGroup union type (OPERATION|PERSONAL|COMMAND|LOGISTICS|PERSONNEL|POLITICS|INTELLIGENCE), CpType ('PCP'|'MCP'), CommandEntry interface, CpPool interface, CommandProposal interface (for proposal system), CommandResult interface ({ success: boolean; log: string[] }).

    All types use Korean comments for field descriptions.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit --strict 2>&1 | grep -E "types/officer|types/fleet|types/planet|types/command" | head -20 || echo "type files OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 type files created with exported interfaces
    - No TypeScript errors in the new type files themselves
    - Officer has exactly 8 stat fields matching CLAUDE.md mapping
  </acceptance_criteria>
  <done>4 type files exist with gin7-aligned interfaces, no TS compile errors in those files</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite types/index.ts to re-export domain types and replace samguk interfaces</name>
  <files>
    frontend/src/types/index.ts
    frontend/src/types/galaxy.ts
  </files>
  <read_first>
    frontend/src/types/index.ts (current full content)
    frontend/src/types/galaxy.ts (existing galaxy types to keep)
    frontend/src/stores/worldStore.ts (see what WorldState fields are used)
  </read_first>
  <action>
    Rewrite frontend/src/types/index.ts to:
    1. Keep: User, AuthResponse, OAuthProviderInfo, WorldState, JsonValue, JsonObject (infrastructure types — unchanged)
    2. Replace Nation interface → Faction (re-export from types/planet.ts). Add deprecated alias: `export type Nation = Faction` with JSDoc @deprecated comment.
    3. Replace City interface → Planet (re-export from types/planet.ts). Add deprecated alias: `export type City = Planet` with JSDoc @deprecated comment.
    4. Replace General/Officer type → re-export Officer from types/officer.ts. Legacy alias: `export type General = Officer`.
    5. Replace Troop type → re-export Fleet from types/fleet.ts. Legacy alias: `export type Troop = Fleet`.
    6. Re-export everything from: types/officer.ts, types/fleet.ts, types/planet.ts, types/command.ts, types/tactical.ts (if exists).
    7. Keep WorldSnapshot but rename cityOwnership → planetOwnership, nationId → factionId (with backward-compat alias).
    8. Keep CommandArg, CommandTableEntry, NationTurn types (used by commands page) but update NationTurn to use factionId.

    In frontend/src/types/galaxy.ts: ensure StarSystem has factionId (not nationId), fleetPositions?: FleetPosition[] array where FleetPosition = { fleetId, officerName, ships, factionId }.

    Do NOT change types/tactical.ts if it exists — tactical types are already gin7-aligned from Phase 3.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types/index.ts re-exports all domain types
    - Deprecated aliases exist so existing code doesn't immediately break
    - Galaxy types have factionId (not nationId) and FleetPosition type
    - TypeScript compile shows no new errors in types/ directory
  </acceptance_criteria>
  <done>All type files compile, index.ts re-exports domain files, samguk field names only appear as @deprecated aliases</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- 4 new domain type files created with gin7-aligned interfaces
- types/index.ts re-exports all domain types
- Deprecated aliases preserve backward compatibility for existing stores/components
- TypeScript compiles without new errors introduced by these type files
- Officer has 8 stats, Faction has gin7 field names, Planet has gin7 field names
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-01-SUMMARY.md`
</output>
