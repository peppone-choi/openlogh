---
phase: 06-frontend-integration
plan: "05"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/app/(game)/commands/page.tsx
  - frontend/src/components/game/position-card-panel.tsx
  - frontend/src/components/game/command-execution-panel.tsx
  - frontend/src/components/game/cp-display.tsx
  - frontend/src/components/game/officer-info-panel.tsx
  - frontend/src/stores/commandStore.ts
autonomous: true
requirements:
  - FE-04
  - FE-05
must_haves:
  truths:
    - "Strategic screen shows officer info panel with 8 stats, rank badge, PCP/MCP pools"
    - "Position card tabs are shown grouped by CommandGroup — clicking a tab filters command list"
    - "Command list shows commands available for selected position card, with CP cost and cooldown"
    - "CP display shows current PCP and MCP pools with regen bar"
    - "Executing a command sends to /app/command/{sessionId}/execute via WebSocket and shows result toast"
    - "Co-located officers (동스폿) list shows other officers on same planet"
  artifacts:
    - path: "frontend/src/components/game/position-card-panel.tsx"
      provides: "Tabbed position card list grouped by CommandGroup"
      exports: ["PositionCardPanel"]
    - path: "frontend/src/components/game/command-execution-panel.tsx"
      provides: "Command list + execute button for selected card"
      exports: ["CommandExecutionPanel"]
    - path: "frontend/src/components/game/cp-display.tsx"
      provides: "PCP/MCP pool bars with current/max values"
      exports: ["CpDisplay"]
    - path: "frontend/src/components/game/officer-info-panel.tsx"
      provides: "8-stat bars, rank badge, position card summary"
      exports: ["OfficerInfoPanel"]
  key_links:
    - from: "frontend/src/components/game/command-execution-panel.tsx"
      to: "WebSocket /app/command/{sessionId}/execute"
      via: "subscribeWebSocket + publish"
      pattern: "command.*execute|executeCommand"
    - from: "frontend/src/components/game/position-card-panel.tsx"
      to: "frontend/src/stores/officerStore.ts"
      via: "myOfficer.positionCards"
      pattern: "positionCards"
---

<objective>
Implement the strategic game screen: officer info panel with 8 stats, position card tab navigation, command list with CP costs, and real-time command execution via WebSocket.

Purpose: This is the primary gameplay loop. Players select a position card, see available commands, check CP cost, and execute. The flow must be intuitive and show real-time CP state.
Output: PositionCardPanel, CommandExecutionPanel, CpDisplay, OfficerInfoPanel — wired into commands/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- From commandStore (existing): -->
```typescript
// commands: CommandTableEntry[] — list of available commands
// executing: boolean
// lastResult: CommandResult | null
// executeCommand(sessionId, code, args): Promise<void>
```

<!-- From officerStore: -->
```typescript
// myOfficer: Officer — has positionCards: string[], pcpPool, mcpPool, pcpMax, mcpMax
// 8 stats: leadership, command, intelligence, politics, administration, mobility, attack, defense
```

<!-- CommandGroup labels (Korean): -->
// OPERATION → '작전', PERSONAL → '개인', COMMAND → '지휘'
// LOGISTICS → '병참', PERSONNEL → '인사', POLITICS → '정치', INTELLIGENCE → '첩보'

<!-- WebSocket execute channel: /app/command/{sessionId}/execute -->
// payload: { officerId, commandCode, args: Record<string, unknown> }
// result comes back on /topic/world/{sessionId}/events as CommandResultEvent

<!-- 8 stat Korean labels: -->
// leadership→통솔, command→지휘, intelligence→정보, politics→정치
// administration→운영, mobility→기동, attack→공격, defense→방어
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OfficerInfoPanel, CpDisplay, and PositionCardPanel</name>
  <files>
    frontend/src/components/game/officer-info-panel.tsx
    frontend/src/components/game/cp-display.tsx
    frontend/src/components/game/position-card-panel.tsx
  </files>
  <read_first>
    frontend/src/stores/officerStore.ts (myOfficer structure)
    frontend/src/components/game/general-basic-card.tsx (existing card to replace pattern)
    frontend/src/types/officer.ts (Officer interface from Plan 01)
  </read_first>
  <action>
    Create officer-info-panel.tsx:
    - Props: officer (Officer)
    - Layout: dark card (bg-slate-900 border border-slate-700), width full.
    - Top: officer name (bold, white) + rank badge (small Badge with rankTitle Korean string).
    - 8-stat bars: for each stat in [leadership, command, intelligence, politics, administration, mobility, attack, defense], show: Korean label (6chars padded) + progress bar (value/100 fill, color: leadership→yellow, command→orange, intelligence→cyan, politics→green, administration→purple, mobility→sky, attack→red, defense→blue) + value number.
    - Bottom row: "직무카드 {positionCards.length}장" summary text.

    Create cp-display.tsx:
    - Props: pcpCurrent, pcpMax, mcpCurrent, mcpMax, regenRate
    - Two rows: PCP row (label "PCP 정략" + progress bar blue + "{pcpCurrent}/{pcpMax}") and MCP row (label "MCP 군사" + progress bar orange + "{mcpCurrent}/{mcpMax}").
    - Below bars: small text "5분마다 {regenRate}씩 회복" in gray.
    - Poll or subscribe to officer updates every 10s to refresh CP values.

    Create position-card-panel.tsx:
    - Props: positionCards (string[]), selectedGroup (CommandGroup | null), onSelectGroup (CommandGroup | null) → void
    - CommandGroup Korean labels: { OPERATION:'작전', PERSONAL:'개인', COMMAND:'지휘', LOGISTICS:'병참', PERSONNEL:'인사', POLITICS:'정치', INTELLIGENCE:'첩보' }
    - Determine which groups the officer has cards for: scan positionCards against a CARD_GROUP_MAP (hardcoded based on commands.json conventions: cards starting with 'OP_' → OPERATION, 'PER_' → PERSONAL, etc., or use a lookup from commandStore).
    - Render tab buttons for each available group. Active tab: bg-blue-700. Inactive: bg-slate-800.
    - Each tab button shows: group Korean label + card count badge.
    - "전체" tab at start (selectedGroup=null) shows all commands.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "officer-info-panel|cp-display|position-card-panel" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - OfficerInfoPanel shows 8 stat bars with Korean labels
    - CpDisplay shows PCP/MCP progress bars with current/max
    - PositionCardPanel shows group tabs matching officer's held cards
    - All three components compile without errors
  </acceptance_criteria>
  <done>Three panel components render officer stats, CP pools, and position card group tabs</done>
</task>

<task type="auto">
  <name>Task 2: CommandExecutionPanel and wire into commands/page.tsx</name>
  <files>
    frontend/src/components/game/command-execution-panel.tsx
    frontend/src/app/(game)/commands/page.tsx
  </files>
  <read_first>
    frontend/src/app/(game)/commands/page.tsx (current full implementation)
    frontend/src/components/game/command-panel.tsx (existing command panel to replace)
    frontend/src/stores/commandStore.ts (executeCommand, commands list)
    frontend/src/lib/websocket.ts (WebSocket publish pattern)
  </read_first>
  <action>
    Create command-execution-panel.tsx:
    - Props: commands (CommandEntry[]), officerId (number), sessionId (number), pcpCurrent, mcpCurrent, onResult (result: CommandResult) → void
    - Render a scrollable list of CommandEntry items filtered by active group.
    - Each command row: name (Korean from commands.json) | CP cost badge (PCP=blue, MCP=orange, value={cpCost}) | cooldown text ("{cooldown}초 대기") | Execute button.
    - Execute button disabled if: officer lacks CP (pcpCurrent < cpCost for PCP commands, mcpCurrent < cpCost for MCP) OR command is on cooldown.
    - On Execute click: call commandStore.executeCommand(sessionId, command.code, {}). Show loading spinner on button. On result: call onResult, show toast (sonner) with result.log[0] message.
    - If command has required args (from CommandEntry.argSchema), show a minimal arg form using react-hook-form with zod (text inputs for each arg field). Execute sends args object.
    - Command cooldown tracking: store cooldown end times in local state Map<commandCode, timestamp>. Poll Date.now() to re-enable buttons.

    Rewrite commands/page.tsx layout:
    - Left sidebar (w-64, fixed): OfficerInfoPanel + CpDisplay (stacked vertically)
    - Center main area (flex-grow):
      - PositionCardPanel tabs at top (selecting group filters commands)
      - CommandExecutionPanel below (scrollable command list for active group)
    - Right sidebar (w-48): Co-located officers ("동스폿 장교") list — fetch GET /api/{sessionId}/officers?planetId={myOfficer.planetId}, show officer name + rank. Click to navigate to /generals/{id}.
    - Keep existing ServerClock in header area.
    - Remove old drag-select, preset save/load UI (samguk era command queue — no longer needed).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "command-execution-panel|commands/page" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Commands page: left sidebar (officer info + CP), center (card tabs + command list), right (co-located officers)
    - Executing a command publishes to WebSocket and shows toast result
    - Buttons disabled when insufficient CP
    - Cooldown countdown disables/re-enables buttons
    - Co-located officers list shows officers on same planet
  </acceptance_criteria>
  <done>Commands page implements complete position card → command → execute flow with real-time CP state</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- Officer 8-stat bars display in Korean with color-coded progress (FE-04)
- Position card tabs filter command list by group (FE-05)
- CP display shows PCP/MCP with current/max values
- Command execution publishes via WebSocket to /app/command/{sessionId}/execute
- Toast shows command result from backend
- Co-located officers (동스폿) list shown in right panel
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-05-SUMMARY.md`
</output>
