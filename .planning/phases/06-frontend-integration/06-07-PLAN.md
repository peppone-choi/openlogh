---
phase: 06-frontend-integration
plan: "07"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/app/(game)/politics/page.tsx
  - frontend/src/components/game/empire-politics-panel.tsx
  - frontend/src/components/game/alliance-politics-panel.tsx
  - frontend/src/components/game/fezzan-politics-panel.tsx
  - frontend/src/stores/politicsStore.ts
autonomous: true
requirements:
  - FE-07
must_haves:
  truths:
    - "Politics page shows faction-specific UI based on myOfficer.factionType"
    - "Empire UI shows: 쿠데타 단계 indicator, 귀족 작위 목록, 봉토 현황"
    - "Alliance UI shows: 의회 의석 현황, 선거 일정 및 투표, 민주주의 지지도"
    - "Fezzan UI shows: 차관 현황 (대출금/이자/상환일), 정보 거래 목록"
    - "Executing politics commands (야회, 연설 등) goes through CommandExecutionPanel pattern"
  artifacts:
    - path: "frontend/src/components/game/empire-politics-panel.tsx"
      provides: "Empire faction politics: coup phase, nobility ranks, fiefs"
      exports: ["EmpirePoliticsPanel"]
    - path: "frontend/src/components/game/alliance-politics-panel.tsx"
      provides: "Alliance faction politics: council seats, elections, democracy index"
      exports: ["AlliancePoliticsPanel"]
    - path: "frontend/src/components/game/fezzan-politics-panel.tsx"
      provides: "Fezzan NPC panel: loan status, intelligence market"
      exports: ["FezzanPoliticsPanel"]
  key_links:
    - from: "frontend/src/app/(game)/politics/page.tsx"
      to: "faction-specific panel via factionType switch"
      via: "myOfficer.factionId → faction.faction_type"
      pattern: "faction_type|factionType"
    - from: "frontend/src/components/game/empire-politics-panel.tsx"
      to: "GET /api/{sessionId}/politics/empire"
      via: "axios fetch on mount"
      pattern: "politics.*empire"
---

<objective>
Implement faction-specific politics UI: Empire (coup phase, nobility, fiefs), Alliance (council seats, elections), Fezzan (loan status, intelligence market). The politics page switches between panels based on the player's faction type.

Purpose: Each faction has distinct political mechanics from gin7. The UI must surface these per-faction systems so players can engage with the political simulation.
Output: EmpirePoliticsPanel, AlliancePoliticsPanel, FezzanPoliticsPanel — switched in politics/page.tsx by faction_type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- politicsStore (existing): -->
```typescript
// factionPolitics: FactionPolitics | null
// loading: boolean
// fetchPolitics(sessionId): Promise<void>
```

<!-- Empire politics data (from backend Phase 2 CMD-06): -->
// CoupPhase: STABLE | BREWING | ESCALATED | CIVIL_WAR
// NobilityRank: BARON | VISCOUNT | COUNT | MARQUIS | DUKE | ARCHDUKE
// Fief: { planetId, officerName, revenue }
// Backend: GET /api/{sessionId}/politics/empire → { coupPhase, nobilityRanks: [{officerId, rank, fiefs}] }

<!-- Alliance politics data: -->
// CouncilSeatCode: DEFENSE | FOREIGN | INTERNAL | FINANCE | INTELLIGENCE | FLEET_CMD | GROUND_CMD
// ElectionType: PARLIAMENTARY | PRESIDENTIAL
// Backend: GET /api/{sessionId}/politics/alliance → { seats: [{code, holderId, holderName}], elections: [{type, scheduledDate, candidates}], democracyIndex: number }

<!-- Fezzan politics data: -->
// Backend: GET /api/{sessionId}/politics/fezzan → { loans: [{factionId, amount, interestRate, dueDate}], intelligenceItems: [{code, description, price}] }

<!-- Faction type from officer: myOfficer → factionId → fetch faction → faction.faction_type -->
<!-- faction_type: 'empire' | 'alliance' | 'fezzan' | 'rebel' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmpirePoliticsPanel and AlliancePoliticsPanel</name>
  <files>
    frontend/src/components/game/empire-politics-panel.tsx
    frontend/src/components/game/alliance-politics-panel.tsx
  </files>
  <read_first>
    frontend/src/app/(game)/politics/page.tsx (current implementation)
    frontend/src/stores/politicsStore.ts (current politics store)
  </read_first>
  <action>
    Create empire-politics-panel.tsx:
    - Props: sessionId (number), officerId (number)
    - Fetch GET /api/{sessionId}/politics/empire on mount (axios). Show LoadingState while loading.
    - CoupPhase indicator at top: colored badge with label.
      - STABLE → green "안정", BREWING → yellow "긴장", ESCALATED → orange "격화", CIVIL_WAR → red "내전"
      - Brief description below badge (e.g. BREWING: "쿠데타 세력이 결집하고 있습니다")
    - Nobility ranks section ("귀족 작위"):
      - Table with columns: 장교명, 작위 (NobilityRank Korean: BARON→남작, VISCOUNT→자작, COUNT→백작, MARQUIS→후작, DUKE→공작, ARCHDUKE→대공), 봉토 수
      - Each row expandable: shows fief planets (planetName + revenue 자금)
    - My rank display: if myOfficer has a nobility rank, show prominent badge at top
    - Politics commands shortcut: "정치 커맨드 실행" button → navigate to /commands?group=POLITICS

    Create alliance-politics-panel.tsx:
    - Props: sessionId (number), officerId (number)
    - Fetch GET /api/{sessionId}/politics/alliance on mount.
    - Democracy index bar: "민주주의 지수" + progress bar (democracyIndex/100, blue). Value + label ("높음"/"보통"/"낮음").
    - Council seats section ("의회 의석"):
      - Grid of seat cards. Each card: seat Korean name (DEFENSE→국방, FOREIGN→외교, INTERNAL→내무, FINANCE→재무, INTELLIGENCE→정보, FLEET_CMD→우주군사령, GROUND_CMD→지상군사령) + holder name or "공석" (vacant, gray).
      - If myOfficer holds a seat: highlight in blue border.
    - Elections section ("선거 일정"): list of upcoming elections with type label (PARLIAMENTARY→의회선거, PRESIDENTIAL→최고평의원선거) + scheduled date + candidate count. "투표 참가" button (stub: toast "투표 기능 준비 중").

    Styling: Both panels use dark card backgrounds, gin7 color palette (empire blue, alliance red where appropriate), Korean text throughout.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "empire-politics|alliance-politics" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - EmpirePoliticsPanel shows coup phase badge and nobility rank table in Korean
    - AlliancePoliticsPanel shows democracy index bar and council seat grid in Korean
    - Both panels fetch data from correct API endpoints
    - No TypeScript errors
  </acceptance_criteria>
  <done>Empire and Alliance politics panels compile with faction-specific UI in Korean</done>
</task>

<task type="auto">
  <name>Task 2: FezzanPoliticsPanel and politics/page.tsx router</name>
  <files>
    frontend/src/components/game/fezzan-politics-panel.tsx
    frontend/src/app/(game)/politics/page.tsx
    frontend/src/stores/politicsStore.ts
  </files>
  <read_first>
    frontend/src/app/(game)/politics/page.tsx (full current implementation)
    frontend/src/stores/politicsStore.ts (full store)
    frontend/src/stores/officerStore.ts (myOfficer.factionId access)
  </read_first>
  <action>
    Create fezzan-politics-panel.tsx:
    - Props: sessionId (number)
    - Fetch GET /api/{sessionId}/politics/fezzan on mount.
    - Note: Fezzan is NPC-only (no player belongs to Fezzan). This panel shows Fezzan-related info visible to all players.
    - Loan status section ("페잔 차관 현황"):
      - Table: 진영명 | 차관액 | 이자율 | 상환기한 | 상환여부
      - Overdue loans highlighted in red. Fezzan Ending triggered indicator if any faction is overdue.
    - Intelligence market section ("정보 거래"):
      - List of available intel items: item description + price (자금). "구매" button per item (stub if no backend endpoint: toast "페잔 정보 구매 준비 중").
    - Header note: "페잔 자치령은 NPC 전용 진영입니다. 차관 및 정보 거래만 가능합니다."

    Rewrite politics/page.tsx:
    - Fetch myOfficer.factionId → fetch GET /api/{sessionId}/factions/{factionId} to get faction_type.
    - Store faction in local state.
    - Switch on faction_type:
      - 'empire' → render EmpirePoliticsPanel
      - 'alliance' → render AlliancePoliticsPanel
      - 'fezzan' → render FezzanPoliticsPanel (fallback: also show for any faction as Fezzan overview)
      - 'rebel' → render simple card "반란군은 독립 정치 체계 없음. 커맨드를 통해 활동하세요."
    - Page header: "진영 정치" with faction name subtitle.
    - Quick nav tabs at top: [제국 정치 / 동맹 정치 / 페잔 현황] — clicking switches panel regardless of own faction (read-only for other factions — stub: just show FezzanPoliticsPanel for페잔 tab).

    Update politicsStore.ts:
    - Add factionType: string | null field
    - Add fetchFactionType(sessionId, factionId) action
    - Ensure existing fetchPolitics is called on page load
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "fezzan-politics|politics/page|politicsStore" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - politics/page.tsx shows faction-appropriate panel based on faction_type
    - FezzanPoliticsPanel shows loan status table and intel market
    - Tab nav allows switching between faction politics views
    - politicsStore updated with factionType field
    - No TypeScript errors
  </acceptance_criteria>
  <done>Politics page routes to faction-specific panel; all three faction panels render in Korean</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- Empire politics panel shows coup phase, nobility ranks, fiefs in Korean (FE-07)
- Alliance politics panel shows democracy index, council seats, elections in Korean
- Fezzan panel shows loan status and intel market
- politics/page.tsx switches panel based on faction_type
- Tab navigation shows all three faction politics panels
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-07-SUMMARY.md`
</output>
