---
phase: 06-frontend-integration
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/app/(game)/tactical/page.tsx
  - frontend/src/components/tactical/BattleMap.tsx
  - frontend/src/components/tactical/TacticalUnitIcon.tsx
  - frontend/src/components/tactical/CommandRangeCircle.tsx
  - frontend/src/components/tactical/InfoPanel.tsx
  - frontend/src/components/tactical/MiniMap.tsx
autonomous: true
requirements:
  - FE-02
must_haves:
  truths:
    - "Tactical map shows unit icons using gin7 dot shapes: △ flagship, □ battleship/cruiser, ◇ destroyer"
    - "Each unit icon is colored by faction: empire=#4466ff, alliance=#ff4444"
    - "Selected unit shows blue command range circle expanding over time"
    - "Top-right minimap shows orange/black coordinate grid of the star system"
    - "Bottom-right info panel shows: faction name, turn count, UC/RC date, star system name, commander + rank"
    - "Top toolbar has buttons: 작전조회, 함대정보, 성계정보, 자함대, 해결"
  artifacts:
    - path: "frontend/src/components/tactical/TacticalUnitIcon.tsx"
      provides: "Konva shape icon for each ship class (△□◇ + letter)"
      exports: ["TacticalUnitIcon"]
    - path: "frontend/src/components/tactical/CommandRangeCircle.tsx"
      provides: "Animated expanding circle for selected unit"
      exports: ["CommandRangeCircle"]
    - path: "frontend/src/components/tactical/InfoPanel.tsx"
      provides: "Bottom-right panel with battle metadata"
      exports: ["InfoPanel"]
    - path: "frontend/src/components/tactical/MiniMap.tsx"
      provides: "Top-right orange/black star system minimap"
      exports: ["MiniMap"]
  key_links:
    - from: "frontend/src/components/tactical/BattleMap.tsx"
      to: "frontend/src/stores/tacticalStore.ts"
      via: "useTacticalStore hook"
      pattern: "useTacticalStore"
    - from: "frontend/src/components/tactical/TacticalUnitIcon.tsx"
      to: "shipClass shape mapping"
      via: "SHIP_SHAPE map"
      pattern: "SHIP_SHAPE|shipClass"
---

<objective>
Implement the gin7-style tactical strategic map UI — the 2D battle field view with dot-style ship icons, command range circle, minimap, and info panel.

Purpose: The tactical map is the main interface during fleet battles. Unit shapes must follow gin7 conventions (△□◇) and the command range circle must visually expand to show command authority radius.
Output: BattleMap.tsx with TacticalUnitIcon shapes, CommandRangeCircle animation, MiniMap, InfoPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- From tacticalStore (existing, Phase 3 aligned): -->
```typescript
// units: TacticalUnit[] — each unit has position {x,y}, shipClass, factionId, ships, morale
// selectedUnitId: number | null
// commandRange: { radius: number; maxRadius: number } per officer
// recentEvents: BattleEvent[]
// currentBattle: TacticalBattle | null
```

Ship class → gin7 icon mapping:
- flagship         → △ (RegularPolygon sides=3) + no letter
- battleship       → □ (Rect) + "B"
- fast_battleship  → □ + "B" (제국)
- cruiser          → □ + "C"
- strike_cruiser   → □ + "S" (동맹)
- destroyer        → ◇ (Rect rotated 45°) + "D"
- fighter_carrier  → △ + "F"
- torpedo_carrier  → △ + "F" (제국)
- carrier          → △ + "A"
- engineering      → □ + "E"
- transport        → □ + "T"
- hospital         → □ + "H"
- landing          → □ + "L"
- civilian         → □ + "M"

Faction colors: { empire: '#4466ff', alliance: '#ff4444', neutral: '#888888' }
Icon size: 16px base, flagship 20px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TacticalUnitIcon and CommandRangeCircle components</name>
  <files>
    frontend/src/components/tactical/TacticalUnitIcon.tsx
    frontend/src/components/tactical/CommandRangeCircle.tsx
  </files>
  <read_first>
    frontend/src/components/tactical/BattleMap.tsx (existing, to understand current unit rendering)
    frontend/src/types/tactical.ts (TacticalUnit type)
  </read_first>
  <action>
    Create TacticalUnitIcon.tsx as a React Konva component:
    - Accept props: unit (TacticalUnit), x, y, isSelected, onClick
    - SHIP_SHAPE map: flagship→'triangle', battleship/fast_battleship/cruiser/strike_cruiser/engineering/transport/hospital/landing/civilian→'square', destroyer→'diamond', fighter_carrier/torpedo_carrier/carrier→'triangle'
    - SHIP_LETTER map: flagship→'', battleship→'B', fast_battleship→'B', cruiser→'C', strike_cruiser→'S', destroyer→'D', fighter_carrier→'F', torpedo_carrier→'F', carrier→'A', engineering→'E', transport→'T', hospital→'H', landing→'L', civilian→'M'
    - Render: Group at (x, y). For 'triangle': RegularPolygon sides=3 radius=10. For 'square': Rect width=16 height=16 offset={x:8,y:8}. For 'diamond': Rect width=12 height=12 rotation=45 offset={x:6,y:6}.
    - Fill = faction color (empire=#4466ff, alliance=#ff4444). Stroke='white', strokeWidth=0.5.
    - Letter: Text centered on shape, fill='white', fontSize=8, fontStyle='bold'.
    - If isSelected: outer glow circle (Circle radius=14, stroke='white', opacity=0.6, dash=[2,2]).
    - Damaged (ships < 50% max): fill opacity 0.5.

    Create CommandRangeCircle.tsx as a React Konva component:
    - Accept props: x, y, radius, maxRadius, factionId
    - Render Circle at (x,y), radius=radius, no fill, stroke=faction color, strokeWidth=1, opacity=0.4
    - Second Circle at maxRadius: stroke=faction color, strokeWidth=0.5, dash=[4,4], opacity=0.2
    - Use Konva animation (Konva.Animation) to expand radius from 0 to maxRadius over 3s then reset — use useEffect + konva anim ref pattern
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "TacticalUnitIcon|CommandRangeCircle" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - TacticalUnitIcon renders correct shape per ship class
    - Diamond shape for destroyers, triangle for flagships/carriers, square for others
    - Letter label centered in shape
    - CommandRangeCircle animates radius expansion
  </acceptance_criteria>
  <done>Both components compile, shape/letter mapping covers all 14 ship classes</done>
</task>

<task type="auto">
  <name>Task 2: BattleMap with MiniMap and InfoPanel — gin7 tactical layout</name>
  <files>
    frontend/src/components/tactical/BattleMap.tsx
    frontend/src/components/tactical/MiniMap.tsx
    frontend/src/components/tactical/InfoPanel.tsx
    frontend/src/app/(game)/tactical/page.tsx
  </files>
  <read_first>
    frontend/src/components/tactical/BattleMap.tsx (current implementation)
    frontend/src/app/(game)/tactical/page.tsx (current page layout)
    frontend/src/stores/tacticalStore.ts (battle state)
  </read_first>
  <action>
    Update BattleMap.tsx (React Konva):
    - Background: Rect fill='#000008' (near-black space). StarField layer (reuse from galaxy or recreate: 200 random Circle r=0.5-1.5 fill='white').
    - Render TacticalUnitIcon for each unit from tacticalStore.units, at unit.position.x * gridScale, unit.position.y * gridScale.
    - If a unit is selected AND has a commandRange, render CommandRangeCircle at unit position.
    - Grid: faint blue lines every 50px (Line with stroke='#1a2040', opacity=0.3).
    - Click on unit: call selectUnit(unit.id) in tacticalStore.
    - Background click: deselect.

    Create MiniMap.tsx (pure React, not Konva — HTML div with CSS):
    - Container: fixed 120x120px, background='#000', border='1px solid #ff8800'.
    - Draw: orange dot for each unit at proportional position. Player units = brighter orange (#ff8800), enemy = dim (#884400).
    - Show a white rectangle representing the current viewport area.
    - Title: "성계 미니맵" above in orange text, 9px.

    Create InfoPanel.tsx (pure React):
    - Container: dark panel (#0d0d1a), border='1px solid #333', padding 8px, width 200px.
    - Sections:
      1. Faction name + 입력 턴 number (from currentBattle.turnNumber)
      2. UC/RC date display: "UC {year}년 {month}월" from world time
      3. 성계명 (star system name from currentBattle.starSystemName)
      4. 작전명 (operation name from currentBattle.operationName ?? '미지정')
      5. 작전총사령관 name + rank title
      6. 작전공적 (merit points), 총물자량 (total supplies)
    - All labels in Korean. Dark text on dark bg, values in light gray.

    Update tactical/page.tsx layout:
    - Toolbar row at top: Button group [작전조회, 함대정보, 성계정보, 자함대, 해결] — styled as dark buttons, 8-bit style.
    - Main area: BattleMap fills remaining height.
    - MiniMap: absolute top-right corner overlaid on BattleMap (z-index 10).
    - InfoPanel: absolute bottom-right corner overlaid on BattleMap (z-index 10).
    - Keep existing EnergyPanel and FormationSelector below/aside the map.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "BattleMap|MiniMap|InfoPanel|tactical/page" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - BattleMap renders units as gin7 dot icons on dark space background
    - MiniMap appears top-right with orange/black coordinate view
    - InfoPanel appears bottom-right with Korean battle metadata
    - Toolbar has 5 buttons in Korean
    - No TypeScript errors
  </acceptance_criteria>
  <done>Tactical page has gin7-style layout: toolbar + battle map + minimap overlay + info panel overlay</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- Tactical map shows △□◇ unit icons colored by faction (FE-02)
- Command range circle expands from selected unit position
- MiniMap shows orange/black coordinate view top-right
- InfoPanel shows battle metadata bottom-right in Korean
- Toolbar has correct 5 action buttons
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-03-SUMMARY.md`
</output>
