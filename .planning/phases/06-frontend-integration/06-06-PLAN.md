---
phase: 06-frontend-integration
plan: "06"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/app/(game)/troop/page.tsx
  - frontend/src/app/(game)/city/page.tsx
  - frontend/src/components/game/fleet-unit-card.tsx
  - frontend/src/components/game/fleet-composition-panel.tsx
  - frontend/src/components/game/planet-resource-panel.tsx
  - frontend/src/components/game/warehouse-transfer-panel.tsx
autonomous: true
requirements:
  - FE-06
must_haves:
  truths:
    - "Fleet management page shows fleet units with ship class, ship count, morale, training in Korean"
    - "Fleet composition panel lists all units (최대 8개 부대) with add/remove controls"
    - "Planet page shows gin7 resource fields: 인구, 생산, 교역, 치안, 지지도, 궤도방어, 요새방어"
    - "Warehouse transfer panel allows moving supplies between 행성창고 and 부대창고"
    - "Shipyard production section shows current auto-production queue per planet"
  artifacts:
    - path: "frontend/src/components/game/fleet-unit-card.tsx"
      provides: "Single fleet unit display card with gin7 fields"
      exports: ["FleetUnitCard"]
    - path: "frontend/src/components/game/fleet-composition-panel.tsx"
      provides: "Full fleet composition editor with unit list"
      exports: ["FleetCompositionPanel"]
    - path: "frontend/src/components/game/planet-resource-panel.tsx"
      provides: "Planet resource display with gin7 field names"
      exports: ["PlanetResourcePanel"]
    - path: "frontend/src/components/game/warehouse-transfer-panel.tsx"
      provides: "행성창고↔부대창고 transfer UI"
      exports: ["WarehouseTransferPanel"]
  key_links:
    - from: "frontend/src/components/game/warehouse-transfer-panel.tsx"
      to: "POST /api/{sessionId}/warehouse/transfer"
      via: "axios call"
      pattern: "warehouse.*transfer"
    - from: "frontend/src/app/(game)/troop/page.tsx"
      to: "frontend/src/components/game/fleet-composition-panel.tsx"
      via: "direct render"
      pattern: "FleetCompositionPanel"
---

<objective>
Rewrite fleet management (troop) and planet (city) pages with gin7 field names and gin7 mechanics: fleet unit cards with ship classes, fleet composition editor, planet resource panel with 7 gin7 fields, and warehouse transfer UI.

Purpose: Players need to manage their fleet composition and planet resources using gin7 terminology. The current pages use samguk field names (agri, pop, gold, rice).
Output: FleetUnitCard, FleetCompositionPanel, PlanetResourcePanel, WarehouseTransferPanel — plus rewritten troop/page.tsx and city/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- Fleet (gin7) fields: id, name, factionId, leaderOfficerId, ships, shipClass, morale, training, formation, meta -->
<!-- FleetUnit sub-object: unitNumber (1-8), shipClass, ships, shipsMax, morale, training, shipSubtype, officerId -->

<!-- Planet (gin7) fields: -->
// population, production, commerce, security, approval, orbital_defense, fortress, trade_route
// supplyState (0=blockaded, 1=normal)

<!-- Warehouse API (from Phase 4): -->
// POST /api/{sessionId}/warehouse/transfer
// body: { fleetId, planetId, direction: 'TO_FLEET'|'TO_PLANET', resourceType: 'funds'|'supplies', amount }
// GET /api/{sessionId}/warehouse/{planetId} → { planetSupplies, planetFunds, fleetSupplies }

<!-- Shipyard API: -->
// GET /api/{sessionId}/planets/{planetId}/shipyard → { productionQueue: [{shipClass, count, turnsRemaining}] }

<!-- Ship class Korean names: -->
// battleship→전함, fast_battleship→고속전함, cruiser→순양함, strike_cruiser→타격순항함
// destroyer→구축함, fighter_carrier→전투정모함, torpedo_carrier→뇌격정모함
// carrier→항모, engineering→공작함, transport→수송함, hospital→병원선
// landing→양륙함, civilian→민간선

<!-- Formation Korean: SPINDLE→방추형, BY_CLASS→함종별, MIXED→혼성, THREE_COLUMN→삼열종대 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FleetUnitCard, FleetCompositionPanel — fleet management UI</name>
  <files>
    frontend/src/components/game/fleet-unit-card.tsx
    frontend/src/components/game/fleet-composition-panel.tsx
    frontend/src/app/(game)/troop/page.tsx
  </files>
  <read_first>
    frontend/src/app/(game)/troop/page.tsx (current full implementation)
    frontend/src/stores/generalStore.ts (existing fleet/troop state to understand)
  </read_first>
  <action>
    Create fleet-unit-card.tsx:
    - Props: unit (FleetUnit), unitNumber (number), onRemove?: () => void
    - Card layout (dark, border, compact):
      - Header: "{unitNumber}번 부대" + ship class Korean name + faction color dot
      - Row: "함종" label + shipClass label | "함수" label + "{ships}/{shipsMax}"
      - Row: "사기" + progress bar (morale/100, yellow fill) + value
      - Row: "훈련도" + progress bar (training/100, green fill) + CrewProficiency label (elite→정예, veteran→노련, normal→보통, green→신병)
      - If onRemove: small "제거" button top-right corner
    - Border color: faction-colored left border (4px)

    Create fleet-composition-panel.tsx:
    - Props: fleet (Fleet), units (FleetUnit[]), sessionId (number)
    - Header: fleet name + officer name + rank + formation badge (Formation Korean label)
    - Formation selector: 4 radio buttons [방추형, 함종별, 혼성, 삼열종대], sends PATCH /api/{sessionId}/fleets/{fleet.id}/formation
    - Unit list: FleetUnitCard for each unit (unitNumber 1-8). Empty slots shown as dashed placeholder card "빈 부대 슬롯".
    - Stats summary bar: total ships count, average morale, average training
    - "부대 편성 변경" button → navigates to unit assignment sub-page (stub: toast "편성 변경 준비 중")

    Rewrite troop/page.tsx:
    - Fetch GET /api/{sessionId}/fleets?officerId={myOfficer.id} for officer's fleets
    - Fetch units for each fleet: GET /api/{sessionId}/fleets/{fleetId}/units
    - Render FleetCompositionPanel for each fleet (tabbed if multiple fleets)
    - Page title: "함대 편성"
    - Remove all samguk-era references (troop, 부대병종, etc.)
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "fleet-unit-card|fleet-composition|troop/page" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - FleetUnitCard shows gin7 fields (함수, 사기, 훈련도) in Korean
    - FleetCompositionPanel lists up to 8 unit slots with formation selector
    - troop/page.tsx title is "함대 편성" with no samguk terms
  </acceptance_criteria>
  <done>Fleet management page shows gin7 unit cards and fleet composition editor</done>
</task>

<task type="auto">
  <name>Task 2: PlanetResourcePanel, WarehouseTransferPanel — planet/city page</name>
  <files>
    frontend/src/components/game/planet-resource-panel.tsx
    frontend/src/components/game/warehouse-transfer-panel.tsx
    frontend/src/app/(game)/city/page.tsx
  </files>
  <read_first>
    frontend/src/app/(game)/city/page.tsx (current full implementation)
    frontend/src/components/game/city-basic-card.tsx (samguk component to understand and replace)
  </read_first>
  <action>
    Create planet-resource-panel.tsx:
    - Props: planet (Planet), shipyardQueue?: { shipClass: string; count: number; turnsRemaining: number }[]
    - Layout: dark card, full width.
    - Header: planet name (bold) + faction badge + supplyState indicator ("보급정상" green / "보급차단" red)
    - Resource grid (2-col): 
      - 인구: value formatted with K/M suffix (population)
      - 생산력: value bar (production/50000, cyan)
      - 교역: value bar (commerce/50000, yellow)
      - 치안: value bar (security/100, blue)
      - 지지도: value bar (approval/100, green)
      - 궤도방어: value + fortress-icon (orange)
      - 요새방어: value + shield-icon (red)
    - Shipyard section: "조병창 생산 현황" header. List each queue entry: "{Korean shipClass} × {count} ({turnsRemaining}턴 후 완료)". If empty: "생산 대기 없음".

    Create warehouse-transfer-panel.tsx:
    - Props: planetId (number), fleetId (number | null), sessionId (number)
    - State: amounts { funds: number; supplies: number }, direction: 'TO_FLEET'|'TO_PLANET'
    - Fetch warehouse state on mount: GET /api/{sessionId}/warehouse/{planetId}
    - Display: two rows (자금, 물자) each showing: 행성창고 value → [방향 화살표 toggle] → 부대창고 value
    - Amount inputs (number input) + 이송 button per row
    - On submit: POST /api/{sessionId}/warehouse/transfer, show toast result
    - Direction toggle: clicking arrow flips 'TO_FLEET' ↔ 'TO_PLANET', arrow icon rotates

    Rewrite city/page.tsx:
    - Page title: "현재 행성" (was "현재 성")
    - Fetch GET /api/{sessionId}/planets/{myOfficer.planetId}
    - Render PlanetResourcePanel with planet data
    - Render WarehouseTransferPanel below if myOfficer.fleetId is not null
    - Show stationed officers list: GET /api/{sessionId}/officers?planetId={planetId} → compact officer list (name + rank)
    - Remove all city/agri/pop/comm samguk field references
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "planet-resource|warehouse-transfer|city/page" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - PlanetResourcePanel shows all 7 gin7 fields in Korean with progress bars
    - Shipyard queue section shows production status
    - WarehouseTransferPanel has direction toggle and amount inputs
    - city/page.tsx title is "현재 행성" with no city/agri/pop terms
  </acceptance_criteria>
  <done>Planet page shows gin7 resource panel and warehouse transfer UI in Korean</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- Fleet management page uses gin7 fleet unit cards with ship classes, morale, training (FE-06)
- Planet page shows 7 gin7 resource fields with Korean labels
- Warehouse transfer panel posts to /api/{sessionId}/warehouse/transfer
- Shipyard production queue visible on planet page
- No samguk terms (troop/city/agri/pop/gold) visible in these pages
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-06-SUMMARY.md`
</output>
