---
phase: 06-frontend-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/galaxy/StarSystemNode.tsx
  - frontend/src/components/galaxy/GalaxyMap.tsx
  - frontend/src/components/galaxy/StarSystemDetailPanel.tsx
  - frontend/src/components/galaxy/FleetPositionMarker.tsx
  - frontend/src/stores/galaxyStore.ts
  - frontend/src/types/galaxy.ts
autonomous: true
requirements:
  - FE-01
must_haves:
  truths:
    - "Galaxy map displays dot-style star system icons with 5-shade faction color gradient"
    - "Empire systems shown in blue (#4466ff) variants, Alliance in red (#ff4444) variants"
    - "Fleet positions visible as small markers on their star system node"
    - "Fleet movement range highlighted when a fleet is selected"
    - "Clicking a system opens detail panel showing stationed fleets and planet info"
    - "Route connections shown as lines between systems"
  artifacts:
    - path: "frontend/src/components/galaxy/StarSystemNode.tsx"
      provides: "Dot-style star icon with 5-shade faction color"
      exports: ["StarSystemNode"]
    - path: "frontend/src/components/galaxy/FleetPositionMarker.tsx"
      provides: "Fleet icon overlaid on star system node"
      exports: ["FleetPositionMarker"]
    - path: "frontend/src/components/galaxy/StarSystemDetailPanel.tsx"
      provides: "Panel showing planet list, stationed fleets, facilities"
      exports: ["StarSystemDetailPanel"]
  key_links:
    - from: "frontend/src/components/galaxy/GalaxyMap.tsx"
      to: "frontend/src/stores/galaxyStore.ts"
      via: "useGalaxyStore hook"
      pattern: "useGalaxyStore"
    - from: "frontend/src/components/galaxy/StarSystemNode.tsx"
      to: "faction color logic"
      via: "factionId → 5-shade color map"
      pattern: "factionColor|shade"
---

<objective>
Enhance the existing React Konva galaxy map with gin7-style dot star icons, faction 5-shade coloring, fleet position markers, and movement range highlighting.

Purpose: The galaxy map is the primary strategic view. Players need to see faction control (color intensity = control strength), fleet positions, and movement range at a glance.
Output: Enhanced GalaxyMap with StarSystemNode dot icons, FleetPositionMarker, and updated StarSystemDetailPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- Existing GalaxyMap.tsx uses React Konva (Stage, Layer, Rect) — keep this infrastructure -->
<!-- Current StarSystemNode renders a circle — replace with dot-style icon -->

From frontend/src/types/galaxy.ts (to be updated by Plan 01, but this plan can run in parallel):
```typescript
export interface StarSystem {
  mapStarId: number;
  name: string;
  x: number;
  y: number;
  connections: number[];
  factionId?: number | null;      // NEW: controlling faction
  controlStrength?: number;       // NEW: 0-100, drives shade selection
  fleetPositions?: FleetPosition[]; // NEW: fleets stationed here
}

export interface FleetPosition {
  fleetId: number;
  officerName: string;
  ships: number;
  factionId: number;
}
```

Faction colors:
- empire (#4466ff): shades = ['#0a1233','#1a2d66','#2244aa','#3355cc','#4466ff']
- alliance (#ff4444): shades = ['#330a0a','#661a1a','#aa2222','#cc3333','#ff4444']
- neutral (#888888): shades = ['#1a1a1a','#333333','#555555','#777777','#999999']
- Shade index = Math.floor(controlStrength / 20), clamped 0-4

Fleet marker: small triangle shape (gin7 △ for flagship, □ for fleet) using Konva RegularPolygon or Rect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dot-style StarSystemNode with 5-shade faction color</name>
  <files>
    frontend/src/components/galaxy/StarSystemNode.tsx
    frontend/src/components/galaxy/FleetPositionMarker.tsx
  </files>
  <read_first>
    frontend/src/components/galaxy/StarSystemNode.tsx (current implementation)
    frontend/src/types/galaxy.ts (current StarSystem type)
    frontend/src/components/galaxy/StarRouteEdge.tsx (pattern reference)
  </read_first>
  <action>
    Rewrite StarSystemNode.tsx to render a dot-style gin7 star icon:

    Visual: outer glow circle (large, low opacity) + inner solid dot + system name label below.
    - Dot size: radius 8px base, +2px if selected, +1px if hovered
    - Color: determined by factionId + controlStrength via FACTION_SHADES map
    - FACTION_SHADES: { empire: ['#0a1233','#1a2d66','#2244aa','#3355cc','#4466ff'], alliance: ['#330a0a','#661a1a','#aa2222','#cc3333','#ff4444'], neutral: ['#1a1a1a','#333333','#555555','#777777','#999999'] }
    - shadeIdx = Math.floor((controlStrength ?? 50) / 20), clamped 0-4
    - Outer glow: same color, opacity 0.15, radius 18px
    - Selected ring: white stroke circle, radius 12px, strokeWidth 1.5
    - System name: Text below dot, fill '#cccccc', fontSize 10

    Props: system (StarSystem with factionId + controlStrength), isSelected, isHovered, onSelect, onHover.

    Create FleetPositionMarker.tsx: a small Konva RegularPolygon (triangle, sides=3, radius=5) drawn above the star dot, colored by fleet's factionId. Accept props: x, y, factionId, ships. Render at (x - 10 + index*10, y - 14) offset from parent system center. If multiple fleets, stack markers horizontally.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "StarSystemNode|FleetPositionMarker" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - StarSystemNode renders dot with faction-colored glow
    - 5 distinct shade levels visible based on controlStrength
    - Selected system has white ring
    - FleetPositionMarker renders triangle above system dot
  </acceptance_criteria>
  <done>StarSystemNode.tsx and FleetPositionMarker.tsx compile, dot-style with 5-shade faction colors</done>
</task>

<task type="auto">
  <name>Task 2: GalaxyMap fleet markers + movement range + detail panel</name>
  <files>
    frontend/src/components/galaxy/GalaxyMap.tsx
    frontend/src/components/galaxy/StarSystemDetailPanel.tsx
    frontend/src/stores/galaxyStore.ts
  </files>
  <read_first>
    frontend/src/components/galaxy/GalaxyMap.tsx (full current implementation)
    frontend/src/components/galaxy/StarSystemDetailPanel.tsx (current panel)
    frontend/src/stores/galaxyStore.ts (current store)
  </read_first>
  <action>
    Update GalaxyMap.tsx:
    1. Add FleetPositionMarker rendering in the star systems layer: after each StarSystemNode, render FleetPositionMarker for each system.fleetPositions entry.
    2. Add selectedFleetId state. When a fleet marker is clicked, set selectedFleetId.
    3. Movement range highlight: when selectedFleetId is set, fetch the fleet's star system and highlight all connected systems within 2 hops using a semi-transparent circle overlay (radius 20, fill faction color at 0.1 opacity, stroke faction color 0.4). Use StarSystem.connections for adjacency.
    4. Keep all existing zoom/pan/drag behavior unchanged.

    Update galaxyStore.ts:
    - Add fleetPositions: Record<number, FleetPosition[]> field (key = mapStarId)
    - Add fetchFleetPositions(sessionId) action that calls GET /api/{sessionId}/fleets/positions
    - Call fetchFleetPositions in fetchGalaxyMap after systems load
    - Add selectedFleetId: number | null and selectFleet(id) action

    Update StarSystemDetailPanel.tsx:
    - Replace any city/nation/general terminology with 행성/진영/장교
    - Add "주둔 함대" section showing fleetPositions for selected system
    - Each fleet entry: faction badge (colored dot), officer name, ship count
    - Add "행성" section with population, production fields (gin7 names)
    - Panel background: dark card with border, width 280px, positioned top-right of map
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -E "GalaxyMap|galaxyStore|StarSystemDetail" | head -10 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Fleet markers appear on systems that have stationed fleets
    - Selecting a fleet highlights movement range (connected systems glow)
    - Detail panel shows 주둔 함대 and 행성 info in Korean
    - No TypeScript errors in updated files
  </acceptance_criteria>
  <done>GalaxyMap renders fleet markers and movement range highlight; detail panel shows gin7 data in Korean</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
</verification>

<success_criteria>
- Galaxy map shows dot-style star icons in 5 faction color shades (FE-01)
- Fleet positions visible as triangle markers on their star system
- Fleet selection highlights movement range via connected systems
- Detail panel shows stationed fleet and planet info in Korean (no samguk terms)
- All React Konva code compiles with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-02-SUMMARY.md`
</output>
