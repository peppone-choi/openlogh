---
phase: 06-frontend-integration
plan: "08"
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/app/(game)/layout.tsx
  - frontend/src/components/game/city-basic-card.tsx
  - frontend/src/components/game/general-basic-card.tsx
  - frontend/src/components/game/nation-basic-card.tsx
  - frontend/src/components/game/crew-type-browser.tsx
  - frontend/src/components/game/map-3d/
  - frontend/src/app/(tutorial)/
autonomous: true
requirements:
  - FE-08
must_haves:
  truths:
    - "grep -r 'city-basic-card\\|general-basic-card\\|nation-basic-card\\|crew-type-browser' frontend/src returns 0 import references"
    - "grep -r 'map-3d' frontend/src returns 0 import references outside the deleted directory"
    - "grep -rn '\\bcity\\b\\|\\bnation\\b\\|\\btroop\\b\\|\\bgeneral\\b' frontend/src/types/index.ts returns only @deprecated aliases"
    - "grep -rn 'crewType\\|UnitModelType.*infantry\\|UnitModelType.*cavalry' frontend/src returns 0 results"
    - "pnpm typecheck passes with 0 errors"
  artifacts:
    - path: "frontend/src/app/(game)/layout.tsx"
      provides: "Updated nav using gin7 route/label names only"
      contains: "함대 편성"
    - path: "frontend/src/types/index.ts"
      provides: "Clean re-export file with no samguk primary types"
      exports: ["Officer", "Fleet", "Planet", "Faction"]
  key_links:
    - from: "frontend/src/app/(game)/layout.tsx"
      to: "navigation items"
      via: "navSections array"
      pattern: "navSections"
---

<objective>
Complete the legacy cleanup: delete samguk-era UI components, the samguk 3D map directory, samguk tutorial pages, and remove all remaining samguk terms from navigation labels and type aliases.

Purpose: FE-08 requires zero samguk terms in the codebase. Plans 01-07 added gin7 UI; this plan removes the old UI that Plans 01-07 made redundant.
Output: Deleted files, updated layout.tsx nav, cleaned type aliases, zero samguk terms in active code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-integration/06-CONTEXT.md

<interfaces>
<!-- Files to DELETE (samguk-era, replaced by gin7 equivalents): -->
// frontend/src/components/game/city-basic-card.tsx  → replaced by PlanetResourcePanel
// frontend/src/components/game/general-basic-card.tsx → replaced by OfficerInfoPanel
// frontend/src/components/game/nation-basic-card.tsx  → replaced by faction display in layout
// frontend/src/components/game/crew-type-browser.tsx  → samguk crew type browser (병종)
// frontend/src/components/game/equipment-browser.tsx  → samguk weapon/book/horse items
// frontend/src/components/game/map-3d/ (entire directory) → samguk castle 3D map
// frontend/src/app/(tutorial)/ (entire directory) → samguk tutorial pages
// frontend/src/components/game/deployment-selector.tsx → samguk troop deployment

<!-- Files to UPDATE (samguk nav terms → gin7 Korean): -->
// layout.tsx navSections: 
//   '/troop' label '함대 편성' (keep — already correct)
//   '/city' label '현재 행성' (was '현재 성')  
//   '/nation' label '진영 현황' (keep)
//   '/nation-cities' label '진영 행성' (was '진영 도시')
//   '/nation-generals' label '진영 장교' (keep)
//   '/generals' label '전체 장교' (was '전체 장수')
//   '/best-generals' label '명장 목록' (keep — already gin7 compatible)
//   '/emperor' label '원수/의장' (keep)
//   '/general' label '내 장교' (was '내 장수')
//   '/battle' label '전술전 목록' (was '전투 검토')

<!-- After deletion, these imports must also be removed from any referencing files -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete samguk component files and remove their imports</name>
  <files>
    frontend/src/components/game/city-basic-card.tsx
    frontend/src/components/game/general-basic-card.tsx
    frontend/src/components/game/nation-basic-card.tsx
    frontend/src/components/game/crew-type-browser.tsx
    frontend/src/components/game/equipment-browser.tsx
    frontend/src/components/game/deployment-selector.tsx
    frontend/src/components/game/map-3d/
    frontend/src/app/(tutorial)/
  </files>
  <read_first>
    frontend/src/components/game/general-basic-card.tsx (see what it exports — check for imports elsewhere)
    frontend/src/components/game/city-basic-card.tsx (same check)
  </read_first>
  <action>
    Step 1 — Audit imports before deletion. Run these greps and note which files import each component:
    - grep -rn "city-basic-card\|general-basic-card\|nation-basic-card\|crew-type-browser\|equipment-browser\|deployment-selector" frontend/src --include="*.tsx" --include="*.ts"
    - grep -rn "map-3d\|Map3dScene\|CityModel\|TerrainMesh\|SeasonEffects\|NationOverlay" frontend/src --include="*.tsx" --include="*.ts"

    Step 2 — For each file found importing the deleted components: remove the import line and replace any usage with the gin7 equivalent component or remove the usage if the page itself is being rewritten.

    Specific pages that import samguk components (based on code review):
    - Any page importing CityModel/Map3dScene: remove import, remove the <Map3dScene> or related JSX.
    - map/page.tsx: likely uses Map3dScene — replace with a note comment "<!-- gin7 은하맵은 /galaxy 페이지로 이동 -->" and redirect to /galaxy.
    - Any page importing general-basic-card: remove import, those pages should now use officer-info-panel if needed.

    Step 3 — Delete files using the Bash tool:
    - Delete individual component files (city-basic-card.tsx, general-basic-card.tsx, nation-basic-card.tsx, crew-type-browser.tsx, equipment-browser.tsx, deployment-selector.tsx)
    - Delete map-3d directory: rm -rf frontend/src/components/game/map-3d/
    - Delete tutorial directory: rm -rf frontend/src/app/(tutorial)/
    - Delete tutorial tests if any exist: find frontend/src -name "*.test.*" -path "*/tutorial/*" -delete

    Step 4 — Clean general-basic-card.test.tsx and nation-basic-card.test.tsx:
    - Delete test files for deleted components.

    Step 5 — Run tsc to confirm no dangling imports remain.
  </action>
  <verify>
    <automated>grep -rn "city-basic-card\|general-basic-card\|nation-basic-card\|crew-type-browser\|Map3dScene\|CityModel\|TerrainMesh" /Users/apple/Desktop/개인프로젝트/openlogh/frontend/src --include="*.tsx" --include="*.ts" | grep -v "test\." | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 0 import references to deleted samguk component files
    - map-3d directory no longer exists
    - tutorial directory no longer exists
    - tsc --noEmit shows no "cannot find module" errors for deleted files
  </acceptance_criteria>
  <done>All samguk-era component files deleted, all import references cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: Navigation cleanup, final term audit, and typecheck</name>
  <files>
    frontend/src/app/(game)/layout.tsx
    frontend/src/types/index.ts
  </files>
  <read_first>
    frontend/src/app/(game)/layout.tsx (current navSections array — full file)
    frontend/src/types/index.ts (after Plan 01 rewrites — verify @deprecated aliases remain clean)
  </read_first>
  <action>
    Update layout.tsx navSections:
    - '/city' label: change to '현재 행성' (was '현재 성' if samguk)
    - '/nation-cities' label: '진영 행성' (was '진영 도시' if samguk)
    - '/generals' label: '전체 장교' (ensure it's not '전체 장수')
    - '/general' route: change href to '/general' label '내 장교' (not '내 장수')
    - '/battle' label: '전술전 목록'
    - '/troop' href: keep, label '함대 편성'
    - '/board' label: '작전실' (keep)
    - Remove any nav items pointing to deleted tutorial pages
    - Remove any nav item for /battle-simulator if it uses samguk data
    - Add nav item: '/galaxy' label '은하 지도' in 진영 section (if not already present)
    - Update NavRequire type: remove 'secret' requirement that used officerLevel >= 5 (was samguk gating). Use position card presence check instead: keep type as-is but document the TODO.

    Final term audit — scan and fix remaining samguk terms:
    Run: grep -rn "\bcity\b\|\bnation\b\|\btroop\b\|\bgeneral\b\|\bcrewType\b\|\bUnitModelType\b" frontend/src/app frontend/src/components --include="*.tsx" --include="*.ts" -l

    For each file found:
    - If it's a page/component not yet rewritten (e.g. dynasty, auction, betting): these are non-critical pages — add a TODO comment "// TODO Phase 6: 삼국지 용어 제거" at top, do NOT rewrite content now (out of scope).
    - If it's a critical game flow file: fix the term inline (rename variable, update label).

    Verify types/index.ts:
    - Confirm @deprecated aliases (Nation, City, General, Troop) remain for backward compatibility.
    - Confirm NO new code in types/index.ts uses samguk field names as primary fields (only inside @deprecated type bodies).

    Final check: run pnpm typecheck from frontend directory and confirm 0 errors (or same count as before this phase — do not regress).
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" && grep -rn "현재 성\|진영 도시\|전체 장수\|내 장수" frontend/src/app/\(game\)/layout.tsx || echo "nav clean"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx nav labels use gin7 Korean terms only
    - '은하 지도' nav item exists pointing to /galaxy
    - types/index.ts @deprecated aliases are the only samguk type references
    - TypeScript compiles (0 new errors vs baseline)
    - Non-critical pages (auction/betting/dynasty) have TODO comments, not errors
  </acceptance_criteria>
  <done>Navigation uses gin7 terms, type aliases are @deprecated-only, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
cd /Users/apple/Desktop/개인프로젝트/openlogh/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
grep -rn "city-basic-card\|general-basic-card\|nation-basic-card\|map-3d" /Users/apple/Desktop/개인프로젝트/openlogh/frontend/src --include="*.tsx" --include="*.ts" | grep -v "@deprecated\|TODO" | wc -l
</verification>

<success_criteria>
- city-basic-card, general-basic-card, nation-basic-card, crew-type-browser deleted with 0 import refs remaining (FE-08)
- map-3d/ and tutorial/ directories deleted
- layout.tsx nav uses gin7 Korean labels throughout
- '은하 지도' nav item added pointing to /galaxy
- types/index.ts samguk names only appear as @deprecated aliases
- TypeScript compiles without new errors
- grep for city/nation/troop/general as primary class/component names returns 0 active usages
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration/06-08-SUMMARY.md`
</output>
