---
phase: 14
plan: 18
title: "Frontend — Battle end modal with merit breakdown (D-32..D-34)"
type: execute
wave: 5
depends_on: [14-02, 14-06, 14-10]
files_modified:
  - frontend/src/components/tactical/BattleEndModal.tsx
  - frontend/src/lib/gameApi.ts
  - frontend/src/stores/tacticalStore.ts
  - frontend/src/components/tactical/BattleEndModal.test.tsx
requirements: [FE-01]
autonomous: true
must_haves:
  truths:
    - "BattleEndModal opens automatically when currentBattle.phase transitions ACTIVE → ENDED"
    - "Modal fetches BattleSummaryDto from GET /api/{sessionId}/battles/{battleId}/summary (from 14-02)"
    - "Per-unit rows show merit breakdown as '기본 X + 작전 +Y = 총 Z' with font-mono for numbers"
    - "Operation-participant rows highlighted with gold tint + '작전 참가' badge"
    - "Primary CTA '전략맵으로 돌아가기' navigates to /world/{sessionId}/galaxy"
    - "Korean copy matches UI-SPEC Section G"
  artifacts:
    - path: "frontend/src/components/tactical/BattleEndModal.tsx"
      provides: "Full-screen Radix Dialog with battle summary table"
      min_lines: 120
  key_links:
    - from: "tacticalStore phase transition watcher"
      to: "BattleEndModal open state"
      via: "useEffect on currentBattle.phase"
      pattern: "phase === 'ENDED'"
    - from: "BattleEndModal fetch"
      to: "GET /api/{sessionId}/battles/{battleId}/summary"
      via: "gameApi.fetchBattleSummary"
      pattern: "fetchBattleSummary"
---

<objective>
Implement the end-of-battle modal per D-32..D-34. Full-screen Radix Dialog triggered on ACTIVE → ENDED phase transition, fetches BattleSummaryDto from the 14-02 endpoint, renders per-unit merit breakdown with operation-participant highlighting.

Purpose: D-33 requires "기본 X + 작전 +Y = 총 Z" display so OPS-02 is visually verifiable. Without this modal, players have no end-of-battle feedback beyond the raw result.

Output: BattleEndModal.tsx, gameApi.fetchBattleSummary method, tacticalStore phase watcher hook, replaced test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-integration/14-CONTEXT.md
@.planning/phases/14-frontend-integration/14-RESEARCH.md
@.planning/phases/14-frontend-integration/14-UI-SPEC.md
@frontend/src/lib/gameApi.ts
@frontend/src/stores/tacticalStore.ts
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/table.tsx
@frontend/src/types/tactical.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gameApi.fetchBattleSummary + phase transition detector + BattleEndModal component + test</name>
  <files>frontend/src/lib/gameApi.ts, frontend/src/stores/tacticalStore.ts, frontend/src/components/tactical/BattleEndModal.tsx, frontend/src/components/tactical/BattleEndModal.test.tsx</files>
  <read_first>
    - frontend/src/lib/gameApi.ts (existing REST client pattern — axios or fetch wrapper)
    - frontend/src/stores/tacticalStore.ts (after 14-10/14-11/14-15 — onBattleTick reducer)
    - frontend/src/components/ui/dialog.tsx (Radix Dialog wrapper)
    - frontend/src/components/ui/table.tsx (shadcn table primitive)
    - frontend/src/types/tactical.ts (after 14-06 — BattleSummaryDto + BattleSummaryRow interfaces)
    - .planning/phases/14-frontend-integration/14-CONTEXT.md (D-32, D-33, D-34)
    - .planning/phases/14-frontend-integration/14-UI-SPEC.md (Section G. End-of-Battle Modal + Copywriting Contract)
  </read_first>
  <behavior>
    - Test 1: `gameApi.fetchBattleSummary(sessionId, battleId)` calls `GET /api/{sessionId}/battles/{battleId}/summary` and returns the parsed BattleSummaryDto.
    - Test 2: Modal opens automatically when `currentBattle.phase` transitions from 'ACTIVE' to 'ENDED' (detected by a useEffect watching phase).
    - Test 3: Modal fetches summary on open and shows loading state "전투 결과를 집계하는 중입니다…" while pending.
    - Test 4: Modal renders header "승리" in green when winner matches my side; "패배" in destructive red otherwise.
    - Test 5: Each row shows merit cell in format "기본 {base} + 작전 +{bonus} = 총 {total}" using font-mono.
    - Test 6: Operation-participant rows have `data-operation="true"` attribute and display "작전 참가" badge.
    - Test 7: Non-participant rows hide the "+ 작전 +{bonus}" segment when bonus = 0.
    - Test 8: Primary CTA "전략맵으로 돌아가기" triggers router navigation to `/world/{sessionId}/galaxy`.
  </behavior>
  <action>
    File 1 — `frontend/src/lib/gameApi.ts`:
    Add method:
    ```
    export async function fetchBattleSummary(
      sessionId: number,
      battleId: number
    ): Promise<BattleSummaryDto> {
      const res = await axios.get<BattleSummaryDto>(
        `/api/${sessionId}/battles/${battleId}/summary`
      );
      return res.data;
    }
    ```
    (Adjust to the existing gameApi client pattern — read the file first to confirm whether it uses axios, fetch, or a custom wrapper.)

    File 2 — `frontend/src/components/tactical/BattleEndModal.tsx`:

    Imports: `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle` from `@/components/ui/dialog`; `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableCell`, `TableHead` from `@/components/ui/table`; `useTacticalStore`, `useWorldStore`, `useOfficerStore`; `fetchBattleSummary`; `useRouter` from `next/navigation`; types.

    Component `BattleEndModal()`:
    - `const battle = useTacticalStore(s => s.currentBattle)`
    - `const [open, setOpen] = useState(false)`
    - `const [summary, setSummary] = useState<BattleSummaryDto | null>(null)`
    - `const [loading, setLoading] = useState(false)`
    - `useEffect(() => { if (battle?.phase === 'ENDED' && !open) { setOpen(true); } }, [battle?.phase, open])`
    - When `open && !summary`, trigger fetch:
      ```
      useEffect(() => {
        if (!open || summary || !battle) return;
        setLoading(true);
        fetchBattleSummary(sessionId, battle.id)
          .then(setSummary)
          .finally(() => setLoading(false));
      }, [open, summary, battle, sessionId]);
      ```

    Render:
    - Header: `winner === myWinSide ? '승리' (green) : winner === null ? '교전 종료' : '패배' (red)`
    - Subtitle: `{factionName} · {battleDurationTicks}틱 교전`
    - Body: If loading → skeleton + "전투 결과를 집계하는 중입니다…"; otherwise render Table with columns "부대" / "함선 (잔존/초기)" / "격침" / "공적".
    - Each row: `<TableRow data-operation={row.isOperationParticipant ? 'true' : undefined}>` with CSS selector applying gold tint via Tailwind arbitrary class or inline style `background: rgba(245, 158, 11, 0.08)`.
    - Merit cell: `row.operationMultiplier > 1 ? '기본 ' + row.baseMerit + ' + 작전 +' + (row.totalMerit - row.baseMerit) + ' = 총 ' + row.totalMerit : '기본 ' + row.baseMerit + ' = 총 ' + row.totalMerit` in `font-mono`.
    - Operation participant badge: `{row.isOperationParticipant && <Badge>작전 참가</Badge>}`
    - Footer: Outline button "전투 기록 보기" (left) + primary button "전략맵으로 돌아가기" (right). Primary on click: `router.push('/world/' + sessionId + '/galaxy'); setOpen(false)`.
    - ESC closes the dialog (default Dialog behavior) but does NOT navigate.

    File 3 — Replace stub `frontend/src/components/tactical/BattleEndModal.test.tsx`:
    Mock `gameApi.fetchBattleSummary` with `vi.mock('@/lib/gameApi', () => ({ fetchBattleSummary: vi.fn().mockResolvedValue({ battleId: 1, winner: 'ATTACKER_WIN', durationTicks: 120, rows: [...] }) }))`. Seed tacticalStore with `phase: 'ACTIVE'`, render modal, then update store to `phase: 'ENDED'`, await microtask, assert modal is visible + assertions from Tests 2-8.

    Per D-32, D-33, D-34 (CONTEXT.md binding); UI-SPEC Section G.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm typecheck &amp;&amp; pnpm test --run BattleEndModal</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/tactical/BattleEndModal.tsx`
    - `grep -n "fetchBattleSummary" frontend/src/lib/gameApi.ts` returns at least 1 match
    - `grep -n "/summary" frontend/src/lib/gameApi.ts` returns at least 1 match
    - `grep -n "승리" frontend/src/components/tactical/BattleEndModal.tsx` returns 1 match (Korean UI)
    - `grep -n "패배" frontend/src/components/tactical/BattleEndModal.tsx` returns 1 match
    - `grep -n "기본" frontend/src/components/tactical/BattleEndModal.tsx` returns at least 1 match
    - `grep -n "작전" frontend/src/components/tactical/BattleEndModal.tsx` returns at least 1 match
    - `grep -n "전략맵으로 돌아가기" frontend/src/components/tactical/BattleEndModal.tsx` returns 1 match
    - `grep -n "작전 참가" frontend/src/components/tactical/BattleEndModal.tsx` returns 1 match
    - `grep -n "data-operation" frontend/src/components/tactical/BattleEndModal.tsx` returns at least 1 match
    - `pnpm test --run BattleEndModal` exits 0 with all tests passing
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Modal opens on phase transition, fetches summary, renders breakdown table with operation highlighting; all tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm typecheck && pnpm test --run && pnpm build` all exit 0
</verification>

<success_criteria>
- gameApi exposes fetchBattleSummary
- Modal triggers on ACTIVE → ENDED phase transition
- Merit cell shows "기본 X + 작전 +Y = 총 Z" with font-mono
- Operation-participant rows highlighted with gold + badge
- Primary CTA navigates to galaxy map
- OPS-02 verification is visually confirmable in the UI
</success_criteria>

<output>
Create `.planning/phases/14-frontend-integration/14-18-SUMMARY.md` per template.
</output>
