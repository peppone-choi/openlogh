---
phase: 14
plan: 16
title: "Frontend — Unit status markers + NPC mission objective (D-35..D-37)"
type: execute
wave: 4
depends_on: [14-06, 14-10]
files_modified:
  - frontend/src/components/tactical/TacticalUnitIcon.tsx
  - frontend/src/components/tactical/InfoPanel.tsx
  - frontend/src/components/tactical/BattleMap.tsx
requirements: [FE-03]
autonomous: true
must_haves:
  truths:
    - "TacticalUnitIcon renders ●/○/🤖 status marker in the top-right corner"
    - "Online color #10b981 (game-green); offline stroke-only #7a8599; NPC 🤖 glyph purple #a78bfa"
    - "Shape (filled disc / hollow ring / glyph) is the primary discriminator; color is supplementary (color-blind contract)"
    - "InfoPanel shows NPC mission objective rows when unit.isNpc && unit.missionObjective is set"
    - "BattleMap renders a dashed mission target line from NPC unit to target system when selected"
    - "Marker tooltips show Korean copy from UI-SPEC H copywriting contract"
  artifacts:
    - path: "frontend/src/components/tactical/TacticalUnitIcon.tsx"
      provides: "Extended with isOnline/isNpc props + status marker rendering"
      contains: "isOnline"
  key_links:
    - from: "TacticalUnitIcon status marker"
      to: "TacticalUnit.isOnline / isNpc DTO fields (from 14-01 backend)"
      via: "direct prop pass"
      pattern: "isOnline"
---

<objective>
Render online/offline/NPC status markers on each TacticalUnitIcon, display NPC mission objective in InfoPanel, draw mission target line on BattleMap. Satisfies D-35, D-36, D-37.

Purpose: Players currently cannot tell whose command a unit is under (player vs NPC vs offline AI). D-35 adds glyph markers; D-36 surfaces NPC mission context; D-37 requires DTO fields which 14-01 ships.

Output: TacticalUnitIcon extension, InfoPanel NPC block, BattleMap dashed mission line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-integration/14-CONTEXT.md
@.planning/phases/14-frontend-integration/14-RESEARCH.md
@.planning/phases/14-frontend-integration/14-UI-SPEC.md
@frontend/src/components/tactical/TacticalUnitIcon.tsx
@frontend/src/components/tactical/InfoPanel.tsx
@frontend/src/components/tactical/BattleMap.tsx
@frontend/src/stores/galaxyStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TacticalUnitIcon status marker + tooltip</name>
  <files>frontend/src/components/tactical/TacticalUnitIcon.tsx</files>
  <read_first>
    - frontend/src/components/tactical/TacticalUnitIcon.tsx (existing △/□ shape logic)
    - frontend/src/types/tactical.ts (after 14-06 — isOnline / isNpc fields)
    - .planning/phases/14-frontend-integration/14-CONTEXT.md (D-35)
    - .planning/phases/14-frontend-integration/14-UI-SPEC.md (Section H. Status Markers + Color section Status Marker Colors)
  </read_first>
  <behavior>
    - Test 1: `isOnline=true && isNpc=false` → renders filled green Circle radius=4 at (+4, -4) offset from icon top-right.
    - Test 2: `isOnline=false && isNpc=false` → renders hollow ring stroke-only #7a8599.
    - Test 3: `isNpc=true` → renders `🤖` Text element regardless of online state.
    - Test 4: Icon opacity is NOT changed by online state (per D-35 — opacity is reserved for destruction signal).
    - Test 5: Marker has no `listening=true` (pointer events routed to the main icon).
  </behavior>
  <action>
    File: `frontend/src/components/tactical/TacticalUnitIcon.tsx`

    Add optional props `isOnline?: boolean` (default true) and `isNpc?: boolean` (default false). Consume them from the `unit` prop if already passed via the unit object; otherwise add as separate props.

    After rendering the main △/□ shape, add a status marker Group at offset `(+size+4, -size-4)` from the icon center. Inside:
    - If `isNpc` → `<Text text="🤖" fontSize={10} fill="#a78bfa" />`
    - Else if `isOnline` → `<Circle radius={4} fill="#10b981" />`
    - Else → `<Circle radius={4} stroke="#7a8599" strokeWidth={1} fill="transparent" />`

    Do NOT modify the main icon opacity (per D-35). The marker is always fully opaque.

    Per D-35, UI-SPEC Section H + Color section.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm typecheck &amp;&amp; pnpm test --run TacticalUnitIcon</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isOnline" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns at least 2 matches
    - `grep -n "isNpc" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns at least 2 matches
    - `grep -n "🤖" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns 1 match
    - `grep -n "#10b981" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns 1 match
    - `grep -n "#7a8599" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns 1 match
    - `grep -n "#a78bfa" frontend/src/components/tactical/TacticalUnitIcon.tsx` returns 1 match
    - `pnpm typecheck` exits 0
    - `pnpm test --run TacticalUnitIcon` exits 0 (if existing test file)
  </acceptance_criteria>
  <done>Status marker renders per D-35; color + shape both present.</done>
</task>

<task type="auto">
  <name>Task 2: InfoPanel NPC mission objective rows + BattleMap dashed target line</name>
  <files>frontend/src/components/tactical/InfoPanel.tsx, frontend/src/components/tactical/BattleMap.tsx</files>
  <read_first>
    - frontend/src/components/tactical/InfoPanel.tsx (after 14-14 — has command-relation badge)
    - frontend/src/components/tactical/BattleMap.tsx (after 14-10/14-11 — has units layer)
    - frontend/src/stores/galaxyStore.ts (getSystem lookup to resolve starSystemId → name)
    - .planning/phases/14-frontend-integration/14-CONTEXT.md (D-36, D-37)
    - .planning/phases/14-frontend-integration/14-UI-SPEC.md (Section H NPC marker tooltips + Copywriting Contract D-35..D-37)
  </read_first>
  <action>
    File 1 — `frontend/src/components/tactical/InfoPanel.tsx`:

    When the selected unit has `unit.isNpc && unit.missionObjective`, render additional rows after the command-relation badge:
    - "현재 목적: {objectiveLabel}" where `objectiveLabel = { CONQUEST: '점령', DEFENSE: '방어', SWEEP: '소탕' }[unit.missionObjective] ?? unit.missionObjective`
    - "목표: {systemName}" — resolve via `useGalaxyStore(s => s.getSystem?.(targetStarSystemId)?.name)`. Note: targetStarSystemId is NOT currently in TacticalUnitDto — if research confirms the need, surface it as an optional field on the unit. If not available, omit the row and document the limitation in SUMMARY.
    - "추적 대상: {enemyFleetName}" — only if `unit.targetFleetId` exists (optional, omit if not surfaced in DTO)

    When a non-NPC is selected, do NOT render these rows.

    File 2 — `frontend/src/components/tactical/BattleMap.tsx`:

    When the selected unit has `isNpc && targetStarSystemId` (or equivalent), render a dashed Konva Line in the units Layer (or a separate `mission-line` Layer) from the unit's position to the target system's screen-space position. `stroke: #a78bfa` (matches NPC marker color), `dash: [5, 5]`, `strokeWidth: 1`, `listening: false`.

    Since star-system coordinates are in galaxy-map space not tactical-map space, this line makes sense only if the target is within the tactical viewport. Otherwise, point the line toward the edge of the map indicating "target off-map". Planner discretion; simplest option: omit the line when target is off-map and show only a small arrow indicator at the icon.

    Per D-36, D-37, UI-SPEC Section H copywriting.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "현재 목적" frontend/src/components/tactical/InfoPanel.tsx` returns 1 match (Korean UI)
    - `grep -n "점령\|방어\|소탕" frontend/src/components/tactical/InfoPanel.tsx` returns at least 1 match
    - `grep -n "isNpc" frontend/src/components/tactical/InfoPanel.tsx` returns at least 1 match
    - `grep -n "missionObjective" frontend/src/components/tactical/InfoPanel.tsx` returns at least 1 match
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>InfoPanel shows NPC mission objective rows; BattleMap draws mission target line where applicable.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm typecheck && pnpm test --run && pnpm build` all exit 0
</verification>

<success_criteria>
- Status markers visible on all units with correct shape + color
- NPC selection surfaces mission objective in InfoPanel
- Korean labels for CONQUEST / DEFENSE / SWEEP
- Dashed mission line drawn for on-map NPC targets
</success_criteria>

<output>
Create `.planning/phases/14-frontend-integration/14-16-SUMMARY.md` per template.
</output>
