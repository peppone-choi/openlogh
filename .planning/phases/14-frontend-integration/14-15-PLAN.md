---
phase: 14
plan: 15
title: "Frontend — Succession feedback FX (FE-04)"
type: execute
wave: 4
depends_on: [14-06, 14-10]
files_modified:
  - frontend/src/components/tactical/FlagshipFlash.tsx
  - frontend/src/components/tactical/SuccessionCountdownOverlay.tsx
  - frontend/src/components/tactical/BattleMap.tsx
  - frontend/src/stores/tacticalStore.ts
  - frontend/src/hooks/useSoundEffects.ts
  - frontend/src/components/tactical/FlagshipFlash.test.tsx
  - frontend/src/stores/tacticalStore.succession.test.ts
requirements: [FE-04]
autonomous: true
must_haves:
  truths:
    - "tacticalStore.onBattleTick detects FLAGSHIP_DESTROYED events and pushes to activeFlagshipDestroyedFleetIds"
    - "tacticalStore.onBattleTick detects SUCCESSION_STARTED/COMPLETED events and updates activeSuccessionFleetIds"
    - "Konva FX layer renders a 0.5s local ring flash on flagship destruction (no full-screen effect)"
    - "HTML overlay renders '지휘 승계 중 — {N}틱' over vacant commander units"
    - "Sonner toast fires on succession start (destructive) and completion (success)"
    - "useSoundEffects has successionStart and flagshipDestroyed sounds"
  artifacts:
    - path: "frontend/src/components/tactical/FlagshipFlash.tsx"
      provides: "0.5s Konva ring flash FX"
      min_lines: 40
    - path: "frontend/src/components/tactical/SuccessionCountdownOverlay.tsx"
      provides: "HTML overlay for per-unit 30→0 countdown"
      min_lines: 40
  key_links:
    - from: "tacticalStore.onBattleTick event handler"
      to: "FlagshipFlash mount in BattleMap succession-fx layer"
      via: "activeFlagshipDestroyedFleetIds state"
      pattern: "activeFlagshipDestroyedFleetIds"
---

<objective>
Implement succession feedback FX for FE-04. Extend tacticalStore reducer to process new event types, create FlagshipFlash Konva component, create SuccessionCountdownOverlay HTML overlay, wire into BattleMap succession-fx layer (empty placeholder from 14-10), fire Sonner toasts + new sound effects.

Purpose: D-13, D-14, D-15, D-16 specify the exact feedback shapes. Without this plan, succession events fire backend but produce no visual/audio feedback.

Output: FlagshipFlash.tsx, SuccessionCountdownOverlay.tsx, extended tacticalStore reducer, 2 new sound effects, BattleMap wiring, 2 replaced tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-integration/14-CONTEXT.md
@.planning/phases/14-frontend-integration/14-RESEARCH.md
@.planning/phases/14-frontend-integration/14-UI-SPEC.md
@frontend/src/stores/tacticalStore.ts
@frontend/src/hooks/useSoundEffects.ts
@frontend/src/components/tactical/BattleMap.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend onBattleTick event processing + succession reducer test</name>
  <files>frontend/src/stores/tacticalStore.ts, frontend/src/stores/tacticalStore.succession.test.ts, frontend/src/hooks/useSoundEffects.ts</files>
  <read_first>
    - frontend/src/stores/tacticalStore.ts (after 14-10 and 14-11 — has activeSuccessionFleetIds + activeFlagshipDestroyedFleetIds scaffold)
    - frontend/src/hooks/useSoundEffects.ts (existing FREQUENCIES record + SoundType union)
    - frontend/src/types/tactical.ts (after 14-06 — BattleTickEvent type union includes new 4 types)
    - .planning/phases/14-frontend-integration/14-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/14-frontend-integration/14-RESEARCH.md (Section 6 succession FX update + Section 9 sounds + Section 10 toasts)
    - .planning/phases/14-frontend-integration/14-UI-SPEC.md (Section D. Succession FX Timing)
  </read_first>
  <behavior>
    - Test 1: Broadcast with event type='FLAGSHIP_DESTROYED', sourceUnitId=100 → activeFlagshipDestroyedFleetIds includes {fleetId:100, expiresAt:now+500}.
    - Test 2: Broadcast with event type='SUCCESSION_STARTED', sourceUnitId=100 → activeSuccessionFleetIds includes 100.
    - Test 3: Broadcast with event type='SUCCESSION_COMPLETED', sourceUnitId=200 (new commander), targetUnitId=100 (old commander) → 100 removed from activeSuccessionFleetIds.
    - Test 4: Multiple FLAGSHIP_DESTROYED events within one tick → all accumulate; expired entries pruned on next tick.
    - Test 5: Sonner toast fires once per SUCCESSION_STARTED event (use `id: 'succ-${officerId}'` de-dup).
    - Test 6: useSoundEffects.play('flagshipDestroyed') is called on FLAGSHIP_DESTROYED.
    - Test 7: useSoundEffects.play('successionStart') is called on SUCCESSION_STARTED.
  </behavior>
  <action>
    File 1 — `frontend/src/hooks/useSoundEffects.ts`:
    Extend `SoundType` union with `'successionStart' | 'flagshipDestroyed'`. Add to `FREQUENCIES` record (copy pattern from existing entries — Web Audio API tone sequences):
    - `successionStart: { freq: [330, 277, 220], duration: [150, 150, 300], type: 'triangle' }`
    - `flagshipDestroyed: { freq: [110, 82, 55], duration: [100, 150, 400], type: 'sawtooth' }`

    File 2 — `frontend/src/stores/tacticalStore.ts` (extend the onBattleTick reducer from 14-10 / 14-11):
    Import `toast` from `sonner` and `useSoundEffects` or the pure `playSound` helper.

    Inside onBattleTick, after the fog update, iterate `data.events`:
    ```
    const flashExpiry = Date.now() + 500;
    const newFlashes = [...state.activeFlagshipDestroyedFleetIds];
    let newSuccessions = [...state.activeSuccessionFleetIds];

    for (const ev of data.events) {
      if (ev.type === 'FLAGSHIP_DESTROYED' && ev.sourceUnitId) {
        newFlashes.push({ fleetId: ev.sourceUnitId, expiresAt: flashExpiry });
        playSound('flagshipDestroyed');
      }
      if (ev.type === 'SUCCESSION_STARTED' && ev.sourceUnitId) {
        if (!newSuccessions.includes(ev.sourceUnitId)) {
          newSuccessions.push(ev.sourceUnitId);
        }
        playSound('successionStart');
        toast.warning('기함 격침 — ' + (ev.detail ?? '사령관 전사') + ', 30틱 후 승계', {
          id: 'succ-' + ev.sourceUnitId,
          duration: 6000,
        });
      }
      if (ev.type === 'SUCCESSION_COMPLETED' && ev.targetUnitId) {
        newSuccessions = newSuccessions.filter((id) => id !== ev.targetUnitId);
        toast.success('지휘 인수 — ' + (ev.detail ?? '새 사령관'), { duration: 4000 });
      }
    }

    // Prune expired flashes
    const prunedFlashes = newFlashes.filter((f) => f.expiresAt > Date.now());
    ```
    Persist `activeFlagshipDestroyedFleetIds: prunedFlashes` and `activeSuccessionFleetIds: newSuccessions` in the `set(...)` call.

    File 3 — Replace stub `frontend/src/stores/tacticalStore.succession.test.ts`:
    Mock `sonner` with `vi.mock('sonner', () => ({ toast: { warning: vi.fn(), success: vi.fn(), error: vi.fn() } }))`. Seed store. Dispatch onBattleTick with the 7 test cases. Assert state changes + toast call counts.

    Per D-13, D-14, D-15, D-16 (CONTEXT.md binding); UI-SPEC Section D timings.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm typecheck &amp;&amp; pnpm test --run tacticalStore.succession useSoundEffects</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "successionStart" frontend/src/hooks/useSoundEffects.ts` returns at least 1 match
    - `grep -n "flagshipDestroyed" frontend/src/hooks/useSoundEffects.ts` returns at least 1 match
    - `grep -n "FLAGSHIP_DESTROYED" frontend/src/stores/tacticalStore.ts` returns at least 1 match
    - `grep -n "SUCCESSION_STARTED" frontend/src/stores/tacticalStore.ts` returns at least 1 match
    - `grep -n "SUCCESSION_COMPLETED" frontend/src/stores/tacticalStore.ts` returns at least 1 match
    - `grep -n "기함 격침" frontend/src/stores/tacticalStore.ts` returns 1 match (Korean UI)
    - `grep -n "지휘 인수" frontend/src/stores/tacticalStore.ts` returns 1 match
    - `pnpm test --run tacticalStore.succession` exits 0 with 7 tests passing
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Reducer processes all 4 new event types; toasts + sounds fire; 7 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FlagshipFlash + SuccessionCountdownOverlay + BattleMap wiring</name>
  <files>frontend/src/components/tactical/FlagshipFlash.tsx, frontend/src/components/tactical/SuccessionCountdownOverlay.tsx, frontend/src/components/tactical/BattleMap.tsx, frontend/src/components/tactical/FlagshipFlash.test.tsx</files>
  <read_first>
    - frontend/src/components/tactical/BattleMap.tsx (after 14-10 — has empty succession-fx layer)
    - frontend/src/stores/tacticalStore.ts (after Task 1 — has activeFlagshipDestroyedFleetIds + activeSuccessionFleetIds)
    - .planning/phases/14-frontend-integration/14-UI-SPEC.md (Section D timings — 0.5s ring flash, HTML overlay for countdown, green "지휘 인수" ring)
  </read_first>
  <behavior>
    - Test 1: FlagshipFlash mounts a Konva Group with white Circle radius=24 opacity 0.9 at position (cx, cy).
    - Test 2: FlagshipFlash animates via useEffect: opacity 0.9→0 and radius 24→96 over 500ms, then unmounts.
    - Test 3: SuccessionCountdownOverlay renders an HTML pill "지휘 승계 중 — {ticksRemaining}틱" absolute-positioned above the unit's screen-space coords.
    - Test 4: BattleMap succession-fx Layer renders one FlagshipFlash per activeFlagshipDestroyedFleetIds entry.
    - Test 5: BattleMap renders one SuccessionCountdownOverlay per unit whose successionState='PENDING_SUCCESSION'.
  </behavior>
  <action>
    File 1 — `frontend/src/components/tactical/FlagshipFlash.tsx`:
    ```
    'use client';
    import { Group, Circle } from 'react-konva';
    import { useEffect, useState } from 'react';

    export interface FlagshipFlashProps {
      cx: number;
      cy: number;
      onComplete?: () => void;
    }

    // Phase 14 FE-04 D-14: 0.5s local ring flash (no full-screen effect)
    export function FlagshipFlash({ cx, cy, onComplete }: FlagshipFlashProps) {
      const [progress, setProgress] = useState(0);  // 0 → 1 over 500ms
      useEffect(() => {
        const start = performance.now();
        let rafId = 0;
        const tick = (now: number) => {
          const p = Math.min(1, (now - start) / 500);
          setProgress(p);
          if (p < 1) rafId = requestAnimationFrame(tick);
          else onComplete?.();
        };
        rafId = requestAnimationFrame(tick);
        return () => cancelAnimationFrame(rafId);
      }, [onComplete]);

      const opacity = 0.9 * (1 - progress);
      const radius = 24 + 72 * progress;
      return (
        <Group x={cx} y={cy} listening={false}>
          <Circle radius={radius} fill="white" opacity={opacity} />
          <Circle radius={radius * 0.7} stroke="white" strokeWidth={1} opacity={opacity * 0.7} />
        </Group>
      );
    }

    export default FlagshipFlash;
    ```

    File 2 — `frontend/src/components/tactical/SuccessionCountdownOverlay.tsx`:
    HTML overlay (not Konva) — absolute-positioned div. Props `{ screenX: number; screenY: number; ticksRemaining: number }`. Renders a 64x32 pill `rgba(0,0,0,0.85)` background, 1px gold border `#f59e0b`, "지휘 승계 중" label, monospaced countdown number in Display weight (20px / 600). Korean copy from UI-SPEC D Phase 2.

    File 3 — `frontend/src/components/tactical/BattleMap.tsx`:
    In the succession-fx Layer (placeholder from 14-10), iterate `activeFlagshipDestroyedFleetIds` and render `<FlagshipFlash>` for each. Look up the unit by `fleetId` to get `posX`/`posY`; convert to scaled coords.

    Render SuccessionCountdownOverlay as an HTML sibling to the Stage (not inside Konva) — wrap Stage in a relative-positioned div, absolute-position overlays. Iterate `units.filter(u => u.successionState === 'PENDING_SUCCESSION')` and render one overlay per unit using the Stage's bounding rect to compute screen coords.

    File 4 — Replace stub `frontend/src/components/tactical/FlagshipFlash.test.tsx`:
    Use `@testing-library/react` + fake timers. Render `<FlagshipFlash cx={100} cy={100} onComplete={vi.fn()} />`. Advance timers by 500ms. Assert `onComplete` called.

    Per D-13, D-14, D-16 (CONTEXT.md binding); UI-SPEC Section D.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm typecheck &amp;&amp; pnpm test --run FlagshipFlash</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/tactical/FlagshipFlash.tsx`
    - `test -f frontend/src/components/tactical/SuccessionCountdownOverlay.tsx`
    - `grep -n "지휘 승계 중" frontend/src/components/tactical/SuccessionCountdownOverlay.tsx` returns 1 match (Korean UI)
    - `grep -n "FlagshipFlash" frontend/src/components/tactical/BattleMap.tsx` returns at least 1 match
    - `grep -n "SuccessionCountdownOverlay" frontend/src/components/tactical/BattleMap.tsx` returns at least 1 match
    - `pnpm test --run FlagshipFlash` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>FX components + BattleMap wiring + component test green.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm typecheck && pnpm test --run && pnpm build` all exit 0
</verification>

<success_criteria>
- All 4 new event types processed in reducer (FLAGSHIP_DESTROYED / SUCCESSION_STARTED / SUCCESSION_COMPLETED / JAMMING_ACTIVE)
- Sonner toasts fire with Korean copy from UI-SPEC
- 2 new sounds available
- FlagshipFlash Konva component + SuccessionCountdownOverlay HTML component
- BattleMap wires both into the succession-fx layer
- 9+ tests pass
</success_criteria>

<output>
Create `.planning/phases/14-frontend-integration/14-15-SUMMARY.md` per template.
</output>
