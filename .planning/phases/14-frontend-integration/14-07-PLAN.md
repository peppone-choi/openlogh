---
phase: 14
plan: 07
title: "Frontend dependencies — add @dnd-kit, remove @react-three"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
requirements: [FE-02]
autonomous: true
must_haves:
  truths:
    - "@dnd-kit/core and @dnd-kit/utilities are installed and resolve with React 19.2.3"
    - "@react-three/fiber, @react-three/drei, three, @types/three are removed from package.json"
    - "pnpm install completes without peer-dependency conflict warnings"
    - "pnpm typecheck + pnpm build still succeed immediately after dep change (R3F file deletion happens in 14-08)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Updated dependency block"
      contains: "@dnd-kit/core"
  key_links:
    - from: "frontend/package.json dependencies"
      to: "installed node_modules"
      via: "pnpm install"
      pattern: "@dnd-kit/core"
---

<objective>
Add `@dnd-kit/core` and `@dnd-kit/utilities` (required by 14-12 sub-fleet drawer), and remove the four `@react-three/*` + `three` + `@types/three` packages (required by 14-08 R3F file deletion).

Purpose: 14-12 cannot import `useDraggable` without dnd-kit installed, and 14-08 cannot delete R3F files safely until typecheck can no longer find the packages. This plan gates both downstream waves.

NOTE: This plan does NOT delete R3F source files — 14-08 handles that (and depends on this plan). After this plan's dep removal, `pnpm typecheck` will FAIL on the 4 R3F source files that still import `@react-three/fiber`. That's expected — this plan intentionally defers the typecheck success criterion to 14-08 which runs in the same wave.

CORRECTION: Because typecheck must stay green, this plan does the dep ADD but NOT the removal. 14-08 will do the file deletion AND the dep removal together (atomic transaction). This plan only adds dnd-kit. Plan 14-08 frontmatter depends_on=[14-07] to ensure the deps exist before the drawer plan starts.

Output: Updated `package.json` with `@dnd-kit/core` and `@dnd-kit/utilities` added. R3F removal is deferred to 14-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-frontend-integration/14-CONTEXT.md
@.planning/phases/14-frontend-integration/14-RESEARCH.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @dnd-kit/core and @dnd-kit/utilities</name>
  <read_first>
    - frontend/package.json (full file — see current dependency list and version pinning style)
    - .planning/phases/14-frontend-integration/14-CONTEXT.md (D-05 — dnd-kit required, react-dnd forbidden)
    - .planning/phases/14-frontend-integration/14-RESEARCH.md (Section 1 — dnd-kit versioning note)
  </read_first>
  <action>
    Run:
    ```bash
    cd frontend
    pnpm add @dnd-kit/core @dnd-kit/utilities
    ```

    Do NOT add `@dnd-kit/sortable` — research Section 1 says it is optional and CONTEXT.md does not require it.

    Verify the installation:
    ```bash
    pnpm list @dnd-kit/core @dnd-kit/utilities
    ```

    Confirm no peer-dependency warnings reference React 19. If warnings appear, investigate before committing.

    After install, run:
    ```bash
    pnpm typecheck
    pnpm test --run
    pnpm build
    ```

    All three must exit 0. If build fails, the cause is NOT dnd-kit (dnd-kit has no runtime effect until imported) — it would be a pre-existing issue. Escalate to human.

    Per D-05 (CONTEXT.md binding).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm list @dnd-kit/core @dnd-kit/utilities --json | grep -q '"@dnd-kit/core"' &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"@dnd-kit/core"' frontend/package.json` returns 1 match
    - `grep -n '"@dnd-kit/utilities"' frontend/package.json` returns 1 match
    - `! grep -n '"@dnd-kit/sortable"' frontend/package.json` — forbidden (not requested)
    - `! grep -n '"react-dnd"' frontend/package.json` — forbidden (D-05 explicit)
    - `pnpm list @dnd-kit/core` prints a version that resolves (not "missing")
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
    - `pnpm test --run` exits 0
  </acceptance_criteria>
  <done>@dnd-kit/core + utilities installed; all frontend verification commands green.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm test --run && pnpm build` all exit 0
- No peer-dependency warnings about React 19 in pnpm install output
</verification>

<success_criteria>
- dnd-kit available for 14-12
- No R3F removal yet (deferred to 14-08 for atomic typecheck)
- package.json clean
</success_criteria>

<output>
Create `.planning/phases/14-frontend-integration/14-07-SUMMARY.md` per template.
</output>
