---
phase: "07"
plan: "01"
type: backend
autonomous: true
wave: 1
depends_on: []
requirements: [CHAR-04, CHAR-07]
---

# Plan 07-01: Flyway Migration + Officer Entity Update + Rank Config

## Objective
Add merit/evaluation/fame point columns to the officer table via Flyway V36, update the Officer entity, and create RankHeadcount configuration model.

## Tasks

### Task 1: Flyway V36 Migration
type="auto"

Add merit_points, evaluation_points, fame_points columns to officer table with defaults of 0. Add index on (session_id, faction_id, officer_level) for rank ladder queries.

**Done criteria:** V36 SQL file exists in migration directory.

### Task 2: Officer Entity Update
type="auto"

Add meritPoints, evaluationPoints, famePoints fields to Officer entity. Map to new columns.

**Done criteria:** Officer.kt has new fields with correct JPA annotations.

### Task 3: RankHeadcount Config Model
type="auto"

Create RankHeadcount object with headcount limits per tier, and a method to check if a rank tier has capacity within a faction.

**Done criteria:** RankHeadcount.kt exists in model package with correct limits per CLAUDE.md spec.

### Task 4: Compilation Verification
type="auto"

Run gradle compileKotlin to verify all changes compile correctly.

**Done criteria:** Build succeeds without errors.

## Verification
- V36 migration file exists
- Officer entity has 3 new fields
- RankHeadcount model has correct limits
- Project compiles
