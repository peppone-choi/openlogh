---
plan: 24-04-regression-gate-and-deferred-backlog
phase: 24-legacy-test-cleanup
milestone: v2.4
wave: 3
autonomous: true
requirements: [TC-04]
references:
  - audit: .planning/phases/23-gin7-economy-port/legacy-test-audit.md
  - context: .planning/phases/24-legacy-test-cleanup/24-CONTEXT.md
---

# Plan 24-04: Final regression gate + deferred backlog

## Objective

After 24-01..24-03 land, run the full `:game-app:test --continue` gate and
confirm the suite is reduced to **59 residual failures**:

- `BROKEN`: 37
- `OUT-OF-SCOPE`: 22

## Tasks

1. Run `./gradlew :game-app:test --continue`.
2. Compare the residual failure list against the Phase 23 audit buckets.
3. Create/update a deferred backlog document for the `BROKEN` suites with:
   - suite name
   - current failure message anchor
   - why it is harness debt rather than product drift
4. Confirm no COVERED / OBSOLETE / MIGRATABLE failures remain.

## Exit criteria

- Full-suite failure count is exactly 59.
- Residual failures are fully explained by `BROKEN` + `OUT-OF-SCOPE`.
- Phase 24 summary can mark v2.4 shipped.
