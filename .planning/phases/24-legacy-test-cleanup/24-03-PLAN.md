---
plan: 24-03-migratable-port
phase: 24-legacy-test-cleanup
milestone: v2.4
wave: 2
autonomous: true
requirements: [TC-03]
references:
  - audit: .planning/phases/23-gin7-economy-port/legacy-test-audit.md
  - context: .planning/phases/24-legacy-test-cleanup/24-CONTEXT.md
---

# Plan 24-03: MIGRATABLE test port

## Objective

Port the **70 failing MIGRATABLE** legacy tests to current LOGH naming and Gin7
contracts without changing production code.

## Port rules

- Apply the canonical domain mapping:
  `Nation→Faction`, `General→Officer`, `City→Planet`,
  `gold→funds`, `rice→supplies`.
- Rebase fixtures and assertions to Gin7 method boundaries rather than legacy
  `EconomyService` internals when the replacement suite is already canonical.
- Preserve useful golden-value coverage when the underlying invariant still
  exists in LOGH.

## Target buckets

- Faction rank tests (`Nation Level`, `PHP-Verified Nation Level`)
- Golden-value economy ports (`PHP-Verified Golden Values`, population, supply penalty)
- Event/service tests with LOGH fixture drift (`EventActionService`, `ScenarioService`, `PlanetService`)
- Long-horizon regression snapshots (`GoldenSnapshot`, `Turn Pipeline Parity`)

## Verification

- Re-run every migrated suite individually first.
- Then run grouped Phase 23 Gin7 regression suites to ensure migrations match
  the canonical replacements instead of recreating legacy drift.
