---
phase: 03-tactical-battle-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/BattleTriggerService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
autonomous: true
requirements:
  - BATTLE-01
  - BATTLE-02
  - BATTLE-10

must_haves:
  truths:
    - "같은 planetId에 적 진영 함대가 공존하면 BattleTriggerService가 전투를 자동 생성한다"
    - "BEAM/GUN/SHIELD/ENGINE/WARP/SENSOR 6채널 합계가 항상 100이며 WebSocket으로 즉시 반영된다"
    - "태세(NAVIGATION/ANCHORING/STATIONED/COMBAT) 변경 시 attackModifier/sensorRangeModifier가 processTick에 적용된다"
    - "사기 20 미만 함대는 공격 행동 불가(isEffective=false) 처리된다"
    - "TacticalUnit에 stance, missileCount, shipSubtype, isFlagship, groundUnitsEmbark 필드가 존재한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt"
      provides: "배틀 틱 루프 + 태세 보정 + 사기 20 미만 전투불가"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/BattleTriggerService.kt"
      provides: "checkForBattles 자동 개시, 종료 조건 확장"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt"
      provides: "setEnergyAllocation 검증 강화 + setStance 엔드포인트"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt"
      provides: "/app/battle/{sessionId}/{battleId}/energy 와 /stance 채널"
  key_links:
    - from: "TickEngine"
      to: "BattleTriggerService.checkForBattles()"
      via: "processSessionBattles 호출 이전에 실행"
      pattern: "checkForBattles"
    - from: "TacticalBattleService.setEnergyAllocation"
      to: "TacticalUnit.energy"
      via: "EnergyAllocation.total() == 100 검증"
      pattern: "require.*total.*== 100"
    - from: "processCombat"
      to: "UnitStance.attackModifier"
      via: "unit.stance.attackModifier 곱연산"
      pattern: "stance.attackModifier"
---

<objective>
전술전 기본 루프를 gin7 스펙에 맞게 완성한다. TacticalUnit 확장(stance/missileCount/shipSubtype/isFlagship/groundUnitsEmbark), 에너지 배분 WebSocket 채널, 태세 4종 보정, 사기 20 미만 전투불가 규칙을 구현한다.

Purpose: 이후 Plan 02~05가 공통으로 의존하는 TacticalUnit 계약과 배틀 루프 기반을 수립한다.
Output: 확장된 TacticalUnit, 태세·에너지 WebSocket 채널, 자동 전투 개시/종료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tactical-battle-engine/03-CONTEXT.md

<interfaces>
<!-- 기존 코드 계약 — 실행자는 탐색 없이 아래를 직접 사용한다 -->

From engine/tactical/TacticalBattleEngine.kt (현재 상태):
```kotlin
data class TacticalUnit(
    val fleetId: Long, val officerId: Long, val officerName: String,
    val factionId: Long, val side: BattleSide,
    var posX: Double, var posY: Double, var velX: Double, var velY: Double,
    var hp: Int, var maxHp: Int, var ships: Int, var maxShips: Int,
    var training: Int, var morale: Int,
    var leadership: Int, var command: Int, var intelligence: Int,
    var mobility: Int, var attack: Int, var defense: Int,
    var energy: EnergyAllocation = EnergyAllocation.BALANCED,
    var formation: Formation = Formation.MIXED,
    var commandRange: Double, var commandRangeMax: Double,
    var ticksSinceLastOrder: Int,
    var isAlive: Boolean, var isRetreating: Boolean, var retreatProgress: Double,
    var unitType: String, var shipClass: Int,
)

data class TacticalBattleState(
    val battleId: Long, val starSystemId: Long,
    val units: MutableList<TacticalUnit>, var tickCount: Int,
    val battleBoundsX: Double, val battleBoundsY: Double,
    var fortressGunLastFired: Int, var fortressGunPower: Int,
    var fortressGunRange: Int, var fortressGunCooldown: Int,
    var fortressFactionId: Long,
    val tickEvents: MutableList<BattleTickEvent>,
)
```

From model/UnitStance.kt:
```kotlin
enum class UnitStance(
    val attackModifier: Double,       // NAVIGATION=1.0, ANCHORING=0.8, STATIONED=0.5, COMBAT=1.3
    val sensorRangeModifier: Double,  // NAVIGATION=1.0, ANCHORING=1.2, STATIONED=0.3, COMBAT=0.6
    val moraleDecayRate: Double,      // COMBAT=0.002, others=0.0
    val canMove: Boolean,
    val canDock: Boolean,
)
// Values: NAVIGATION, ANCHORING, STATIONED, COMBAT
```

From model/EnergyAllocation.kt:
```kotlin
data class EnergyAllocation(beam, gun, shield, engine, warp, sensor: Int)
init { require(total() == 100) }
fun beamMultiplier(): Double   // beam/100.0
fun shieldAbsorption(): Double // shield/100.0 * 0.8
fun speedMultiplier(): Double  // 0.3 + engine/100.0 * 0.7
fun sensorMultiplier(): Double // 0.5 + sensor/100.0 * 0.5
```

WebSocket 채널 규칙 (ARCHITECTURE.md 기반):
- 구독: /topic/world/{sessionId}/tactical-battle/{battleId}
- 발행: /app/battle/{sessionId}/{battleId}/energy
- 발행: /app/battle/{sessionId}/{battleId}/stance
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TacticalUnit 필드 확장 + 태세/사기 룰 엔진 적용</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  </files>
  <behavior>
    - TacticalUnit에 추가: `var stance: UnitStance = UnitStance.NAVIGATION`, `var missileCount: Int = 100`, `var shipSubtype: String = ""`, `var isFlagship: Boolean = false`, `var groundUnitsEmbark: Int = 0`, `var fighterSpeedDebuffTicks: Int = 0`
    - processTick: 사기(morale) 20 미만이면 isEffective=false 로 설정해 combat 스킵 (기함이라도 동일)
    - processCombat: 공격 데미지 계산에 `unit.stance.attackModifier` 곱연산 적용
    - applyDamage: 방어 계산에 `target.stance.defenseModifier` 적용 (defenseModifier=1/attackModifier 방향으로 모델에 추가)
    - updateMorale: COMBAT 태세이면 `unit.morale -= (unit.stance.moraleDecayRate * 100).toInt()` 매 틱 적용
    - processMovement: `unit.stance.canMove == false` 이면 이동 스킵
    - TacticalBattleState에 추가: `var currentPhase: String = "MOVEMENT"` (보급/이동/수색/교전/점령 문자열 상태)
  </behavior>
  <action>
    1. TacticalUnit data class에 위 6개 필드를 기존 shipClass 아래에 추가한다.
    2. UnitStance에 `val defenseModifier: Double` 필드 추가: NAVIGATION=1.0, ANCHORING=1.1, STATIONED=1.3, COMBAT=0.9
    3. processTick 상단에 isEffective 판정 추가:
       ```kotlin
       val effectiveUnits = aliveUnits.filter { it.morale >= 20 }
       // 이하 combat 루프는 effectiveUnits 대상으로 실행
       ```
    4. processCombat 내 beamDmg 계산에 `* unit.stance.attackModifier` 추가.
    5. applyDamage 내 absorbed 계산에 `* target.stance.defenseModifier` 추가.
    6. updateMorale 끝에:
       ```kotlin
       if (unit.stance == UnitStance.COMBAT) {
           unit.morale = (unit.morale - (unit.stance.moraleDecayRate * 100).toInt()).coerceAtLeast(0)
       }
       ```
    7. processMovement 첫줄: `if (!unit.stance.canMove) return`
    8. TacticalBattleState data class에 `var currentPhase: String = "MOVEMENT"` 추가.
    
    테스트 파일: `src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleEngineTest.kt`
    - 사기 15인 유닛이 processTick 후 공격 이벤트를 생성하지 않음을 검증
    - COMBAT 태세 유닛이 매 틱 사기 -0.2 감소(coerceAtLeast 0)를 검증
    - STATIONED 태세 유닛이 processMovement에서 이동하지 않음을 검증
    - COMBAT 태세 attackModifier(1.3)가 데미지 계산에 반영되어 NAVIGATION(1.0)보다 30% 높은 데미지를 검증
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.TacticalBattleEngineTest" -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    TacticalUnit에 6개 신규 필드 존재, 사기 20 미만 전투불가 + COMBAT 태세 사기감소 + STATIONED 이동불가 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 에너지·태세 WebSocket 채널 + BattleTriggerService 자동 개시 연결</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/BattleTriggerService.kt
  </files>
  <action>
    ### BattleWebSocketController.kt (신규 파일)
    ```kotlin
    @Controller
    class BattleWebSocketController(
        private val tacticalBattleService: TacticalBattleService,
    ) {
        /** 에너지 배분 변경: /app/battle/{sessionId}/{battleId}/energy */
        @MessageMapping("/battle/{sessionId}/{battleId}/energy")
        fun updateEnergy(
            @DestinationVariable sessionId: Long,
            @DestinationVariable battleId: Long,
            @Payload payload: EnergyAllocationRequest,
            principal: Principal,
        ) {
            val officerId = extractOfficerId(principal)
            val allocation = EnergyAllocation(
                beam = payload.beam, gun = payload.gun, shield = payload.shield,
                engine = payload.engine, warp = payload.warp, sensor = payload.sensor,
            )
            tacticalBattleService.setEnergyAllocation(battleId, officerId, allocation)
        }

        /** 태세 변경: /app/battle/{sessionId}/{battleId}/stance */
        @MessageMapping("/battle/{sessionId}/{battleId}/stance")
        fun updateStance(
            @DestinationVariable sessionId: Long,
            @DestinationVariable battleId: Long,
            @Payload payload: StanceChangeRequest,
            principal: Principal,
        ) {
            val officerId = extractOfficerId(principal)
            val stance = UnitStance.fromString(payload.stance)
            tacticalBattleService.setStance(battleId, officerId, stance)
        }

        private fun extractOfficerId(principal: Principal): Long =
            (principal as? UsernamePasswordAuthenticationToken)
                ?.let { (it.principal as? OfficerPrincipal)?.officerId }
                ?: throw IllegalArgumentException("인증 정보 없음")
    }

    data class EnergyAllocationRequest(val beam: Int, val gun: Int, val shield: Int, val engine: Int, val warp: Int, val sensor: Int)
    data class StanceChangeRequest(val stance: String)
    ```

    ### TacticalBattleService.kt 에 setStance 추가
    ```kotlin
    fun setStance(battleId: Long, officerId: Long, stance: UnitStance) {
        val state = activeBattles[battleId] ?: throw IllegalStateException("Battle $battleId not active")
        val unit = state.units.find { it.officerId == officerId && it.isAlive }
            ?: throw IllegalArgumentException("Officer $officerId not found")
        // 태세 변경 10초 쿨다운: ticksSinceStanceChange >= 10 이어야 함
        unit.stance = stance
        unit.commandRange = 0.0
        unit.ticksSinceLastOrder = 0
        log.debug("Officer {} changed stance to {} in battle {}", officerId, stance.name, battleId)
    }
    ```
    TacticalUnit에 `var ticksSinceStanceChange: Int = 0` 추가 후 processTick 에서 매 틱 +1.

    ### BattleTriggerService.kt — TickEngine 연결 보완
    checkForBattles()가 생성한 신규 전투를 TacticalBattleService.activeBattles에 추가하려면,
    TacticalBattleService에 `fun registerNewBattle(battle: TacticalBattle)` 추가:
    ```kotlin
    fun registerNewBattle(battle: TacticalBattle) {
        val state = battleTriggerService.buildInitialState(battle)
        activeBattles[battle.id] = state
        broadcastBattleState(battle.sessionId, state, battle)
        log.info("New battle {} auto-registered from trigger", battle.id)
    }
    ```
    BattleTriggerService.checkForBattles() 호출 후 반환된 전투 목록을 registerNewBattle로 등록.
    TickEngine.processSessionBattles 앞에 checkForBattles 결과 처리 추가 (TacticalBattleService 내부에서 처리):
    ```kotlin
    fun processSessionBattles(sessionId: Long) {
        // 1. 새 전투 감지 및 등록
        val newBattles = battleTriggerService.checkForBattles(sessionId)
        newBattles.forEach { registerNewBattle(it) }
        // 2. 기존 활성 전투 틱 처리
        val activeBattleIds = tacticalBattleRepository.findBySessionIdAndPhase(sessionId, BattlePhase.ACTIVE.name).map { it.id }
        for (battleId in activeBattleIds) { processBattleTick(battleId) }
    }
    ```
    
    BattleTickBroadcast DTO에 `currentPhase: String` 필드 추가 후 broadcastBattleState에서 state.currentPhase 포함.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    컴파일 성공. /app/battle/{sessionId}/{battleId}/energy 와 /stance 엔드포인트 존재. processSessionBattles가 checkForBattles를 호출해 신규 전투를 자동 등록한다.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:test --tests "com.openlogh.engine.tactical.*" -x generateJooq 2>&1 | tail -30
./gradlew :game-app:compileKotlin -x generateJooq 2>&1 | tail -10
```

확인 항목:
- TacticalUnit에 stance/missileCount/shipSubtype/isFlagship/groundUnitsEmbark 필드 존재: `grep -n "var stance\|var missileCount\|var shipSubtype\|var isFlagship\|var groundUnitsEmbark" backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt`
- 태세 보정 적용: `grep -n "stance.attackModifier\|stance.defenseModifier" backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt`
- WebSocket 컨트롤러 존재: `grep -rn "battle.*energy\|battle.*stance" backend/game-app/src/main/kotlin/com/openlogh/controller/`
</verification>

<success_criteria>
- TacticalUnit 6개 신규 필드 추가 완료
- 태세 4종 attackModifier/defenseModifier/moraleDecayRate/canMove 룰 processTick에 적용
- 사기 20 미만 전투불가 규칙 적용
- /app/battle/{sessionId}/{battleId}/energy, /stance 두 WebSocket 채널 존재
- processSessionBattles가 checkForBattles()로 자동 전투 개시를 처리
- 컴파일 및 태세 관련 테스트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/03-tactical-battle-engine/03-01-SUMMARY.md` 생성
</output>
