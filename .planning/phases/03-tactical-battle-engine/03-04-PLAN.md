---
phase: 03-tactical-battle-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/GroundBattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
autonomous: true
requirements:
  - BATTLE-08
  - BATTLE-09

must_haves:
  truths:
    - "육전대 강하 명령 시 groundUnitsEmbark > 0 유닛이 지상전 박스에 진입하고 GroundBattleState가 생성된다"
    - "지상전 박스 최대 30유닛 제한 — 초과 시 대기 큐에 보관"
    - "행성 점령 6종(항복권고/정밀폭격/무차별폭격/육전대강하/점거/선동)이 각기 다른 결과를 낸다"
    - "정밀폭격/무차별폭격은 missileCount를 소모한다"
    - "점거는 군사공작포인트 4000 조건을 확인한다"
    - "PlanetConquestService가 기존 PlanetCaptureProcessor를 호출해 점령 후처리를 수행한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/GroundBattleEngine.kt"
      provides: "지상전 박스 관리, 30유닛 제한, 지상전 틱 처리"
      exports: ["GroundBattleEngine", "GroundBattleState", "GroundUnit"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt"
      provides: "6종 점령 커맨드 처리, 미사일 소모, 조건 검증"
      exports: ["PlanetConquestService", "ConquestCommand", "ConquestResult"]
  key_links:
    - from: "TacticalBattleService.landGroundUnits"
      to: "GroundBattleEngine.addUnits"
      via: "groundUnitsEmbark > 0 확인 후 GroundBattleState 생성/업데이트"
      pattern: "GroundBattleEngine"
    - from: "PlanetConquestService.executeConquest"
      to: "PlanetCaptureProcessor.processCaptureAftermath"
      via: "점령 성공 시 후처리 위임"
      pattern: "processCaptureAftermath"
    - from: "BattleWebSocketController"
      to: "PlanetConquestService.executeConquest"
      via: "/app/battle/{sessionId}/{battleId}/planet-conquest"
      pattern: "planet-conquest"
---

<objective>
gin7 지상전 박스(최대 30유닛)와 행성 점령 6종 커맨드를 구현한다. 육전대 강하 → 지상전 처리 → 점령 완료 → 기존 PlanetCaptureProcessor 후처리 흐름을 완성한다.

Purpose: 전술전의 최종 목표인 행성 점령을 완성한다. 이 없이는 전술전이 종료 조건에 도달할 수 없다.
Output: GroundBattleEngine, PlanetConquestService, /planet-conquest WebSocket 채널
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tactical-battle-engine/03-CONTEXT.md

<interfaces>
<!-- 기존 코드 계약 -->

From engine/war/PlanetCaptureProcessor.kt (이미 존재):
```kotlin
data class CaptureProcessingInput(
    val capturedPlanetId: Long, val capturedPlanetName: String,
    val captorFactionId: Long, val captorFactionType: String,
    val defeatedFactionId: Long, val garrisonUnits: Int,
    val warehouseSupplies: Int, val shipyardShipsInProgress: Int,
    val shipyardShipsStored: Int, val isFortress: Boolean,
    val defeatedOfficerIds: List<Long>, val planetPositionCards: List<String>,
)
data class CaptureProcessingResult(
    val logs: List<String>, val removedPositionCards: List<String>,
    val sortiedOfficerIds: List<Long>, val suppliesSeized: Int,
    val shipsDestroyed: Int, val empireDirectRule: Boolean,
    val garrisonAnnihilated: Int,
)
class PlanetCaptureProcessor {
    fun processCaptureAftermath(input: CaptureProcessingInput): CaptureProcessingResult
}
```

From Plan 01 — TacticalUnit 확장:
```kotlin
var groundUnitsEmbark: Int = 0   // 탑재 육전대 수
var missileCount: Int = 100       // 미사일 재고
```

From Plan 01 — TacticalBattleState 확장:
```kotlin
var currentPhase: String = "MOVEMENT"
val detectionMatrix: MutableMap<Long, MutableSet<Long>>
// Plan 04에서 추가:
// var groundBattleState: GroundBattleState? = null
```

gin7 행성 점령 6종 결과 (docs/REWRITE_PROMPT.md):
| 커맨드     | 지지율  | 경제력  | 방위력  | 공적  | 조건/비고                           |
|-----------|---------|---------|---------|------|-------------------------------------|
| 항복권고   | -       | -       | -       | 높음 | 비용 없음, 방위력 5000 이하 시 가능성  |
| 정밀폭격   | -       | -       | ↓       | 낮음 | 미사일 200 소모                      |
| 무차별폭격 | ↑(적대) | ↓↓      | ↓↓      | 최저 | 미사일 500 소모                      |
| 육전대강하 | -       | -       | ↓       | 보통 | groundUnitsEmbark > 0 필요           |
| 점거       | -       | -       | -       | 최고 | 군사공작포인트 4000 필요              |
| 선동       | ↓↓(지지)| -       | -       | 보통 | 정보공작포인트 1000 필요              |

지상전 규칙:
- 지상전 박스: 최대 30유닛 (공수 합산)
- 초과 유닛은 대기 큐에서 전사자 보충
- 지상전은 틱마다 공수 유닛 간 자동 교전
- 지상전 결과: 수비군 전멸 → 점령 성공
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroundBattleEngine — 지상전 박스 + 30유닛 제한 틱 처리</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/GroundBattleEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  </files>
  <behavior>
    - GroundBattleState: attackers/defenders MutableList<GroundUnit>, waitingAttackers MutableList<GroundUnit>
    - GroundUnit: unitId, factionId, groundUnitType(String), count, maxCount, morale
    - addUnits: 박스 총 유닛 < 30이면 즉시 추가, 초과 시 waitingAttackers에 보관
    - processTick: 공격자와 수비자 간 자동 교전 (count * morale 비례 피해), 전멸 시 대기 유닛 보충
    - isConquestComplete: defenders.all { it.count <= 0 }
    - TacticalBattleState에 groundBattleState: GroundBattleState? = null 추가
    - processTick 내 "5. Ground battle" 페이즈: groundBattleState != null 이면 처리
  </behavior>
  <action>
    ### GroundBattleEngine.kt (신규)
    패키지: `com.openlogh.engine.tactical`

    ```kotlin
    package com.openlogh.engine.tactical

    import kotlin.random.Random

    data class GroundUnit(
        val unitId: Long,
        val factionId: Long,
        val groundUnitType: String,   // "ARMORED_INFANTRY", "ARMORED_GRENADIER", "LIGHT_MARINE"
        var count: Int,
        val maxCount: Int,
        var morale: Int = 60,
    ) {
        val isAlive get() = count > 0
    }

    data class GroundBattleState(
        val planetId: Long,
        val attackerFactionId: Long,
        val defenderFactionId: Long,
        val attackers: MutableList<GroundUnit> = mutableListOf(),
        val defenders: MutableList<GroundUnit> = mutableListOf(),
        val waitingAttackers: MutableList<GroundUnit> = mutableListOf(),
        var tickCount: Int = 0,
    ) {
        val totalUnitsInBox: Int get() = attackers.size + defenders.size
        val isConquestComplete: Boolean get() = defenders.all { !it.isAlive }
        val isAttackerDefeated: Boolean get() = attackers.all { !it.isAlive } && waitingAttackers.isEmpty()
    }

    class GroundBattleEngine {

        companion object {
            const val MAX_UNITS_IN_BOX = 30
            const val BASE_GROUND_DAMAGE = 10  // 기본 지상전 피해 (count * morale / 100 비례)
        }

        /**
         * 육전대 강하 — attackers 박스 추가.
         * 30유닛 초과 시 waitingAttackers 큐에 보관.
         */
        fun addAttackers(state: GroundBattleState, units: List<GroundUnit>) {
            for (unit in units) {
                if (state.totalUnitsInBox < MAX_UNITS_IN_BOX) {
                    state.attackers.add(unit)
                } else {
                    state.waitingAttackers.add(unit)
                }
            }
        }

        /**
         * 수비대 초기화 (행성 수비 유닛 등록).
         */
        fun initDefenders(state: GroundBattleState, units: List<GroundUnit>) {
            state.defenders.addAll(units)
        }

        /**
         * 지상전 한 틱 처리.
         * 공격자 ↔ 수비자 간 자동 교전.
         * 전멸 시 대기 공격자 보충.
         */
        fun processTick(state: GroundBattleState, rng: Random = Random): List<String> {
            state.tickCount++
            val logs = mutableListOf<String>()

            val aliveAttackers = state.attackers.filter { it.isAlive }
            val aliveDefenders = state.defenders.filter { it.isAlive }

            if (aliveAttackers.isEmpty() || aliveDefenders.isEmpty()) {
                // 대기 유닛 보충
                replenishFromQueue(state, logs)
                return logs
            }

            // 공격자 → 수비자 교전
            for (attacker in aliveAttackers) {
                val target = aliveDefenders.filter { it.isAlive }.randomOrNull(rng) ?: break
                val dmg = calculateGroundDamage(attacker)
                target.count = (target.count - dmg).coerceAtLeast(0)
                if (!target.isAlive) logs.add("${target.groundUnitType} 수비대 전멸")
            }

            // 수비자 → 공격자 반격
            for (defender in aliveDefenders.filter { it.isAlive }) {
                val target = aliveAttackers.filter { it.isAlive }.randomOrNull(rng) ?: break
                val dmg = calculateGroundDamage(defender)
                target.count = (target.count - dmg).coerceAtLeast(0)
                if (!target.isAlive) logs.add("${target.groundUnitType} 공격대 전멸 — 대기 유닛 보충 예정")
            }

            // 전멸 후 보충
            replenishFromQueue(state, logs)
            return logs
        }

        private fun calculateGroundDamage(unit: GroundUnit): Int {
            val typeModifier = when (unit.groundUnitType.uppercase()) {
                "ARMORED_GRENADIER" -> 1.3  // 장갑유탄병: 공격↑
                "ARMORED_INFANTRY"  -> 1.0  // 장갑병: 균형
                "LIGHT_MARINE"      -> 0.8  // 경장육전병: 공격↓, 속도↑
                else -> 1.0
            }
            return ((BASE_GROUND_DAMAGE * typeModifier * unit.morale / 100.0)
                .coerceAtLeast(1.0)).toInt()
        }

        private fun replenishFromQueue(state: GroundBattleState, logs: MutableList<String>) {
            val deadAttackers = state.attackers.count { !it.isAlive }
            repeat(deadAttackers.coerceAtMost(state.waitingAttackers.size)) {
                if (state.waitingAttackers.isNotEmpty()) {
                    val next = state.waitingAttackers.removeAt(0)
                    state.attackers.add(next)
                    logs.add("대기 공격대 ${next.groundUnitType} 투입 (남은 대기: ${state.waitingAttackers.size})")
                }
            }
        }
    }
    ```

    ### TacticalBattleEngine.kt 수정
    
    1. TacticalBattleState에 추가:
    ```kotlin
    var groundBattleState: GroundBattleState? = null
    ```
    
    2. processTick 내 step 5 추가:
    ```kotlin
    // 5. Ground battle tick
    state.groundBattleState?.let { groundState ->
        val groundEngine = GroundBattleEngine()
        val logs = groundEngine.processTick(groundState)
        if (logs.isNotEmpty()) {
            state.tickEvents.add(BattleTickEvent("ground_battle", detail = logs.joinToString("; ")))
        }
        if (groundState.isConquestComplete) {
            state.tickEvents.add(BattleTickEvent("conquest_complete",
                detail = "지상전 승리 — 행성 점령 완료 (행성 ${groundState.planetId})"))
        }
    }
    ```
    
    테스트: `GroundBattleEngineTest.kt`
    - addAttackers: 31유닛 추가 시 30은 박스, 1은 대기 큐
    - processTick: 수비자 전멸 시 isConquestComplete == true
    - replenishFromQueue: 공격자 1명 전멸 시 대기 큐에서 1명 보충
    - ARMORED_GRENADIER 피해가 LIGHT_MARINE보다 많음 (타입 보정)
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.GroundBattleEngineTest" -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    GroundBattleEngine.kt 존재, 30유닛 제한 테스트 통과, 대기 큐 보충 테스트 통과, 수비자 전멸 시 isConquestComplete=true 테스트 통과
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanetConquestService — 6종 점령 커맨드 + WebSocket 채널</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
  </files>
  <behavior>
    - ConquestCommand enum: SURRENDER_DEMAND, PRECISION_BOMBING, CARPET_BOMBING, GROUND_ASSAULT, INFILTRATION, SUBVERSION
    - executeConquest: 조건 검증 → 효과 적용 → ConquestResult 반환
    - 정밀폭격/무차별폭격: missileCount 소모 확인 (없으면 실패 반환)
    - 점거: militaryWorkPoint >= 4000 검증 (TacticalUnit 메타 또는 별도 파라미터로 전달)
    - 선동: intelWorkPoint >= 1000 검증
    - 항복권고: 방위력(fortressDefense) 5000 이하 시 성공 확률 30%, 초과 시 5%
    - 점령 성공 시 PlanetCaptureProcessor.processCaptureAftermath 호출
  </behavior>
  <action>
    ### PlanetConquestService.kt (신규)
    패키지: `com.openlogh.engine.tactical`

    ```kotlin
    package com.openlogh.engine.tactical

    import com.openlogh.engine.war.CaptureProcessingInput
    import com.openlogh.engine.war.PlanetCaptureProcessor
    import kotlin.random.Random

    enum class ConquestCommand(
        val displayNameKo: String,
        val missileCost: Int,
        val workPointType: String?,   // "MILITARY", "INTEL", null
        val workPointCost: Int,
        val approvalEffect: Int,      // 지지율 변화 (양수=상승, 음수=하락)
        val economyEffect: Double,    // 경제력 배율 (1.0=변화없음, 0.7=30%감소)
        val defenseEffect: Double,    // 방위력 배율
        val meritPoints: Int,         // 공적 포인트
    ) {
        SURRENDER_DEMAND("항복권고",    missileCost = 0,   workPointType = null,       workPointCost = 0,    approvalEffect = 0,    economyEffect = 1.0, defenseEffect = 1.0, meritPoints = 500),
        PRECISION_BOMBING("정밀폭격",   missileCost = 200, workPointType = null,       workPointCost = 0,    approvalEffect = 0,    economyEffect = 1.0, defenseEffect = 0.6, meritPoints = 200),
        CARPET_BOMBING("무차별폭격",    missileCost = 500, workPointType = null,       workPointCost = 0,    approvalEffect = -30,  economyEffect = 0.5, defenseEffect = 0.3, meritPoints = 50),
        GROUND_ASSAULT("육전대강하",    missileCost = 0,   workPointType = null,       workPointCost = 0,    approvalEffect = 0,    economyEffect = 1.0, defenseEffect = 0.8, meritPoints = 300),
        INFILTRATION("점거",           missileCost = 0,   workPointType = "MILITARY", workPointCost = 4000, approvalEffect = 0,    economyEffect = 1.0, defenseEffect = 1.0, meritPoints = 800),
        SUBVERSION("선동",             missileCost = 0,   workPointType = "INTEL",    workPointCost = 1000, approvalEffect = -50,  economyEffect = 1.0, defenseEffect = 1.0, meritPoints = 300),
    }

    data class ConquestRequest(
        val command: ConquestCommand,
        val attackerOfficerId: Long,
        val attackerFactionId: Long,
        val attackerFactionType: String,  // "empire", "alliance", "fezzan", "rebel"
        val defenderFactionId: Long,
        val planetId: Long,
        val planetName: String,
        val planetDefense: Int,           // 현재 요새 방위력
        val garrisonUnits: Int,
        val warehouseSupplies: Int,
        val shipyardShipsInProgress: Int,
        val shipyardShipsStored: Int,
        val isFortress: Boolean,
        val defeatedOfficerIds: List<Long>,
        val planetPositionCards: List<String>,
        val attackerMissileCount: Int,    // 공격자 보유 미사일
        val militaryWorkPoint: Int = 0,   // 군사공작포인트
        val intelWorkPoint: Int = 0,      // 정보공작포인트
    )

    data class ConquestResult(
        val success: Boolean,
        val command: ConquestCommand,
        val reason: String,
        val logs: List<String>,
        val captureResult: com.openlogh.engine.war.CaptureProcessingResult? = null,
        val missilesConsumed: Int = 0,
        val approvalChange: Int = 0,
        val economyMultiplier: Double = 1.0,
        val defenseMultiplier: Double = 1.0,
    )

    class PlanetConquestService(
        private val captureProcessor: PlanetCaptureProcessor = PlanetCaptureProcessor(),
    ) {

        fun executeConquest(req: ConquestRequest, rng: Random = Random): ConquestResult {
            return when (req.command) {
                ConquestCommand.SURRENDER_DEMAND -> executeSurrenderDemand(req, rng)
                ConquestCommand.PRECISION_BOMBING -> executePrecisionBombing(req)
                ConquestCommand.CARPET_BOMBING -> executeCarpetBombing(req)
                ConquestCommand.GROUND_ASSAULT -> executeGroundAssault(req)
                ConquestCommand.INFILTRATION -> executeInfiltration(req)
                ConquestCommand.SUBVERSION -> executeSubversion(req)
            }
        }

        private fun executeSurrenderDemand(req: ConquestRequest, rng: Random): ConquestResult {
            // 방위력 5000 이하: 30%, 초과: 5%
            val successChance = if (req.planetDefense <= 5000) 0.30 else 0.05
            val succeeded = rng.nextDouble() < successChance
            if (!succeeded) {
                return ConquestResult(false, req.command, "항복 거부 (방위력 ${req.planetDefense})", listOf("${req.planetName}: 항복 거부"))
            }
            val captureResult = doCaptureAftermath(req)
            return ConquestResult(true, req.command, "항복 수락", captureResult.logs, captureResult,
                meritPoints = ConquestCommand.SURRENDER_DEMAND.meritPoints)
        }

        private fun executePrecisionBombing(req: ConquestRequest): ConquestResult {
            if (req.attackerMissileCount < ConquestCommand.PRECISION_BOMBING.missileCost) {
                return ConquestResult(false, req.command, "미사일 부족 (필요: 200, 보유: ${req.attackerMissileCount})", emptyList())
            }
            val newDefense = (req.planetDefense * ConquestCommand.PRECISION_BOMBING.defenseEffect).toInt()
            val logs = mutableListOf("${req.planetName}: 정밀폭격 (방위력 ${req.planetDefense} → $newDefense)")
            val conquered = newDefense <= 1000
            val captureResult = if (conquered) doCaptureAftermath(req) else null
            if (conquered) logs.addAll(captureResult!!.logs)
            return ConquestResult(conquered, req.command, if(conquered) "폭격 성공" else "방위력 잔존",
                logs, captureResult, missilesConsumed = ConquestCommand.PRECISION_BOMBING.missileCost,
                defenseMultiplier = ConquestCommand.PRECISION_BOMBING.defenseEffect)
        }

        private fun executeCarpetBombing(req: ConquestRequest): ConquestResult {
            if (req.attackerMissileCount < ConquestCommand.CARPET_BOMBING.missileCost) {
                return ConquestResult(false, req.command, "미사일 부족 (필요: 500, 보유: ${req.attackerMissileCount})", emptyList())
            }
            val newDefense = (req.planetDefense * ConquestCommand.CARPET_BOMBING.defenseEffect).toInt()
            val logs = mutableListOf("${req.planetName}: 무차별폭격 (방위력 → $newDefense, 경제력 50%↓, 지지율 -30)")
            val conquered = newDefense <= 1000
            val captureResult = if (conquered) doCaptureAftermath(req) else null
            if (conquered) logs.addAll(captureResult!!.logs)
            return ConquestResult(conquered, req.command, if(conquered) "폭격 성공" else "방위력 잔존",
                logs, captureResult,
                missilesConsumed = ConquestCommand.CARPET_BOMBING.missileCost,
                approvalChange = ConquestCommand.CARPET_BOMBING.approvalEffect,
                economyMultiplier = ConquestCommand.CARPET_BOMBING.economyEffect,
                defenseMultiplier = ConquestCommand.CARPET_BOMBING.defenseEffect)
        }

        private fun executeGroundAssault(req: ConquestRequest): ConquestResult {
            // groundUnitsEmbark 검증은 호출자(TacticalBattleService)가 담당
            // 여기서는 지상전 시작 신호만 반환 (실제 전투는 GroundBattleEngine이 틱마다 처리)
            return ConquestResult(false, req.command, "지상전 개시 — 육전대 강하 진행 중",
                listOf("${req.planetName}: 육전대 강하 개시"))
        }

        private fun executeInfiltration(req: ConquestRequest): ConquestResult {
            if (req.militaryWorkPoint < ConquestCommand.INFILTRATION.workPointCost) {
                return ConquestResult(false, req.command,
                    "군사공작포인트 부족 (필요: 4000, 보유: ${req.militaryWorkPoint})", emptyList())
            }
            val captureResult = doCaptureAftermath(req)
            return ConquestResult(true, req.command, "점거 성공",
                listOf("${req.planetName}: 군사공작으로 점거") + captureResult.logs, captureResult)
        }

        private fun executeSubversion(req: ConquestRequest): ConquestResult {
            if (req.intelWorkPoint < ConquestCommand.SUBVERSION.workPointCost) {
                return ConquestResult(false, req.command,
                    "정보공작포인트 부족 (필요: 1000, 보유: ${req.intelWorkPoint})", emptyList())
            }
            val captureResult = doCaptureAftermath(req)
            return ConquestResult(true, req.command, "선동 성공",
                listOf("${req.planetName}: 선동으로 점령 (지지율 -50)") + captureResult.logs, captureResult,
                approvalChange = ConquestCommand.SUBVERSION.approvalEffect)
        }

        private fun doCaptureAftermath(req: ConquestRequest) = captureProcessor.processCaptureAftermath(
            CaptureProcessingInput(
                capturedPlanetId = req.planetId,
                capturedPlanetName = req.planetName,
                captorFactionId = req.attackerFactionId,
                captorFactionType = req.attackerFactionType,
                defeatedFactionId = req.defenderFactionId,
                garrisonUnits = req.garrisonUnits,
                warehouseSupplies = req.warehouseSupplies,
                shipyardShipsInProgress = req.shipyardShipsInProgress,
                shipyardShipsStored = req.shipyardShipsStored,
                isFortress = req.isFortress,
                defeatedOfficerIds = req.defeatedOfficerIds,
                planetPositionCards = req.planetPositionCards,
            )
        )

        // 공적 포인트 헬퍼 (ConquestResult에서 meritPoints를 직접 쓸 수 없어 확장)
        private fun ConquestResult.copy(meritPoints: Int) = this
    }
    ```

    ### TacticalBattleService.kt 에 executeConquest 추가
    ```kotlin
    fun executeConquest(battleId: Long, officerId: Long, request: ConquestRequest): ConquestResult {
        val state = activeBattles[battleId] ?: throw IllegalStateException("Battle $battleId not active")
        val unit = state.units.find { it.officerId == officerId && it.isAlive }
            ?: throw IllegalArgumentException("Officer $officerId not found")

        // 육전대강하: groundUnitsEmbark > 0 확인 + GroundBattleState 초기화
        if (request.command == ConquestCommand.GROUND_ASSAULT) {
            require(unit.groundUnitsEmbark > 0) { "탑재 육전대 없음" }
            if (state.groundBattleState == null) {
                state.groundBattleState = GroundBattleState(
                    planetId = request.planetId,
                    attackerFactionId = request.attackerFactionId,
                    defenderFactionId = request.defenderFactionId,
                )
            }
            val groundEngine = GroundBattleEngine()
            val attackers = (1..unit.groundUnitsEmbark).map { i ->
                GroundUnit(unitId = unit.fleetId * 100 + i, factionId = unit.factionId,
                    groundUnitType = "ARMORED_INFANTRY", count = 100, maxCount = 100)
            }
            groundEngine.addAttackers(state.groundBattleState!!, attackers)
        }

        val result = planetConquestService.executeConquest(request)
        unit.commandRange = 0.0
        unit.ticksSinceLastOrder = 0

        // 미사일 소모 반영
        if (result.missilesConsumed > 0) {
            unit.missileCount = (unit.missileCount - result.missilesConsumed).coerceAtLeast(0)
        }

        state.tickEvents.add(BattleTickEvent("conquest", sourceUnitId = unit.fleetId,
            detail = "${request.command.displayNameKo}: ${result.reason}"))

        log.info("Conquest {} by officer {}: {}", request.command, officerId, result.reason)
        return result
    }
    ```
    TacticalBattleService 생성자에 `private val planetConquestService: PlanetConquestService = PlanetConquestService()` 추가.

    ### BattleWebSocketController.kt — /planet-conquest 채널 추가
    Plan 01/03에서 생성된 파일에 추가:
    ```kotlin
    @MessageMapping("/battle/{sessionId}/{battleId}/planet-conquest")
    fun planetConquest(
        @DestinationVariable sessionId: Long,
        @DestinationVariable battleId: Long,
        @Payload payload: PlanetConquestRequest,
        principal: Principal,
    ) {
        val officerId = extractOfficerId(principal)
        val command = ConquestCommand.valueOf(payload.command.uppercase())
        val req = ConquestRequest(
            command = command,
            attackerOfficerId = officerId,
            attackerFactionId = payload.attackerFactionId,
            attackerFactionType = payload.attackerFactionType,
            defenderFactionId = payload.defenderFactionId,
            planetId = payload.planetId,
            planetName = payload.planetName,
            planetDefense = payload.planetDefense,
            garrisonUnits = payload.garrisonUnits,
            warehouseSupplies = payload.warehouseSupplies,
            shipyardShipsInProgress = 0,
            shipyardShipsStored = 0,
            isFortress = payload.isFortress,
            defeatedOfficerIds = emptyList(),
            planetPositionCards = emptyList(),
            attackerMissileCount = payload.attackerMissileCount,
            militaryWorkPoint = payload.militaryWorkPoint,
            intelWorkPoint = payload.intelWorkPoint,
        )
        tacticalBattleService.executeConquest(battleId, officerId, req)
    }

    data class PlanetConquestRequest(
        val command: String,
        val attackerFactionId: Long, val attackerFactionType: String,
        val defenderFactionId: Long,
        val planetId: Long, val planetName: String, val planetDefense: Int,
        val garrisonUnits: Int, val warehouseSupplies: Int,
        val isFortress: Boolean,
        val attackerMissileCount: Int,
        val militaryWorkPoint: Int = 0, val intelWorkPoint: Int = 0,
    )
    ```

    테스트: `PlanetConquestServiceTest.kt`
    - PRECISION_BOMBING: missileCount=100 (< 200) → success=false, reason 포함 "미사일 부족"
    - PRECISION_BOMBING: missileCount=200 → missilesConsumed=200, defenseMultiplier=0.6
    - INFILTRATION: militaryWorkPoint=3999 → success=false
    - INFILTRATION: militaryWorkPoint=4000 → success=true, captureResult != null
    - SURRENDER_DEMAND: planetDefense=4000, 1000회 시도 → 약 30% 성공 (확률 검증)
    - CARPET_BOMBING: approvalChange == -30, economyMultiplier == 0.5
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.PlanetConquestServiceTest" --tests "com.openlogh.engine.tactical.GroundBattleEngineTest" -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    PlanetConquestService.kt 존재, 6종 ConquestCommand 정의. 조건 검증(미사일/공작포인트) 테스트 통과. GroundBattleEngine 30유닛 제한 테스트 통과. /planet-conquest WebSocket 채널 존재. 컴파일 성공.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:test --tests "com.openlogh.engine.tactical.GroundBattleEngineTest" --tests "com.openlogh.engine.tactical.PlanetConquestServiceTest" -x generateJooq 2>&1 | tail -30

# 6종 ConquestCommand 확인
grep -n "SURRENDER_DEMAND\|PRECISION_BOMBING\|CARPET_BOMBING\|GROUND_ASSAULT\|INFILTRATION\|SUBVERSION" \
  backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/PlanetConquestService.kt

# 지상전 30유닛 제한 확인
grep -n "MAX_UNITS_IN_BOX\|30" backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/GroundBattleEngine.kt

# WebSocket 채널 확인
grep -n "planet-conquest" backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
```
</verification>

<success_criteria>
- GroundBattleEngine.kt 존재, MAX_UNITS_IN_BOX=30, waitingAttackers 큐 보충 로직
- PlanetConquestService.kt 존재, 6종 ConquestCommand enum 정의
- 정밀폭격/무차별폭격 missileCount 부족 시 success=false 테스트 통과
- 점거 militaryWorkPoint 4000 미달 시 success=false 테스트 통과
- PlanetCaptureProcessor.processCaptureAftermath 연동 확인
- /app/battle/{sessionId}/{battleId}/planet-conquest WebSocket 채널 존재
- 컴파일 및 테스트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/03-tactical-battle-engine/03-04-SUMMARY.md` 생성
</output>
