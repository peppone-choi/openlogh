---
phase: 03-tactical-battle-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/FortressGunSystem.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
autonomous: true
requirements:
  - BATTLE-07

must_haves:
  truths:
    - "요새포 발사 시 사선 위 전체 유닛(아군 포함)에게 피해가 적용된다"
    - "요새포 위력: 토르해머=10000, 가이에스하켄=7000, 아르테미스=3000, 경X선=1500 (1000척 기준)"
    - "요새포는 에너지 충전 후 자동 발사 — fortressGunCooldown 틱 경과 후 재발사"
    - "POST /api/{sessionId}/battles/start REST 엔드포인트가 동작한다"
    - "/app/battle/{sessionId}/{battleId}/retreat WebSocket 채널이 동작한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/FortressGunSystem.kt"
      provides: "요새포 4종 상수, 사선 통과 피해, 아군 피격 로직"
      exports: ["FortressGunSystem", "FortressGunType", "processFortressGunFire"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt"
      provides: "POST /api/{sessionId}/battles/start, GET /api/{sessionId}/battles/active"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt"
      provides: "/app/battle/{sessionId}/{battleId}/retreat, /attack-target 채널 추가"
  key_links:
    - from: "TacticalBattleEngine.processFortressGun"
      to: "FortressGunSystem.processFortressGunFire"
      via: "state.fortressGunPower > 0 조건 하 위임"
      pattern: "FortressGunSystem"
    - from: "BattleRestController"
      to: "TacticalBattleService.startBattle"
      via: "POST /api/{sessionId}/battles/start"
      pattern: "startBattle"

---

<objective>
요새포(토르해머/가이에스하켄/아르테미스/경X선) 4종 스펙을 FortressGunSystem으로 분리 구현하고, 전투 REST API와 추가 WebSocket 배틀 채널(퇴각/공격대상)을 완성한다.

Purpose: 요새포는 gin7 전술전의 핵심 지형 요소이며, REST/WebSocket 채널은 프론트엔드(Phase 6) 통합에 필요하다.
Output: FortressGunSystem, BattleRestController, BattleWebSocketController 추가 채널
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tactical-battle-engine/03-CONTEXT.md

<interfaces>
<!-- Plan 01 완료 후 존재하는 계약 -->

From engine/tactical/TacticalBattleEngine.kt:
```kotlin
// 기존 processFortressGun — 이 Plan에서 FortressGunSystem으로 위임 리팩터
private fun processFortressGun(state: TacticalBattleState, rng: Random) {
    if (state.tickCount - state.fortressGunLastFired < state.fortressGunCooldown) return
    // 현재: fortressGunPower를 직접 사용해 단순 피해 분산
    // 변경: FortressGunSystem.processFortressGunFire(state, rng)로 위임
}

data class TacticalBattleState(
    var fortressGunLastFired: Int,
    var fortressGunPower: Int,   // 실제 요새포 위력값 (10000=토르해머 등)
    var fortressGunRange: Int,
    var fortressGunCooldown: Int,
    var fortressFactionId: Long,
)
```

gin7 요새포 스펙 (docs/REWRITE_PROMPT.md):
- 경X선 빔포:   1,500 (가르미슈/렌텐베르크 요새)
- 아르테미스:    3,000 (하이네센/케니히그라흐 요새)
- 가이에스하켄:  7,000 (가이에스부르크 요새)
- 토르 해머:    10,000 (이제르론 요새) — 1,000척 부대 기준 950척 소멸

사선 통과 규칙: 포문 위치에서 목표까지의 직선 위에 있는 모든 유닛(아군 포함) 명중.
쿨다운: 에너지 충전 시간 = fortressGunCooldown 틱 (이제르론=120, 가이에스부르크=90, 기타=60)

From service/TacticalBattleService.kt:
```kotlin
fun startBattle(sessionId, starSystemId, attackerFleetIds, defenderFleetIds): TacticalBattle
fun getActiveBattles(sessionId): List<TacticalBattleDto>
fun getBattleState(sessionId, battleId): TacticalBattleDto?
fun retreat(battleId, officerId)
```

BattleWebSocketController (Plan 01에서 /energy, /stance 이미 추가됨):
- 이 Plan: /retreat, /attack-target 채널 추가
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FortressGunSystem — 4종 요새포 + 사선 아군 피격</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/FortressGunSystem.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  </files>
  <behavior>
    - FortressGunType enum: THOR_HAMMER(10000, 120틱), GAIESBURGHER(7000, 90틱), ARTEMIS(3000, 60틱), LIGHT_XRAY(1500, 60틱)
    - processFortressGunFire: cooldown 체크 → 최대 HP 함대를 목표로 사선 계산 → 사선 위 전체 유닛 피해
    - 사선 위 아군 피격: tickEvents에 "(아군 피해!)" suffix
    - 피해량: fortressGunPower / max(1, unitsInPath.size) — 사선 통과 유닛 수로 분산 없음, 각 유닛 전체 피해
    - 실제 gin7 스펙: 각 유닛 전체 위력 적용 (분산 아님). 기존 코드의 분산 로직을 수정.
  </behavior>
  <action>
    ### FortressGunSystem.kt (신규)
    패키지: `com.openlogh.engine.tactical`
    
    ```kotlin
    package com.openlogh.engine.tactical

    import kotlin.math.sqrt
    import kotlin.random.Random

    /**
     * Fortress gun system (要塞砲) — gin7 tactical combat.
     *
     * Fires along a line of sight, hitting ALL units in the path including friendlies.
     * Each unit in the path takes FULL damage (not split).
     * Gun position: center-top of battle field (battleBoundsX/2, 0).
     */
    enum class FortressGunType(
        val displayNameKo: String,
        val power: Int,
        val cooldownTicks: Int,
        val range: Int,
        val lineWidth: Double,
    ) {
        /** 토르 해머 — 이제르론 요새 전용 */
        THOR_HAMMER("토르 해머", power = 10_000, cooldownTicks = 120, range = 1000, lineWidth = 50.0),
        /** 가이에스하켄 — 가이에스부르크 요새 전용 */
        GAIESBURGHER("가이에스하켄", power = 7_000, cooldownTicks = 90, range = 1000, lineWidth = 45.0),
        /** 아르테미스의 목걸이 — 하이네센/케니히그라흐 */
        ARTEMIS("아르테미스의 목걸이", power = 3_000, cooldownTicks = 60, range = 800, lineWidth = 35.0),
        /** 경X선 빔포 — 가르미슈/렌텐베르크 요새 */
        LIGHT_XRAY("경X선 빔포", power = 1_500, cooldownTicks = 60, range = 600, lineWidth = 25.0);

        companion object {
            fun fromPower(power: Int): FortressGunType = when {
                power >= 10_000 -> THOR_HAMMER
                power >= 7_000  -> GAIESBURGHER
                power >= 3_000  -> ARTEMIS
                else            -> LIGHT_XRAY
            }
        }
    }

    class FortressGunSystem {

        /**
         * Process fortress gun fire for one tick.
         * Modifies state in-place (hp reduction + tickEvents).
         */
        fun processFortressGunFire(state: TacticalBattleState, rng: Random = Random) {
            if (state.fortressGunPower <= 0) return
            if (state.tickCount - state.fortressGunLastFired < state.fortressGunCooldown) return

            // 목표: 적 함대 중 가장 큰 함대 (HP 최대)
            val enemies = state.units.filter {
                it.isAlive && it.factionId != state.fortressFactionId && !it.isRetreating
            }
            if (enemies.isEmpty()) return
            val target = enemies.maxByOrNull { it.ships } ?: return

            // 포문 위치: 전장 중앙 상단
            val gunX = state.battleBoundsX / 2.0
            val gunY = 0.0

            // 사선 위 전체 유닛 계산 (아군 포함)
            val unitsInPath = calculateLineOfFire(
                state = state,
                gunX = gunX, gunY = gunY,
                targetX = target.posX, targetY = target.posY,
                range = state.fortressGunRange.toDouble() * 100,
                lineWidth = FortressGunType.fromPower(state.fortressGunPower).lineWidth,
            )

            if (unitsInPath.isEmpty()) {
                // 사선 위 유닛 없어도 목표에게 직접 명중
                applyFortressHit(target, state.fortressGunPower, state, state.fortressFactionId)
            } else {
                // 사선 위 각 유닛에게 전체 위력 적용 (gin7: 분산 아님)
                for (hitUnit in unitsInPath) {
                    applyFortressHit(hitUnit, state.fortressGunPower, state, state.fortressFactionId)
                }
            }

            state.fortressGunLastFired = state.tickCount
            val gunType = FortressGunType.fromPower(state.fortressGunPower)
            state.tickEvents.add(BattleTickEvent("fortress_fire", detail = "${gunType.displayNameKo} 발사 (${unitsInPath.size}유닛 피격)"))
        }

        private fun applyFortressHit(unit: TacticalUnit, power: Int, state: TacticalBattleState, fortressFactionId: Long) {
            // 실드 흡수 적용
            val absorbed = (power * unit.energy.shieldAbsorption()).toInt()
            val finalDmg = (power - absorbed).coerceAtLeast(1)

            unit.hp -= finalDmg
            val shipLoss = (finalDmg.toDouble() / unit.maxHp.coerceAtLeast(1) * unit.maxShips).toInt()
            unit.ships = (unit.ships - shipLoss).coerceAtLeast(0)

            val isFriendlyFire = unit.factionId == fortressFactionId
            state.tickEvents.add(BattleTickEvent(
                "fortress_fire", targetUnitId = unit.fleetId, value = finalDmg,
                detail = "요새포 → ${unit.officerName} ($finalDmg 피해)" +
                    if (isFriendlyFire) " [아군 피해!]" else ""
            ))
        }

        /** 사선 위 유닛 계산 — 포문에서 목표까지 직선, lineWidth 이내 유닛 전부 */
        private fun calculateLineOfFire(
            state: TacticalBattleState,
            gunX: Double, gunY: Double,
            targetX: Double, targetY: Double,
            range: Double,
            lineWidth: Double,
        ): List<TacticalUnit> {
            return state.units.filter { unit ->
                if (!unit.isAlive || unit.isRetreating) return@filter false
                val dist = distanceToLine(unit.posX, unit.posY, gunX, gunY, targetX, targetY)
                val proj = projectOnLine(unit.posX, unit.posY, gunX, gunY, targetX, targetY)
                dist <= lineWidth && proj >= 0 && proj <= range
            }
        }

        private fun distanceToLine(px: Double, py: Double, lx1: Double, ly1: Double, lx2: Double, ly2: Double): Double {
            val dx = lx2 - lx1; val dy = ly2 - ly1
            val lenSq = dx * dx + dy * dy
            if (lenSq == 0.0) return sqrt((px - lx1) * (px - lx1) + (py - ly1) * (py - ly1))
            val t = ((px - lx1) * dx + (py - ly1) * dy) / lenSq
            val cx = lx1 + t.coerceIn(0.0, 1.0) * dx
            val cy = ly1 + t.coerceIn(0.0, 1.0) * dy
            return sqrt((px - cx) * (px - cx) + (py - cy) * (py - cy))
        }

        private fun projectOnLine(px: Double, py: Double, lx1: Double, ly1: Double, lx2: Double, ly2: Double): Double {
            val dx = lx2 - lx1; val dy = ly2 - ly1
            val lenSq = dx * dx + dy * dy
            if (lenSq == 0.0) return 0.0
            return ((px - lx1) * dx + (py - ly1) * dy) / sqrt(lenSq)
        }
    }
    ```

    ### TacticalBattleEngine.kt 수정
    
    1. FortressGunSystem 인스턴스 추가:
       `private val fortressGunSystem = FortressGunSystem()`
    
    2. 기존 processFortressGun() 메서드를 FortressGunSystem으로 위임:
    ```kotlin
    private fun processFortressGun(state: TacticalBattleState, rng: Random) {
        fortressGunSystem.processFortressGunFire(state, rng)
    }
    ```
    기존 processFortressGun 내부 로직(calculateLineOfFire 등)은 FortressGunSystem으로 이동됐으므로 제거.
    
    테스트: `FortressGunSystemTest.kt`
    - cooldown 미경과 시 발사 없음 (tickEvents.none { it.type == "fortress_fire" })
    - 사선 위 유닛에게 전체 위력 피해 (분산 아님: 2유닛이 사선 위 있어도 각각 10000 피해)
    - 아군(fortressFactionId 동일)이 사선 위에 있으면 "[아군 피해!]" 포함 이벤트 생성
    - FortressGunType.fromPower(10000) == THOR_HAMMER
    - FortressGunType.fromPower(1000) == LIGHT_XRAY
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.FortressGunSystemTest" -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    FortressGunSystem.kt 존재, 4종 enum 정의, 사선 전체 위력 적용 테스트 통과, 아군 피격 이벤트 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: BattleRestController + WebSocket 배틀 채널 완성</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
  </files>
  <action>
    ### BattleRestController.kt (신규)
    패키지: `com.openlogh.controller`
    
    ```kotlin
    @RestController
    @RequestMapping("/api/{sessionId}/battles")
    class BattleRestController(
        private val tacticalBattleService: TacticalBattleService,
    ) {
        /** 전투 수동 시작 (테스트/관리 용도) */
        @PostMapping("/start")
        fun startBattle(
            @PathVariable sessionId: Long,
            @RequestBody body: StartBattleRequest,
        ): ResponseEntity<TacticalBattleDto> {
            val battle = tacticalBattleService.startBattle(
                sessionId = sessionId,
                starSystemId = body.starSystemId,
                attackerFleetIds = body.attackerFleetIds,
                defenderFleetIds = body.defenderFleetIds,
            )
            return ResponseEntity.ok(TacticalBattleDto(
                id = battle.id, sessionId = battle.sessionId,
                starSystemId = battle.starSystemId,
                attackerFactionId = battle.attackerFactionId,
                defenderFactionId = battle.defenderFactionId,
                phase = battle.phase,
                startedAt = battle.startedAt.toString(),
                endedAt = null, result = null,
                tickCount = 0,
                attackerFleetIds = body.attackerFleetIds,
                defenderFleetIds = body.defenderFleetIds,
                units = emptyList(),
            ))
        }

        /** 세션의 활성 전투 목록 조회 */
        @GetMapping("/active")
        fun getActiveBattles(@PathVariable sessionId: Long): List<TacticalBattleDto> =
            tacticalBattleService.getActiveBattles(sessionId)

        /** 특정 전투 상태 조회 */
        @GetMapping("/{battleId}")
        fun getBattleState(
            @PathVariable sessionId: Long,
            @PathVariable battleId: Long,
        ): ResponseEntity<TacticalBattleDto> {
            val dto = tacticalBattleService.getBattleState(sessionId, battleId)
                ?: return ResponseEntity.notFound().build()
            return ResponseEntity.ok(dto)
        }
    }

    data class StartBattleRequest(
        val starSystemId: Long,
        val attackerFleetIds: List<Long>,
        val defenderFleetIds: List<Long>,
    )
    ```

    ### BattleWebSocketController.kt — /retreat, /attack-target 채널 추가
    Plan 01에서 생성된 파일에 아래 두 핸들러를 추가한다:

    ```kotlin
    /** 퇴각 명령: /app/battle/{sessionId}/{battleId}/retreat */
    @MessageMapping("/battle/{sessionId}/{battleId}/retreat")
    fun retreat(
        @DestinationVariable sessionId: Long,
        @DestinationVariable battleId: Long,
        principal: Principal,
    ) {
        val officerId = extractOfficerId(principal)
        tacticalBattleService.retreat(battleId, officerId)
    }

    /** 공격 대상 지정: /app/battle/{sessionId}/{battleId}/attack-target */
    @MessageMapping("/battle/{sessionId}/{battleId}/attack-target")
    fun setAttackTarget(
        @DestinationVariable sessionId: Long,
        @DestinationVariable battleId: Long,
        @Payload payload: AttackTargetRequest,
        principal: Principal,
    ) {
        val officerId = extractOfficerId(principal)
        tacticalBattleService.setAttackTarget(battleId, officerId, payload.targetFleetId)
    }

    data class AttackTargetRequest(val targetFleetId: Long)
    ```

    ### TacticalBattleService.kt — setAttackTarget 추가
    ```kotlin
    fun setAttackTarget(battleId: Long, officerId: Long, targetFleetId: Long) {
        val state = activeBattles[battleId] ?: throw IllegalStateException("Battle $battleId not active")
        val unit = state.units.find { it.officerId == officerId && it.isAlive }
            ?: throw IllegalArgumentException("Officer $officerId not found")
        // TacticalUnit에 var targetFleetId: Long? = null 필드 추가 (Plan 01 수정)
        unit.targetFleetId = targetFleetId
        unit.commandRange = 0.0
        unit.ticksSinceLastOrder = 0
        log.debug("Officer {} set attack target fleet {} in battle {}", officerId, targetFleetId, battleId)
    }
    ```
    TacticalUnit에 `var targetFleetId: Long? = null` 필드 추가.
    TacticalBattleEngine.processCombat에서 targetFleetId가 null이 아니면 해당 유닛을 우선 공격:
    ```kotlin
    val target = if (unit.targetFleetId != null) {
        enemies.find { it.fleetId == unit.targetFleetId && it.isAlive } 
            ?: enemies.filter { distance(unit, it) <= BEAM_RANGE }.minByOrNull { distance(unit, it) }
    } else {
        enemies.filter { distance(unit, it) <= BEAM_RANGE }.minByOrNull { distance(unit, it) }
    } ?: return
    ```
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    컴파일 성공. BattleRestController 존재 (POST /start, GET /active, GET /{battleId}). BattleWebSocketController에 /retreat, /attack-target 채널 추가. TacticalUnit.targetFleetId 필드 존재.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:test --tests "com.openlogh.engine.tactical.FortressGunSystemTest" -x generateJooq 2>&1 | tail -20
./gradlew :game-app:compileKotlin -x generateJooq 2>&1 | tail -10

# REST 컨트롤러 확인
grep -n "battles/start\|getActiveBattles\|getBattleState" backend/game-app/src/main/kotlin/com/openlogh/controller/BattleRestController.kt

# WebSocket 채널 확인
grep -n "retreat\|attack-target" backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt

# 요새포 위임 확인
grep -n "fortressGunSystem\|FortressGunSystem" backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
```
</verification>

<success_criteria>
- FortressGunSystem.kt 존재, FortressGunType 4종 (THOR_HAMMER/GAIESBURGHER/ARTEMIS/LIGHT_XRAY)
- 사선 통과 유닛 각각 전체 위력 피해 (분산 아님) 테스트 통과
- 아군 피격 시 "[아군 피해!]" 이벤트 생성 테스트 통과
- TacticalBattleEngine.processFortressGun이 FortressGunSystem으로 위임
- BattleRestController: POST /api/{sessionId}/battles/start, GET /api/{sessionId}/battles/active 존재
- BattleWebSocketController: /retreat, /attack-target 채널 추가
- TacticalUnit.targetFleetId 필드 및 우선공격 로직 존재
</success_criteria>

<output>
완료 후 `.planning/phases/03-tactical-battle-engine/03-03-SUMMARY.md` 생성
</output>
