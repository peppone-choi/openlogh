---
phase: 03-tactical-battle-engine
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
  - backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleIntegrationTest.kt
autonomous: true
requirements:
  - BATTLE-11
  - BATTLE-12

must_haves:
  truths:
    - "기함(isFlagship=true) 유닛이 hp <= 0 되면 InjuryEvent가 생성되고 officerRepository에 injury가 기록된다"
    - "부상 후 귀환성(returnPlanetId)으로 워프 — officer.planetId가 귀환성으로 변경된다"
    - "전술 유닛 커맨드 11종(이동/회전/평행이동/반전/공격/사격/공전/대열/수리/보급/출격)이 WebSocket으로 수신된다"
    - "통합 테스트: 2함대 전투 → 에너지 배분 변경 → 미사일 공격 → 기함 격침 → 부상 워프 흐름 검증"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt"
      provides: "기함 격침 InjuryEvent 생성, processTick 내 injury 처리"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt"
      provides: "processFlagshipDestruction: officer.injury 기록 + returnPlanetId 워프"
    - path: "backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt"
      provides: "전술 유닛 커맨드 11종 /app/battle/{sessionId}/{battleId}/unit-command 채널"
    - path: "backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleIntegrationTest.kt"
      provides: "전투 흐름 전체 통합 테스트"
  key_links:
    - from: "TacticalBattleEngine.processTick step 5"
      to: "TacticalBattleService.processFlagshipDestruction"
      via: "unit.isFlagship && unit.hp <= 0 → injuryEvents 수집 → endBattle 후 처리"
      pattern: "processFlagshipDestruction"
    - from: "InjuryEvent.resolveReturnPlanet"
      to: "officer.planetId"
      via: "officerRepository.save(officer.copy(planetId = returnPlanetId, injury = severity))"
      pattern: "resolveReturnPlanet"
---

<objective>
기함 격침 → 부상 → 귀환성 워프 시스템을 완성하고, 전술 유닛 커맨드 11종 WebSocket 채널을 추가한다. 마지막으로 Phase 3 전체 흐름을 검증하는 통합 테스트를 작성한다.

Purpose: 전술전 라이프사이클 완성 — 기함 손실의 인물 연동 효과와 커맨드 입력 채널이 없으면 플레이어가 전술전에 참여할 수 없다.
Output: 부상/워프 처리, 전술 커맨드 채널, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tactical-battle-engine/03-CONTEXT.md

<interfaces>
<!-- 기존 모델 계약 -->

From model/DeathInjurySystem.kt (이미 존재):
```kotlin
data class InjuryEvent(
    val officerId: Long, val officerName: String,
    val severity: Int,             // 0-80 스케일
    val returnPlanetId: Long,      // 귀환성
    val isDeath: Boolean = false,
    val tick: Int = 0,
) {
    companion object {
        const val DEFAULT_INJURY_SEVERITY = 20
        const val MAX_INJURY = 80
        fun calculateSeverity(currentInjury: Int, damageRatio: Double): Int
        fun resolveReturnPlanet(configuredReturnPlanetId: Long?, factionCapitalPlanetId: Long?, currentPlanetId: Long): Long
    }
}
```

From Plan 01 — TacticalUnit (이미 확장됨):
```kotlin
var isFlagship: Boolean = false
var shipSubtype: String = ""
var missileCount: Int = 100
var targetFleetId: Long? = null
```

Officer 엔티티 (기존 존재):
```kotlin
// officer.injury: Short  (부상 수치, 0~80)
// officer.planetId: Long (현재 위치 행성)
// officer.returnPlanetId: Long? (귀환성 설정)
// officerRepository.save(officer) — JPA 저장
```

전술 유닛 커맨드 11종 (gin7 매뉴얼 Chapter 4):
1. 이동 (MOVE): 방향+속도 설정
2. 회전 (TURN): 진행방향 변경
3. 평행이동 (STRAFE): 방향 유지 횡이동
4. 반전 (REVERSE): 180도 반전
5. 공격 (ATTACK): 공격 대상 지정 + 교전
6. 사격 (FIRE): 지정 위치/유닛 일제사격
7. 공전 (ORBIT): 지정 유닛 주위 선회
8. 대열 (FORMATION_CHANGE): 진형 변경
9. 수리 (REPAIR): 자함 수리 (사기+훈련 소모)
10. 보급 (RESUPPLY): 미사일 재보급 (행성 인근)
11. 출격 (SORTIE): 전투정/함재기 발진
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기함 격침 → 부상 → 귀환성 워프</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
  </files>
  <behavior>
    - processTick step 5(기존 "Remove destroyed units") 에서 isFlagship==true인 유닛이 hp<=0 이면 injuryEvents 목록에 추가
    - TacticalBattleState에 pendingInjuryEvents: MutableList<InjuryEvent> 추가
    - endBattle 호출 후 TacticalBattleService.processFlagshipDestructions(state) 호출
    - processFlagshipDestructions: officerRepository에서 officer 조회 → injury 수치 갱신 → returnPlanetId로 planetId 변경 → 저장
    - 다음 부대가 기함 부대로 대체: 기함 전멸 후 남은 유닛 중 가장 많은 ships 보유 유닛을 isFlagship=true로 설정
  </behavior>
  <action>
    ### TacticalBattleEngine.kt 수정
    
    1. TacticalBattleState에 추가:
    ```kotlin
    val pendingInjuryEvents: MutableList<InjuryEvent> = mutableListOf()
    ```
    
    2. processTick "5. Remove destroyed units" 수정:
    ```kotlin
    for (unit in state.units) {
        if (unit.hp <= 0 && unit.isAlive) {
            unit.isAlive = false
            unit.ships = 0
            state.tickEvents.add(BattleTickEvent("destroy", targetUnitId = unit.fleetId,
                detail = "${unit.officerName} 함대 궤멸"))

            // 기함 격침 → 부상 이벤트 생성
            if (unit.isFlagship) {
                val damageRatio = 1.0  // 기함 격침 = 최대 피해
                val severity = InjuryEvent.calculateSeverity(0, damageRatio)
                // returnPlanetId는 나중에 processFlagshipDestructions에서 DB 조회로 결정
                state.pendingInjuryEvents.add(InjuryEvent(
                    officerId = unit.officerId,
                    officerName = unit.officerName,
                    severity = severity,
                    returnPlanetId = 0L,  // 플레이스홀더, 서비스에서 실제 값으로 교체
                    tick = state.tickCount,
                ))
                state.tickEvents.add(BattleTickEvent("flagship_destroyed",
                    sourceUnitId = unit.fleetId, value = severity,
                    detail = "${unit.officerName} 기함 격침 → 부상 (중증도 $severity)"))

                // 다음 부대가 기함 대체
                val replacementUnit = state.units
                    .filter { it.isAlive && it.side == unit.side && it.fleetId != unit.fleetId }
                    .maxByOrNull { it.ships }
                if (replacementUnit != null) {
                    replacementUnit.isFlagship = true
                    state.tickEvents.add(BattleTickEvent("flagship_transfer",
                        sourceUnitId = replacementUnit.fleetId,
                        detail = "${replacementUnit.officerName} 기함 부대 대체"))
                }
            }
        }
    }
    ```
    
    ### TacticalBattleService.kt 수정
    
    1. endBattle 내부, `activeBattles.remove(battle.id)` 이후에 호출:
    ```kotlin
    // Process flagship destructions (injury + warp)
    if (state.pendingInjuryEvents.isNotEmpty()) {
        processFlagshipDestructions(state)
    }
    ```
    
    2. processFlagshipDestructions 메서드 추가:
    ```kotlin
    @Transactional
    fun processFlagshipDestructions(state: TacticalBattleState) {
        for (injuryEvent in state.pendingInjuryEvents) {
            val officer = officerRepository.findById(injuryEvent.officerId).orElse(null) ?: continue

            // 귀환성 결정: 설정된 귀환성 → 진영 수도 → 현재 행성
            val factionCapitalPlanetId: Long? = null  // TODO: FactionRepository에서 수도 조회 (Phase 4에서 구현)
            val returnPlanetId = InjuryEvent.resolveReturnPlanet(
                configuredReturnPlanetId = null,  // officer.returnPlanetId 미구현, 추후 추가
                factionCapitalPlanetId = factionCapitalPlanetId,
                currentPlanetId = officer.planetId ?: 1L,
            )

            // 부상 수치 갱신 (누적)
            val newInjury = (officer.injury.toInt() + injuryEvent.severity).coerceAtMost(InjuryEvent.MAX_INJURY)
            officer.injury = newInjury.toShort()

            // 귀환성 워프
            officer.planetId = returnPlanetId

            officerRepository.save(officer)

            log.info("Officer {} injured (severity={}) → warped to planet {}",
                officer.id, injuryEvent.severity, returnPlanetId)

            gameEventService.broadcastToSession(state.battleId, mapOf(
                "type" to "officer_injured",
                "officerId" to officer.id,
                "officerName" to officer.name,
                "severity" to newInjury,
                "returnPlanetId" to returnPlanetId,
            ))
        }
        state.pendingInjuryEvents.clear()
    }
    ```
    
    GameEventService에 `broadcastToSession(battleId: Long, data: Map<String, Any>)` 메서드가 없으면 추가:
    ```kotlin
    fun broadcastToSession(sessionId: Long, data: Map<String, Any>) {
        messagingTemplate.convertAndSend("/topic/world/$sessionId/events", data)
    }
    ```
    
    테스트: `TacticalBattleEngineTest.kt` 에 추가
    - isFlagship=true 유닛 hp=0 → pendingInjuryEvents.size == 1
    - pendingInjuryEvents[0].severity == InjuryEvent.DEFAULT_INJURY_SEVERITY (20)
    - 기함 전멸 후 같은 진영 잔존 유닛 중 ships 최대 유닛이 isFlagship=true로 변경
    - isFlagship=false 유닛 hp=0 → pendingInjuryEvents에 추가되지 않음
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.TacticalBattleEngineTest" -x generateJooq 2>&1 | tail -20</automated>
  </verify>
  <done>
    기함 격침 시 pendingInjuryEvents 생성 테스트 통과. 기함 대체 테스트 통과. processFlagshipDestructions 메서드 존재. 컴파일 성공.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 전술 유닛 커맨드 11종 채널 + 통합 테스트</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/TacticalBattleService.kt
    backend/game-app/src/test/kotlin/com/openlogh/engine/tactical/TacticalBattleIntegrationTest.kt
  </files>
  <behavior>
    - 단일 /unit-command 채널로 11종 커맨드 수신 (command: String 필드로 구분)
    - MOVE: unit.velX/velY 직접 설정 (dirX, dirY, speed 파라미터)
    - TURN/REVERSE/STRAFE: velX/velY 벡터 변환
    - ATTACK: setAttackTarget 재활용
    - FIRE: setAttackTarget + commandRange = 0 리셋
    - ORBIT: targetFleetId 설정 + 선회 플래그 (unit.isOrbiting=true)
    - FORMATION_CHANGE: setFormation 재활용
    - REPAIR: unit.morale 증가 (훈련 비례, 사기 -5 비용)
    - RESUPPLY: unit.missileCount 증가 (행성 인근 100 units 이내일 때만)
    - SORTIE: processFighterAttack 직접 트리거
    - 통합 테스트: H2 in-memory DB, 두 TacticalUnit 생성 → 틱 50회 처리 → 최종 상태 검증
  </behavior>
  <action>
    ### BattleWebSocketController.kt — /unit-command 채널 추가

    ```kotlin
    /**
     * 전술 유닛 커맨드 11종.
     * command 필드: MOVE/TURN/STRAFE/REVERSE/ATTACK/FIRE/ORBIT/FORMATION_CHANGE/REPAIR/RESUPPLY/SORTIE
     */
    @MessageMapping("/battle/{sessionId}/{battleId}/unit-command")
    fun unitCommand(
        @DestinationVariable sessionId: Long,
        @DestinationVariable battleId: Long,
        @Payload payload: UnitCommandRequest,
        principal: Principal,
    ) {
        val officerId = extractOfficerId(principal)
        tacticalBattleService.executeUnitCommand(battleId, officerId, payload)
    }

    data class UnitCommandRequest(
        val command: String,          // MOVE/TURN/STRAFE/REVERSE/ATTACK/FIRE/ORBIT/FORMATION_CHANGE/REPAIR/RESUPPLY/SORTIE
        val dirX: Double = 0.0,       // MOVE/TURN/STRAFE
        val dirY: Double = 0.0,
        val speed: Double = 1.0,      // MOVE
        val targetFleetId: Long? = null,  // ATTACK/FIRE/ORBIT
        val formation: String? = null,    // FORMATION_CHANGE
    )
    ```

    ### TacticalBattleService.kt — executeUnitCommand 추가

    ```kotlin
    fun executeUnitCommand(battleId: Long, officerId: Long, cmd: UnitCommandRequest) {
        val state = activeBattles[battleId] ?: throw IllegalStateException("Battle $battleId not active")
        val unit = state.units.find { it.officerId == officerId && it.isAlive }
            ?: throw IllegalArgumentException("Officer $officerId not found in battle $battleId")

        // 모든 커맨드는 commandRange 리셋
        unit.commandRange = 0.0
        unit.ticksSinceLastOrder = 0

        when (cmd.command.uppercase()) {
            "MOVE" -> {
                val norm = Math.sqrt(cmd.dirX * cmd.dirX + cmd.dirY * cmd.dirY).coerceAtLeast(0.001)
                val spd = TacticalBattleEngine.BASE_SPEED * cmd.speed.coerceIn(0.0, 2.0)
                unit.velX = (cmd.dirX / norm) * spd
                unit.velY = (cmd.dirY / norm) * spd
            }
            "TURN" -> {
                val norm = Math.sqrt(cmd.dirX * cmd.dirX + cmd.dirY * cmd.dirY).coerceAtLeast(0.001)
                unit.velX = (cmd.dirX / norm) * Math.sqrt(unit.velX * unit.velX + unit.velY * unit.velY)
                unit.velY = (cmd.dirY / norm) * Math.sqrt(unit.velX * unit.velX + unit.velY * unit.velY)
            }
            "STRAFE" -> {
                // 방향 유지, 횡이동 (velX 고정, velY만 변경)
                unit.velY = cmd.dirY * TacticalBattleEngine.BASE_SPEED
            }
            "REVERSE" -> {
                unit.velX = -unit.velX
                unit.velY = -unit.velY
            }
            "ATTACK", "FIRE" -> {
                if (cmd.targetFleetId != null) unit.targetFleetId = cmd.targetFleetId
            }
            "ORBIT" -> {
                if (cmd.targetFleetId != null) unit.targetFleetId = cmd.targetFleetId
                // isOrbiting 플래그 설정 (TacticalUnit에 추가)
                unit.isOrbiting = true
            }
            "FORMATION_CHANGE" -> {
                val formation = cmd.formation?.let { Formation.fromString(it) } ?: Formation.MIXED
                unit.formation = formation
            }
            "REPAIR" -> {
                // 수리: 사기 5 소모 → 훈련 비례 HP 회복
                if (unit.morale >= 5) {
                    unit.morale -= 5
                    val repairAmount = (unit.training / 100.0 * unit.maxHp * 0.05).toInt().coerceAtLeast(1)
                    unit.hp = (unit.hp + repairAmount).coerceAtMost(unit.maxHp)
                    state.tickEvents.add(BattleTickEvent("repair", sourceUnitId = unit.fleetId,
                        value = repairAmount, detail = "${unit.officerName} 자함 수리 (+$repairAmount HP)"))
                }
            }
            "RESUPPLY" -> {
                // 보급: 행성 인근(100 units) 이내에서만 가능
                // 간단한 판정: posX < 100 (attacker side near planet) or posX > 900 (defender side)
                val nearPlanet = unit.posX < 100.0 || unit.posX > 900.0
                if (nearPlanet && unit.missileCount < 100) {
                    val resupplyAmount = 20
                    unit.missileCount = (unit.missileCount + resupplyAmount).coerceAtMost(100)
                    state.tickEvents.add(BattleTickEvent("resupply", sourceUnitId = unit.fleetId,
                        value = resupplyAmount, detail = "${unit.officerName} 미사일 보급 (+$resupplyAmount)"))
                }
            }
            "SORTIE" -> {
                // 전투정 발진 (carrier 유닛만)
                val enemies = state.units.filter { it.side != unit.side && it.isAlive }
                val target = enemies.minByOrNull {
                    Math.sqrt((unit.posX - it.posX) * (unit.posX - it.posX) + (unit.posY - it.posY) * (unit.posY - it.posY))
                }
                if (target != null) {
                    missileWeaponSystem.processFighterAttack(unit, target, state)
                }
            }
            else -> log.warn("Unknown unit command: {}", cmd.command)
        }
    }
    ```
    TacticalUnit에 `var isOrbiting: Boolean = false` 추가.
    TacticalBattleService에 `private val missileWeaponSystem = MissileWeaponSystem()` 추가.
    BASE_SPEED를 TacticalBattleEngine companion에서 `const val`로 public 접근 가능하게 이미 정의됨 확인.

    ### TacticalBattleIntegrationTest.kt (신규)

    ```kotlin
    package com.openlogh.engine.tactical

    import com.openlogh.model.EnergyAllocation
    import com.openlogh.model.Formation
    import com.openlogh.model.UnitStance
    import org.junit.jupiter.api.Test
    import org.junit.jupiter.api.Assertions.*
    import kotlin.random.Random

    /**
     * Integration test: full tactical battle lifecycle.
     * Uses in-memory state only (no DB dependencies).
     */
    class TacticalBattleIntegrationTest {

        private val engine = TacticalBattleEngine()
        private val rng = Random(42L)  // deterministic

        @Test
        fun `full battle lifecycle - 2 units fight until one side is eliminated`() {
            val attacker = TacticalUnit(
                fleetId = 1L, officerId = 101L, officerName = "라인하르트",
                factionId = 1L, side = BattleSide.ATTACKER,
                posX = 100.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                training = 80, morale = 80,
                leadership = 90, command = 90, intelligence = 70,
                mobility = 70, attack = 90, defense = 60,
                energy = EnergyAllocation(beam=30, gun=25, shield=20, engine=15, warp=5, sensor=5),
                formation = Formation.WEDGE,
                stance = UnitStance.COMBAT,
                isFlagship = true, missileCount = 100,
            )

            val defender = TacticalUnit(
                fleetId = 2L, officerId = 201L, officerName = "양웬리",
                factionId = 2L, side = BattleSide.DEFENDER,
                posX = 900.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                training = 80, morale = 80,
                leadership = 90, command = 85, intelligence = 95,
                mobility = 75, attack = 80, defense = 80,
                energy = EnergyAllocation(beam=20, gun=20, shield=30, engine=15, warp=5, sensor=10),
                formation = Formation.THREE_COLUMN,
                stance = UnitStance.COMBAT,
                isFlagship = true, missileCount = 100,
            )

            val state = TacticalBattleState(
                battleId = 1L, starSystemId = 100L,
                units = mutableListOf(attacker, defender),
            )

            // 최대 600틱 실행 — 종료 조건 도달 또는 타임아웃
            var outcome: BattleOutcome? = null
            repeat(600) {
                engine.processTick(state, rng)
                outcome = engine.checkBattleEnd(state)
                if (outcome != null) return@repeat
            }

            // 검증: 600틱 내 종료 조건 도달
            assertNotNull(outcome, "600틱 내 전투가 종료되어야 함")
            // 승자 존재 (무승부 가능하지만 최소한 종료 조건 달성)
            assertTrue(outcome!!.reason.isNotBlank())
        }

        @Test
        fun `energy allocation change resets commandRange to 0`() {
            val unit = TacticalUnit(
                fleetId = 1L, officerId = 101L, officerName = "테스트",
                factionId = 1L, side = BattleSide.ATTACKER,
                posX = 100.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                commandRange = 50.0,  // 이미 확대된 상태
            )
            val state = TacticalBattleState(
                battleId = 1L, starSystemId = 100L,
                units = mutableListOf(unit),
            )

            // 에너지 배분 변경 시뮬레이션 (TacticalBattleService.setEnergyAllocation 동작)
            unit.energy = EnergyAllocation(beam=30, gun=25, shield=20, engine=15, warp=5, sensor=5)
            unit.commandRange = 0.0
            unit.ticksSinceLastOrder = 0

            assertEquals(0.0, unit.commandRange)
            assertEquals(0, unit.ticksSinceLastOrder)
        }

        @Test
        fun `flagship destroyed creates pendingInjuryEvent`() {
            val flagship = TacticalUnit(
                fleetId = 1L, officerId = 101L, officerName = "라인하르트",
                factionId = 1L, side = BattleSide.ATTACKER,
                posX = 100.0, posY = 300.0,
                hp = 1, maxHp = 1000, ships = 1, maxShips = 300,
                isFlagship = true,
            )
            val enemy = TacticalUnit(
                fleetId = 2L, officerId = 201L, officerName = "양웬리",
                factionId = 2L, side = BattleSide.DEFENDER,
                posX = 110.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                energy = EnergyAllocation(beam=40, gun=30, shield=15, engine=10, warp=3, sensor=2),
            )
            val state = TacticalBattleState(
                battleId = 1L, starSystemId = 100L,
                units = mutableListOf(flagship, enemy),
            )

            // 충분히 많은 틱으로 격침 유도
            repeat(20) { engine.processTick(state, rng) }

            // 기함이 격침되었다면 pendingInjuryEvents에 기록
            if (!flagship.isAlive) {
                assertTrue(state.pendingInjuryEvents.any { it.officerId == 101L },
                    "기함 격침 시 pendingInjuryEvent 생성")
            }
        }

        @Test
        fun `missile count decreases on missile attack`() {
            val attacker = TacticalUnit(
                fleetId = 1L, officerId = 101L, officerName = "공격자",
                factionId = 1L, side = BattleSide.ATTACKER,
                posX = 100.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                missileCount = 10,
                energy = EnergyAllocation(beam=10, gun=10, shield=20, engine=20, warp=20, sensor=20),
            )
            val defender = TacticalUnit(
                fleetId = 2L, officerId = 201L, officerName = "방어자",
                factionId = 2L, side = BattleSide.DEFENDER,
                posX = 500.0, posY = 300.0,  // 미사일 사거리(800) 이내
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
            )
            val state = TacticalBattleState(
                battleId = 1L, starSystemId = 100L,
                units = mutableListOf(attacker, defender),
            )

            val initialMissiles = attacker.missileCount
            repeat(30) { engine.processTick(state, rng) }

            // 미사일이 발사됐으면 감소해야 함
            assertTrue(attacker.missileCount <= initialMissiles,
                "미사일 발사 후 missileCount 감소 (초기: $initialMissiles, 현재: ${attacker.missileCount})")
        }

        @Test
        fun `morale below 20 prevents combat`() {
            val lowMoraleUnit = TacticalUnit(
                fleetId = 1L, officerId = 101L, officerName = "사기저하",
                factionId = 1L, side = BattleSide.ATTACKER,
                posX = 100.0, posY = 300.0,
                hp = 1000, maxHp = 1000, ships = 300, maxShips = 300,
                morale = 15,  // 20 미만
                attack = 90,
                energy = EnergyAllocation(beam=30, gun=30, shield=20, engine=10, warp=5, sensor=5),
            )
            val enemy = TacticalUnit(
                fleetId = 2L, officerId = 201L, officerName = "적",
                factionId = 2L, side = BattleSide.DEFENDER,
                posX = 110.0, posY = 300.0,
                hp = 5000, maxHp = 5000, ships = 300, maxShips = 300,
            )
            val state = TacticalBattleState(
                battleId = 1L, starSystemId = 100L,
                units = mutableListOf(lowMoraleUnit, enemy),
            )

            engine.processTick(state, rng)

            // 사기 20 미만 유닛은 damage 이벤트 생성하지 않음
            val damageEvents = state.tickEvents.filter {
                it.type == "damage" && it.sourceUnitId == lowMoraleUnit.fleetId
            }
            assertTrue(damageEvents.isEmpty(), "사기 20 미만 유닛은 공격 불가")
        }
    }
    ```

    테스트 추가 확인:
    - 모든 TacticalUnit() 생성자 호출이 Plan 01~04에서 추가된 신규 필드(stance, missileCount 등)의 기본값을 사용함
    - TacticalUnit에 stance, missileCount, shipSubtype, isFlagship, groundUnitsEmbark, fighterSpeedDebuffTicks, ticksSinceStanceChange, targetFleetId, isOrbiting 필드가 기본값과 함께 존재하는지 확인 후 테스트 작성
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:test --tests "com.openlogh.engine.tactical.TacticalBattleIntegrationTest" -x generateJooq 2>&1 | tail -30</automated>
  </verify>
  <done>
    통합 테스트 5개 전체 통과. 전술 유닛 커맨드 11종 /unit-command WebSocket 채널 존재. 기함 격침 → pendingInjuryEvent 생성 테스트 통과.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/apple/Desktop/개인프로젝트/openlogh/backend
./gradlew :game-app:test --tests "com.openlogh.engine.tactical.*" -x generateJooq 2>&1 | tail -40

# Phase 3 전체 테스트
./gradlew :game-app:test -x generateJooq 2>&1 | grep -E "tests|PASSED|FAILED|ERROR" | tail -20

# 부상 시스템 확인
grep -n "pendingInjuryEvents\|processFlagshipDestructions\|isFlagship" \
  backend/game-app/src/main/kotlin/com/openlogh/engine/tactical/TacticalBattleEngine.kt

# 전술 커맨드 11종 채널 확인
grep -n "unit-command\|UnitCommandRequest" \
  backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt

# WebSocket 채널 전체 목록 확인
grep -n "@MessageMapping" backend/game-app/src/main/kotlin/com/openlogh/controller/BattleWebSocketController.kt
```
</verification>

<success_criteria>
- 기함(isFlagship=true) hp<=0 시 pendingInjuryEvents 생성 테스트 통과
- 기함 전멸 후 잔존 유닛 isFlagship=true 대체 테스트 통과
- processFlagshipDestructions: officer.injury 갱신 + officer.planetId = returnPlanetId
- /app/battle/{sessionId}/{battleId}/unit-command 채널 존재, 11종 커맨드 처리
- 통합 테스트 4개 이상 통과 (600틱 전투 종료, 에너지 리셋, 기함 격침, 사기 전투불가)
- ./gradlew :game-app:test -x generateJooq 전체 통과
</success_criteria>

<output>
완료 후 `.planning/phases/03-tactical-battle-engine/03-05-SUMMARY.md` 생성
</output>
