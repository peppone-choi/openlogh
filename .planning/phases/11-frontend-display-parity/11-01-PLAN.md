---
phase: 11-frontend-display-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-frontend-display-parity/11-AUDIT.md
autonomous: true
requirements: [FE-01, FE-02, FE-03, FE-04]

must_haves:
  truths:
    - "11-AUDIT.md gap summary table has at least 10 rows with non-empty Action Needed column"
    - "11-AUDIT.md contains field-by-field comparison tables for dashboard, general card, nation card, and city card sections"
    - "11-AUDIT.md battle log section documents small_war_log HTML structure with at least 5 CSS class-to-color mappings"
    - "11-AUDIT.md documents calcInjury, lbonus, win rate, kill ratio, dex level, tech level, next execute time, and age coloring formulas with exact code"
    - "11-AUDIT.md covers at least 15 legacy pages in the non-core page audit section"
  artifacts:
    - path: ".planning/phases/11-frontend-display-parity/11-AUDIT.md"
      provides: "Complete field-by-field audit of all legacy pages vs current frontend"
      min_lines: 200
  key_links:
    - from: "11-AUDIT.md"
      to: "11-02-PLAN.md"
      via: "Gap list drives implementation tasks"
      pattern: "Gap Type.*fullstack|FE-only|type-only"
---

<objective>
Conduct a full field-by-field audit of all legacy PHP/Vue pages against the current Next.js frontend, producing a structured gap document (11-AUDIT.md) that catalogs every missing field, missing display, and format difference.

Purpose: Per D-01 and D-02, this audit is the foundation for Plan 02's implementation work. Without a complete inventory, implementation would miss fields.
Output: `.planning/phases/11-frontend-display-parity/11-AUDIT.md` -- structured audit document with per-page field comparison tables and gap classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-display-parity/11-CONTEXT.md
@.planning/phases/11-frontend-display-parity/11-RESEARCH.md
@.planning/phases/11-frontend-display-parity/11-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs for field comparison -->

From frontend/src/types/index.ts:
```typescript
export interface GlobalInfo {
    year: number; month: number; turnTerm: number; startyear: number;
    genCount: number[][]; onlineNations: OnlineNationInfo[];
    onlineUserCnt: number; auctionCount: number; tournamentState: number;
    // ... ~30 fields total
    // NOTE: autorunUser is MISSING from this type but exists in backend response
}

export interface GeneralFrontInfo {
    // ~60 fields including: no, name, officerCity, lbonus, bill, specage, specage2,
    // killturn, betray, refreshScoreTotal, refreshScore, troopInfo, dex1-5, warnum, killnum, etc.
}

export interface NationFrontInfo {
    // ~20 fields including: tech, power, strategicCmdLimit, impossibleStrategicCommand, etc.
}

export interface CityFrontInfo {
    // ~15 fields including: pop, agri, comm, secu, def, wall, trade, officerList, trust, etc.
}
```

From frontend/src/lib/game-utils.ts:
```typescript
export function calcInjury(baseStat: number, injury: number): number;
export function formatInjury(injury: number): { text: string; color: string };
export function formatDexLevel(dex: number): DexInfo;
export function formatHonor(experience: number): string;
```

From frontend/src/lib/formatLog.ts:
```typescript
export function formatLog(text: string): ReactNode[];
// Handles color tags: R, B, G, M, C, L, S, O, D, Y, W
// Does NOT handle small_war_log HTML structure
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit core pages -- dashboard, general card, nation card, city card (FE-01, FE-02, FE-03)</name>
  <files>.planning/phases/11-frontend-display-parity/11-AUDIT.md</files>
  <read_first>
    legacy-core/hwe/ts/PageFront.vue
    legacy-core/hwe/ts/components/GameInfo.vue
    legacy-core/hwe/ts/components/GeneralBasicCard.vue
    legacy-core/hwe/ts/components/GeneralSupplementCard.vue
    legacy-core/hwe/ts/components/NationBasicCard.vue
    legacy-core/hwe/ts/components/CityBasicCard.vue
    legacy-core/hwe/ts/components/GameBottomBar.vue
    legacy-core/hwe/sammo/API/General/GetFrontInfo.php
    frontend/src/types/index.ts
    frontend/src/components/game/game-dashboard.tsx
    frontend/src/components/game/general-basic-card.tsx
    frontend/src/components/game/general-supplement-card.tsx
    frontend/src/components/game/nation-basic-card.tsx
    frontend/src/components/game/city-basic-card.tsx
    frontend/src/lib/game-utils.ts
  </read_first>
  <action>
    Create `.planning/phases/11-frontend-display-parity/11-AUDIT.md` with the following sections:

    **Section 1: Dashboard / GlobalInfo (FE-01)**
    For every field in legacy `GetFrontInfo.php` response and `GameInfo.vue` display:
    - List the field name, whether it exists in backend DTO, FE type (`GlobalInfo`), and FE display
    - Mark gap type: "None", "display-only" (type exists but not rendered), "type-missing" (not in FE type), "fullstack" (not in BE DTO)
    - Per D-01: compare 1:1 with FrontInfoResponse type
    - Known gaps from RESEARCH.md to verify: autorunUser (type-missing), joinMode/develCost/noticeMsg (display-only)

    **Section 2: General Card (FE-02)**
    For every field in legacy `GeneralBasicCard.vue` + `GeneralSupplementCard.vue`:
    - Compare with `general-basic-card.tsx` and `general-supplement-card.tsx`
    - Specifically verify these calculated values (per D-03):
      - `calcInjury` applied to displayed stats (legacy reduces displayed stats by injury%)
      - `lbonus` shown next to leadership (legacy shows "+N" in cyan)
      - Win rate: `(killnum / max(warnum, 1)) * 100`
      - Kill ratio: `(killcrew / max(deathcrew, 1)) * 100`
      - Dex level: `formatDexLevel()` with colored grade names
      - Age coloring by retirement proximity
      - Next execute time calculation: `Math.max(0, Math.floor((turnTime - lastExecuted) / 60000))` displayed as "N분 남음"
    - Known gaps: officerCity, bill, specage, killturn, refreshScore, betray, troopInfo not displayed in dashboard general card

    **Section 3: Nation Card (FE-03)**
    For every field in legacy `NationBasicCard.vue`:
    - Compare with `nation-basic-card.tsx`
    - Check tech level grade display (`convTechLevel` with dynamic max based on startyear)
    - Check impossible strategic command tooltip detail
    - Check population/crew aggregation formulas

    **Section 4: City Card (FE-03 supplement)**
    Quick comparison of `CityBasicCard.vue` vs `city-basic-card.tsx` -- RESEARCH.md says no significant gaps but verify.

    **Section 5: Gap Summary Table**
    Create a master table:
    ```
    | # | Field | Page | Gap Type | FE File | Legacy File | Action Needed |
    ```
    Classify each gap as:
    - `display-only`: Field exists in FE type, just not rendered -> add to component
    - `type-missing`: Field exists in BE response but not FE type -> add to types/index.ts + component
    - `fullstack`: Field only in legacy, not in BE DTO -> add BE DTO + FE type + component
    - `calc-missing`: Calculated value not implemented -> add calculation logic + display
    - `format-diff`: Field displayed but with different format -> update format

    Per D-06 and D-07: every gap gets an action, nothing deferred.
  </action>
  <verify>
    <automated>grep -c "^|" .planning/phases/11-frontend-display-parity/11-AUDIT.md | xargs test 20 -le</automated>
  </verify>
  <acceptance_criteria>
    - 11-AUDIT.md contains "## Section 1: Dashboard" heading
    - 11-AUDIT.md contains "## Section 2: General Card" heading
    - 11-AUDIT.md contains "## Section 3: Nation Card" heading
    - 11-AUDIT.md contains "## Section 5: Gap Summary" heading
    - 11-AUDIT.md gap summary table has at least 10 rows (known gaps from RESEARCH.md)
    - Every row in gap summary has a non-empty "Action Needed" column
    - 11-AUDIT.md contains "autorunUser" in the gap list
    - 11-AUDIT.md contains "calcInjury" or "injury reduction" in the general card section
    - 11-AUDIT.md contains "lbonus" in the general card section
    - 11-AUDIT.md contains "next execute time" or "다음 실행" in the general card section
    - 11-AUDIT.md contains "age coloring" or "나이 색상" in the general card section
  </acceptance_criteria>
  <done>All 4 core page categories audited field-by-field with legacy source. Gap summary table lists every missing field with classification and action.</done>
</task>

<task type="auto">
  <name>Task 2: Audit remaining 30+ pages and battle log format (FE-01, FE-04)</name>
  <files>.planning/phases/11-frontend-display-parity/11-AUDIT.md</files>
  <read_first>
    .planning/phases/11-frontend-display-parity/11-AUDIT.md
    legacy-core/hwe/ts/PageBattleCenter.vue
    legacy-core/hwe/ts/PageBoard.vue
    legacy-core/hwe/ts/PageChiefCenter.vue
    legacy-core/hwe/ts/PageGlobalDiplomacy.vue
    legacy-core/hwe/ts/PageHistory.vue
    legacy-core/hwe/ts/PageInheritPoint.vue
    legacy-core/hwe/ts/PageNPCControl.vue
    legacy-core/hwe/ts/PageNationBetting.vue
    legacy-core/hwe/ts/PageNationGeneral.vue
    legacy-core/hwe/ts/PageNationStratFinan.vue
    legacy-core/hwe/ts/PageTroop.vue
    legacy-core/hwe/ts/PageVote.vue
    legacy-core/hwe/ts/PageAuction.vue
    legacy-core/hwe/templates/small_war_log.php
    legacy-core/hwe/scss/battleLog.scss
    legacy-core/hwe/sammo/ActionLogger.php
    frontend/src/lib/formatLog.ts
    frontend/src/components/game/record-zone.tsx
  </read_first>
  <action>
    Append to the existing `11-AUDIT.md`:

    **Section 6: Non-Core Page Audit (D-02: 40+ route full audit)**
    For each legacy page listed in RESEARCH.md (PageBattleCenter, PageBoard, PageChiefCenter, PageGlobalDiplomacy, PageHistory, PageInheritPoint, PageNPCControl, PageNationBetting, PageNationGeneral, PageNationStratFinan, PageTroop, PageVote, PageAuction, and TS pages: battleCenter, bestGeneral, betting, currentCity, diplomacy, hallOfFame, msg, myPage, battle_simulator, select_npc, v_processing, extKingdoms, extExpandCity):
    - Read the legacy file
    - Find the corresponding current route page
    - Compare key displayed fields
    - Record gaps in the same format as Section 5
    - For pages that are opensamguk extensions (no legacy equivalent), note "opensamguk-only, skip"

    Priority order (per Claude's discretion): core game pages first (battle, troop, diplomacy, nation-generals, chief), then support pages (history, board, vote, auction, betting), then remaining.

    **Section 7: Battle Log Format Analysis (FE-04)**
    Document the exact battle log rendering pipeline:
    1. Read `legacy-core/hwe/templates/small_war_log.php` and document the HTML structure:
       - Outer `<div class="small_war_log">`
       - `.me` and `.you` spans with `.name_plate` and `.crew_plate` children
       - `.war_type` span with `.war_type_attack` / `.war_type_defense` / `.war_type_siege` variants
    2. Read `legacy-core/hwe/scss/battleLog.scss` and document CSS class-to-color mapping:
       - `.war_type_attack` -> cyan
       - `.war_type_defense` -> magenta
       - `.war_type_siege` -> white
       - `.name_plate_cover` -> yellow
       - `.crew_plate` -> orangered
       - `.name_plate` -> font-size: 0.75em
    3. Read `legacy-core/hwe/sammo/ActionLogger.php` lines 290-318 for battle log generation
    4. Document the detection heuristic: if message contains `class="small_war_log"`, route to HTML renderer; otherwise use `formatLog.ts` color tag parser
    5. Document the two rendering paths needed:
       - Path A: Color tag logs (existing `formatLog.ts`) -- no changes needed
       - Path B: HTML template battle logs -- new `BattleLogEntry` component needed
    6. Document wiring point: `record-zone.tsx` line 42 calls `formatLog(message)` -- this must be updated to check `isBattleLogHtml(message)` first, then route to `BattleLogEntry` for battle logs

    **Section 8: Updated Gap Summary**
    Append any new gaps found in Section 6 to the master gap table from Section 5.
    Add battle log gaps as separate entries with gap type "component-new".
  </action>
  <verify>
    <automated>grep -c "Section 7: Battle Log" .planning/phases/11-frontend-display-parity/11-AUDIT.md | xargs test 0 -lt</automated>
  </verify>
  <acceptance_criteria>
    - 11-AUDIT.md contains "## Section 6: Non-Core Page Audit"
    - 11-AUDIT.md contains "## Section 7: Battle Log Format Analysis"
    - 11-AUDIT.md contains "## Section 8: Updated Gap Summary"
    - Section 7 contains "small_war_log" string
    - Section 7 contains "war_type_attack" and "war_type_defense" and "war_type_siege"
    - Section 7 documents the CSS class-to-color mapping (at least 5 entries)
    - Section 7 documents two rendering paths (color tag vs HTML template)
    - Section 7 documents record-zone.tsx as the wiring point for BattleLogEntry
    - At least 15 legacy pages are mentioned in Section 6
    - Each page in Section 6 has a status (matched, gap found, or opensamguk-only)
  </acceptance_criteria>
  <done>All 40+ routes audited. Battle log HTML template fully documented. Complete gap inventory ready for Plan 02 implementation.</done>
</task>

</tasks>

<verification>
- 11-AUDIT.md exists with all 8 sections
- Gap summary table covers all FE-01 through FE-04 requirement areas
- Every identified gap has a classification and action
- Battle log format fully documented for Plan 02 implementation
</verification>

<success_criteria>
- Complete field-by-field audit document covering all legacy pages
- At least 10 concrete gaps identified with classification
- Battle log HTML structure documented with CSS mappings
- Calculated value formulas documented (injury, win rate, kill ratio, dex level, tech level, next execute time, age coloring)
- Document is actionable enough for Plan 02 to implement without re-reading legacy source
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-display-parity/11-01-SUMMARY.md`
</output>
