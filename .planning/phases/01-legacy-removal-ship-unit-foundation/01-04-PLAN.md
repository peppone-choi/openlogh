---
phase: 01-legacy-removal-ship-unit-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt
  - backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt
  - backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03

must_haves:
  truths:
    - "ship_unit 테이블이 DB에 존재한다 (V45 마이그레이션)"
    - "ShipUnit 엔티티가 Fleet FK로 연결된다"
    - "ShipUnit이 ShipClass + ShipSubtype + CrewProficiency를 보유한다"
    - "ShipUnitRepository가 findByFleetId()를 제공한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt"
      provides: "함선 유닛 엔티티 (300척 단위)"
      exports: ["ShipUnit"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt"
      provides: "ShipUnit CRUD + findByFleetId"
      exports: ["ShipUnitRepository"]
    - path: "backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql"
      provides: "ship_unit 테이블 DDL"
      contains: "CREATE TABLE ship_unit"
  key_links:
    - from: "ShipUnit.fleetId"
      to: "fleet.id FK"
      via: "REFERENCES fleet(id)"
      pattern: "REFERENCES fleet\\(id\\)|fleetId.*Fleet"
---

<objective>
ShipUnit 엔티티(300척 단위 함선 부대), ShipUnitRepository, V45 Flyway 마이그레이션을 생성한다. ShipUnit은 Fleet에 FK로 연결되며 ShipClass, ShipSubtype(이미 존재하는 enum), CrewProficiency(이미 존재하는 enum)를 사용한다.

Purpose: Phase 3 전술전과 Phase 2 커맨드 시스템이 ShipUnit 엔티티를 기반으로 동작한다. 엔티티가 없으면 함선 스탯 계산, 전투 유닛 생성, 물자 보급 커맨드가 모두 불가능하다.
Output: ShipUnit.kt, ShipUnitRepository.kt, V45__create_ship_unit_table.sql
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-legacy-removal-ship-unit-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- 기존 enum (이미 존재, import 대상) -->
<!-- backend/game-app/src/main/kotlin/com/openlogh/model/ShipSubtype.kt -->
```kotlin
enum class ShipSubtype(
    val code: Int,
    val shipClass: ShipClass,
    val displayNameKo: String,
    val displayNameEn: String,
    val beamPower: Int, val gunPower: Int, val shieldPower: Int,
    val enginePower: Int, val sensorPower: Int, val hullPoints: Int,
    val factionRestriction: String? = null,
) { BATTLESHIP_I(2001,...), BATTLESHIP_II(2002,...), ... FORTRESS(2601,...) }

enum class ShipClass(val code: Int, val displayNameKo: String, val shipsPerUnit: Int, ...) {
    BATTLESHIP(1), CRUISER(2), DESTROYER(3), CARRIER(4), TRANSPORT(5), HOSPITAL(6), FORTRESS(7)
}
```

<!-- backend/game-app/src/main/kotlin/com/openlogh/model/CrewProficiency.kt -->
```kotlin
enum class CrewProficiency(val level: Int, val displayName: String, val combatMultiplier: Double) {
    GREEN(0, "신병", 0.7), NORMAL(1, "일반", 1.0), VETERAN(2, "숙련", 1.2), ELITE(3, "정예", 1.5)
}
```

<!-- 기존 Fleet 엔티티 (FK 참조 대상) -->
<!-- @Table(name = "fleet"), id: Long -->
<!-- Fleet.id → ShipUnit.fleet_id REFERENCES fleet(id) -->

<!-- 최신 마이그레이션: V44__drop_turn_tables.sql -->
<!-- 다음 번호: V45 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V45 Flyway 마이그레이션 — ship_unit 테이블 생성</name>
  <files>
    backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql
  </files>
  <read_first>
    - backend/game-app/src/main/resources/db/migration/V44__drop_turn_tables.sql (최신 마이그레이션 패턴 확인)
    - backend/game-app/src/main/resources/db/migration/V37__tactical_battle.sql (테이블 구조 패턴 참조)
  </read_first>
  <action>
    파일 위치: `backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql`

    다음 DDL을 작성한다:

    ```sql
    -- V45: ShipUnit 엔티티 테이블 (gin7 함종 유닛 — 300척 단위 부대)
    -- ShipClass: BATTLESHIP/CRUISER/DESTROYER/CARRIER/TRANSPORT/HOSPITAL/FORTRESS
    -- ShipSubtype: 각 함종별 세부 타입 (예: BATTLESHIP_I, BATTLESHIP_II 등)
    -- CrewProficiency: GREEN/NORMAL/VETERAN/ELITE

    CREATE TABLE ship_unit (
        id                  BIGSERIAL PRIMARY KEY,
        session_id          BIGINT NOT NULL,
        fleet_id            BIGINT NOT NULL REFERENCES fleet(id) ON DELETE CASCADE,
        slot_index          SMALLINT NOT NULL DEFAULT 0,  -- 함대 내 부대 번호 (0~7)

        -- 함종 정보
        ship_class          VARCHAR(32) NOT NULL DEFAULT 'BATTLESHIP',
        ship_subtype        VARCHAR(32) NOT NULL DEFAULT 'BATTLESHIP_I',

        -- 함선 수
        ship_count          INT NOT NULL DEFAULT 0,
        max_ship_count      INT NOT NULL DEFAULT 300,

        -- 기본 전투 수치 (ShipStatRegistry에서 로드하여 갱신)
        armor               INT NOT NULL DEFAULT 0,
        shield              INT NOT NULL DEFAULT 0,
        weapon_power        INT NOT NULL DEFAULT 0,
        speed               INT NOT NULL DEFAULT 0,
        crew_capacity       INT NOT NULL DEFAULT 0,
        supply_capacity     INT NOT NULL DEFAULT 0,

        -- 상태
        morale              SMALLINT NOT NULL DEFAULT 50,
        training            SMALLINT NOT NULL DEFAULT 50,
        missile_stock       INT NOT NULL DEFAULT 100,
        stance              VARCHAR(16) NOT NULL DEFAULT 'CRUISE',
        crew_proficiency    VARCHAR(16) NOT NULL DEFAULT 'GREEN',

        -- 기함 정보
        is_flagship         BOOLEAN NOT NULL DEFAULT FALSE,
        flagship_code       VARCHAR(64) NOT NULL DEFAULT '',

        -- 지상부대 탑재 (수송함/착륙함)
        ground_unit_type    VARCHAR(32) NOT NULL DEFAULT '',
        ground_unit_count   INT NOT NULL DEFAULT 0,

        -- 유연 필드
        meta                JSONB NOT NULL DEFAULT '{}',

        created_at          TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
        updated_at          TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
    );

    -- 인덱스
    CREATE INDEX idx_ship_unit_fleet_id   ON ship_unit(fleet_id);
    CREATE INDEX idx_ship_unit_session_id ON ship_unit(session_id);
    CREATE INDEX idx_ship_unit_flagship   ON ship_unit(session_id, is_flagship) WHERE is_flagship = TRUE;
    ```

    주의사항:
    - fleet 테이블이 존재하는 마이그레이션 번호를 확인한다. `V34__add_unit_type_and_crew_table.sql` 또는 `V1__core_tables.sql`에서 fleet 테이블 정의를 찾는다.
    - fleet 테이블이 없으면 REFERENCES fleet(id) 대신 BIGINT NOT NULL로 작성하고 주석으로 표시한다.
    - JSONB 컬럼은 PostgreSQL 9.4+에서 지원된다 (현재 PostgreSQL 16 사용).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE ship_unit" /Users/apple/Desktop/개인프로젝트/openlogh/backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `V45__create_ship_unit_table.sql` 존재
    - `grep "CREATE TABLE ship_unit" V45__create_ship_unit_table.sql` — 1건
    - `grep "REFERENCES fleet" V45__create_ship_unit_table.sql` — 1건 (또는 fleet 테이블 부재 시 주석 처리)
    - `grep "crew_proficiency" V45__create_ship_unit_table.sql` — 1건
    - `grep "is_flagship" V45__create_ship_unit_table.sql` — 1건
  </acceptance_criteria>
  <done>V45 마이그레이션 파일 생성, ship_unit DDL 포함</done>
</task>

<task type="auto">
  <name>Task 2: ShipUnit 엔티티 + ShipUnitRepository 생성</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt
    backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/entity/Fleet.kt (엔티티 패턴, @Table 어노테이션 스타일)
    - backend/game-app/src/main/kotlin/com/openlogh/repository/OfficerRepository.kt (Repository 패턴)
    - backend/game-app/src/main/kotlin/com/openlogh/model/ShipSubtype.kt (import 경로 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/model/CrewProficiency.kt (import 경로 확인)
  </read_first>
  <action>
    **ShipUnit.kt 생성:**

    파일 위치: `backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt`

    ```kotlin
    package com.openlogh.entity

    import com.openlogh.model.CrewProficiency
    import com.openlogh.model.ShipClass
    import com.openlogh.model.ShipSubtype
    import jakarta.persistence.*
    import org.hibernate.annotations.JdbcTypeCode
    import org.hibernate.type.SqlTypes

    /**
     * 함선 유닛 — 300척 단위 부대.
     * Fleet 내 슬롯(0~7번)에 배속되며, 함종과 서브타입으로 전투 스탯이 결정된다.
     * 스탯은 ShipStatRegistry에서 로드 후 갱신된다 (Plan 05).
     */
    @Entity
    @Table(
        name = "ship_unit",
        indexes = [
            Index(name = "idx_ship_unit_fleet_id", columnList = "fleet_id"),
            Index(name = "idx_ship_unit_session_id", columnList = "session_id"),
        ]
    )
    class ShipUnit(

        @Id
        @GeneratedValue(strategy = GenerationType.IDENTITY)
        var id: Long = 0,

        @Column(name = "session_id", nullable = false)
        var sessionId: Long = 0,

        /** 소속 Fleet ID (fleet.id FK) */
        @Column(name = "fleet_id", nullable = false)
        var fleetId: Long = 0,

        /** 함대 내 부대 번호 (0~7). 0번이 사령관 직할 부대. */
        @Column(name = "slot_index", nullable = false)
        var slotIndex: Int = 0,

        // ── 함종 정보 ──────────────────────────────────────────────
        /** 함종 (ShipClass enum의 name). 예: "BATTLESHIP" */
        @Column(name = "ship_class", nullable = false, length = 32)
        var shipClass: String = ShipClass.BATTLESHIP.name,

        /** 서브타입 (ShipSubtype enum의 name). 예: "BATTLESHIP_I" */
        @Column(name = "ship_subtype", nullable = false, length = 32)
        var shipSubtype: String = ShipSubtype.BATTLESHIP_I.name,

        // ── 함선 수 ────────────────────────────────────────────────
        /** 현재 함선 수 (전투 피해로 감소). 최대 300. */
        @Column(name = "ship_count", nullable = false)
        var shipCount: Int = 0,

        @Column(name = "max_ship_count", nullable = false)
        var maxShipCount: Int = 300,

        // ── 기본 전투 수치 (ShipStatRegistry 로드 후 갱신) ────────
        @Column(nullable = false) var armor: Int = 0,
        @Column(nullable = false) var shield: Int = 0,
        @Column(name = "weapon_power", nullable = false) var weaponPower: Int = 0,
        @Column(nullable = false) var speed: Int = 0,
        @Column(name = "crew_capacity", nullable = false) var crewCapacity: Int = 0,
        @Column(name = "supply_capacity", nullable = false) var supplyCapacity: Int = 0,

        // ── 상태 ───────────────────────────────────────────────────
        @Column(nullable = false) var morale: Short = 50,
        @Column(nullable = false) var training: Short = 50,

        /** 미사일 재고 (소모형 무기). 0이 되면 미사일 발사 불가. */
        @Column(name = "missile_stock", nullable = false)
        var missileStock: Int = 100,

        /** 부대 자세: NAVIGATION / DOCK / CRUISE / COMBAT */
        @Column(nullable = false, length = 16)
        var stance: String = "CRUISE",

        /** 승조원 수련도 (CrewProficiency enum의 name). */
        @Column(name = "crew_proficiency", nullable = false, length = 16)
        var crewProficiency: String = CrewProficiency.GREEN.name,

        // ── 기함 정보 ──────────────────────────────────────────────
        /** 이 유닛이 기함 유닛이면 true. */
        @Column(name = "is_flagship", nullable = false)
        var isFlagship: Boolean = false,

        /** 고유 기함 코드 (빌헬미나 등). 기함이 아니면 빈 문자열. */
        @Column(name = "flagship_code", nullable = false, length = 64)
        var flagshipCode: String = "",

        // ── 지상부대 탑재 ──────────────────────────────────────────
        /** 탑재된 지상부대 타입 코드 (GroundUnitType). 없으면 빈 문자열. */
        @Column(name = "ground_unit_type", nullable = false, length = 32)
        var groundUnitType: String = "",

        /** 탑재된 지상부대 수. */
        @Column(name = "ground_unit_count", nullable = false)
        var groundUnitCount: Int = 0,

        // ── 유연 필드 ──────────────────────────────────────────────
        @JdbcTypeCode(SqlTypes.JSON)
        @Column(columnDefinition = "jsonb", nullable = false)
        var meta: MutableMap<String, Any> = mutableMapOf(),
    ) {
        /** ShipSubtype enum 조회 편의 메서드 */
        fun resolveSubtype(): ShipSubtype? = runCatching { ShipSubtype.valueOf(shipSubtype) }.getOrNull()

        /** CrewProficiency enum 조회 편의 메서드 */
        fun resolveProficiency(): CrewProficiency = runCatching { CrewProficiency.valueOf(crewProficiency) }.getOrElse { CrewProficiency.GREEN }

        /** 전투 유효 전투력 (수련도 승수 적용) */
        fun effectiveCombatPower(): Double = shipCount * resolveProficiency().combatMultiplier
    }
    ```

    **ShipUnitRepository.kt 생성:**

    파일 위치: `backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt`

    ```kotlin
    package com.openlogh.repository

    import com.openlogh.entity.ShipUnit
    import org.springframework.data.jpa.repository.JpaRepository
    import org.springframework.stereotype.Repository

    @Repository
    interface ShipUnitRepository : JpaRepository<ShipUnit, Long> {

        fun findByFleetId(fleetId: Long): List<ShipUnit>

        fun findByFleetIdAndSlotIndex(fleetId: Long, slotIndex: Int): ShipUnit?

        fun findBySessionId(sessionId: Long): List<ShipUnit>

        fun findByFleetIdAndIsFlagshipTrue(fleetId: Long): ShipUnit?

        fun countByFleetId(fleetId: Long): Long

        fun deleteByFleetId(fleetId: Long)
    }
    ```

    주의사항:
    - 기존 Fleet.kt 엔티티의 어노테이션 스타일(@Table, @Column 방식)을 따른다.
    - jakarta.persistence 임포트를 사용한다 (Spring Boot 3, not javax.persistence).
    - @JdbcTypeCode(SqlTypes.JSON)은 hibernate-core가 이미 game-app에 포함되어 있으면 사용 가능. 없으면 `var meta: String = "{}"`으로 대체하고 주석 표시.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | grep -E "error:|BUILD" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt` — 파일 존재
    - `ls backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt` — 파일 존재
    - `grep "fleetId" backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt` — 1건 이상
    - `grep "crew_proficiency\|crewProficiency" backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt` — 1건 이상
    - `grep "isFlagship\|is_flagship" backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt` — 1건 이상
    - `grep "findByFleetId" backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt` — 1건
    - `./gradlew :game-app:compileKotlin` BUILD SUCCESS
  </acceptance_criteria>
  <done>ShipUnit.kt, ShipUnitRepository.kt 생성, 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
# 파일 존재 확인
ls backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt
ls backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt
ls backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql

# DDL 핵심 확인
grep "CREATE TABLE ship_unit" \
  backend/game-app/src/main/resources/db/migration/V45__create_ship_unit_table.sql

# 엔티티 핵심 필드 확인
grep "fleetId\|shipSubtype\|crewProficiency\|isFlagship" \
  backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt

# 컴파일 확인
cd backend && ./gradlew :game-app:compileKotlin
```
</verification>

<success_criteria>
- V45__create_ship_unit_table.sql 존재, ship_unit CREATE TABLE 포함
- ShipUnit.kt 존재, fleetId FK + shipClass + shipSubtype + crewProficiency + isFlagship 필드
- ShipUnitRepository.kt 존재, findByFleetId() 포함
- `./gradlew :game-app:compileKotlin` BUILD SUCCESS
</success_criteria>

<output>
완료 후 `.planning/phases/01-legacy-removal-ship-unit-foundation/01-04-SUMMARY.md` 생성
</output>
