---
phase: 01-legacy-removal-ship-unit-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/general/
  - backend/game-app/src/main/kotlin/com/openlogh/command/nation/
autonomous: true
requirements:
  - LEGACY-01
  - LEGACY-02
  - LEGACY-03

must_haves:
  truths:
    - "CommandRegistry에 삼국지 커맨드가 0개 등록된다"
    - "Gin7CommandRegistry에 gin7 81종 stub이 등록되어 Spring 빈으로 주입된다"
    - "CommandExecutor가 Gin7CommandRegistry를 우선 사용한다 (@Primary)"
    - "기존 삼국지 command/general/ 및 command/nation/ 구현 파일이 삭제된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt"
      provides: "gin7 81종 stub 레지스트리"
      exports: ["Gin7CommandRegistry"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt"
      provides: "빈 레거시 레지스트리 (삭제되거나 비워짐)"
  key_links:
    - from: "CommandExecutor"
      to: "Gin7CommandRegistry"
      via: "@Primary Spring DI"
      pattern: "@Primary.*Gin7CommandRegistry|Gin7CommandRegistry.*@Primary"
---

<objective>
삼국지 커맨드 93종(officerCommands + factionCommands)을 CommandRegistry에서 모두 제거하고, gin7 81종 stub을 등록하는 Gin7CommandRegistry를 @Primary 빈으로 생성한다. 삼국지 커맨드 구현체 파일(command/general/, command/nation/)을 삭제한다.

Purpose: CommandRegistry에 삼국지 커맨드가 남아 있으면 Phase 2에서 gin7 커맨드 시스템을 구현할 때 두 시스템이 혼재하여 디버그가 불가능해진다. 클린 stub-first 접근으로 컴파일 가능한 상태를 유지하며 삭제한다.
Output: Gin7CommandRegistry.kt (gin7 81종 stub), 빈 CommandRegistry (또는 삭제), 삼국지 커맨드 파일 전량 삭제
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-removal-ship-unit-foundation/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- 기존 CommandRegistry 구조 (교체 대상) -->
<!-- backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt -->
```kotlin
typealias OfficerCommandFactory = (Officer, CommandEnv, Map<String, Any>?) -> OfficerCommand
typealias FactionCommandFactory = (Officer, CommandEnv, Map<String, Any>?) -> FactionCommand

@Component
class CommandRegistry {
    private val officerCommands = mutableMapOf<String, OfficerCommandFactory>()
    private val factionCommands = mutableMapOf<String, FactionCommandFactory>()
    // public API:
    fun registerOfficerCommand(key: String, factory: OfficerCommandFactory)
    fun registerFactionCommand(key: String, factory: FactionCommandFactory)
    fun createOfficerCommand(actionCode: String, general: Officer, env: CommandEnv, arg: Map<String, Any>?): OfficerCommand
    fun createFactionCommand(actionCode: String, general: Officer, env: CommandEnv, arg: Map<String, Any>?): FactionCommand?
    fun hasGeneralCommand(actionCode: String): Boolean
    fun hasNationCommand(actionCode: String): Boolean
    fun getSchema(actionCode: String): ArgSchema
    fun getGeneralCommandNames(): Set<String>
    fun getNationCommandNames(): Set<String>
}
```

<!-- CommandExecutor 생성자 (의존성 교체 대상) -->
```kotlin
@Service
class CommandExecutor @Autowired constructor(
    private val commandRegistry: CommandRegistry,  // ← Gin7CommandRegistry로 교체
    ...
)
```

<!-- commands.json 구조 (gin7 81종 데이터 소스) -->
<!-- backend/shared/src/main/resources/data/commands.json -->
<!-- 7개 그룹: operations(16), personal(15), commander(8), logistics(6), personnel(10), politics(12), intelligence(14) -->
<!-- 각 커맨드: id(영문), nameKo(한국어), cpCost, waitTime, duration -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gin7CommandRegistry 생성 (gin7 81종 stub)</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt (교체할 공개 API 확인)
    - backend/shared/src/main/resources/data/commands.json (81종 커맨드 목록 — id, nameKo, cpCost, waitTime, duration)
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandEnv.kt (CommandEnv 타입)
    - backend/game-app/src/main/kotlin/com/openlogh/command/OfficerCommand.kt (OfficerCommand 인터페이스)
  </read_first>
  <action>
    commands.json에서 81종 커맨드를 읽어 Gin7CommandRegistry를 작성한다.

    파일 위치: `backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt`

    구조:
    ```kotlin
    package com.openlogh.command

    import com.openlogh.entity.Officer
    import org.springframework.context.annotation.Primary
    import org.springframework.stereotype.Component

    /**
     * gin7 81종 커맨드 레지스트리. Phase 2에서 실제 구현체로 교체된다.
     * @Primary로 CommandExecutor에 주입되어 기존 CommandRegistry를 대체한다.
     */
    @Primary
    @Component
    class Gin7CommandRegistry : CommandRegistryPort {

        private val officerCommands = mutableMapOf<String, OfficerCommandFactory>()
        private val factionCommands = mutableMapOf<String, FactionCommandFactory>()

        init {
            // === 작전커맨드 (Operations, MCP, 16종) ===
            registerOfficer("워프항행")      // warp_navigation
            registerOfficer("연료보급")      // fuel_resupply
            registerOfficer("성계내항행")    // intra_system_navigation
            registerOfficer("군기유지")      // maintain_discipline
            registerOfficer("항공훈련")      // flight_training
            registerOfficer("육전훈련")      // ground_combat_training
            registerOfficer("함대훈련")      // fleet_training
            registerOfficer("전략회의")      // strategic_meeting
            registerOfficer("정찰")         // reconnaissance
            registerOfficer("포위망구축")    // encirclement
            registerOfficer("공세준비")      // offensive_preparation
            registerOfficer("수비준비")      // defensive_preparation
            registerOfficer("긴급발진")      // emergency_sortie
            registerOfficer("퇴각")         // retreat
            registerOfficer("전략적후퇴")    // strategic_retreat
            registerOfficer("대기")         // standby  ← ALWAYS_ALLOWED 기본 커맨드

            // === 개인커맨드 (Personal, PCP, 15종) ===
            registerOfficer("특기연마")      // skill_training
            registerOfficer("체력단련")      // physical_training
            registerOfficer("전술연구")      // tactics_study
            registerOfficer("지식습득")      // knowledge_acquisition
            registerOfficer("네트워크구축")  // network_building
            registerOfficer("사교활동")      // social_activity
            registerOfficer("개인훈련")      // personal_training
            registerOfficer("무기정비")      // weapon_maintenance
            registerOfficer("함선점검")      // ship_inspection
            registerOfficer("요양")         // recuperation
            registerOfficer("독서")         // reading
            registerOfficer("지원서작성")    // application_writing
            registerOfficer("자기개발")      // self_improvement
            registerOfficer("정보수집")      // information_gathering
            registerOfficer("개인접촉")      // personal_contact

            // === 지휘커맨드 (Commander, MCP, 8종) ===
            registerOfficer("함대편성")      // fleet_organization
            registerOfficer("진형변경")      // formation_change
            registerOfficer("에너지재배분")  // energy_reallocation
            registerOfficer("집합명령")      // rally_order
            registerOfficer("분산명령")      // disperse_order
            registerOfficer("전투배치")      // combat_deployment
            registerOfficer("전투중지")      // cease_fire
            registerOfficer("긴급귀환")      // emergency_return

            // === 병참커맨드 (Logistics, MCP, 6종) ===
            registerOfficer("물자배분")      // supply_distribution
            registerOfficer("보급요청")      // supply_request
            registerOfficer("함선보충")      // ship_replenishment
            registerOfficer("미사일보급")    // missile_resupply
            registerOfficer("창고이동")      // warehouse_transfer
            registerOfficer("생산발주")      // production_order

            // === 인사커맨드 (Personnel, PCP, 10종) ===
            registerOfficer("인사발령")      // personnel_assignment
            registerOfficer("승진추천")      // promotion_recommendation
            registerOfficer("강등처분")      // demotion
            registerOfficer("표창")         // commendation
            registerOfficer("징계")         // disciplinary_action
            registerOfficer("등용")         // recruitment
            registerOfficer("직책부여")      // position_assignment
            registerOfficer("부관임명")      // aide_appointment
            registerOfficer("전속명령")      // transfer_order
            registerOfficer("예비역전환")    // reserve_transfer

            // === 정치커맨드 (Politics, PCP, 12종) ===
            registerOfficer("세율조정")      // tax_adjustment
            registerOfficer("예산편성")      // budget_planning
            registerOfficer("기지건설")      // base_construction
            registerOfficer("조병창확장")    // arsenal_expansion
            registerOfficer("행성개발")      // planet_development
            registerOfficer("교역협정")      // trade_agreement
            registerOfficer("선전포고")      // declaration_of_war
            registerOfficer("종전제의")      // peace_proposal
            registerOfficer("불가침조약")    // non_aggression_pact
            registerOfficer("페잔차관")      // fezzan_loan
            registerOfficer("의회연설")      // parliament_speech
            registerOfficer("귀족회의")      // noble_council

            // === 첩보커맨드 (Intelligence, PCP, 14종) ===
            registerOfficer("정보분석")      // intelligence_analysis
            registerOfficer("적정탐지")      // enemy_detection
            registerOfficer("통신감청")      // communication_intercept
            registerOfficer("이중첩자")      // double_agent
            registerOfficer("허위정보유포")  // disinformation
            registerOfficer("암살시도")      // assassination_attempt
            registerOfficer("공작원파견")    // agent_dispatch
            registerOfficer("코드해독")      // code_decryption
            registerOfficer("위성감시")      // satellite_surveillance
            registerOfficer("내통공작")      // internal_subversion
            registerOfficer("선동공작")      // agitation_operation
            registerOfficer("시설파괴")      // facility_sabotage
            registerOfficer("탈출작전")      // escape_operation
            registerOfficer("중요인물보호")  // vip_protection
        }

        private fun registerOfficer(nameKo: String) {
            officerCommands[nameKo] = { officer, env, args ->
                object : OfficerCommand {
                    override fun run(): CommandResult {
                        return CommandResult(
                            success = false,
                            message = "[$nameKo] Phase 2에서 구현 예정 (stub)"
                        )
                    }
                }
            }
        }

        // CommandRegistryPort 구현 — 기존 CommandRegistry와 동일한 공개 API 유지
        override fun createOfficerCommand(actionCode: String, general: Officer, env: CommandEnv, arg: Map<String, Any>?): OfficerCommand {
            val factory = officerCommands[actionCode] ?: officerCommands["대기"]!!
            return factory(general, env, arg)
        }

        override fun createFactionCommand(actionCode: String, general: Officer, env: CommandEnv, arg: Map<String, Any>?): FactionCommand? = null

        override fun hasGeneralCommand(actionCode: String): Boolean = actionCode in officerCommands
        override fun hasNationCommand(actionCode: String): Boolean = false
        override fun getSchema(actionCode: String): ArgSchema = ArgSchema.NONE
        override fun getOfficerSchema(actionCode: String): ArgSchema = ArgSchema.NONE
        override fun getFactionSchema(actionCode: String): ArgSchema = ArgSchema.NONE
        override fun getGeneralCommandNames(): Set<String> = officerCommands.keys
        override fun getNationCommandNames(): Set<String> = emptySet()
    }
    ```

    주의사항:
    - CommandRegistry의 공개 메서드 시그니처를 그대로 유지하되, 인터페이스(CommandRegistryPort)가 없다면 클래스 상속이 아닌 동일한 메서드를 구현한다.
    - 실제 CommandRegistry 파일을 먼저 읽어 정확한 메서드 시그니처를 확인한다.
    - Gin7CommandRegistry가 @Primary이므로 CommandExecutor의 생성자 주입은 변경 없이 자동 교체된다.
    - "대기" 커맨드가 ALWAYS_ALLOWED (기본 휴식 대체). CommandExecutor의 ALWAYS_ALLOWED_COMMANDS 상수도 `setOf("대기")`로 교체한다 (현재 `setOf("휴식", "Nation휴식", "NPC능동", "CR건국", "CR맹훈련")`).

    커맨드 수 검증: init{} 블록에서 registerOfficer 호출 총계가 81이어야 한다. commands.json에서 정확한 nameKo를 확인하여 사용한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "registerOfficer\|registerFaction" backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt` 결과가 81 이상
    - `grep "@Primary" backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt` 결과 1건
    - `./gradlew :game-app:compileKotlin` 성공 (0 errors)
  </acceptance_criteria>
  <done>Gin7CommandRegistry.kt 생성, @Primary, 81종 stub 등록, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: 삼국지 커맨드 파일 삭제 + CommandRegistry 비우기</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt
    backend/game-app/src/main/kotlin/com/openlogh/command/general/
    backend/game-app/src/main/kotlin/com/openlogh/command/nation/
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandRegistry.kt (삭제할 import 및 init{} 블록 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/command/general/ (삭제 대상 파일 목록)
    - backend/game-app/src/main/kotlin/com/openlogh/command/nation/ (삭제 대상 파일 목록)
  </read_first>
  <action>
    Task 1 완료 후 실행한다. Gin7CommandRegistry가 @Primary이므로 CommandRegistry는 더 이상 주입되지 않지만, 파일이 존재하면 삼국지 import가 컴파일러를 오염시킨다.

    **단계 1: 삼국지 커맨드 구현 파일 삭제**

    삭제할 파일 목록 (command/general/):
    - CR건국.kt, CR맹훈련.kt, DomesticCommand.kt, DomesticUtils.kt, NPC능동.kt
    - che_군량매매.kt, che_기술연구.kt, che_농지개간.kt, che_단련.kt, che_모병.kt
    - che_물자조달.kt, che_사기진작.kt, che_상업투자.kt, che_성벽보수.kt, che_소집해제.kt
    - che_수비강화.kt, che_숙련전환.kt, che_정착장려.kt, che_주민선정.kt, che_징병.kt
    - che_치안강화.kt, che_헌납.kt, che_훈련.kt
    - 강행.kt, 거병.kt, 건국.kt, 견문.kt, 귀환.kt, 내정특기초기화.kt, 등용.kt
    - 등용수락.kt, 랜덤임관.kt, 모반시도.kt, 방랑.kt, 선양.kt, 선동.kt, 소집해제.kt
    - 순찰.kt, 요격.kt, 요양.kt, 은퇴.kt, 이동.kt, 임관.kt, 인재탐색.kt
    - 작전수립.kt, 장거리워프.kt, 장수대상임관.kt, 장비매매.kt, 전투특기초기화.kt
    - 전투태세.kt, 접경귀환.kt, 점거.kt, 정찰.kt, 증여.kt, 집합.kt, 첩보.kt
    - 출병.kt, 탈취.kt, 통신방해.kt, 파괴.kt, 하야.kt, 할당.kt, 함대재편.kt
    - 함선보급.kt, 화계.kt, 휴식.kt, 작전지시.kt, 생산감독.kt, 보충.kt, 재편성.kt
    - 물자배분.kt, 좌표이동.kt, 워프항행.kt, 장거리워프.kt
    - 그 외 command/general/ 내 모든 삼국지 전용 .kt 파일

    삭제할 파일 목록 (command/nation/):
    - Nation휴식.kt, 작전지시.kt, 예산편성.kt, 무작위수도이전.kt, 부대탈퇴지시.kt, cr_인구이동.kt
    - che_감축.kt, che_국기변경.kt, che_국호변경.kt, che_급습.kt, che_독립선언.kt
    - che_몰수.kt, che_무작위수도이전.kt, che_물자원조.kt, che_발령.kt, che_백성동원.kt
    - che_부대탈퇴지시.kt, che_불가침수락.kt, che_불가침제의.kt, che_불가침파기수락.kt
    - che_불가침파기제의.kt, che_선양요구.kt, che_선전포고.kt, che_수몰.kt, che_신속.kt
    - che_증축.kt, che_천도.kt, che_칭제.kt, che_천자맞이.kt, che_초토화.kt
    - che_필사즉생.kt, che_이호경식.kt, che_피장파장.kt, che_의병모집.kt, che_포상.kt
    - event_극병연구.kt, event_대검병연구.kt, event_무희연구.kt, event_산저병연구.kt
    - event_상병연구.kt, event_원융노병연구.kt, event_음귀병연구.kt, event_화륜차연구.kt
    - event_화시병연구.kt
    - 그 외 command/nation/ 내 모든 삼국지 전용 .kt 파일

    실제 삭제 전에 `ls` 로 파일 목록을 확인하고, 삭제한다:
    ```bash
    rm -f backend/game-app/src/main/kotlin/com/openlogh/command/general/*.kt
    rm -f backend/game-app/src/main/kotlin/com/openlogh/command/nation/*.kt
    ```

    **단계 2: CommandRegistry.kt의 init{} 블록 비우기**

    CommandRegistry.kt에서 삼국지 커맨드 등록 init{} 블록을 전부 제거한다.
    import 문에서 `command.general.*`, `command.nation.*` 를 제거한다.
    클래스 구조(메서드)는 유지한다 — Gin7CommandRegistry 이외의 테스트가 CommandRegistry를 직접 참조하는 경우 컴파일 실패 방지.

    **단계 3: CommandExecutor의 ALWAYS_ALLOWED_COMMANDS 교체**

    CommandExecutor.kt에서 다음을 찾아 교체한다:
    ```kotlin
    // 기존 (삼국지)
    private val ALWAYS_ALLOWED_COMMANDS = setOf("휴식", "Nation휴식", "NPC능동", "CR건국", "CR맹훈련")
    // 변경 후 (gin7)
    private val ALWAYS_ALLOWED_COMMANDS = setOf("대기")
    ```

    **단계 4: 컴파일 확인**

    `./gradlew :game-app:compileKotlin`이 성공해야 한다. 실패하면 삭제된 파일을 참조하는 다른 파일에서 unresolved reference 오류가 발생한다 — 해당 import/참조를 제거하거나 stub으로 대체한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | grep -E "error:|BUILD" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/game-app/src/main/kotlin/com/openlogh/command/general/*.kt 2>/dev/null | wc -l` 결과가 0
    - `ls backend/game-app/src/main/kotlin/com/openlogh/command/nation/*.kt 2>/dev/null | wc -l` 결과가 0
    - `grep -r "농지개간\|상업투자\|칭제\|천자맞이\|CR건국" backend/game-app/src/ 2>/dev/null | grep -v ".md"` 결과가 0
    - `grep "ALWAYS_ALLOWED" backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt` 에 "대기" 포함
    - `./gradlew :game-app:compileKotlin` BUILD SUCCESS
  </acceptance_criteria>
  <done>삼국지 커맨드 구현 파일 전량 삭제, CommandRegistry init{} 비움, ALWAYS_ALLOWED 교체, 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
# CommandRegistry에 삼국지 커맨드 0건
grep -r "농지개간\|상업투자\|징병\|칭제\|선양\|CR건국" \
  backend/game-app/src/main/kotlin/com/openlogh/command/ 2>/dev/null | grep -v ".md"

# Gin7CommandRegistry @Primary 확인
grep "@Primary" backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt

# stub 커맨드 수 확인
grep -c "registerOfficer" backend/game-app/src/main/kotlin/com/openlogh/command/Gin7CommandRegistry.kt

# 컴파일 최종 확인
cd backend && ./gradlew :game-app:compileKotlin
```
</verification>

<success_criteria>
- Gin7CommandRegistry.kt 존재, @Primary, gin7 81종 stub 등록
- command/general/, command/nation/ 삼국지 파일 전량 삭제
- CommandRegistry init{} 비어 있음
- ALWAYS_ALLOWED_COMMANDS = setOf("대기")
- `./gradlew :game-app:compileKotlin` BUILD SUCCESS
</success_criteria>

<output>
완료 후 `.planning/phases/01-legacy-removal-ship-unit-foundation/01-01-SUMMARY.md` 생성
</output>
