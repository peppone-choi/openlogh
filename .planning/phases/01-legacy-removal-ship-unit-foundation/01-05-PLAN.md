---
phase: 01-legacy-removal-ship-unit-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/CommandService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/FactionService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/service/FrontInfoService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/UnificationService.kt
autonomous: true
requirements:
  - SHIP-04
  - SHIP-05
  - SHIP-06

must_haves:
  truths:
    - "ship_stats_empire.json / ship_stats_alliance.json의 서브타입 스탯이 ShipStatRegistry에 로드된다"
    - "ShipUnit 생성 시 ShipStatRegistry에서 스탯을 주입받는다"
    - "grep -r 'officerLevel >= 5' backend/game-app/src/ 결과가 0건이다 (authority bypass 완전 제거)"
    - "CommandExecutor가 PositionCard 권한만으로 커맨드 접근을 제어한다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt"
      provides: "JSON → in-memory 서브타입 스탯 조회"
      exports: ["ShipStatRegistry"]
    - path: "backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt"
      provides: "ShipUnit CRUD + 스탯 주입"
      exports: ["ShipUnitService"]
  key_links:
    - from: "ShipStatRegistry"
      to: "ship_stats_empire.json / ship_stats_alliance.json"
      via: "ClassPathResource + Jackson"
      pattern: "ship_stats_empire\\.json|ship_stats_alliance\\.json"
    - from: "ShipUnitService.createShipUnit()"
      to: "ShipStatRegistry.getStats()"
      via: "직접 호출"
      pattern: "shipStatRegistry\\.getStats|ShipStatRegistry"
---

<objective>
ship_stats_empire.json과 ship_stats_alliance.json을 로드하는 ShipStatRegistry를 생성하고, ShipUnit 생성 시 서브타입 스탯을 주입하는 ShipUnitService를 작성한다. 동시에 코드베이스 전체에서 `officerLevel >= 5` authority bypass를 제거하고 PositionCard 기반 권한 체크만 남긴다.

Purpose: ShipUnit 서브타입 스탯이 하드코딩 상수로 남으면 88 서브타입 전투가 무의미해진다. officerLevel >= 5 우회가 남아 있으면 Phase 2 PositionCard 시스템이 구현되어도 실제로 작동하지 않는다. 두 문제를 이 Plan에서 동시에 해결한다.
Output: ShipStatRegistry.kt, ShipUnitService.kt, officerLevel >= 5 제거
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-legacy-removal-ship-unit-foundation/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- ship_stats JSON 경로 (backend/shared/src/main/resources/data/) -->
<!-- ship_stats_empire.json, ship_stats_alliance.json, ground_unit_stats.json -->
<!-- 구조 예시: -->
<!--   { "subtypes": { "BATTLESHIP_I": { "armor": 100, "shield": 80, "weaponPower": 80, ... }, ... } } -->
<!-- 실제 구조는 파일을 읽어 확인 필요 -->

<!-- officerLevel >= 5 위치 (grep 결과 기준) -->
<!-- 1. CommandExecutor.kt:186 — "Legacy fallback: officerLevel >= 5 still grants access during migration" -->
<!-- 2. CommandService.kt:160 — non-PERSONAL/CAPTAIN card granting faction-level groups, OR officerLevel >= 5 (legacy) -->
<!-- 3. CommandService.kt:181 — Position card authority filter (with legacy officerLevel >= 5 fallback) -->
<!-- 4. FrontInfoService.kt:405 — if (g.officerLevel >= 5) return 2 -->
<!-- 5. FrontInfoService.kt:413 — officerLevel >= 5 -> nationLevel -->
<!-- 6. FactionService.kt:88 — if (officer.officerLevel >= 5 && officer.penalty["noChief"] == true) -->
<!-- 7. FactionService.kt:94 — return officer.officerLevel >= 5 || officer.permission == "ambassador" -->
<!-- 8. FactionService.kt:220 — if (officerLevel >= 5) -->
<!-- 9. InMemoryTurnProcessor.kt:250 — if (officer.officerLevel >= 5 && officer.factionId > 0) -->
<!-- 10. UnificationService.kt:99 — .filter { it.officerLevel >= 5 && it.npcState.toInt() < 2 } -->

<!-- ShipUnit 엔티티 (Plan 04에서 생성됨) -->
```kotlin
class ShipUnit(
    var fleetId: Long, var shipClass: String, var shipSubtype: String,
    var armor: Int, var shield: Int, var weaponPower: Int,
    var speed: Int, var crewCapacity: Int, var supplyCapacity: Int,
    var crewProficiency: String, var isFlagship: Boolean, ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShipStatRegistry 생성 — JSON 서브타입 스탯 로더</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt
  </files>
  <read_first>
    - backend/shared/src/main/resources/data/ship_stats_empire.json (실제 JSON 구조 확인 — 전체)
    - backend/shared/src/main/resources/data/ship_stats_alliance.json (첫 30줄 확인)
    - backend/shared/src/main/resources/data/ground_unit_stats.json (첫 20줄 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/model/ShipSubtype.kt (enum 이름 확인)
  </read_first>
  <action>
    **사전 확인 — JSON 구조 파악:**
    ```bash
    cat backend/shared/src/main/resources/data/ship_stats_empire.json | head -40
    ```
    JSON의 최상위 키 구조를 확인한다. 예상 구조:
    ```json
    {
      "BATTLESHIP_I": { "armor": 100, "shield": 80, "weaponPower": 80, "speed": 50, ... },
      "BATTLESHIP_II": { ... },
      ...
    }
    ```
    또는 중첩 구조:
    ```json
    { "subtypes": { "BATTLESHIP_I": { ... } } }
    ```
    실제 구조에 맞게 파싱 코드를 작성한다.

    **ShipStatRegistry.kt 생성:**

    파일 위치: `backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt`

    ```kotlin
    package com.openlogh.service

    import com.fasterxml.jackson.module.kotlin.jacksonObjectMapper
    import com.fasterxml.jackson.module.kotlin.readValue
    import com.openlogh.model.ShipSubtype
    import org.slf4j.LoggerFactory
    import org.springframework.core.io.ClassPathResource
    import org.springframework.stereotype.Component
    import jakarta.annotation.PostConstruct

    /**
     * ship_stats_empire.json / ship_stats_alliance.json / ground_unit_stats.json을
     * 애플리케이션 시작 시 로드하여 in-memory 캐시에 보관한다.
     *
     * TacticalBattleService가 전투 초기화 시 ShipUnit 서브타입 스탯을 조회하는 데 사용된다.
     */
    @Component
    class ShipStatRegistry {

        private val log = LoggerFactory.getLogger(javaClass)
        private val mapper = jacksonObjectMapper()

        /** 서브타입 코드(String) → 스탯 맵 */
        private val empireStats  = mutableMapOf<String, ShipSubtypeStat>()
        private val allianceStats = mutableMapOf<String, ShipSubtypeStat>()
        private val groundStats  = mutableMapOf<String, GroundUnitStat>()

        @PostConstruct
        fun load() {
            loadShipStats("data/ship_stats_empire.json",  empireStats)
            loadShipStats("data/ship_stats_alliance.json", allianceStats)
            loadGroundStats("data/ground_unit_stats.json")
            log.info("ShipStatRegistry loaded: empire={}, alliance={}, ground={}",
                empireStats.size, allianceStats.size, groundStats.size)
        }

        private fun loadShipStats(path: String, target: MutableMap<String, ShipSubtypeStat>) {
            try {
                val resource = ClassPathResource(path)
                // JSON 구조에 따라 파싱 — 실제 구조 확인 후 조정
                val raw: Map<String, Map<String, Any>> = mapper.readValue(resource.inputStream)
                raw.forEach { (subtypeName, statMap) ->
                    target[subtypeName] = ShipSubtypeStat(
                        armor        = (statMap["armor"] as? Number)?.toInt() ?: 0,
                        shield       = (statMap["shield"] as? Number)?.toInt() ?: 0,
                        weaponPower  = (statMap["weaponPower"] as? Number)?.toInt() ?: 0,
                        speed        = (statMap["speed"] as? Number)?.toInt() ?: 0,
                        crewCapacity = (statMap["crewCapacity"] as? Number)?.toInt() ?: 0,
                        supplyCapacity = (statMap["supplyCapacity"] as? Number)?.toInt() ?: 0,
                        beamPower    = (statMap["beamPower"] as? Number)?.toInt() ?: 0,
                        gunPower     = (statMap["gunPower"] as? Number)?.toInt() ?: 0,
                        hullPoints   = (statMap["hullPoints"] as? Number)?.toInt() ?: 0,
                    )
                }
            } catch (e: Exception) {
                log.warn("ShipStatRegistry: failed to load $path — ${e.message}")
            }
        }

        private fun loadGroundStats(path: String) {
            try {
                val resource = ClassPathResource(path)
                val raw: Map<String, Map<String, Any>> = mapper.readValue(resource.inputStream)
                raw.forEach { (typeName, statMap) ->
                    groundStats[typeName] = GroundUnitStat(
                        attack  = (statMap["attack"] as? Number)?.toInt() ?: 0,
                        defense = (statMap["defense"] as? Number)?.toInt() ?: 0,
                        morale  = (statMap["morale"] as? Number)?.toInt() ?: 0,
                    )
                }
            } catch (e: Exception) {
                log.warn("ShipStatRegistry: failed to load $path — ${e.message}")
            }
        }

        /**
         * 함종 서브타입 스탯 조회.
         * @param subtypeName ShipSubtype.name (예: "BATTLESHIP_I")
         * @param factionType "empire" 또는 "alliance"
         */
        fun getShipStat(subtypeName: String, factionType: String): ShipSubtypeStat? =
            when (factionType.lowercase()) {
                "empire"   -> empireStats[subtypeName] ?: allianceStats[subtypeName]
                "alliance" -> allianceStats[subtypeName] ?: empireStats[subtypeName]
                else       -> empireStats[subtypeName] ?: allianceStats[subtypeName]
            }

        fun getGroundStat(typeName: String): GroundUnitStat? = groundStats[typeName]

        fun allEmpireStats(): Map<String, ShipSubtypeStat> = empireStats.toMap()
        fun allAllianceStats(): Map<String, ShipSubtypeStat> = allianceStats.toMap()
    }

    data class ShipSubtypeStat(
        val armor: Int,
        val shield: Int,
        val weaponPower: Int,
        val speed: Int,
        val crewCapacity: Int,
        val supplyCapacity: Int,
        val beamPower: Int,
        val gunPower: Int,
        val hullPoints: Int,
    )

    data class GroundUnitStat(
        val attack: Int,
        val defense: Int,
        val morale: Int,
    )
    ```

    주의사항:
    - ClassPathResource는 공유 모듈(shared)의 리소스를 game-app에서 접근 가능하면 사용. 불가하면 `javaClass.getResourceAsStream("/data/ship_stats_empire.json")` 사용.
    - JSON 실제 구조를 반드시 먼저 확인하고 파싱 로직을 조정한다.
    - ShipSubtype enum에 이미 스탯이 정의되어 있으므로 (beamPower, gunPower 등), JSON과 enum 중 어느 것을 우선할지 결정한다. JSON 데이터 소스를 우선하고 enum은 fallback으로 사용한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | grep -E "error:|BUILD" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt` — 파일 존재
    - `grep "ship_stats_empire.json\|ship_stats_alliance.json" backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt` — 2건
    - `grep "@PostConstruct" backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt` — 1건
    - `grep "getShipStat" backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt` — 1건 이상
    - `./gradlew :game-app:compileKotlin` BUILD SUCCESS
  </acceptance_criteria>
  <done>ShipStatRegistry.kt 생성, JSON 로드, getShipStat() 제공, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: ShipUnitService 생성 + officerLevel >= 5 authority bypass 전량 제거</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt
    backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/CommandService.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/FactionService.kt
    backend/game-app/src/main/kotlin/com/openlogh/service/FrontInfoService.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/turn/cqrs/memory/InMemoryTurnProcessor.kt
    backend/game-app/src/main/kotlin/com/openlogh/engine/UnificationService.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/command/CommandExecutor.kt (officerLevel >= 5 위치 186번째 줄 근처, 전체 로직 파악을 위해 80~220줄 읽기)
    - backend/game-app/src/main/kotlin/com/openlogh/service/CommandService.kt (155~195줄 읽기 — authority 로직)
    - backend/game-app/src/main/kotlin/com/openlogh/service/FactionService.kt (85~100줄, 215~230줄 읽기)
    - backend/game-app/src/main/kotlin/com/openlogh/entity/ShipUnit.kt (Plan 04 생성 파일 확인)
    - backend/game-app/src/main/kotlin/com/openlogh/repository/ShipUnitRepository.kt (Plan 04 생성 파일 확인)
  </read_first>
  <action>
    **단계 1: ShipUnitService.kt 생성**

    파일 위치: `backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt`

    ```kotlin
    package com.openlogh.service

    import com.openlogh.entity.ShipUnit
    import com.openlogh.model.CrewProficiency
    import com.openlogh.model.ShipClass
    import com.openlogh.model.ShipSubtype
    import com.openlogh.repository.ShipUnitRepository
    import org.springframework.stereotype.Service
    import org.springframework.transaction.annotation.Transactional

    /**
     * ShipUnit CRUD 서비스.
     * 유닛 생성 시 ShipStatRegistry에서 서브타입 스탯을 주입한다.
     */
    @Service
    class ShipUnitService(
        private val shipUnitRepository: ShipUnitRepository,
        private val shipStatRegistry: ShipStatRegistry,
    ) {

        /**
         * 새 ShipUnit을 생성하고 서브타입 스탯을 주입한다.
         * @param sessionId 세션 ID
         * @param fleetId 소속 함대 ID
         * @param slotIndex 함대 내 슬롯 번호 (0~7)
         * @param shipSubtypeName ShipSubtype.name 예: "BATTLESHIP_I"
         * @param factionType "empire" 또는 "alliance"
         * @param shipCount 초기 함선 수 (0~300)
         */
        @Transactional
        fun createShipUnit(
            sessionId: Long,
            fleetId: Long,
            slotIndex: Int,
            shipSubtypeName: String,
            factionType: String,
            shipCount: Int = 0,
        ): ShipUnit {
            val subtype = runCatching { ShipSubtype.valueOf(shipSubtypeName) }.getOrNull()
            val shipClassName = subtype?.shipClass?.name ?: ShipClass.BATTLESHIP.name

            val unit = ShipUnit(
                sessionId    = sessionId,
                fleetId      = fleetId,
                slotIndex    = slotIndex,
                shipClass    = shipClassName,
                shipSubtype  = shipSubtypeName,
                shipCount    = shipCount.coerceIn(0, 300),
                maxShipCount = 300,
                crewProficiency = CrewProficiency.GREEN.name,
            )

            // 서브타입 스탯 주입
            val stat = shipStatRegistry.getShipStat(shipSubtypeName, factionType)
            if (stat != null) {
                unit.armor         = stat.armor
                unit.shield        = stat.shield
                unit.weaponPower   = stat.weaponPower
                unit.speed         = stat.speed
                unit.crewCapacity  = stat.crewCapacity
                unit.supplyCapacity = stat.supplyCapacity
            }

            return shipUnitRepository.save(unit)
        }

        fun findByFleet(fleetId: Long): List<ShipUnit> =
            shipUnitRepository.findByFleetId(fleetId)

        fun findFlagship(fleetId: Long): ShipUnit? =
            shipUnitRepository.findByFleetIdAndIsFlagshipTrue(fleetId)

        @Transactional
        fun applyBattleDamage(unitId: Long, lostShips: Int): ShipUnit? {
            val unit = shipUnitRepository.findById(unitId).orElse(null) ?: return null
            unit.shipCount = (unit.shipCount - lostShips).coerceAtLeast(0)
            return shipUnitRepository.save(unit)
        }
    }
    ```

    **단계 2: officerLevel >= 5 authority bypass 전량 제거**

    다음 파일들을 각각 읽고, `officerLevel >= 5` 패턴을 찾아 제거한다.
    각 위치에 맞는 처리를 한다:

    **2-1. CommandExecutor.kt (line ~186):**
    ```kotlin
    // 제거 대상:
    // Legacy fallback: officerLevel >= 5 still grants access during migration
    // if (officer.officerLevel >= 5) return true  (또는 유사 로직)
    ```
    해당 줄 또는 블록을 삭제한다. PositionCard 권한 체크만 남긴다.
    주석 추가: `// Phase 2에서 gin7 81종 PositionCard 매핑으로 완성 예정`

    **2-2. CommandService.kt (lines ~160, ~181):**
    `officerLevel >= 5` 조건이 포함된 OR 절을 제거한다.
    예: `|| officer.officerLevel >= 5` → 삭제
    PositionCard 권한 체크 로직만 유지한다.

    **2-3. FactionService.kt (lines ~88, ~94, ~220):**
    - line 88: `officer.officerLevel >= 5 && officer.penalty["noChief"] == true` → officerLevel 조건 제거, penalty 체크만 유지
    - line 94: `return officer.officerLevel >= 5 || officer.permission == "ambassador"` → gin7에서 faction chief는 PositionCard로 결정. stub: `return officer.permission == "ambassador"` + TODO 주석
    - line 220: `if (officerLevel >= 5)` → 블록 제거 또는 TODO stub

    **2-4. FrontInfoService.kt (lines ~405, ~413):**
    - officerLevel >= 5 기반 레벨 계산을 TODO stub으로 대체:
      ```kotlin
      // TODO Phase 2: gin7 직무권한카드 기반 접근 레벨로 교체
      // 임시: PositionCard 미구현 상태에서 기본값 반환
      return 1
      ```

    **2-5. InMemoryTurnProcessor.kt (line ~250):**
    `officer.officerLevel >= 5 && officer.factionId > 0` → officerLevel 조건 제거, `officer.factionId > 0` 만 유지

    **2-6. UnificationService.kt (line ~99):**
    `.filter { it.officerLevel >= 5 && it.npcState.toInt() < 2 }` → officerLevel 조건 제거:
    `.filter { it.npcState.toInt() < 2 }`

    **단계 3: 최종 검증**
    ```bash
    grep -rn "officerLevel >= 5" backend/game-app/src/main/kotlin/com/openlogh/ --include="*.kt"
    ```
    결과가 0건이어야 한다. 남은 건이 있으면 해당 위치도 처리한다.

    (참고: `che_초토화.kt` 내 `gen.officerLevel >= 5` 는 Plan 02에서 nation command 파일이 삭제되므로 자동 제거됨)

    **단계 4: 컴파일 확인**
    `./gradlew :game-app:compileKotlin` BUILD SUCCESS
  </action>
  <verify>
    <automated>grep -rn "officerLevel >= 5" /Users/apple/Desktop/개인프로젝트/openlogh/backend/game-app/src/main/kotlin/com/openlogh/ --include="*.kt" 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "officerLevel >= 5" backend/game-app/src/main/kotlin/ --include="*.kt"` — 0건
    - `ls backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt` — 파일 존재
    - `grep "shipStatRegistry.getShipStat\|ShipStatRegistry" backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt` — 1건 이상
    - `grep "createShipUnit" backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt` — 1건
    - `cd backend && ./gradlew :game-app:compileKotlin 2>&1 | grep "BUILD SUCCESS"` — BUILD SUCCESS
  </acceptance_criteria>
  <done>ShipUnitService 생성, officerLevel >= 5 전량 제거 (0건), 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
# officerLevel >= 5 완전 제거 확인
grep -rn "officerLevel >= 5" \
  backend/game-app/src/main/kotlin/com/openlogh/ --include="*.kt"
# 기대값: 0건

# ShipStatRegistry JSON 로드 경로 확인
grep "ship_stats_empire\|ship_stats_alliance" \
  backend/game-app/src/main/kotlin/com/openlogh/service/ShipStatRegistry.kt

# ShipUnitService 스탯 주입 확인
grep "getShipStat" \
  backend/game-app/src/main/kotlin/com/openlogh/service/ShipUnitService.kt

# 컴파일 최종 확인
cd backend && ./gradlew :game-app:compileKotlin
```
</verification>

<success_criteria>
- ShipStatRegistry.kt 존재, @PostConstruct, ship_stats JSON 두 파일 로드
- ShipUnitService.kt 존재, createShipUnit() + getShipStat() 연결
- `grep -rn "officerLevel >= 5" backend/game-app/src/` — 0건
- `./gradlew :game-app:compileKotlin` BUILD SUCCESS
</success_criteria>

<output>
완료 후 `.planning/phases/01-legacy-removal-ship-unit-foundation/01-05-SUMMARY.md` 생성
</output>
