---
phase: 01-legacy-removal-ship-unit-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/NationTypeModifiers.kt
  - backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/IncomeContext.kt
autonomous: true
requirements:
  - LEGACY-06

must_haves:
  truths:
    - "EconomyService의 삼국지 농업(agri)/상업(comm) 경제 계산 로직이 제거된다"
    - "EconomyService.processMonthly()가 stub으로 대체되어 Phase 4 연결을 준비한다"
    - "TickEngine이 참조하는 EconomyService 메서드가 컴파일 오류 없이 유지된다"
  artifacts:
    - path: "backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt"
      provides: "삼국지 로직 제거된 stub EconomyService"
      contains: "TODO Phase 4"
  key_links:
    - from: "TickEngine.runMonthlyPipeline()"
      to: "EconomyService.processMonthly()"
      via: "Spring 주입 호출"
      pattern: "economyService\\.processMonthly|runMonthlyPipeline"
---

<objective>
EconomyService.kt에서 삼국지 농업/상업/아이템 기반 경제 계산 로직을 제거하고, Phase 4에서 gin7 Gin7EconomyService로 대체되도록 processMonthly()를 TODO stub으로 비운다.

Purpose: 삼국지 EconomyService가 TickEngine.runMonthlyPipeline()에 연결된 상태에서 Phase 4를 구현하면 두 경제 시스템이 동시에 실행된다. 삼국지 로직을 먼저 비워 Phase 4의 진입점을 명확히 한다.
Output: EconomyService.kt — 삼국지 로직 비움, processMonthly() stub, NationTypeModifiers 삼국지 의존성 제거
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-legacy-removal-ship-unit-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- TickEngine에서 EconomyService 호출 패턴 -->
<!-- backend/game-app/src/main/kotlin/com/openlogh/engine/turn/TickEngine.kt 또는 TickDaemon.kt -->
<!-- runMonthlyPipeline() 내부에서 economyService.processMonthly(sessionId) 호출 -->
<!-- 이 호출이 컴파일 오류 없이 유지되어야 한다 -->

<!-- EconomyService 현재 구조 (상위 60줄 확인됨) -->
```kotlin
@Service
class EconomyService @Autowired constructor(
    private val worldPortFactory: JpaWorldPortFactory,
    private val officerRepository: OfficerRepository,
    private val messageRepository: MessageRepository,
    private val mapService: MapService,
    private val historyService: HistoryService,
    private val inheritanceService: InheritanceService,
)
```
<!-- processMonthly(), preUpdateMonthly(), postUpdateMonthly() 메서드 존재 -->
<!-- 삼국지 농업(agri), 상업(comm), 수비(def), 성벽(wall) 기반 계산 로직 포함 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EconomyService 삼국지 로직 제거 및 stub 대체</name>
  <files>
    backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt
  </files>
  <read_first>
    - backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt (전체 내용 — offset/limit로 나눠 읽기)
    - backend/game-app/src/main/kotlin/com/openlogh/engine/turn/ (TickEngine.kt 위치 확인 및 runMonthlyPipeline 호출 패턴)
  </read_first>
  <action>
    **사전 확인:**
    ```bash
    grep -n "processMonthly\|preUpdateMonthly\|postUpdateMonthly\|agri\|comm\|농업\|상업" \
      backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt | head -30
    grep -rn "EconomyService\|processMonthly" \
      backend/game-app/src/main/kotlin/com/openlogh/engine/turn/ --include="*.kt"
    ```

    **단계 1: EconomyService 메서드 본문 비우기**

    EconomyService.kt를 읽어 다음 패턴의 메서드들을 찾는다:
    - `processMonthly(sessionId: Long)` 또는 `processMonthly(world: SessionState)`
    - `preUpdateMonthly(...)` / `postUpdateMonthly(...)`
    - 삼국지 농업/상업 계산 private 메서드들

    각 메서드 본문을 비우고 stub으로 교체한다:
    ```kotlin
    /**
     * TODO Phase 4: gin7 Gin7EconomyService로 대체 예정
     * 삼국지 농업/상업 기반 경제 로직 제거됨.
     * 현재 stub — 아무 처리도 하지 않음.
     */
    fun processMonthly(sessionId: Long) {
        // TODO Phase 4: gin7EconomyService.processMonthly(sessionId)
    }
    ```

    TickEngine이 호출하는 메서드 시그니처(파라미터 타입)는 변경하지 않는다.

    **단계 2: 삼국지 전용 import 제거**

    EconomyService.kt에서 삼국지 전용 import 제거:
    - `import com.openlogh.engine.modifier.IncomeContext` (삼국지 수입 계산용이면 제거)
    - `import com.openlogh.engine.modifier.NationTypeModifiers` (삼국지 국가타입 수정자면 제거)
    - `import com.openlogh.engine.turn.cqrs.*` 계열 중 stub에서 불필요한 것 제거
    - HistoryService, InheritanceService 등 stub에서 사용하지 않는 주입 의존성은 `@Suppress("unused")` 추가 또는 제거

    **단계 3: NationTypeModifiers 삼국지 의존성 확인**

    ```bash
    grep -n "agri\|comm\|농업\|상업\|rice\|gold\|병종" \
      backend/game-app/src/main/kotlin/com/openlogh/engine/modifier/NationTypeModifiers.kt
    ```

    NationTypeModifiers.kt에 삼국지 농업/상업 수정자가 있으면 해당 로직을 제거하거나 TODO 주석으로 대체한다. gin7 faction_type(empire/alliance/fezzan)에 대한 수정자 구조만 남긴다.

    **단계 4: IncomeContext 삼국지 의존성 확인**

    IncomeContext.kt가 삼국지 전용 데이터 클래스라면 (gold, rice, agri, comm 필드) 파일을 삭제하고 참조를 제거한다. gin7 경제 컨텍스트는 Phase 4에서 새로 정의한다.

    **컴파일 확인:**
    stub으로 대체 후 `./gradlew :game-app:compileKotlin` 이 성공해야 한다.
    실패하면 남은 참조를 추적하여 제거한다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/openlogh/backend && ./gradlew :game-app:compileKotlin 2>&1 | grep -E "error:|BUILD" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "agri\|comm\|농업\|상업\|ARM_PER_PHASE" backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt` — 0건 (계산 로직 없음)
    - `grep "TODO Phase 4" backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt` — 1건 이상
    - `grep -rn "EconomyService" backend/game-app/src/main/kotlin/com/openlogh/engine/turn/ --include="*.kt"` — 메서드 호출 시그니처가 여전히 유효함 (컴파일 오류 없음)
    - `./gradlew :game-app:compileKotlin` BUILD SUCCESS
  </acceptance_criteria>
  <done>EconomyService 삼국지 로직 제거, processMonthly() stub으로 대체, 컴파일 성공</done>
</task>

</tasks>

<verification>
```bash
# 삼국지 경제 로직 제거 확인
grep -n "agri\|comm\|농업\|상업" \
  backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt

# Phase 4 진입점 확인
grep "TODO Phase 4" backend/game-app/src/main/kotlin/com/openlogh/engine/EconomyService.kt

# 컴파일 확인
cd backend && ./gradlew :game-app:compileKotlin
```
</verification>

<success_criteria>
- EconomyService.processMonthly()가 비어 있는 stub
- 삼국지 agri/comm 계산 로직 0건
- TODO Phase 4 주석 존재
- `./gradlew :game-app:compileKotlin` BUILD SUCCESS
</success_criteria>

<output>
완료 후 `.planning/phases/01-legacy-removal-ship-unit-foundation/01-03-SUMMARY.md` 생성
</output>
