---
phase: quick
plan: 260405-9yd
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/game/map-3d/city/CityModel.tsx
  - frontend/src/components/game/map-3d/city/CityModel.test.ts
autonomous: true
requirements: [QUICK-3D-LOCATION]

must_haves:
  truths:
    - "3D 맵에서 자신의 장수가 위치한 도시에 시각적 표시(펄스 링)가 보인다"
    - "다른 도시에는 해당 표시가 나타나지 않는다"
    - "2D 맵의 기존 위치 표시 동작에 영향 없다"
  artifacts:
    - path: "frontend/src/components/game/map-3d/city/CityModel.tsx"
      provides: "내 도시 펄스 링 렌더링"
      contains: "isMyCity"
    - path: "frontend/src/components/game/map-3d/city/CityModel.test.ts"
      provides: "CityModel isMyCity 테스트"
  key_links:
    - from: "frontend/src/components/game/map-viewer.tsx"
      to: "frontend/src/components/game/map-3d/city/CityModel.tsx"
      via: "renderCities[].isMyCity prop passed through Map3dScene"
      pattern: "isMyCity"
---

<objective>
3D 맵에서 플레이어 장수의 현재 위치(도시)를 시각적으로 표시하는 기능 추가.

Purpose: 2D 맵에는 이미 `isMyCity`일 때 빨간 펄스 테두리가 표시되지만, 3D 맵(`CityModel`)에는 동일한 표시가 없음. 3D에서도 자신의 도시를 쉽게 식별할 수 있도록 한다.
Output: CityModel에 펄스 링 이펙트 추가, 테스트 파일 생성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/game/map-3d/city/CityModel.tsx
@frontend/src/components/game/map-3d/scene/Map3dScene.tsx
@frontend/src/components/game/map-canvas.tsx (RenderCity interface, 2D pulse ring reference)
@frontend/src/lib/map-3d-utils.ts (WORLD_SCALE, toWorld3d)

<interfaces>
<!-- RenderCity already has isMyCity — passed from map-viewer.tsx to Map3dScene to CityModel -->

From frontend/src/components/game/map-canvas.tsx:
```typescript
export interface RenderCity {
    id: number;
    name: string;
    x: number; y: number;
    level: number;
    region?: number;
    nationColor: string | null;
    nationName: string | null;
    nationAbbr: string | null;
    isCapital: boolean;
    supplyState: number;
    state: number;
    isMyCity: boolean;      // <-- This is the key field
    isEmperorCity: boolean;
}
```

From frontend/src/components/game/map-canvas.tsx (2D reference, line 412):
```tsx
{city.isMyCity && (
    <div className="absolute -inset-[2px] rounded-[33%] border-[4px] border-solid border-red-500 animate-pulse" />
)}
```

From frontend/src/lib/map-3d-utils.ts:
```typescript
export const WORLD_SCALE = 3;
```

CityModel already receives `city: RenderCity` as prop — no new prop threading needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CityModel에 내 도시 펄스 링 이펙트 추가</name>
  <files>frontend/src/components/game/map-3d/city/CityModel.tsx, frontend/src/components/game/map-3d/city/CityModel.test.ts</files>
  <behavior>
    - Test 1: isMyCity=true인 RenderCity를 전달하면 MyLocationRing mesh가 렌더된다
    - Test 2: isMyCity=false인 RenderCity를 전달하면 MyLocationRing mesh가 렌더되지 않는다
  </behavior>
  <action>
    1. `CityModel.tsx`에 `MyLocationRing` 내부 컴포넌트를 추가한다:
       - 빨간색(`#ef4444`) 링 (`ringGeometry`) — 바닥 원형 위에 약간 높게(y=0.3) 배치
       - `useFrame`으로 opacity를 sin 함수로 0.3~0.9 사이에서 펄싱 (2D의 `animate-pulse`에 대응)
       - 링 크기는 기존 `baseR`(도시 레벨별 바닥 원 반지름)보다 약간 크게 (`baseR * 1.15` ~ `baseR * 1.3`)
       - `meshBasicMaterial`에 `transparent`, `depthWrite={false}`, `side={THREE.DoubleSide}`

    2. `CityModel` 컴포넌트 JSX에서, 바닥 원형 mesh 바로 아래에 조건부 렌더링:
       ```tsx
       {city.isMyCity && <MyLocationRing baseR={baseR} />}
       ```

    3. 테스트 파일 `CityModel.test.ts` 생성:
       - `@react-three/test-renderer` 대신 단위 테스트로: `isMyCity` prop에 따른 조건 분기 로직만 검증
       - R3F 컴포넌트의 Three.js mesh 렌더링은 통합 테스트 범위이므로, 여기서는 `city.isMyCity` 조건 분기가 올바른지 확인하는 간단한 로직 테스트 작성
       - 예: `MyLocationRing`이 export되어 있으면 컴포넌트 존재 확인, 아니면 조건 분기 함수 추출 후 테스트

    주의: CityModel은 `useGLTF`를 사용하므로 테스트에서 Three.js mock이 필요할 수 있다. 테스트가 복잡해지면 순수 로직 테스트(펄스 계산 함수 등)로 범위를 좁힌다.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/개인프로젝트/opensamguk/frontend && pnpm vitest run --reporter=verbose src/components/game/map-3d/city/CityModel.test.ts</automated>
  </verify>
  <done>
    - CityModel에서 city.isMyCity===true일 때 빨간 펄스 링이 바닥에 렌더됨
    - isMyCity===false일 때 링이 렌더되지 않음
    - 기존 CityModel 기능(GLB 모델, 깃발, 라벨, 클릭/호버)에 변경 없음
    - 테스트 통과
  </done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm vitest run` — 전체 프론트엔드 테스트 통과
- `cd frontend && pnpm build` — 빌드 성공 (타입 에러 없음)
- 브라우저에서 3D 맵 모드 전환 후 자신의 도시에 빨간 펄스 링 확인
</verification>

<success_criteria>
- 3D 맵에서 플레이어 장수가 위치한 도시에 빨간색 펄스 링이 표시됨
- 다른 도시에는 펄스 링 없음
- 2D 맵 기능 영향 없음
- 테스트 + 빌드 통과
</success_criteria>

<output>
After completion, create `.planning/quick/260405-9yd-3d/260405-9yd-SUMMARY.md`
</output>
