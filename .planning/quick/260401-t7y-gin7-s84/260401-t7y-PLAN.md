---
phase: quick
plan: 260401-t7y
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/mdx_parse_mds.py
  - backend/scripts/mdx_watertight_test.py
autonomous: true
requirements: [R-1, R-2, R-3, R-4]

must_haves:
  truths:
    - "s84 parts (Brunhild) achieve >= 95% face coverage (up from 84%)"
    - "All 66 warship models convert successfully (66/66)"
    - "Watertight manifold ratio >= 95% average across all ships"
    - "No regression in s72 coverage (97%+ maintained)"
    - "No spaghetti faces introduced by parser changes"
  artifacts:
    - path: "backend/scripts/mdx_parse_mds.py"
      provides: "Improved s84 decoder + heuristic gap strip"
    - path: "backend/scripts/mdx_watertight_test.py"
      provides: "Edge manifold validation script"
  key_links:
    - from: "mdx_parse_mds.py:decode_s84_mesh"
      to: "mdx_parse_mds.py:find_s84_strip_end"
      via: "PV boundary detection for strip extension"
      pattern: "find_s84_strip_end.*ib_start.*ic.*vc"
    - from: "mdx_parse_mds.py:extract_mdx Phase 4"
      to: "mdx_parse_mds.py:decode_gap_strip"
      via: "Gap strip for heuristic parts"
      pattern: "decode_gap_strip.*gap_start.*next_vb"
---

<objective>
Complete the gin7 warship model reverse engineering pipeline: forensic analysis of s84 binary
structure, parser improvements for full coverage, and batch verification with watertight mesh testing.

Purpose: Combat UI requires watertight 3D ship meshes for correct lighting/shadows.
Current s84 parts achieve only ~84% face coverage; target is 95%+.

Output: Improved mdx_parse_mds.py + verified OBJ output for all 66 warships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/scripts/mdx_parse_mds.py
@backend/scripts/mdx_watertight_test.py
@docs/01-plan/features/ship-reverse-engineering.plan.md
@docs/02-design/features/ship-reverse-engineering.design.md

Key existing implementation to understand:
- `decode_s84_mesh()` (L278-352): Dedicated s84 decoder with strip extension + topology spaghetti filter
- `find_s84_strip_end()` (L253-275): PV boundary detection via consecutive OOB run (>50)
- `decode_gap_strip()` (L228-250): Strip IB decoder with OOB restart markers
- `extract_heuristic()` (L748-829): W=1.0 heuristic VB finder, sets `ib_end` for gap strip
- Phase 4 gap strip (L900-925): Applies gap strip to non-s84 parts; s84 skip with comment "handled by decode_s84_mesh()"

Game data: logh-game/installed/warshipmodel_mdx.arc (needs arc extraction to get individual _h.mdx files)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract warship MDX files + capture baseline</name>
  <files>logh-game/installed/warshipmodel_mdx.arc</files>
  <action>
1. Check if extracted MDX files already exist somewhere (e.g., /tmp/mdx_extracted/ or similar).
   If not, find or write an arc extraction utility. The .arc format from gin7 is a simple
   archive: check existing scripts in backend/scripts/ for any arc extractor.

2. Run the CURRENT parser on all 66 warship _h.mdx files to capture baseline:
   ```
   python backend/scripts/mdx_parse_mds.py <extracted_dir> --out /tmp/mdx_baseline
   ```
   Save the full console output (per-ship vertex/face counts) to a baseline text file.

3. Run the CURRENT watertight test on baseline output:
   ```
   python backend/scripts/mdx_watertight_test.py /tmp/mdx_baseline
   ```
   Save output as baseline watertight results.

4. Record baseline metrics:
   - Total ships successfully converted (expect 66/66)
   - Per-ship face counts
   - Average manifold ratio
   - Specific metrics for Brunhild, battleship, berlin s84 parts
  </action>
  <verify>
    <automated>ls /tmp/mdx_baseline/*/high.obj | wc -l</automated>
  </verify>
  <done>66 OBJ files exist in baseline output, baseline metrics recorded in console output</done>
</task>

<task type="auto">
  <name>Task 2: s84 binary forensics — Brunhild hex analysis (R-1)</name>
  <files>backend/scripts/mdx_parse_mds.py</files>
  <action>
Write a focused forensic analysis script (or inline investigation) for Brunhild's s84 area:

1. Load brunhild_h.mdx, find all s84 descriptors via find_all_descriptors().
2. For each s84 descriptor, print:
   - Descriptor offset, vc, ic, prim type
   - VB start (from find_vb_for_descriptor), VB end
   - IB start (VB end), IB end per descriptor ic (ib_start + ic*2)
   - Actual strip end from find_s84_strip_end()
   - Bytes between descriptor ic end and strip end (the "extension zone")
   - Bytes between strip end and next VB start (the "dead zone" — should be PV)

3. Hex dump the extension zone (first 256 bytes):
   - Count valid indices (< vc) vs OOB markers (>= vc)
   - Look for patterns: list IB (consecutive triples < vc), strip restart markers, or PV data

4. Hex dump the dead zone after strip end:
   - Verify it looks like PV (sequential u16 values, or values close to 0)
   - Check if the consecutive OOB threshold (currently 50) is appropriate

5. Calculate coverage: faces from decode_s84_mesh() / theoretical max (2*vc - 4)
   If still < 95%, investigate:
   - Are there list IB faces BEFORE the strip IB? (dual structure hypothesis)
   - Does the strip extension zone contain valid faces rejected by the topology filter?
   - Adjust find_s84_strip_end consecutive_oob threshold if PV detection is too aggressive

6. Based on findings, tune the parser:
   - If consecutive_oob threshold too aggressive: lower from 50 to a better value
   - If topology filter too strict: relax seed-grow to also accept faces sharing vertices (not just edges)
   - If list+strip dual structure found: add list IB decode before strip in decode_s84_mesh()
   - If extension zone has more valid strip data: ensure find_s84_strip_end captures it

Document findings as comments in the code changes.
  </action>
  <verify>
    <automated>python3 backend/scripts/mdx_parse_mds.py <brunhild_mdx_path> --out /tmp/mdx_s84_test 2>&1 | grep -i brunhild</automated>
  </verify>
  <done>s84 binary structure documented, parser tuned based on forensic findings, Brunhild s84 face count increased toward 95% target</done>
</task>

<task type="auto">
  <name>Task 3: Heuristic gap strip verification + parser fixes (R-2)</name>
  <files>backend/scripts/mdx_parse_mds.py</files>
  <action>
Verify and fix heuristic part gap strip application (R-2):

1. Add temporary logging to Phase 4 gap strip (L900-925) to trace:
   - Which parts are processed (source, stride, vc)
   - Which parts are skipped and why
   - Gap size between ib_end and next_vb
   - Number of gap faces decoded per part

2. Run on full 66-ship batch with logging, capture which heuristic parts get gap strip.

3. Investigate potential issue: heuristic parts set `ib_end = ib_off + ic * 2` (L828),
   but `all_vb_starts` (L904-906) includes heuristic VBs. Verify the gap between
   a heuristic part's ib_end and the next VB is correctly calculated:
   - The ib_end should point to the end of the list IB
   - The next_vb should be the next VB start (not the same VB)
   - Gap should contain strip IB data if gin7 layout is [VB][list_IB][strip_IB][next_VB]

4. If heuristic parts are not getting gap strip due to incorrect ib_end or next_vb calculation:
   - Fix the gap detection logic
   - Ensure `all_vb_starts` sorted correctly includes heuristic VB offsets

5. After fixes, compare total face count per ship vs baseline.
   Remove temporary logging, keep only permanent debug-level logging if needed.

Note: Phase 4 s84 skip (L908-909) is correct — s84 gap strip is handled in decode_s84_mesh().
Do NOT change this.
  </action>
  <verify>
    <automated>python3 backend/scripts/mdx_parse_mds.py <extracted_dir> --out /tmp/mdx_gapfix 2>&1 | tail -5</automated>
  </verify>
  <done>Heuristic gap strip confirmed working or fixed, face counts equal or improved vs baseline for non-s84 parts</done>
</task>

<task type="auto">
  <name>Task 4: Full batch verification + watertight test + regression check (R-3, R-4)</name>
  <files>backend/scripts/mdx_watertight_test.py</files>
  <action>
Run complete verification pipeline:

1. Run the improved parser on all 66 warship models:
   ```
   python backend/scripts/mdx_parse_mds.py <extracted_dir> --out /tmp/mdx_final
   ```

2. Verify 66/66 conversion success (R-4 regression check).

3. Run watertight test on all outputs:
   ```
   python backend/scripts/mdx_watertight_test.py /tmp/mdx_final
   ```

4. Compare against baseline (Task 1):
   - Total face count: must be >= baseline (no regression)
   - Per-ship face count: identify any ships with FEWER faces (regression candidates)
   - Conversion success: must remain 66/66
   - s72 coverage: spot-check a few s72-heavy ships to confirm 97%+ maintained

5. Check s84 improvement specifically:
   - Brunhild: compare s84 face count before/after
   - battleship: compare s84 face count before/after
   - berlin: compare s84 face count before/after
   - Target: 84% -> 95%+ coverage

6. Watertight results analysis:
   - Average manifold ratio across all 66 ships
   - List any ships with ratio < 90% (FAIL) for further investigation
   - If average < 95%, investigate common failure patterns (boundary edges = holes in mesh)

7. If any ship has FEWER faces than baseline, investigate and fix before proceeding.

8. Print final summary table:
   | Metric | Baseline | Final | Status |
   |--------|----------|-------|--------|
   | Ships OK | 66/66 | 66/66 | PASS/FAIL |
   | Total faces | N | M | +X% |
   | Avg manifold | N% | M% | PASS/FAIL |
   | s84 coverage | 84% | M% | PASS/FAIL |
  </action>
  <verify>
    <automated>python3 backend/scripts/mdx_watertight_test.py /tmp/mdx_final 2>&1 | tail -3</automated>
  </verify>
  <done>66/66 ships convert, average manifold ratio >= 95%, s84 coverage >= 95%, no face count regressions vs baseline</done>
</task>

</tasks>

<verification>
1. `python3 backend/scripts/mdx_parse_mds.py <extracted_dir> --out /tmp/mdx_final` succeeds with 66/66
2. `python3 backend/scripts/mdx_watertight_test.py /tmp/mdx_final` shows average manifold ratio >= 95%
3. s84 face coverage for Brunhild >= 95% of theoretical max (2*vc - 4)
4. Total face count >= baseline (no regression)
</verification>

<success_criteria>
- SC-1: 66/66 warship models convert to OBJ successfully
- SC-2: s84 face coverage improved from ~84% to >= 95% (Brunhild primary target)
- SC-3: Heuristic gap strip verified working (face counts equal or improved)
- SC-4: Average manifold ratio >= 95% across all 66 ships
- SC-5: No regression — every ship has >= baseline face count
</success_criteria>

<output>
After completion, create `.planning/quick/260401-t7y-gin7-s84/260401-t7y-SUMMARY.md`
</output>
